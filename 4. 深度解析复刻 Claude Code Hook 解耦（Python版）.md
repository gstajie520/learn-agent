# 深度解析复刻 Claude Code：顶级 AI Agent 是如何利用 Hook 解耦的？（Python 版）

> 本文是第四章 TypeScript 教程的完整 Python 迁移版，配套代码位于 `python/ch04_agent`。
> 原文的验收标准、设计推导、Hook 事件说明、失败分支和边界讨论全部保留；代码、命令和实现说明替换成 Python 版本，并面向 Java 后端开发者补了语言对照。

上周帮一个朋友看他写的 Agent。最初的代码很简单：调用模型，拿到工具调用就执行，把结果放回 messages，再调用模型。

后来，同一个循环膨胀到了两百多行。PowerShell 日志、写文件确认、结果通知、自动备份都直接塞进了循环。每项需求单独看都合理，放在一起却让核心流程变成了谁也不敢改的 if-else 堆。

问题不在需求太多，而在扩展行为和核心编排没有分开。

第 4 章要做的事情很明确：在现有 Agent Loop 中加入四个结构化 Hook 挂载点，同时不破坏第 1—3 章已经建立的消息配对、工具校验和权限边界。

Claude Code 官方目前定义了 14 类以上 Hook 事件，例如 `PermissionRequest`、`SessionStart`、`SubagentStart`、`PreCompact`、`Elicitation` 等，完整清单参见 https://code.claude.com/docs/en/hooks.md。

第 4 章按教学需要先选择覆盖一次完整工具交互的四类核心事件。

---

## 本章导读

### 读完这一章，你能做到什么

1. 说出四个 Hook 事件各自触发在流水线的哪一步，以及为什么是这四个位置。
2. 解释为什么 Hook 的返回值必须是结构化对象，而不是 `"stop"` 这类字符串。
3. 说清「Hook 说 allow」和「权限系统说 allow」的区别——前者只是建议。
4. 讲出一个 Post Hook 要求停止时，同一轮里其他工具调用会发生什么，以及为什么不能直接 `return`。
5. 解释 Stop Hook 为什么最多只能强制续跑一次。
6. 判断一个 Hook 抛异常时，会得到哪个错误码、handler 有没有执行过。

### 你需要先具备什么

| 需要 | 说明 |
| --- | --- |
| 读完第 1—3 章 | 本章不新增任何工具或权限规则，只在已有流水线上开孔 |
| 记得 `prepare → permission → invoke` | 四个挂载点全部围绕这条顺序摆放 |
| 记得 `deny > ask > allow > passthrough` | 本章的 Hook 建议要挂进这个合并逻辑 |
| 记得 `validate_tool_pairing()` | 本章一半的设计约束都来自「每个调用必须配对」 |

### 术语速查（本章第一次出现的词）

| 术语 | 一句话解释 |
| --- | --- |
| **Hook** | 注册在固定位置的回调。由应用注册，不是插件沙箱 |
| **挂载点** | Loop 里发布结构化上下文的确定位置。本章共四个 |
| **HookEvent** | 四个事件名之一：`UserPromptSubmit` / `PreToolUse` / `PostToolUse` / `Stop` |
| **HookContext** | 事件发布给回调的数据。**不同事件的可用字段不同** |
| **HookResult** | 回调的返回值。每个字段只有一种语义，且只在特定事件合法 |
| **HookRegistry** | 按事件保存回调列表；**注册顺序 = 执行顺序** |
| **permission_behavior** | Pre Hook 提出的权限**建议**，不是决定 |
| **updated_input** | Pre Hook 改写后的参数。会被重新校验并深度冻结 |
| **blocking_error** | Pre Hook 直接阻断，权限和 handler 都不跑 |
| **updated_output** | Post Hook 改写后的工具结果 |
| **additional_context** | 追加的 system 消息。**只能是 system**，且延后写入 |
| **prevent_continuation** | Post Hook 表示本轮不再请求模型 |
| **force_continue** | Stop Hook 追加一条 user 消息并再问一次模型 |
| **stop_hook_active** | 标记「已经强制续跑过一次」，防死循环 |
| **短路** | 出现 `blocking_error` 或 `force_continue` 时，后续同类回调不再执行 |
| **HookContractError** | 回调违反契约（返回值类型不对、用了非法字段、改了工具身份） |
| **MappingProxyType** | Python 标准库的只读字典包装，冻结参数的底层工具 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先看一眼真实的执行顺序    ← 一次运行的完整日志，四个事件各打一行
  ↓
② 验收结果 + 为什么需要扩展点 ← 本章要证明什么、循环为什么不该继续长
  ↓
③ 四个事件 + HookContext    ← 位置固定、数据按事件裁剪
  ↓
④ HookResult                ← 本章设计的核心：为什么不用字符串
  ↓
⑤ HookRegistry              ← 顺序、合并规则、短路
  ↓
⑥ 四个事件逐个看            ← Submit / Pre / Post / Stop 各自能做什么
  ↓
⑦ 两个硬约束                ← Hook allow 只是建议；停止也要保持配对
  ↓
⑧ 故障映射 + P03/P04 差异    ← Hook 出错时的稳定错误码、和第 3 章逐项对比
  ↓
⑨ 组合根、CLI、边界、代码地图 ← Hook 怎么接进真实入口，刻意不做什么
  ↓
⑩ 运行、验证与小结           ← 动手 + 排错 + 四个实验 + 自测
  ↓
⑪ 小白阅读路线              ← Java/Spring 视角的源码考古顺序
```

---

## 先看一眼真实的执行顺序

先不看代码。P04 的真实 CLI 注册了四个只打日志的 Hook。跑一个只读任务：

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch04.cli --prompt "读取 README.md 并概括运行方式"
```

你会在 stderr 上看到这样一串（`[权限审计]` 是第 3 章的审计，`[Hook]` 是本章新增的）：

```text
[Hook] 触发事件: UserPromptSubmit                ← ① prompt 已提交，模型还没收到请求
[Hook] 触发事件: PreToolUse                     ← ② 工具已查到、参数已过校验，权限还没判
[权限审计] read_file: allow (default) - 没有权限规则阻止请求
[Hook] 触发事件: PostToolUse                    ← ③ handler 跑完了，结果还没写进历史
[Hook] 触发事件: Stop                           ← ④ 模型给出了无工具调用的回答，准备结束
```

把这四行的位置摆到第 3 章那条流水线上：

```text
用户提交 prompt
        │
        ├─── ① UserPromptSubmit ────── 能加 system 上下文，不能改/删 prompt
        ↓
   模型返回 tool_calls
        ↓
   ToolRegistry.prepare（查工具 + 解析 JSON + validator 校验）
        │
        ├─── ② PreToolUse ──────────── 能改参数、提权限建议、直接阻断
        ↓
   PermissionPolicy / approval
        ↓
   handler（真正的副作用在这里）
        │
        ├─── ③ PostToolUse ─────────── 能改结果、能要求本轮结束
        ↓
   写入配对的 tool result
        ↓
   模型返回无工具调用的回答
        │
        └─── ④ Stop ────────────────── 能强制再问一次模型（最多一次）
        ↓
      结束
```

四个位置各自的选择理由，用一句话概括：

| 事件 | 为什么在这里 |
| --- | --- |
| `UserPromptSubmit` | 模型还没看到任何东西，是**唯一**能影响本轮输入的时机 |
| `PreToolUse` | 参数已经可信（过了校验），但副作用还没发生——**唯一**能无损阻断的时机 |
| `PostToolUse` | 结果已经产生，但还没进历史——**唯一**能改结果的时机 |
| `Stop` | 循环准备结束，是**唯一**能判断「任务真的做完了吗」的时机 |

注意每一行的「唯一」。**挂载点不是随便挑的位置，是那个信息刚好齐备、动作又还来得及的位置。** 早一步信息不全，晚一步无法挽回。

### 一个更有用的例子

只打日志看不出 Hook 的价值。假设你有三个真实需求：

| 需求 | 不用 Hook 会怎样 | 用 Hook 挂在哪 |
| --- | --- | --- |
| 每次运行都提醒模型核对验收标准 | 改 system prompt，但那是静态前缀，改了就破坏 KV Cache | `UserPromptSubmit` 返回 `additional_context` |
| 禁止写某个由构建脚本生成的文件 | 在 `write_file` handler 里加 `if`，工具实现被业务规则污染 | `PreToolUse` 返回 `blocking_error` |
| 工具输出太长就截断 | 在每个工具的 handler 里各写一遍截断逻辑 | `PostToolUse` 返回 `updated_output` |

三个需求，三个不同位置，**Agent Loop 一行不改，工具实现一行不改。** 这就是本章要换来的东西。

---

## 先定义验收结果

P04 必须产生以下可观察行为：

| 编号 | 验收标准 | 主要事件 | 对应测试 |
| --- | --- | --- | --- |
| 1 | 只支持 `UserPromptSubmit`、`PreToolUse`、`PostToolUse` 和 `Stop` 四类事件 | 全部 | `test_hooks.py` |
| 2 | 同一事件的同步、异步回调都严格按注册顺序运行 | 全部 | `test_hooks.py` |
| 3 | Pre Hook 可以补上下文、改写合法输入、提出权限建议或返回结构化阻断错误 | Pre | `test_hooks.py`、`test_ch04_integration.py` |
| 4 | Post Hook 可以按顺序观察并改写前一个 Hook 的输出 | Post | `test_hooks.py` |
| 5 | 系统 `deny` 的优先级始终高于 Hook `allow` | Pre | `test_ch04_integration.py` |
| 6 | Pre 阻断、Hook 异常和非法 Hook 更新都要保留原 `tool_call_id` 配对 | Pre | `test_ch04_integration.py` |
| 7 | Post 请求停止后，同一 assistant 消息里尚未执行的调用也必须得到明确错误结果 | Post | `test_ch04_integration.py` |
| 8 | Stop Hook 第一次可以强制续跑，第二次仍执行回调，但不能再次续跑 | Stop | `test_hooks.py`、`test_ch04_integration.py` |
| 9 | P04 只比 P03 增加 `hooks` capability，前 3 章行为保持不变 | 全部 | `test_ch04_integration.py` |

直接测试位于 `python/ch04_agent/tests/test_hooks.py` 和 `python/ch04_agent/tests/test_ch04_integration.py`。

---

## Agent Loop 为什么需要扩展点

第 3 章的循环已经包含模型调用、工具准备、权限和执行。把细节压缩后，结构大致是：

```python
for turn in range(1, self._max_turns + 1):
    reply = self._model.complete(request)
    self._history.append(reply.message)

    if not reply.message.tool_calls:
        return self._complete(reply.message.content, turn)

    for call in reply.message.tool_calls:
        prepared = tools.prepare(call)
        if prepared.error is not None:
            result = prepared.error
        else:
            decision = self._permission_policy.decide(
                PermissionRequest(prepared, context)
            )
            result = (
                tools.invoke(prepared, context)
                if decision.is_allowed
                else decision.to_tool_result()
            )
        self._history.append(tool_message(result.content, call.id))
```

这个循环应该继续只负责确定性编排。日志、通知、额外上下文和完成检查不是新的核心流程，它们需要的是固定挂载点。

Hook 的作用就是：在确定的位置发布结构化上下文，让已注册回调按顺序响应。

---

## 四个事件覆盖一次完整交互

`agent_ch04/core/hooks.py` 用一个不可变 `tuple` 定义运行时事件集合，并用 `Literal` 限制类型：

```python
HookEvent = Literal["UserPromptSubmit", "PreToolUse", "PostToolUse", "Stop"]

HOOK_EVENTS: tuple[HookEvent, ...] = (
    "UserPromptSubmit",
    "PreToolUse",
    "PostToolUse",
    "Stop",
)
```

四个事件的位置固定：

```text
用户提交 prompt
        ↓
UserPromptSubmit
        ↓
模型返回 tool_calls
        ↓
ToolRegistry.prepare
        ↓
PreToolUse
        ↓
PermissionPolicy / approval
        ↓
handler
        ↓
PostToolUse
        ↓
模型返回无工具回答
        ↓
Stop
```

它们不是四个随意命名的回调。每个事件都有自己的上下文和允许返回的字段。

---

## HookContext：不同事件只拿到自己需要的数据

`HookContext` 是冻结 dataclass，核心字段如下。Python 中 `None` 类似 Java 的 `null`，但 `__post_init__` 会按事件检查哪些字段必须为空：

```python
@dataclass(frozen=True, slots=True)
class HookContext:
    event: HookEvent
    message: ChatMessage | None = None
    prepared: PreparedToolCall | None = None
    result: ToolResult | None = None
    history: tuple[ChatMessage, ...] = ()
    stop_hook_active: bool = False
```

字段和事件一一对应：

| 事件 | 可用字段 | 不能出现的字段 |
| --- | --- | --- |
| `UserPromptSubmit` | 一条 user `message` | prepared、result、history、stop 状态 |
| `PreToolUse` | 已校验的 `prepared` | message、result、history、stop 状态 |
| `PostToolUse` | `prepared` 和 `result` | message、history、stop 状态 |
| `Stop` | `history`、`stop_hook_active` | message、prepared、result |

运行时会验证这些规则。比如给 Pre Hook 上下文塞一条 user message，会立即得到 `HookContractError`（`PreToolUse 收到了其他事件的字段`），而不是让回调猜测该使用哪个字段。`test_runtime_contract_rejects_cross_event_fields_and_orphan_messages` 专门验证了这些拒绝分支。

Pre 和 Post 只接收已经通过工具查找、JSON 解析和 Python 参数校验的 `PreparedToolCall`。未知工具和非法参数不会进入 Hook。

---

## HookResult：不用字符串暗号表达控制流

一个 Hook 可能只观察，也可能改写或阻断。用裸字符串返回值会很快失控：`"stop"` 到底是拒绝工具、结束当前轮，还是要求模型再回答一次？

把这个问题摊开看，一个 `"stop"` 至少有四种可能的意思：

```text
"stop" =? 这次工具调用不要执行     （对应 blocking_error）
      =? 执行，但本轮别再问模型了  （对应 prevent_continuation）
      =? 整个 run() 立刻失败退出
      =? 别让 Stop Hook 续跑了
```

四种语义的副作用完全不同，而字符串本身没有任何地方能表达「我到底是哪一种」。更糟的是它无法校验：`"stpo"` 拼错了，运行时只会当成「未知返回值」忽略掉——**一个本该阻断危险写入的 Hook 静默失效了**，而你什么都看不到。

本章使用明确的 `HookResult`：

```python
@dataclass(frozen=True, slots=True)
class HookResult:
    permission_behavior: PermissionBehavior = "passthrough"
    updated_input: PreparedToolCall | None = None
    updated_output: ToolResult | None = None
    additional_context: tuple[SystemMessage, ...] = ()
    blocking_error: ToolResult | None = None
    prevent_continuation: bool = False
    force_continue: UserMessage | None = None
```

每个字段的语义只有一个：

| 字段 | 含义 | 允许事件 |
| --- | --- | --- |
| `permission_behavior` | 向权限策略提交建议 | Pre |
| `updated_input` | 替换当前调用的已验证输入 | Pre |
| `blocking_error` | 在权限和 handler 前阻断 | Pre |
| `updated_output` | 替换工具结果 | Post |
| `prevent_continuation` | 正常结束当前工具轮 | Post |
| `force_continue` | 追加一条 user 消息并再请求模型 | Stop |
| `additional_context` | 延后写入 system 上下文 | 四类事件 |

错误事件使用不属于自己的字段时，`validate_for()` 会列出非法字段并拒绝执行：

```python
invalid.append("updated_output")  # …
raise HookContractError(f"{event} HookResult 不允许字段: {', '.join(invalid)}")
```

这条校验值得多说一句。假设有人在 `PostToolUse` 里返回了 `blocking_error`——按字面意思是「阻断这次调用」，可 handler 早就跑完了，副作用已经发生。**这时候「阻断」是个不可能完成的请求。** 与其悄悄忽略它（Hook 作者会一直以为自己拦住了），不如立刻报错让他改到 `PreToolUse` 去。

`additional_context` 只接受 system message。这样 Hook 无法注入孤儿 tool result，也无法伪造带 `tool_calls` 的 assistant message 来破坏 OpenAI 消息配对。

三种被禁止的注入分别会造成什么：

| 如果允许 Hook 注入 | 后果 |
| --- | --- |
| `tool` 消息 | `validate_tool_pairing()` 抛「工具结果找不到对应的调用 ID」，会话直接废掉 |
| 带 `tool_calls` 的 `assistant` 消息 | 模型会以为自己请求过这些工具，但永远等不到结果 |
| `user` 消息 | 模型无法区分「真实用户说的」和「Hook 塞的」，指令来源被伪造 |

**只允许 system 是这三种风险的最小交集**：system 消息不参与工具配对，也不会被误认为用户意图。

另外注意「延后写入」这个词。工具阶段产生的 `additional_context` 不会立刻 append，而是攒到 `deferred_context` 里，等**所有** tool result 都写完才追加。原因还是配对：

```text
❌ 立刻写入
   assistant(tool_calls: call-1, call-2)
   tool(call-1, ...)
   system("Hook 加的上下文")     ← 插在两个 tool result 之间
   tool(call-2, ...)
   → 结构被破坏

✅ 延后写入
   assistant(tool_calls: call-1, call-2)
   tool(call-1, ...)
   tool(call-2, ...)
   system("Hook 加的上下文")     ← 配对完成之后才追加
```

通过校验后，`HookResult.__post_init__` 会把 `updated_output` 和 `blocking_error` 通过 `copy_tool_result()` 复制成新对象；`additional_context` 用 `system_message()` 重建，`force_continue` 用 `user_message()` 重建。

Pre 的 `updated_input` 不直接进入注册表。`HookRegistry._normalize_input()` 先用原 `ToolDefinition.validator` 重新校验，再通过 `copy_prepared_tool_call()` 创建参数副本并递归冻结（`_freeze_json` 用 `MappingProxyType` 包装 dict、把 list 转成 tuple）。

因此，即使回调保留了原对象引用，并在之后修改这个引用，也不会污染注册表结果或 Agent 历史；审批与 handler 只能看到同一个不可变副本。

---

## HookRegistry：注册顺序就是执行顺序

注册表为每个事件保存独立回调列表：

```python
hooks = HookRegistry()

def log_prompt(context: HookContext) -> HookResult:
    print(f"[Hook] {context.event}", file=sys.stderr)
    return HookResult()

hooks.register("UserPromptSubmit", log_prompt)
```

回调可以同步返回，也可以由 `async def` 返回 awaitable。`run()` 会用 `inspect.isawaitable` 判断结果，并逐个 `await`，因此两种形式都保持注册顺序：

```python
for callback in self._callbacks[context.event]:
    outcome = callback(current)
    if inspect.isawaitable(outcome):
        outcome = await outcome
    if not isinstance(outcome, HookResult):
        raise HookContractError(
            f"{context.event} hook callback 必须返回 HookResult"
        )
    outcome.validate_for(context.event)
    # 合并结果，并把改写后的输入或输出传给下一个 Hook。
```

返回普通 object 不算合法 `HookResult`。这项运行时检查很重要，因为回调可能来自配置或插件边界，不能只依赖静态类型标注（Python 运行时不检查它）。

合并时使用以下规则：

- 后一个 `updated_input` 或 `updated_output` 覆盖前一个，并成为再下一个 Hook 看到的值；
- `additional_context` 按注册顺序连接；
- `prevent_continuation` 使用布尔 OR；
- 权限建议继续使用 `deny > ask > allow > passthrough`；
- Pre 遇到 `blocking_error` 立即停止后续 Pre 回调；
- Stop 第一次遇到 `force_continue` 后停止后续 Stop 回调。

六条规则背后是同一个判断：**「后来的意见」和「更保守的意见」哪个优先？** 答案按字段分：

| 字段 | 合并方式 | 为什么 |
| --- | --- | --- |
| `updated_input` / `updated_output` | 后覆盖前 | 这是**流水线加工**。后一个 Hook 看到的就是前一个的产出，覆盖才符合直觉 |
| `additional_context` | 顺序拼接 | 每条上下文都有价值，不该互相取消 |
| `prevent_continuation` | 布尔 OR | 只要有一个要求停，就停。**更保守的赢** |
| `permission_behavior` | `deny > ask > allow > passthrough` | 同上，**更保守的赢**。后一个 `allow` 覆盖不了前一个 `deny` |

用一个具体例子把「后覆盖前」看清楚。三个 Post Hook 依次注册（`test_post_rewrites_chain_and_permission_uses_strictest_value` 的真实断言）：

```text
handler 产出            "原始"
  ↓
Post Hook 1  context.result.content == "原始"
             返回 updated_output: "第一次"
  ↓
Post Hook 2  context.result.content == "第一次"   ← 看到的是上一个的产出
             （测试里的 observed 序列就是 ["原始", "第一次"]）
  ↓
Post Hook 3  返回 updated_output: "第二次"
  ↓
最终写入历史             "第二次"
```

所以注册顺序不只影响日志先后，**它决定了加工链的顺序**。想做「先脱敏、再截断」还是「先截断、再脱敏」，靠的就是注册顺序。

除字段合并外，`run()` 还会在每轮回调后做三件固定的事：Pre 返回 `updated_input` 时，`_normalize_input()` 会重新校验并冻结新参数；`stop_hook_active` 为 true 时，Stop 返回的 `force_continue` 会在合并前被移除；`blocking_error` 或 `force_continue` 一旦产生，就短路剩余回调。

`_merge_results()` 使用 `_stronger_permission()` 按 `deny > ask > allow > passthrough` 合并权限建议（`priority` 字典给四种行为打分 0–3，取分高的），因此后一个 `allow` 不会覆盖前一个 `deny`。

---

## UserPromptSubmit：补充本轮上下文

User Prompt 刚提交、模型还没收到请求时触发这个事件：

```python
def on_prompt(context: HookContext) -> HookResult:
    if not isinstance(context.message, UserMessage):
        raise HookContractError("UserPromptSubmit 上下文不完整")
    print(f"[Hook] prompt: {context.message.content}", file=sys.stderr)
    return HookResult(
        additional_context=(system_message("本轮需要特别核对验收结果。"),)
    )

hooks.register("UserPromptSubmit", on_prompt)
```

`AgentRunner._run()` 先运行 Hook，再按固定顺序写入历史：

```python
submitted = user_message(prompt)
prompt_hook = await self._hooks.run_user_prompt(submitted)
self._history.extend((submitted, *prompt_hook.additional_context))
```

Hook 可以记录输入或增加 system context，但 P04 不允许它替换、删除或阻断原始 user message。

---

## PreToolUse：副作用之前的扩展点

Pre Hook 运行时，工具已经存在，参数也已通过 `ToolDefinition.validator`，但权限尚未判断，handler 尚未运行。

下面的 Hook 阻止写入一个受管理文件：

```python
def block_managed_file(context: HookContext) -> HookResult:
    arguments_value = context.prepared.arguments if context.prepared else None
    if arguments_value is not None and arguments_value.get("path") == "generated.txt":
        return HookResult(
            blocking_error=tool_error("hook_blocked", "generated.txt 由其他系统管理")
        )
    return HookResult()

hooks.register("PreToolUse", block_managed_file)
```

命中 `blocking_error` 后，后续 Pre Hook、权限、handler 和 Post Hook 都不会运行。Loop 仍会把这个错误回填给原调用：

```text
工具执行错误 [hook_blocked]: generated.txt 由其他系统管理
```

Pre Hook 还可以返回 `updated_input`。但它只能改参数，不能换工具身份。注册表会验证：

- `tool_call_id` 不变；
- 工具名不变；
- `ToolDefinition` 必须是同一个对象；
- 新 arguments 仍通过原 `ToolDefinition.validator`；
- prepared call 不能带 error，也不能缺 definition 或 arguments。

锁定 definition 对象同时锁定了 effect、handler 和 validator。否则恶意或有 bug 的 Hook 可以把一个 write 工具伪装成 read，再绕过权限。

这个攻击路径值得具体走一遍：

```text
① 模型请求 write_file{"path":"a.txt","content":"..."}
② prepare 完成，definition.effect === "write"
③ 恶意 Pre Hook 返回 updated_input，其中 definition 换成了 read_file 的定义
   （effect: "read"）
④ 如果注册表不检查 definition 身份：
      工作区硬边界看 effect != "write" → 不表态
      confirm-file-write 规则看 name != "write_file" → 不匹配
      → 最终 allow(default)，一次写操作零审批通过
```

所以「`ToolDefinition` 必须是同一个对象」这条检查用的是**引用相等**（`updated.definition is not original.definition` 就拒绝），不是名字相等——名字可以伪造，对象引用不行。`test_pre_rewrite_cannot_change_trusted_identity_or_schema` 用参数化测试把 id、name、definition、schema 四种篡改逐一断言成 `HookContractError`。

注册表还会冻结准备完成的 definition、arguments 和 prepared 外壳。Hook 不能就地修改原参数；需要改写时必须显式返回新的 `updated_input`。注册表随后用原 validator 重新校验，复制参数并深度冻结规范化副本，审批和 handler 只读取这个副本。

这一步消除了异步审批中的竞态：Hook 即使保留了原对象引用，并在用户审批期间把「值 A」改成「值 B」，权限请求和 handler 仍看到同一个已经冻结的 A，不会出现「批准 A、执行 B」。

竞态的时间线是这样的：

```text
时间 →
Hook:  返回 updated_input{path:"safe.txt"}，同时自己留了个引用 ref
       │                                              │
Loop:  ├─ _freeze_json（MappingProxyType + tuple）→ 副本 A
       ├─ 审批框显示 A：{'path': 'safe.txt'} ─────────┤
       │  （等用户敲 y，可能等好几秒）                  │
       │                                    Hook 这时候偷偷改 ref["path"] = "C:/系统文件"
       ├─ 用户敲 y，批准的是 A                          │
       └─ handler 读取的还是副本 A ✅

如果没有深度冻结：
       └─ handler 读取 ref → 执行的是 "C:/系统文件" ❌ 批准 A 执行 B
```

**「审批看到的」和「执行用到的」必须是同一个不可变值。** 异步审批只要存在等待窗口，这个保证就不能靠约定，只能靠拷贝加冻结。Python 的 `MappingProxyType` 保证了「改 ref 中的 dict」这一步直接抛 `TypeError`（`test_pre_rewrite_flows_to_later_callback_and_is_immutable` 断言了这一点）。

---

## Hook allow 只是建议，不是授权

Pre Hook 可以返回：

```python
return HookResult(permission_behavior="allow")
```

Loop 不会因此直接执行工具，而是把它转换为结构化建议：

```python
recommendations = (
    PermissionDecision(
        hook.permission_behavior,
        f"PreToolUse Hook 建议 {hook.permission_behavior}",
        "pre-tool-hook",
    ),
)

decision = permission_policy.decide(
    PermissionRequest(effective, context, recommendations)
)
```

第 3 章的工作区硬边界、Shell 默认审批和固定规则仍一起参与合并。只要任一系统参与方给出 `deny`，Hook 的 `allow` 就不会触发审批或 handler。

这条顺序不能交换：

```text
Pre Hook → hard permission policy → approval → handler
```

Hook 是扩展机制，不是第二套权限系统。

Claude Code 官方同样强调这点（https://code.claude.com/docs/en/hooks-guide.md）：Hook 可以拒绝工具调用，但保持沉默并不会自动批准调用；没有返回决策时，调用仍走正常权限流程。

P04 的 `passthrough` 正是这个语义：Hook 不产生任何建议，工具是否执行完全由 `PermissionPolicy` 决定。

---

## PostToolUse：串联观察和结果改写

Post Hook 在 handler 完成后、tool message 写入历史前运行：

```python
def truncate_output(context: HookContext) -> HookResult:
    result = context.result
    if result is None or result.is_error or len(result.content) <= 100_000:
        return HookResult()
    return HookResult(
        updated_output=tool_success(result.content[:100_000] + "\n[输出已截断]")
    )

hooks.register("PostToolUse", truncate_output)
```

如果第一个 Post Hook 把 `original` 改成 `第一次`，第二个 Hook 的 `context.result` 看到的就是 `第一次`。注册顺序因此也是改写顺序。

handler 或权限拒绝时不会错误地运行 Post Hook：

- handler 成功或返回结构化工具错误：运行 Post；
- Pre 阻断：不运行 Post；
- permission deny：不运行 Post；
- Pre Hook 异常：不运行 Post。

---

## prevent_continuation 也必须保持消息配对

Post Hook 可以用 `prevent_continuation=True` 表示当前结果已经足够，本轮不再请求模型：

```python
return HookResult(prevent_continuation=True)
```

困难在于，一个 assistant message 可能同时包含多个 tool call。假设第一个调用的 Post Hook 要求停止，Loop 不能直接 return，否则其余调用会成为孤儿。

「孤儿」具体长什么样：

```text
❌ 直接 return
   assistant(tool_calls: call-1, call-2, call-3)
   tool(call-1, "结果")
   ← 这里 return 了，call-2 和 call-3 没有结果
   → 这段历史如果再发给模型，OpenAI 会拒绝；
     validate_tool_pairing() 也会抛 以下工具调用缺少返回结果: ['call-2', 'call-3']
```

正确处理是：

```text
call-1 → 正常执行并触发停止
call-2 → 工具执行错误 [hook_stopped_continuation]: PostToolUse 已要求停止，当前调用未执行
call-3 → 工具执行错误 [hook_stopped_continuation]: PostToolUse 已要求停止，当前调用未执行
先写入全部三个 tool result
再以 call-1 的结果正常结束
```

注意 call-2 和 call-3 **不是被执行后失败**，而是**根本没被执行**。`AgentRunner._run()` 里那个 `stopped_result_index` 一旦被设置，后续调用直接跳过 `_execute_tool()`：

```python
for call in assistant.tool_calls:
    if stopped_result_index is not None:
        result = tool_error("hook_stopped_continuation", "PostToolUse 已要求停止，当前调用未执行")
    else:
        execution = await self._execute_tool(call, context, snapshot)
        ...
```

于是有两条保证同时成立：**后续工具的副作用没有发生**，而且**消息结构仍然完整**。

停止流程不能以破坏协议为代价。

一句话记住这一节：**「提前结束」是业务需求，「消息配对」是协议约束。业务需求要在协议约束内实现，不能反过来。**

---

## Stop：最多强制续跑一次

当模型返回一条没有工具调用的 assistant message 时，Loop 准备结束，此时运行 Stop Hook：

```python
def check_completion(context: HookContext) -> HookResult:
    tool_count = sum(message.role == "tool" for message in context.history)
    print(f"[Hook] 本次会话共执行 {tool_count} 次工具调用", file=sys.stderr)
    if tool_count == 0:
        return HookResult(force_continue=user_message("请先用工具核对结果。"))
    return HookResult()

hooks.register("Stop", check_completion)
```

第一次 `force_continue` 会把明确的 user message 追加到历史，然后再次请求模型：

```python
stop_hook = await self._hooks.run_stop(self._history, stop_hook_active)
if stop_hook.force_continue is not None:
    self._history.extend((*stop_hook.additional_context, stop_hook.force_continue))
    stop_hook_active = True
    continue
```

第二次模型停止时，Hook 仍然执行，并看到 `stop_hook_active is True`。即使回调再次返回 `force_continue`，注册表也会移除这项请求。Loop 使用第二次回答结束。

因此一次 `run()` 最多增加一个模型 turn，不会形成 Stop Hook 死循环；生命周期日志仍能完整记录两次 Stop。

### 为什么必须限制成一次

上面那个示例 Hook 的逻辑是「工具调用数为 0 就要求模型先用工具」。如果不限次数，遇到一个固执的模型会怎样：

```text
turn 1  模型：「我认为不需要用工具，答案是 X」
        Stop Hook：tool_count === 0 → force_continue("请先用工具核对结果。")
turn 2  模型：「我还是认为不需要用工具，答案是 X」
        Stop Hook：tool_count 仍然是 0 → force_continue(...)
turn 3  同上
...
turn 20 AgentLimitError（撞上 max_turns 才停）
```

这条路径没有语法错误，每一步都「合理」，结果是烧掉 20 轮模型调用换来一个 `AgentLimitError`。`max_turns` 确实兜住了，但代价太高——**兜底机制不该被当成正常退出路径。**

`stop_hook_active` 把它压到最多两轮：

```text
turn N    模型停止 → Stop Hook（stop_hook_active: False）→ force_continue → 续跑
turn N+1  模型停止 → Stop Hook（stop_hook_active: True）→ force_continue 被移除 → 结束
```

有两个细节容易搞错：

| 细节 | 实际行为 |
| --- | --- |
| 第二次还会调用 Stop 回调吗 | **会。** 只是它的 `force_continue` 被丢弃 |
| 回调能看出自己被限制了吗 | **能。** `context.stop_hook_active is True` |

第二次仍然执行回调是刻意的：日志、指标、清理这些副作用不该因为「不能续跑了」就被跳过。**限制的是控制权，不是执行权。** `test_stop_can_force_only_one_continuation_but_callback_runs_twice` 把这两个断言钉在一起：`force_continue` 第二次返回 `None`，但 `states == [False, True]` 证明回调跑了两次。

---

## Hook 故障如何返回

扩展代码同样可能出错。工具阶段不能因为 Hook 抛异常而遗留未配对调用。

本章固定映射如下（Python 版源码 `_execute_tool` 的真实分支）：

| 故障 | 工具结果 | handler 是否已执行 |
| --- | --- | --- |
| `prepare()` 自身抛异常 | `tool_preparation_error`（「工具准备失败」） | 否 |
| Pre 返回非法输入更新 | `hook_contract_error`（「PreToolUse Hook 返回了非法更新」） | 否 |
| Pre 抛出其他异常 | `hook_execution_error`（「PreToolUse Hook 执行失败」） | 否 |
| 权限评估或审计异常 | `permission_evaluation_error`（「权限评估失败」） | 否 |
| Post 抛出异常 | `hook_execution_error`（「PostToolUse Hook 执行失败」） | **是** |

最后一行的「是」是这张表的重点。Post 发生在 handler 之后，所以它失败时不能声称副作用没有发生。返回稳定错误并保留配对，让模型和调用方能看到这次生命周期失败。

把两端对比一下就清楚了：

```text
Pre Hook 抛异常
  → handler 没跑，磁盘没变化，返回 hook_execution_error
  → 模型可以放心重试（重试是安全的）

Post Hook 抛异常
  → handler 跑过了，文件已经写了，返回 hook_execution_error
  → 模型不该盲目重试（可能重复写入）
```

两种情况错误码相同，但**语义上的可重试性完全不同**。P04 没有为此区分错误码——这是一处刻意的教学简化，代价是模型无法从错误码判断副作用有没有发生。真正需要这个区分的是幂等性设计，第 11 章讨论恢复策略时才会正面处理。

还有一处不对称值得注意：`hook_contract_error` 只出现在 Pre，不出现在 Post。原因在源码里——只有 Pre 的 `updated_input` 会走 `_normalize_input()` 那套严格校验（工具身份、validator 重校验），Post 的 `updated_output` 只要是个合法 `ToolResult` 就行。**Pre 改的是「即将执行什么」，Post 改的是「已经发生的事怎么描述」，前者必须更严。**

UserPromptSubmit 和 Stop 发生在工具配对之外；它们的非法结果会让当前 `run()` 显式失败，不会静默忽略扩展错误。

这也是一处有意的差异：

| 阶段 | Hook 故障怎么处理 | 为什么 |
| --- | --- | --- |
| 工具阶段（Pre / Post） | 转成配对的工具错误，循环继续 | 不转的话消息结构会坏掉，代价太大 |
| 非工具阶段（Submit / Stop） | 直接让 `run()` 抛错 | 这里没有配对约束，失败就该显式失败 |

---

## P03 与 P04 的准确差异

P04 是在 P03 固定能力上增加 `hooks`：

```python
P04 = ChapterProfile(
    4,
    frozenset({
        "loop", "powershell", "tool_registry", "files", "policy", "hooks"
    }),
)
```

组合根只允许 P04 或后续 profile 注入 `HookRegistry`：

```python
if hooks is not None and profile is not P04:
    raise ValueError("Hook 需要第四章或更高章节")
```

真实 P04 CLI 注册四个只记录生命周期的默认 Hook：

```text
[Hook] 触发事件: UserPromptSubmit
[Hook] 触发事件: PreToolUse
[Hook] 触发事件: PostToolUse
[Hook] 触发事件: Stop
```

它们不修改参数、不覆盖输出，也不授予权限。若模型选择 `shell` 或文件写入，仍执行第 3 章的显式审批。

新手最容易问的一句是：「加了 Hook，第 3 章那套权限还算不算数？」下面这张表逐项回答。

| 维度 | P03 | P04 | 变了吗 |
| --- | --- | --- | --- |
| Agent Loop 的循环结构 | 请求 → 工具 → 追加 → 再请求 | 完全相同 | **没变** |
| 工具查找、validator、prepare | 一样 | 一样 | **没变** |
| 权限四态与合并优先级 | `deny > ask > allow > passthrough` | 完全相同 | **没变** |
| 工作区硬边界 | 在规则之前跑 | 位置不动 | **没变** |
| 审批与审计 | 合并后、执行前 | 位置不动 | **没变** |
| `validate_tool_pairing()` | 每轮都校验 | 每轮都校验 | **没变** |
| 循环里的固定挂载点 | 无 | 四个 | **新增** |
| 权限候选来源 | 规则 + 硬边界 + 审批 | 再多一路 Hook 建议 | **新增** |
| 参数改写能力 | 无 | `updated_input`（要重校验） | **新增** |
| 结果改写能力 | 无 | `updated_output` | **新增** |
| 提前结束能力 | 无 | `prevent_continuation` | **新增** |
| 强制续跑能力 | 无 | `force_continue`（最多一次） | **新增** |

一句话结论：**P04 只在循环上开了四个口，没有动循环内部的任何裁决顺序。** 这就是「解耦」的具体含义——扩展点增加了，被扩展的东西一行没改。

如果你想亲眼确认这一点，可以对比着跑两条命令：

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch03.cli --prompt "读取 README.md 并概括运行方式"
& ..\.venv\Scripts\python.exe -m agent_ch04.cli --prompt "读取 README.md 并概括运行方式"
```

两次的 `[权限审计]` 行应当一模一样，唯一差别是 P04 多出四行 `[Hook]`。

---

## 组合根与 CLI：Hook 在真实入口中如何接入

`agent_ch04/bootstrap.py` 是组合根，负责按 `ChapterProfile` 组装模型、工具、权限策略和 Hook。P04 使用同一个 `build_agent`，只是多了 `hooks` capability：

```python
def build_agent(profile, model, workspace, *, hooks=None, approval_provider=None, ...):
    if profile not in (P01, P02, P03, P04):
        raise ValueError("必须传入固定的章节配置对象")
    if hooks is not None and profile is not P04:
        raise ValueError("Hook 需要第四章或更高章节")
    # 这里继续装配工具和第三章 PermissionPolicy。
    return AgentRunner(model, tools, SYSTEM_PROMPT, workspace,
                       permission_policy=policy, hooks=hooks)
```

这段代码把「章节能力」变成运行时约束：P01–P03 即使有人错误地传入 `HookRegistry`，也会在创建 Agent 前直接失败。

`AgentRunner` 构造函数里还有一行值得单独讲，它决定了「不传 Hook」和「只传 Hook」这两种情况怎么办：

```python
self._permission_policy = PermissionPolicy() if permission_policy is None and hooks is not None else permission_policy
self._hooks = hooks or HookRegistry()
```

读懂这两行，就读懂了 Hook 为什么能「零配置接入」：

| 你传了什么 | `_hooks` | `_permission_policy` | 后果 |
| --- | --- | --- | --- |
| 都不传（P01/P02 的样子） | 空 `HookRegistry` | `None` | 循环里的 Hook 调用全部命中空队列，等于没有；权限阶段走旧授权器 |
| 只传 `permission_policy`（P03） | 空 `HookRegistry` | 你传的 | 第 3 章的行为 |
| 都传（P04） | 你传的 | 你传的 | 本章的完整行为 |
| **只传 `hooks`，不传 policy** | 你传的 | **自动补一个空 `PermissionPolicy()`** | Hook 返回的 `permission_behavior` 仍然要经过一次真实的权限评估 |

第四行是关键。假设没有这行兜底，一个只注册了 Hook、没有配置权限策略的 Agent 会怎样？Hook 返回 `permission_behavior: "deny"` 时，因为没有策略对象，这条建议**根本没有地方被处理**，工具就直接执行了——Hook 的拒绝会被静默丢掉。

补一个空 `PermissionPolicy()` 之后，语义变得确定：

```text
空 PermissionPolicy = 没有规则、没有审批器、没有硬边界
  ↓
默认候选是 allow（"没有权限规则阻止请求"）
  ↓
Hook 的 deny 参与合并 → deny > allow → 拒绝生效
Hook 的 ask 参与合并 → 没有审批器 → fail closed → 拒绝
```

**扩展点不能因为「没配置另一个模块」而失效。** 这条原则在第 3 章表现为 fail closed，在这里表现为「宁可自动补一个空策略，也不让 Hook 的建议无处落地」。

真实 CLI 的 `terminal_hooks()` 位于 `agent_ch04/cli.py`，它注册四个只输出 `stderr` 的观察型回调：

```python
def terminal_hooks() -> HookRegistry:
    hooks = HookRegistry()
    hooks.register("UserPromptSubmit", lambda context: _log_hook(context.event))
    hooks.register("PreToolUse", lambda context: _log_hook(context.event))
    hooks.register("PostToolUse", lambda context: _log_hook(context.event))
    hooks.register("Stop", lambda context: _log_hook(context.event))
    return hooks

def _log_hook(event: str) -> HookResult:
    print(f"[Hook] 触发事件: {event}", file=sys.stderr)
    return HookResult()
```

每个回调都返回空 `HookResult`，因此默认运行只增加生命周期日志，不改变参数、结果或权限建议。Python CLI 统一用 `print(..., file=sys.stderr)` 输出 Hook 日志，`stdout` 只写最终答案。

---

## 当前边界与刻意不做的事

本章只实现四个当前确实使用的事件：`UserPromptSubmit`、`PreToolUse`、`PostToolUse`、`Stop`，没有提前增加子 Agent、压缩、任务或 MCP 专属事件。

Claude Code 官方目前定义了 14 类以上 Hook 事件，例如 `PermissionRequest`、`SessionStart`、`SubagentStart`、`PreCompact`、`Elicitation` 等，完整清单参见 https://code.claude.com/docs/en/hooks.md。第 4 章的事件集合是教学子集。

Hook 回调在当前进程内按顺序执行。P04 不提供插件隔离、独立超时或并行 Hook；并行会让改写顺序和短路语义变得含混。

与 Claude Code 官方实现的一项差异：官方将匹配的所有同类 Hook 执行完成后合并结果（合并优先级 `deny > defer > ask > allow`），而 P04 在 `blocking_error` 或 Stop 的 `force_continue` 时短路，提前停止后续回调。

这是教学简化：阻断错误意味着后续回调对该工具已无意义，而且短路位置明确。并发场景下如果需要所有回调的聚合结果，可以自行扩展。

Hook 也不是沙箱。它是由应用注册的受信任扩展代码，可以抛异常或执行自己的副作用。结构化契约保护 Agent Loop 的状态和权限顺序，不提供操作系统隔离。

这句话很容易被读轻，所以说清楚它的确切含义：

```text
Hook 契约管得住的：
  ✅ 不能把 write_file 换成 read_file 执行（definition 引用相等）
  ✅ 不能让参数和被批准的内容不一致（重校验 + 深度冻结）
  ✅ 不能绕过 deny（合并优先级）
  ✅ 不能破坏 tool_call_id 配对（阻断也补齐结果）
  ✅ 不能无限续跑（stop_hook_active）

Hook 契约管不住的：
  ❌ Hook 回调里自己 open('C:/anything', 'w')
  ❌ Hook 回调里自己发 HTTP 请求
  ❌ Hook 回调里读环境变量、读密钥
```

原因很直白：Hook 是你自己写的、和 Loop 跑在同一个进程里的普通函数。**契约保护的是「Loop 的状态和裁决顺序不被扩展代码搞坏」，不是「扩展代码本身被限制能做什么」。** 前者是本章的目标，后者需要进程隔离或操作系统级沙箱，不在教学范围内。

换句话说：模型是不受信的，所以工具调用要过权限；Hook 是受信的，所以只需要过契约。搞混这两者，会得出「加了 Hook 就有沙箱了」这种危险结论。

---

## 配套代码地图

为了让读者能直接定位源码，本章把 `python/ch04_agent/agent_ch04/` 的全部模块列成一张覆盖表：

| 文件 | 职责 | 讲解位置 |
| --- | --- | --- |
| `core/hooks.py` | Hook 事件、上下文、结果与注册表 | 本章核心实现 |
| `core/loop.py` | Agent Loop 和四个 Hook 挂载点 | 本章核心实现 |
| `core/profiles.py` | P01–P04 固定能力快照 | 上文「P03 与 P04 的准确差异」 |
| `bootstrap.py` | 按 profile 组装基础设施、权限和 Hook | 上文「组合根与 CLI」 |
| `cli.py` | 终端入口、人工审批和默认观察 Hook | 上文「组合根与 CLI」 |
| `config.py` | 共享 `python/.env` 模型配置校验 | 下文「运行第 4 章」 |
| `core/messages.py` | 消息模型、`tool_call_id` 配对契约 | 前文 HookResult 与 prevent_continuation |
| `core/tools.py` | 工具注册、prepare、invoke、递归冻结参数 | 上文「PreToolUse」 |
| `core/permissions.py` | 权限规则、审批、审计与合并 | 上文「Hook allow 只是建议」 |
| `core/filesystem.py`、`core/commands.py`、`core/model.py` | 文件、命令、模型的 Protocol | 第 2-3 章继承契约 |
| `adapters/powershell.py`、`adapters/openai_chat.py`、`adapters/filesystem.py` | PowerShell、OpenAI、文件系统实现 | 第 2-3 章继承实现 |
| `features/builtin_tools.py` | `shell` 与文件工具集 | 第 2 章继承实现 |

这张图也说明了 Hook 的边界：本章没有为每个 adapter 增加回调，而是把扩展点集中在 Loop 的四个固定位置。外部实现差异仍由各自 adapter 收敛。

---

## 从 ai-agent-book 学到什么

`ai-agent-book` 第 4 章讲述的是另一条互补路线：工具分类、MCP 协议和事件驱动异步 Agent。它把「事件触发工具」定义为 Agent 注册、外部触发的一类工具，例如定时器、后台命令监控、外部消息频道；这些工具负责在用户没有主动提问时唤醒 Agent。

P04 的 Hook 不是这套机制。Hook 是 Agent 内部生命周期的拦截点：`UserPromptSubmit`、`PreToolUse`、`PostToolUse` 和 `Stop` 都发生在同一次 `run()` 内部，用来追加日志、改写工具输入输出、建议权限或控制是否续跑。两者可以组合：

```text
外部事件（定时器、Webhook、IM）
        ↓
事件触发工具 → 生成新 prompt
        ↓
UserPromptSubmit → PreToolUse → handler → PostToolUse → Stop
```

---

## 运行第 4 章

### 第 0 步：准备环境

先进入第四章目录。项目复用上一级 `python/.venv` 和 `python/.env`，不需要再次创建虚拟环境或复制密钥文件：

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch04_agent'
& ..\.venv\Scripts\python.exe -m pip install -e ".[dev]"
```

### 第 1 步：先跑离线测试

```powershell
& ..\.venv\Scripts\python.exe -m pytest tests
```

预期最后一行：

```text
39 passed in 3.1s
```

这 10 个测试文件、39 个测试全部离线——本章新增的是 `test_hooks.py`（8 个，其中一个还是 4 组参数化）和 `test_ch04_integration.py`（7 个）。不需要 API Key，不发网络请求。

### 第 2 步：看四个 Hook 各打一行日志

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch04.cli --prompt "读取 README.md 并概括运行方式"
```

预期在 stderr 上依次看到（`[Hook]` 是本章新增的，`[权限审计]` 是第 3 章的）：

```text
[Hook] 触发事件: UserPromptSubmit
[Hook] 触发事件: PreToolUse
[权限审计] read_file: allow (default) - 没有权限规则阻止请求
[Hook] 触发事件: PostToolUse
[Hook] 触发事件: Stop
```

请特别确认 `[Hook] PreToolUse` **在** `[权限审计]` **之前**。这一行顺序就是本章最核心的设计结论：Hook 先发言，权限系统后裁决。

### 第 3 步：让 Hook 和审批同时出现

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch04.cli --prompt "把一句话写入 hook-demo.txt"
```

预期顺序：

```text
[Hook] 触发事件: UserPromptSubmit
[Hook] 触发事件: PreToolUse

工具调用需要批准: write_file
原因: 第三章的文件写入需要明确审批
参数: {'path': 'hook-demo.txt', 'content': '...'}
允许本次调用? [y/N] y

[权限审计] write_file: allow (terminal-approval) - 用户批准了本次工具调用
[Hook] 触发事件: PostToolUse
[Hook] 触发事件: Stop
```

这一步能同时看到两件事：Pre Hook 在审批框之前跑完；Post Hook 在写入完成后才跑。**默认的四个 Hook 只打日志，不会替你批准任何东西**——审批框照样弹。

### 第 4 步（可选）：完整离线门禁

```powershell
& ..\.venv\Scripts\python.exe -m compileall -q agent_ch04
& ..\.venv\Scripts\python.exe -m ruff check agent_ch04 tests
& ..\.venv\Scripts\python.exe -m mypy agent_ch04
```

测试通过 `FakeModel`、结构化权限替身和内存 Hook 验证行为，不读取真实 API Key，也不访问网络。终端审批只有明确输入 `y`/`yes` 才允许；非交互环境默认拒绝。

### 常见报错排查

| 现象 | 原因 | 怎么处理 |
| --- | --- | --- |
| 看不到任何 `[Hook]` 行 | Hook 日志写 stderr，你可能把 stderr 重定向掉了 | 别加 `2>$null`；或者确认你跑的是 `agent_ch04.cli` 而不是 `agent_ch03.cli` |
| `AgentRunner.run` 报「当前线程已有 asyncio 事件循环」 | 你在异步上下文里调了同步入口 | `run()` 会检测正在运行的事件循环；异步代码应直接 `await runner._run(prompt)` 或另起线程 |
| 只有 `PreToolUse` 没有 `PostToolUse` | 调用被 Pre 阻断，或被权限拒绝了 | 正常行为。见「PostToolUse」末尾那四条 |
| `工具执行错误 [hook_contract_error]` | 你写的 Pre Hook 返回的 `updated_input` 不合法 | 检查：`tool_call_id` 变了？工具名变了？definition 换了对象？新参数没过 validator？ |
| `工具执行错误 [hook_execution_error]` | 你写的 Hook 抛异常了 | 看 stderr 里有没有你自己打的日志。注意 Pre 和 Post 共用这个码，得自己判断在哪一端 |
| `工具执行错误 [hook_stopped_continuation]` | 同一轮里前面的调用触发了 `prevent_continuation` | 正常行为。这些调用**没有被执行** |
| 启动就报 `Hook 需要第四章或更高章节` | 给 P01–P03 传了 `HookRegistry` | 换成 P04，或者别传 hooks |
| 自己写的 Hook 返回了普通 dict，报 `必须返回 HookResult` | 返回了 `{"blocking_error": ...}` 字面量 | 必须 `return HookResult(blocking_error=...)`。运行时用 `isinstance` 检查 |
| Stop Hook 里 `force_continue` 第二次没生效 | `stop_hook_active is True` | 正常行为。一次 `run()` 最多续跑一次 |

---

## 验证与实验

`pytest tests` 会执行 10 个测试文件、39 个测试。本章新增的两个是：

| 测试文件 | 验证内容 |
| --- | --- |
| `test_hooks.py` | 单元层：四事件集合、同步/异步注册顺序、Pre 阻断短路、改写流动性与不可变性、四种身份/参数篡改被拒、Post 链式改写与权限取最严、Stop 只续跑一次但回调跑两次、跨事件字段拒绝、只允许 system 上下文 |
| `test_ch04_integration.py` | 端到端：Hook 与权限的优先级、阻断后 handler 未执行、`prevent_continuation` 的配对、Stop 只续跑一次、P01–P03 行为未变 |

其余 8 个是前三章的累计测试，继续保证「加 Hook 没改坏旧行为」——这是验收标准第 9 条的直接体现。

值得单独看的是**验收标准 5**（系统 `deny` 优先于 Hook `allow`）的测试写法。它必须证明的不是「返回了拒绝」，而是「Hook 的 allow 被正确降级成了建议」：

```text
构造：Pre Hook 返回 permission_behavior: "allow"
      + 一个会命中工作区硬边界的越界写入
断言：最终结果是 工具执行错误 [permission_denied]: 禁止写入工作区之外
      而不是 Hook 的 allow 生效
      并且文件确实没被创建
```

这条测试是本章和第 3 章的接缝。**只要它是绿的，「Hook 不是第二套权限系统」就不只是一句设计口号。**

### 四个建议动手做的小实验

**实验一：注册两个 Post Hook，观察加工链**

参考 `test_post_rewrites_chain_and_permission_uses_strictest_value` 的写法，在 `cli.py` 的 `terminal_hooks()` 里追加两个改写型 Post Hook：

```python
hooks.register("PostToolUse", lambda context: HookResult(updated_output=tool_success("AAA")))
hooks.register("PostToolUse", lambda context: HookResult(updated_output=tool_success("BBB")))
```

预期：最终写入历史的是 `BBB`；第二个 Hook 的 `context.result.content` 是 `AAA`。
学到：Post Hook 是流水线，不是并列的观察者。**实验完记得改回去。**

**实验二：让 Pre Hook 阻断一个写入**

```python
def block_demo(context: HookContext) -> HookResult:
    arguments_value = context.prepared.arguments if context.prepared else None
    if arguments_value is not None and arguments_value.get("path") == "blocked.txt":
        return HookResult(blocking_error=tool_error("hook_blocked", "blocked.txt 由其他系统管理"))
    return HookResult()
```

然后跑 `--prompt "把 hello 写入 blocked.txt"`。

预期：**审批框不弹**，看不到 `[Hook] PostToolUse`，也没有 `[权限审计]` 行，模型收到 `工具执行错误 [hook_blocked]: blocked.txt 由其他系统管理`。
学到：`blocking_error` 短路掉了权限、handler 和 Post 三个阶段。**实验完记得改回去。**

**实验三：让 Pre Hook 的 allow 去挑战硬边界**

把上面那个 Hook 改成 `return HookResult(permission_behavior="allow")`，然后跑越界写入：

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch04.cli --prompt "把 hello 写入 ../outside.txt"
```

预期：仍然是 `[权限审计] write_file: deny (workspace-boundary) - 禁止写入工作区之外`。
学到：这就是验收标准 5。Hook 的 `allow` 只是候选决定之一，`deny` 照样赢。**实验完记得改回去。**

**实验四：在 Pre Hook 里就地修改参数**

```python
hooks.register("PreToolUse", lambda context: (
    context.prepared.arguments.__setitem__("path", "hacked.txt")  # 就地改
    or HookResult()                                                # 不返回 updated_input
))
```

预期：抛 `TypeError`（参数已经被 `MappingProxyType` 冻结），写入的还是原路径。
学到：想改参数必须显式返回 `updated_input`，走重校验和重冻结那条路。**实验完记得改回去。**

---

## 本章小结

### 三句话版本

1. Hook 是在四个确定位置发布结构化上下文，让扩展逻辑从核心编排里搬出去。
2. 返回值用结构化 `HookResult` 而不是字符串，因为控制流语义必须能被校验。
3. Hook 能建议、能改写、能阻断，但**不能授权**——系统 `deny` 永远优先。

### 一定要记住的七条

| # | 结论 | 出现在哪一节 |
| ---: | --- | --- |
| 1 | 挂载点选在「信息刚好齐备、动作还来得及」的位置 | 先看一眼真实的执行顺序 |
| 2 | `additional_context` 只能是 system，且延后到配对完成后才写入 | HookResult |
| 3 | `updated_input` 必须重过 validator 并深度冻结，否则会「批准 A 执行 B」 | PreToolUse |
| 4 | definition 用**引用相等**锁定，防止 write 伪装成 read | PreToolUse |
| 5 | 注册顺序 = 执行顺序 = 加工顺序；保守字段例外（取更严的） | HookRegistry |
| 6 | 提前结束也要把同轮剩余调用补成配对错误 | prevent_continuation |
| 7 | Stop 最多续跑一次；第二次回调仍执行，只是控制权被收回 | Stop |

### 本章代码边界（明确「还没做什么」）

| 已经有 | 还没有 | 说明 |
| --- | --- | --- |
| 四个生命周期事件 | 官方 14+ 类事件（`SessionStart`、`PreCompact`、`SubagentStart` 等） | 教学子集；按需在后续章节增加 |
| 顺序执行的进程内回调 | 插件隔离、独立超时、并行执行 | **不做**。并行会让改写顺序和短路语义含混 |
| `blocking_error` / `force_continue` 短路 | 官方那种「跑完全部再合并」 | 刻意简化；短路位置明确 |
| 结构化契约保护 Loop 状态 | 操作系统级沙箱 | **不做**。Hook 是受信任的应用代码 |
| Pre 的 `hook_contract_error` | Post 的等价严格校验 | 刻意不对称，见「Hook 故障如何返回」 |
| 统一的 `hook_execution_error` | 区分「副作用已发生 / 未发生」 | 第 11 章讨论恢复与幂等时处理 |
| `UserPromptSubmit` 追加上下文 | 替换、删除或阻断原始 prompt | **不做**。用户输入不该被扩展代码改写 |

### 检查你是否真的读懂了

1. `[Hook] PreToolUse` 和 `[权限审计]` 哪个先出现？为什么不能反过来？（先看一眼真实的执行顺序）
2. 为什么 `additional_context` 只允许 system 消息？允许 tool 消息会怎样？（HookResult）
3. 一个恶意 Pre Hook 想把 `write_file` 伪装成 `read_file`，哪一条检查拦住了它？（PreToolUse）
4. 三个 Post Hook 依次改写结果，第三个看到的是哪个版本？（HookRegistry）
5. 同一轮有三个调用，第一个触发 `prevent_continuation`，另外两个的 tool result 内容是什么？它们执行了吗？（prevent_continuation）
6. Stop Hook 第二次被调用时，`force_continue` 会生效吗？回调还会执行吗？（Stop）
7. Post Hook 抛异常和 Pre Hook 抛异常，错误码相同，但语义差别在哪？（Hook 故障如何返回）

---

第 4 章最终保留的是一条可验证的固定流水线：

```text
UserPromptSubmit
        ↓
model
        ↓
prepare
        ↓
PreToolUse
        ↓
permission / approval
        ↓
handler
        ↓
PostToolUse
        ↓
paired tool results
        ↓
Stop
```

以后增加日志、通知或结果检查时，只需要注册新的回调。权限优先级、工具身份和 OpenAI 消息配对仍由同一个 Agent Loop 维护。

---

## 小白阅读路线：先把它当成 Spring 事件扩展

如果你是 Java 后端开发，可以先用下面的对应关系建立地图：

| Python 文件/概念 | Java/Spring 类比 | 本章职责 |
| --- | --- | --- |
| `HookContext` | 不可变事件 DTO / record | 告诉回调当前处于哪个生命周期以及允许读取哪些字段 |
| `HookResult` | 事件处理结果 DTO | 声明补充上下文、改写输入、阻断或停止，而不是直接改循环 |
| `HookRegistry` | 事件监听器注册中心 | 按注册顺序串行调用同步或异步回调 |
| `AgentRunner` | Application Service | 在固定位置发布事件并保持工具消息配对 |
| `PermissionPolicy` | Authorization Service | 把 Hook 建议与系统规则合并，`deny` 仍然最高 |
| `bootstrap.py` | Spring `@Configuration` | 只负责装配依赖，不负责业务编排 |

本章第一次出现的 Python 语法也可以这样理解：`Protocol` 类似 Java interface；`dataclass(frozen=True, slots=True)` 类似 Java record；`tuple` 是不可变的 `List`；`async def`/`await` 类似等待 `CompletionStage`；`inspect.isawaitable` 类似 `CompletionStage` 的类型判断；`MappingProxyType` 类似 `Collections.unmodifiableMap`。

推荐先读 `python/ch04_agent/tests/test_hooks.py` 中一个测试，再读 `agent_ch04/core/hooks.py` 对应方法，最后回到 `tests/test_ch04_integration.py` 看它如何接进循环。不要一开始从 `cli.py` 或 OpenAI 适配器读起，那些是基础设施，不是本章的核心。
