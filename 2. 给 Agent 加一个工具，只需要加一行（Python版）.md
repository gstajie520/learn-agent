上一篇，我们跑通了最小 Agent Loop：模型可以请求 `shell`，运行时执行 PowerShell，再把结果按 `tool_call_id` 回填给模型。

但只有 `shell` 还不够。

模型想读文件时，必须自己拼 `Get-Content`；想写文件时，还要处理路径、引号、换行和编码。也就是说，模型的真实意图是“读取这个文件”，程序却把这份意图翻译成一条 PowerShell 命令。翻译越复杂，越容易出错，也会让模型在无关细节上耗费推理。

这一章给 Agent 增加四个专用工具：

- `read_file`：读取 UTF-8 文本；
- `write_file`：写入 UTF-8 文本；
- `edit_file`：精确替换第一处文本；
- `glob`：按模式查找工作区文件。

加完后，P02 一共有五个工具：`shell`、`read_file`、`write_file`、`edit_file`、`glob`。

最关键的是：Agent Loop 不需要改。

本章配套代码位于 `python/ch02_agent/`。它是一个独立的 Python 快照，但复用上一级 `python/.venv` 和 `python/.env`，不会每章重复创建环境。

从上下文工程的视角看，工具定义不是一份独立注册表，而是模型每次调用时实际看到的上下文。API 请求里的 `tools` 字段与 `system` 一起构成静态前缀：只要系统提示词和工具定义不变，服务端就能复用此前缀的 KV Cache，后续轮次只需处理新增的对话历史。

这带来一条硬约束：工具集合确定后，不应每轮动态重排或改写描述。前缀一变，之前缓存的结果就会作废；模型同时也会看到不一致的能力边界。这一章的固定注册顺序、不可变快照和同源参数校验，都是在为这个稳定前缀打基础。

---

## 先定义可观察结果

本章不是“写四个函数”就算完成。最终行为必须满足下面这些条件：

1. P01 仍然只向模型暴露 `shell`；
2. P02 按固定顺序暴露五个工具；
3. 工具参数必须先经过严格校验，再产生副作用；
4. 文件路径只能落在当前 workspace 内；
5. `..`、绝对路径、Windows 设备名和工作区外链接必须失败；
6. 写入使用 UTF-8，并返回真实字节数；
7. 编辑只替换第一处精确文本，找不到时不修改文件；
8. glob 返回稳定排序的 POSIX 风格相对路径；
9. 一条 assistant 消息里的多个工具调用仍要逐个回填，并保持 `tool_call_id` 配对。

### 配套代码结构

本章源码放在 `python/ch02_agent/agent_ch02/`，职责如下：

| Python 文件 | Java 角度的职责 |
| --- | --- |
| `core/tools.py` | 工具注册表、ToolDefinition、参数 prepare、不可变 snapshot、模型 schema 导出 |
| `core/filesystem.py` | `WorkspaceFileSystem` Protocol 与文件领域异常 |
| `core/loop.py` | AgentRunner 核心循环、消息配对、最大轮次、授权边界 |
| `core/messages.py` | 消息对象与 tool_call_id 配对校验 |
| `core/model.py` | ModelClient、ModelReply、OpenAIToolSchema 模型边界 |
| `core/commands.py` | CommandRunner、CommandResult 命令执行边界 |
| `core/profiles.py` | P01/P02 固定能力快照 |
| `features/builtin_tools.py` | shell 与四个文件工具的参数校验、描述、handler、注册函数 |
| `adapters/filesystem.py` | `LocalWorkspaceFileSystem` 与 `safe_path` 路径边界 |
| `adapters/powershell.py` | PowerShell 子进程、超时与输出截断 |
| `adapters/openai_chat.py` | OpenAI/DeepSeek Chat Completions 适配器 |
| `bootstrap.py` | 按 profile 选择工具集并组装 AgentRunner |
| `config.py` | 共享 `.env` 解析和校验 |
| `cli.py` | 第二章命令行入口和终端批准 |

后面的小节会深入工具层、文件适配器和 Agent Loop；`model`、`commands`、`openai_chat`、`config` 和 `cli` 是运行入口与供应商边界。阅读源码时，可以先从 `core/` 的契约看起，再看 `adapters/` 如何实现这些契约。

---

## ToolRegistry：循环只负责分发

如果把工具写死在循环里，第二个工具出现时就会堆条件分支：

```python
# 反例：不要让 Agent Loop 认识具体工具。
if call.name == "shell":
    return run_shell(call.arguments)
if call.name == "read_file":
    return read_file(call.arguments)
```

这种结构的问题不只是代码变长。每增加一个工具，都要修改循环；工具参数解析、错误处理和调用协议也会散落在不同分支里。更好的做法是让循环只认识“一次调用返回一个结果”这种抽象协议，不关心工具具体做什么。

配套实现继续使用第一章的 `ToolRegistry`。循环只处理 `ToolCall`、`PreparedToolCall` 和 `ToolResult`：

```python
for call in assistant.tool_calls:
    prepared = tools.prepare(call)
    result = prepared.error if prepared.error is not None else tools.invoke(prepared, context)
    history.append(tool_message(result.content, call.id))
```

注册表负责四件事：

1. 按名称查找工具；
2. 把模型返回的 JSON 字符串解析成对象；
3. 用每个工具自己的 validator 严格校验参数；
4. 调用与参数校验绑定在同一个 `ToolDefinition` 里的 handler。

未知工具、坏 JSON、错误类型和多余字段都会在 handler 执行前变成结构化错误。因此，非法参数不会先写文件再报错。

所谓“加一个工具，只需要加一行”，准确含义是：工具定义完成后，只需注册这个定义，不必修改 Agent Loop，也不必维护另一张 handler 表。

```python
registry.register(create_read_file_tool(file_system))
```

Java 对照：这和 Spring 中新增一个 `@Bean` 后交给注册表很像；`AgentRunner` 是应用服务，`ToolRegistry` 是按名称查找 Handler 的注册中心。

---

## 先把文件系统做成边界

文件操作属于外部边界。`features` 不应该直接创建 `Path` 或操作系统文件对象，否则业务层会反向依赖具体实现。

因此，`agent_ch02/core/filesystem.py` 只声明运行时需要的能力：

```python
class WorkspaceFileSystem(Protocol):
    def read_file(self, workspace: str, relative_path: str, limit: int | None = None) -> str: ...
    def write_file(self, workspace: str, relative_path: str, content: str) -> int: ...
    def edit_file(self, workspace: str, relative_path: str, old_text: str, new_text: str) -> None: ...
    def glob_files(self, workspace: str, pattern: str) -> tuple[str, ...]: ...
```

这里的 `Protocol` 就是 Python 版的 Java `interface`。`features/builtin_tools.py` 只依赖它，`bootstrap.py` 才创建 `LocalWorkspaceFileSystem` 并注入。

依赖方向如下：

```text
bootstrap
   ├─ 创建 LocalWorkspaceFileSystem
   └─ 注入 features

features ──依赖──> core/WorkspaceFileSystem
adapters ──实现──> core/WorkspaceFileSystem
```

测试可以注入替身，生产入口可以注入 pathlib 实现，feature 不需要知道二者区别。这就是 Java 后端常说的依赖倒置和构造器注入。

---

## 用 Python validator 定义严格输入

TypeScript 版本使用 Zod；本 Python 版本使用 `ToolDefinition.validator` 保存一个显式参数校验函数。它实现的是同一件事：模型输入先校验，校验失败就返回 `invalid_arguments`，绝不进入文件操作。

四个文件工具的输入规则是：

```python
def _read_args(value: dict[str, Any]) -> bool:
    return (
        set(value) <= {"path", "limit"}
        and isinstance(value.get("path"), str)
        and bool(value["path"])
        and (
            "limit" not in value
            or (
                isinstance(value["limit"], int)
                and not isinstance(value["limit"], bool)
                and value["limit"] > 0
            )
        )
    )
```

这里有几个 Python 初学者需要注意的点：`set(value)` 得到字典的键集合；`T | None` 表示可能没有值；Python 的 `bool` 是 `int` 的子类，所以校验整数时必须额外排除 `True` 和 `False`。

规则分别是：

- `path`、`old_text` 和 `pattern` 不能为空；
- `limit` 必须是大于 0 的整数；
- `true`、`"2"`、`0` 和 `-1` 都不是合法 limit；
- `content` 和 `new_text` 可以是空字符串，因为清空文件或删除一段文本是有效操作；
- 所有 schema 外字段都拒绝。

工具定义、模型描述和运行时 validator 放在一个 `ToolDefinition` 中，避免维护三份容易漂移的契约：

```python
ToolDefinition(
    name="read_file",
    description="读取工作区内的 UTF-8 文本文件，可按行数限制输出。",
    parameters={"type": "object", "required": ["path"]},
    effect="read",
    handler=handler,
    validator=_read_args,
)
```

| 工具 | 输入 | effect | 成功结果 |
| --- | --- | --- | --- |
| `read_file` | `path`, `limit?` | `read` | 文件文本 |
| `write_file` | `path`, `content` | `write` | 写入字节数 |
| `edit_file` | `path`, `old_text`, `new_text` | `write` | 编辑确认 |
| `glob` | `pattern` | `read` | 匹配路径列表 |

`effect` 在第二章只是结构化元数据。第三章会用它参与权限决策。

---

## 描述也是模型上下文

工具的 `description` 和参数描述不是普通注释，而是模型选择工具时实际读到的上下文。描述要说明工具做什么、什么时候用、返回什么，以及边界和失败语义，而不是只写一句“读文件”。

在请求结构上，工具定义位于 API 顶层 `tools` 字段，与 `system` 消息共同构成每轮不变的静态前缀。KV Cache 依赖前缀字节稳定：如果每轮动态重排工具、修改描述，前缀就会变化，之前缓存的中间结果无法复用。`ToolRegistry` 按注册顺序导出工具，`snapshot()` 又禁止中途修改注册表，正是为了保持这个前缀。

P02 的描述把关键决策信息直接放进模型 schema：

- `read_file` 说明 `limit` 语义和 UTF-8 失败行为；
- `edit_file` 说明只替换第一处精确文本、找不到时保持文件不变；
- `glob` 说明返回稳定排序的 `/` 相对路径；
- `shell` 说明需要交互批准，避免模型在文件工具可用时仍去拼 PowerShell 字符串。

可以把描述检查拆成四个问题：做什么、什么时候用、返回什么、失败怎么办。描述不是越长越好，而是让模型在决策点不需要猜测。

---

## safe_path：先守住 workspace

四个工具共享同一条路径边界。公开契约是“工作区相对路径”，所以运行时不会猜测或修复危险输入。

下面这些路径直接拒绝：`../secret.txt`、`nested/../secret.txt`、`C:\\secret.txt`、`/etc/passwd`、`NUL.txt`、`nested/CON.log`、`file:stream` 和 `trailing.`。

`NUL`、`CON`、`COM1` 和 `LPT9` 等名称在 Windows 中可能指向设备，而不是普通文件。冒号可能形成 NTFS Alternate Data Stream，尾随点或空格也会被 Win32 特殊处理。因此 Python 实现即使运行在其他平台，也按 Windows 运行环境拒绝这些名字。

`safe_path` 分两步检查：先做词法检查，拒绝空值、NUL、绝对路径、盘符、`..` 和 Windows 保留组件；再解析已存在父目录的真实路径，检查 junction 或符号链接没有把目标带出 workspace。

```python
def safe_path(workspace: str, relative_path: str) -> Path:
    root = _workspace_root(workspace)
    parts = _relative_parts(relative_path, "路径")
    target = root.joinpath(*parts)
    # 对已经存在的父目录调用 resolve，检查 junction/符号链接的真实位置。
    existing = target
    while not existing.exists() and existing != root:
        existing = existing.parent
    physical_parent = existing.resolve(strict=True)
    candidate = physical_parent.joinpath(*target.relative_to(existing).parts).resolve()
    candidate.relative_to(root)
    return candidate
```

如果 `workspace/escape` 是指向工作区外的 junction 或符号链接，那么 `escape/secret.txt` 的文本看似在 workspace 下，真实路径却在外部。解析后的路径不在 root 内时，操作立即失败。

这不是完整沙箱。它只保护四个文件工具。`shell` 的 `cwd` 虽然是 workspace，但 PowerShell 仍能使用绝对路径、访问网络或启动子进程。

---

## read_file：限制上下文体积

`read_file` 使用严格 UTF-8 解码。非法字节不会被悄悄替换成 `�`，而会返回 `invalid_utf8`。读取结果统一换行，并在限制行数时追加剩余行数提示，例如 `one\ntwo\n... (1 more lines)`。

限制读取行数可以避免一个大文件占满模型上下文。`limit` 生效时，尾部会明确标注还剩多少行；不传 `limit` 时则返回完整文件，模型不需要猜测内容是否被截断。

Java 对照：`target.read_bytes().decode("utf-8")` 类似用 `Files.readAllBytes(path)` 后以 `StandardCharsets.UTF_8` 解码；`str.splitlines()` 类似把字符串按行拆开。

---

## write_file：字节数以磁盘内容为准

`write_file` 会先通过 `safe_path`，再创建父目录，将 `content.encode("utf-8")` 写入磁盘，最后返回字节数。返回的是字节数，不是 Python 字符串长度。

例如 `你好` 的字符数是 2，但 UTF-8 实际占 6 字节。工具成功时会返回类似：`已写入 6 个 UTF-8 字节: note.txt`。Java 对照：这和 `content.getBytes(StandardCharsets.UTF_8).length` 的含义一致。

---

## edit_file：只改第一处精确文本

`edit_file` 不接受行号。它要求模型提供旧文本和新文本，并执行 `current.replace(old_text, new_text, 1)`。第三个参数 `1` 表示只替换一次。

这带来三个明确行为：旧文本必须精确存在；只替换第一处；找不到旧文本时不写回文件。模型正确的工作流应当是先读文件，再用读到的精确片段调用 `edit_file`。如果文件已经变化，工具会失败，模型需要重新读取，而不是盲目覆盖。

---

## glob：先看见项目，再决定读什么

模型通常不知道 workspace 里有哪些文件。`glob` 让它先查找，再选择要读取的目标，例如 `{"pattern":"chapters/ch02/src/**/*.ts"}`。

Python 版返回值统一使用 `/`，并稳定排序；没有匹配时返回 `(无匹配文件)`。glob 模式本身也必须是相对路径，不能包含 `..`。遍历时不会递归进入符号链接目录，每个匹配结果还会再次经过 `safe_path` 检查真实路径。

Python 的标准库 `fnmatch` 对 `**/` 的根目录匹配语义和 TypeScript 实现略有差异，因此适配器显式补充了“零层目录”情况，保证 `**/*.txt` 能匹配根目录和嵌套目录文件。

---

## handler：只在工具边界映射预期错误

文件适配器先把底层 `OSError`、严格 UTF-8 解码失败和路径检查失败转换成 core 定义的领域异常。feature 不读取 Python 或 Windows 错误码，因此替换文件系统适配器时，工具层的稳定语义不会变化。

handler 位于工具边界，负责把预期失败转换成模型能理解的稳定错误码。错误码保留英文机器标识，用户和模型可见的说明使用中文，例如：`工具执行错误 [text_not_found]: 文件中找不到精确文本: note.txt`。

| error_code | 含义 |
| --- | --- |
| `path_escape` | 路径违反 workspace 边界 |
| `file_not_found` | 目标文件不存在 |
| `text_not_found` | 精确旧文本不存在 |
| `invalid_path` | 文件与目录类型不符合操作要求 |
| `invalid_utf8` | 文件不是合法 UTF-8 |
| `filesystem_error` | 其他受控文件系统失败 |

`tool_error` 统一产生稳定错误码和中文说明，机器可以读取错误码，学习者也不用先翻译英文日志。

---

## P01 与 P02 只差构建期工具集

`agent_ch02/core/profiles.py` 定义固定能力快照：

```python
P01 = ChapterProfile(1, frozenset({"loop", "powershell"}))
P02 = ChapterProfile(2, frozenset({"loop", "powershell", "tool_registry", "files"}))
```

`frozenset` 是不可变集合，Java 可以把它理解成不会暴露 `add`/`remove` 的只读集合。

bootstrap 根据固定 profile 选择注册表：

```python
command = command_runner or PowerShellRunner()
tools = (
    create_chapter_one_tools(command)
    if profile.chapter == 1
    else create_chapter_two_tools(command, file_system or LocalWorkspaceFileSystem())
)
```

P02 的工具构建也很直接：

```python
registry = create_chapter_one_tools(command_runner)
registry.register(create_read_file_tool(file_system))
registry.register(create_write_file_tool(file_system))
registry.register(create_edit_file_tool(file_system))
registry.register(create_glob_tool(file_system))
```

Agent Loop 每轮仍然从同一个注册表快照生成 tools schema。模型调用、消息历史、最大轮次和结果配对都不需要复制或修改。

---

## 一轮多个工具调用仍然必须配对

模型可以在同一条 assistant 消息里请求多个工具：

```python
assistant_message(None, (
    tool_call("write-1", "write_file", '{"path":"note.txt","content":"value"}'),
    tool_call("read-1", "read_file", '{"path":"note.txt"}'),
))
```

当前实现按顺序执行。结果历史必须是：

```text
assistant(tool_calls: write-1, read-1)
tool(tool_call_id: write-1, 写入结果)
tool(tool_call_id: read-1, 读取结果)
```

不能漏掉失败调用的结果，也不能在两个 tool 结果之间插入新的 user 或 assistant 消息。即使参数无效，也要生成一个与原 ID 配对的错误结果。

串行执行是刻意选择。读写同一文件存在顺序依赖；在没有并发安全契约前，不应为了速度擅自并发。

---

## 第 2 章的权限边界

P02 还没有第三章的完整策略系统。本章保持以下固定行为：

- `shell` 是 `execute`，真实 CLI 每次调用前要求明确输入 `y` 或 `yes`；
- 四个文件工具依靠 `safe_path` 边界直接执行；
- 无交互输入、默认回车或审批异常都会拒绝 `shell`；
- `cwd` 不是沙箱，不能把文件工具的路径保证外推给 PowerShell。

第三章会增加结构化权限规则，让写工具也能进入审批和审计流程，并保证强拒绝不会被弱允许覆盖。

---

## 运行第 2 章

第二章使用共享 Python 环境，不需要新建 `.venv` 或 `.env`：

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch02_agent'
& ..\.venv\Scripts\python.exe -m pip install -e ".[dev]"
```

离线测试：

```powershell
& ..\.venv\Scripts\python.exe -m pytest tests
& ..\.venv\Scripts\python.exe -m compileall -q agent_ch02
& ..\.venv\Scripts\python.exe -m ruff check agent_ch02 tests
& ..\.venv\Scripts\python.exe -m mypy agent_ch02
```

真实运行前，在共享的 `python/.env` 中配置 `OPENAI_BASE_URL`、`OPENAI_API_KEY` 和 `OPENAI_MODEL`：

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch02.cli --prompt "写入 demo/note.txt，内容为 alpha；把第一处 alpha 改成 gamma；读取文件并列出 txt 文件"
```

当前 PowerShell 目录既是 `.env` 向上查找位置，也是 Agent workspace。真实运行会要求批准 `shell`，文件工具会在当前工作区边界内执行。测试不需要 API Key，也不会访问网络。

---

这一章真正增加的不是四个孤立函数，而是一条稳定扩展路径：

```text
参数校验函数
   ↓
ToolDefinition（name + description + effect + handler）
   ↓
ToolRegistry（schema 快照 + 参数准备 + 分发）
   ↓
Agent Loop（保持不变）
```

以后新增工具，先定义明确输入和副作用，再实现 handler，最后注册一次。循环不需要知道工具细节，模型看到的 schema 和运行时执行的 handler 也不会分家。

### 小白阅读路线

不要从 `cli.py` 开始。建议按下面顺序考古：

1. 先看 `tests/test_files.py`，理解路径、UTF-8、写入、编辑和 glob 的可观察结果；
2. 看 `tests/test_ch02_tools.py`，观察模型如何一次提出四个工具调用；
3. 跳到 `features/builtin_tools.py`，看每个 handler 如何映射错误；
4. 看 `core/tools.py` 的 `prepare()` 和 `invoke()`；
5. 看 `adapters/filesystem.py` 的 `safe_path()`，理解真实路径安全边界；
6. 最后回到 `core/loop.py`，确认循环只新增了工具注册表内容，没有新增文件分支。

Python 与 Java 对照：`Protocol` 是 interface，`dataclass(frozen=True, slots=True)` 是不可变 DTO/record，`Path` 是比 `File` 更现代的路径值对象，`with pytest.raises(...)` 类似 JUnit `assertThrows`，`dict` 类似 `Map`，`tuple` 类似不可变 `List`。
