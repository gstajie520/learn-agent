# 给 Agent 加一个工具，只需要加一行（Python 版）

这篇是原第 2 章 TypeScript 文章的 Python 完整迁移版。设计讨论、章节结构和边界说明全部保留；代码、文件路径和运行命令改为 Python，并面向有 Java 后端背景的读者补了语言对照。

配套项目位于 `python/ch02_agent`。

上一篇，我们跑通了最小 Agent Loop：模型可以请求 `shell`，运行时执行 PowerShell，再把结果按 `tool_call_id` 回填给模型。

但只有 `shell` 还不够。

模型想读文件时，必须自己拼 `Get-Content`；想写文件时，还要处理路径、引号、换行和编码。也就是说，模型的真实意图是「读取这个文件」，程序却把这份意图翻译成一条 PowerShell 命令。翻译越复杂，越容易出错，也会让模型在无关细节上耗费推理。

这一章给 Agent 增加四个专用工具：

- `read_file`：读取 UTF-8 文本；
- `write_file`：写入 UTF-8 文本；
- `edit_file`：精确替换第一处文本；
- `glob`：按模式查找工作区文件。

加完后，P02 一共有五个工具：`shell`、`read_file`、`write_file`、`edit_file`、`glob`。

最关键的是：Agent Loop 不需要改。

从上下文工程的视角看，工具定义不是一份独立注册表，而是模型每次调用时实际看到的上下文。API 请求里的 `tools` 字段与 `system` 一起构成了**静态前缀**：只要系统提示词和工具定义不变，服务端就能复用此前缀的 KV Cache，后续轮次只需处理新增的对话历史。

这带来一条硬约束：工具集合确定后，不应每轮动态重排或改写描述。前缀一变，之前缓存的结果就会作废；模型同时也会看到不一致的能力边界。这一章的固定注册顺序、不可变快照和同源 schema，都是在为这个稳定前缀打基础。

---

## 本章导读

### 读完这一章，你能做到什么

1. 独立给 Agent 加一个新工具，并说清为什么不用改 Agent Loop。
2. 写出一份「模型看到的说明」和「运行时校验规则」同源的输入契约（Python 版用 validator 函数 + JSON Schema 字典，对应 TS 版的 Zod）。
3. 说出为什么 `edit_file` 不能接受行号，以及模型正确的编辑工作流是什么。
4. 解释 `safe_path` 的两道检查各拦住什么，并说明为什么第二道必须访问磁盘。
5. 跑通第 2 章，让 Agent 只用文件工具完成一次「写 → 改 → 读 → 找」的完整链路。

### 你需要先具备什么

| 需要 | 程度 |
| --- | --- |
| 第 1 章 | **必须**。要理解 `ToolRegistry`、`ToolResult`、`tool_call_id` 配对和审批边界 |
| Python | 能看懂 `dict`、`isinstance`、`Protocol`；`dataclass` 不熟没关系（正文有 Java 对照） |
| 校验概念 | 知道「严格校验 / 隐式转换」大致是什么 |
| 文件系统概念 | 知道「相对路径 / 绝对路径」「符号链接 / junction」大致是什么 |

### 术语速查（本章第一次出现的词）

| 术语 | 一句话解释 |
| --- | --- |
| **ToolDefinition** | 一个工具的完整声明：名字、描述、参数 schema、副作用类别、handler、validator。六件事写在一个 `@dataclass` 里 |
| **validator** | Python 版的参数校验函数：输入 `dict`，输出「过/不过」。对应 TS 版 Zod 的运行时校验角色 |
| **JSON Schema 字典** | `ToolDefinition.parameters` 字段里手写的 `{"type": "object", ...}`，作为发给模型的说明书 |
| **静态前缀 / KV Cache** | 请求里每轮不变的开头部分（`system` + `tools`）。字节稳定时服务端可复用计算结果，省钱省延迟 |
| **safe_path** | 把「工作区相对路径」翻译成安全绝对路径的函数。翻译不了就拒绝，绝不猜测修复 |
| **junction / 符号链接** | 指向别处的目录「快捷方式」。文本路径看着在工作区内，真实位置可能在外面 |
| **NTFS ADS** | Windows 的 Alternate Data Stream，靠路径里的冒号触发，例如 `file:stream` |
| **Windows 保留名** | `NUL`、`CON`、`COM1`、`LPT9` 等。它们指向设备而不是文件 |
| **effect（副作用类别）** | 工具自我声明的影响范围：`read`、`write`、`execute`。第 2 章的 CLI 已按它分流审批（见正文），第 3 章参与完整决策 |
| **WorkspaceFileSystem** | core 层声明的文件能力接口（Python `Protocol`）。feature 只认这个接口，不认 `pathlib` |
| **profile（P01/P02）** | 章节能力白名单。P02 = P01 + `tool_registry` + `files` |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先定义可观察结果        ← 本章的验收标准，9 条
  ↓
② 一个完整例子            ← 模型怎么用四个工具走完一次任务
  ↓
③ ToolRegistry            ← 「加一行」到底加在哪
  ↓
④ 文件系统边界 + 校验     ← 依赖方向、同源契约、描述也是上下文
  ↓
⑤ safe_path               ← 本章安全性的核心，两道检查
  ↓
⑥ 四个工具逐个看          ← read / write / edit / glob 各自的取舍
  ↓
⑦ 错误映射 + profile      ← 稳定错误码、P01 与 P02 的差别
  ↓
⑧ 配对与权限边界          ← 多工具单轮的硬规则、本章故意留的缺口
  ↓
⑨ 运行、验证与小结        ← 动手 + 排错表 + 自测题
  ↓
⑩ 小白阅读路线            ← Java 视角的源码考古顺序
```

---

## 先定义可观察结果

本章不是「写四个函数」就算完成。最终行为必须满足下面这些条件：

1. P01 仍然只向模型暴露 `shell`；
2. P02 按固定顺序暴露五个工具；
3. 工具参数必须先经过严格校验，再产生副作用；
4. 文件路径只能落在当前 workspace 内；
5. `..`、绝对路径、Windows 设备名和工作区外链接必须失败；
6. 写入使用 UTF-8，并返回真实字节数；
7. 编辑只替换第一处精确文本，找不到时不修改文件；
8. glob 返回稳定排序的 POSIX 风格相对路径；
9. 一条 assistant 消息里的多个工具调用仍要逐个回填，并保持 `tool_call_id` 配对。

### 配套代码结构

本章源码放在 `python/ch02_agent/agent_ch02/`，每个文件职责如下：

| 文件 | Java 角度的职责 |
| --- | --- |
| `core/tools.py` | 工具注册表、ToolDefinition、参数 prepare、不可变 snapshot、OpenAI schema 导出 |
| `core/filesystem.py` | `WorkspaceFileSystem` Protocol 与文件领域异常 |
| `core/loop.py` | AgentRunner 核心循环、消息配对、最大轮次、授权边界 |
| `core/messages.py` | 消息角色联合、不可变工厂函数、tool_call_id 配对校验 |
| `core/model.py` | ModelClient、ModelReply、OpenAIToolSchema 模型边界 |
| `core/commands.py` | CommandRunner、CommandResult 命令执行边界 |
| `core/profiles.py` | P01/P02 固定能力快照与章节入口校验 |
| `features/builtin_tools.py` | shell 与四个文件工具的 schema、描述、effect、validator、handler |
| `adapters/filesystem.py` | `LocalWorkspaceFileSystem` 与 `safe_path` 双重路径边界检查 |
| `adapters/powershell.py` | PowerShellRunner 子进程执行、超时与输出截断 |
| `adapters/openai_chat.py` | OpenAI Chat Completions 适配器、响应归一化 |
| `bootstrap.py` | 按 profile 选择工具集并组装 AgentRunner |
| `config.py` | 共享 `.env` 解析、必需字段校验 |
| `cli.py` | 命令行入口、`TerminalAuthorizer`、错误码退出 |

后面的小节会深入工具层、文件适配器和 Agent Loop；`model`、`commands`、`openai_chat`、`config` 和 `cli` 则是运行入口与供应商边界。阅读源码时，可以先从 `core/` 的契约看起，再看 `adapters/` 如何实现这些契约。

---

## 一个完整例子：四个工具怎么串起来

抽象契约不好记，先看一遍实际发生的事。用户输入：

> 只使用文件工具：写入 `demo/note.txt`，内容为 `alpha`；把第一处 `alpha` 改成 `gamma`；读取文件并用 glob 列出 `demo/*.txt`

模型会自己拆成四步，一步一轮。下面只列每轮的关键字段（第 1 章那套 `tool_call_id` 配对规则完全不变）：

| 轮 | 模型请求的工具 | `arguments` | 回填给模型的结果 |
| ---: | --- | --- | --- |
| 1 | `write_file` | `{"path":"demo/note.txt","content":"alpha"}` | `已写入 5 个 UTF-8 字节: demo/note.txt` |
| 2 | `edit_file` | `{"path":"demo/note.txt","old_text":"alpha","new_text":"gamma"}` | `已编辑文件: demo/note.txt` |
| 3 | `read_file` | `{"path":"demo/note.txt"}` | `gamma` |
| 4 | `glob` | `{"pattern":"demo/*.txt"}` | `demo/note.txt` |
| 5 | （无工具调用） | — | 模型给出最终文本，循环退出 |

注意第 1 轮的返回值是 `5`：`alpha` 五个 ASCII 字符正好 5 字节。换成 `你好` 就会返回 `6`——这是本章「字节数以磁盘内容为准」那一节要讲的事。

配套测试 `test_ch02_tools.py` 的 `test_model_can_write_edit_read_and_glob_in_one_turn` 就是把这张表的四个调用压缩进同一条 assistant 消息里跑的。

### 换成上一章的做法有多难

同样一件事，只有 `shell` 时模型得这么写：

```powershell
Set-Content -Path .\demo\note.txt -Value 'alpha' -Encoding utf8
(Get-Content .\demo\note.txt -Raw) -replace 'alpha','gamma' |
    Set-Content .\demo\note.txt -Encoding utf8 -NoNewline
Get-Content .\demo\note.txt
Get-ChildItem .\demo\*.txt | Select-Object -ExpandProperty Name
```

问题不在于「长」，而在于**每一处都可能悄悄出错**：

| 隐患 | 后果 |
| --- | --- |
| `demo` 目录不存在 | `Set-Content` 直接失败，模型得先想到 `New-Item` |
| 忘了 `-Encoding utf8` | PowerShell 5 默认写 UTF-16，中文变乱码 |
| 用 `-replace` | 那是**正则**替换，`alpha.beta` 里的 `.` 会当成任意字符 |
| 用 `-replace` | 它替换**所有**匹配，不是第一处 |
| 忘了 `-NoNewline` | 文件末尾多出一个换行，下次精确匹配就失败 |
| 内容里有单引号 | 引号转义写错，命令语法直接崩 |

**这就是加专用工具的真实理由**：不是为了少打字，是为了把这些隐患从「模型每次都要想起来」变成「工具默认就对」。`write_file` 自动建父目录、固定 UTF-8；`edit_file` 是精确文本匹配而非正则，且只改第一处。模型的推理预算可以省下来用在任务本身。

### 失败也是完整链路的一部分

模型可能试图访问工作区外的文件。这时不会抛异常打断循环，而是回填一条错误结果：

```text
assistant  tool_calls=[ { id:"call_9", name:"read_file",
                          arguments:'{"path":"../secret.txt"}' } ]
tool       tool_call_id="call_9"
           content="工具执行错误 [path_escape]: 路径逃逸工作区: ../secret.txt"
```

模型看到 `path_escape` 就知道换个路径，而不是卡死。这继承的正是第 1 章那条原则：**工具失败是数据，不是异常**。

---

## ToolRegistry：循环只负责分发

如果把工具写死在循环里，第二个工具出现时就会开始堆条件分支：

```python
# 反例：不要让 Agent Loop 认识具体工具。
if call.name == "shell":
    return run_shell(call.arguments)
if call.name == "read_file":
    return read_file(call.arguments)
```

这种结构的问题不只是代码变长。每增加一个工具，都要修改循环；工具参数解析、错误处理和调用协议也会散落在不同分支里。更好的做法是让循环只认识「一次调用返回一个结果」这种抽象协议，不关心工具具体做什么。

配套实现继续使用第 1 章的 `ToolRegistry`。循环只处理 `ToolCall`、`PreparedToolCall` 和 `ToolResult`：

```python
for call in assistant.tool_calls:
    prepared = tools.prepare(call)
    result = prepared.error if prepared.error is not None else tools.invoke(prepared, context)
    history.append(tool_message(result.content, call.id))
```

注册表负责四件事：

1. 按名称查找工具；
2. 把模型返回的 JSON 字符串 `json.loads` 成 dict；
3. 用每个工具自己的 validator 严格校验参数；
4. 调用与校验绑定在同一个 `ToolDefinition` 里的 handler。

未知工具、坏 JSON、错误类型和多余字段都会在 handler 执行前变成结构化错误。因此，非法参数不会先写文件再报错。

**这个顺序是本章安全性的第一层。** 想象一下反过来：先执行 `write_file`，再校验参数——文件已经被改了，校验失败也来不及了。所以校验必须全部集中在 `prepare()`，而 `prepare()` 保证不产生任何副作用。

所谓「加一个工具，只需要加一行」，准确含义是：工具定义完成后，只需注册这个定义，不必修改 Agent Loop，也不必维护另一张 handler 表。

```python
registry.register(create_read_file_tool(file_system))
```

「加一行」加的是这行 `register`。工具本身当然还要写——但**写在哪里、循环要不要跟着改**，才是架构问题。Java 视角看，这和 Spring 中新增一个 `@Bean` 后交给注册中心很像；`AgentRunner` 是应用服务，`ToolRegistry` 是按名称查找 Handler 的注册中心。对比一下两种做法要动几个地方：

| 需要改动 | 写死在循环里 | 用注册表 |
| --- | --- | --- |
| Agent Loop | 加一个 `if` 分支 | **不动** |
| 参数解析 | 每个分支自己 `json.loads` | 注册表统一做 |
| 参数校验 | 每个分支自己写，容易漏 | validator 绑在定义上，不可能漏 |
| 错误处理 | 每个分支各写一套 | 统一转成 `ToolResult` |
| 给模型的 schema | 手写第二份，容易和实现不一致 | 与 validator 同放在 `ToolDefinition` |
| 新增工具的改动点 | 3～4 处 | 1 处（`register`） |

右边那一列的「不动」是本章最重要的成果。第 3 章到第 20 章会不断往里加工具，如果每次都要动循环，这条路走不到第 20 章。

---

## 先把文件系统做成边界

文件操作属于外部边界。`features` 不应该直接 `import pathlib` 或创建文件对象，否则业务层会反向依赖具体实现。

换成大白话：**工具的 handler 里不要出现 `from pathlib import Path`。**

为什么？看看直接依赖 pathlib 会付出什么代价：

| 直接用 `pathlib` | 通过接口注入 |
| --- | --- |
| 测「路径越界被拒绝」要在磁盘上真造一个 junction | 注入一个替身，让它直接抛 `WorkspacePathError` |
| 测「文件不是合法 UTF-8」要造一个坏字节文件 | 替身返回 `InvalidUtf8Error` |
| 测试会真的写文件，跑完得清理 | 替身只在内存里记一笔 |
| 想换成内存文件系统 / 远程存储，得改 handler | 换一个实现类就行 |

所以 `agent_ch02/core/filesystem.py` 只声明运行时需要的能力：

```python
class WorkspaceFileSystem(Protocol):
    def read_file(self, workspace: str, relative_path: str, limit: int | None = None) -> str: ...
    def write_file(self, workspace: str, relative_path: str, content: str) -> int: ...
    def edit_file(self, workspace: str, relative_path: str, old_text: str, new_text: str) -> None: ...
    def glob_files(self, workspace: str, pattern: str) -> tuple[str, ...]: ...
```

四个方法，对应四个工具。`Protocol` 就是 Python 版的 Java `interface`（结构化类型：只要方法签名对得上就算实现，连 `implements` 关键字都不需要）。注意两个共同点：

1. **每个方法第一个参数都是 `workspace`。** 接口不持有「当前目录」这种隐式状态，每次调用都要显式说明边界在哪。这样同一个实例可以服务不同工作区，也不会因为某处改了状态影响别处。
2. **`relative_path` 就叫 relative。** 命名本身就是契约：绝对路径不是「也支持」，而是明确拒绝。

具体实现位于 `agent_ch02/adapters/filesystem.py` 的 `LocalWorkspaceFileSystem`。工具定义只依赖 `WorkspaceFileSystem` 接口，`bootstrap.py` 才负责创建真实实现并注入。

这条依赖方向很重要：

```text
bootstrap
   ├─ 创建 LocalWorkspaceFileSystem
   └─ 注入 features

features ──依赖──> core/WorkspaceFileSystem
adapters ──实现──> core/WorkspaceFileSystem
```

读这张图的关键：**两个箭头都指向 `core`。** `features`（业务）和 `adapters`（技术细节）互相不认识，各自只认中间那份接口。所以换掉任何一侧，另一侧不用改。

测试可以注入替身，生产入口可以注入 pathlib 实现，feature 不需要知道二者的区别。这就是 Java 后端常说的依赖倒置和构造器注入。

---

## 用 validator 函数定义严格输入

TypeScript 版本用 Zod 一份 schema 同时生成模型说明书和运行时校验。Python 版本没有引入等价的校验库，改为在 `ToolDefinition` 里放两样同源的东西：

- `parameters`：手写的 JSON Schema 字典，作为发给模型的说明书；
- `validator`：一个显式校验函数，输入解析后的 `dict`，返回「过/不过」。

两者放在同一个 `ToolDefinition` 里，谁写谁负责保持一致。以 `read_file` 为例（真实源码位于 `agent_ch02/features/builtin_tools.py`）：

```python
def _read_args(value: dict[str, Any]) -> bool:
    return (
        set(value) <= {"path", "limit"}
        and isinstance(value.get("path"), str)
        and bool(value["path"])
        and (
            "limit" not in value
            or (
                isinstance(value["limit"], int)
                and not isinstance(value["limit"], bool)
                and value["limit"] > 0
            )
        )
    )
```

这里有三个 Python 初学者需要注意的写法：

- `set(value)` 得到字典的键集合，`<=` 判断是否为子集——`{"path": "x"}` 合法，`{"path": "x", "extra": 1}` 非法；
- `T | None` 表示可能没有值；
- **Python 的 `bool` 是 `int` 的子类**，`True` 会被 `isinstance(x, int)` 判定为整数，所以校验整数时必须额外排除 `bool`。这是 Python 类型系统的著名陷阱。

这里的约束都是实际契约：

- `path`、`old_text` 和 `pattern` 不能为空；
- `limit` 必须是大于 0 的整数；
- `True`、`"2"`、`0` 和 `-1` 都不是合法 limit；
- `content` 和 `new_text` 允许空字符串，因为清空文件或删除一段文本是有效操作；
- 所有 schema 外字段都拒绝。

用 `read_file` 的实际输入举例，一眼看清哪些过、哪些不过：

| 模型传来的 `arguments` | 结果 | 原因 |
| --- | --- | --- |
| `{"path":"note.txt"}` | 通过 | `limit` 是可选的 |
| `{"path":"note.txt","limit":50}` | 通过 | 正整数 |
| `{"path":""}` | 拒绝 | 空路径没有意义 |
| `{"path":"note.txt","limit":"50"}` | 拒绝 | 字符串不是 int，**不做隐式转换** |
| `{"path":"note.txt","limit":0}` | 拒绝 | 读 0 行是无意义请求 |
| `{"path":"note.txt","limit":true}` | 拒绝 | `bool` 被显式排除 |
| `{"path":"note.txt","encoding":"gbk"}` | 拒绝 | 键集合超出 `{path, limit}` |
| `{}` | 拒绝 | 缺必填字段 `path` |

第 4 行和倒数第 2 行最值得注意：

- **`"50"` 被拒绝而不是自动转成 `50`。** 隐式转换看着「宽容」，实际是在掩盖模型的错误——它下次还会传字符串。明确报错，模型这一轮就能改对。
- **多余字段被拒绝而不是忽略。** 模型传 `encoding` 说明它以为这个参数有用。静默忽略等于骗它「我处理了」，结果文件按 UTF-8 读出乱码，谁也不知道哪里出了问题。

这两条合起来是同一个原则：**不要替模型猜意图。** 契约之外的输入一律明确失败。

对应的模型侧声明也放在同一个 `ToolDefinition` 里：

```python
ToolDefinition(
    "read_file",
    "读取工作区内的 UTF-8 文本文件，可按行数限制输出。",
    {"type": "object", "properties": {"path": {"type": "string"}, "limit": {"type": "integer", "minimum": 1}}, "required": ["path"], "additionalProperties": False},
    "read",
    handler,
    _read_args,
)
```

`parameters` 字典会由 `openai_tools()` 原样发给模型；`_read_args` 在 `prepare()` 阶段拦截非法输入。模型看到的契约和 handler 真正接受的契约来自同一个定义，不会出现「说明书说要 `command`，校验时却要 `cmd`」的错位。

| 工具 | 输入 | effect | 成功结果 |
| --- | --- | --- | --- |
| `read_file` | `path`, `limit?` | `read` | 文件文本 |
| `write_file` | `path`, `content` | `write` | 写入字节数 |
| `edit_file` | `path`, `old_text`, `new_text` | `write` | 编辑确认 |
| `glob` | `pattern` | `read` | 匹配路径列表 |

---

## 描述也是模型上下文

工具的 `description` 和参数描述不是文档，而是模型选择工具时实际读到的上下文。[OpenAI 的 function calling 指南](https://developers.openai.com/api/docs/guides/function-calling)和 [Anthropic 的 tool use 文档](https://platform.claude.com/docs/en/agents-and-tools/tool-use/define-tools)都建议：描述要说明工具做什么、什么时候用、返回什么，以及边界和失败语义，而不是只写一句话。

在请求结构上，工具定义位于 API 顶层 `tools` 字段，与 `system` 消息共同构成每轮不变的静态前缀。KV Cache 依赖前缀的字节稳定性：如果每轮动态重排工具、修改描述，或者把时间戳之类的变化内容塞进工具说明，前缀就会变化，之前缓存的中间结果无法复用。`ToolRegistry` 按注册顺序稳定导出工具，`snapshot()` 又禁止中途修改注册表，正是为了在运行时保持这个前缀。

描述缺失的代价也有实验依据：保留函数签名和参数定义、只移除描述性文本时，工具调用错误率会明显增加。类似消融实验来自上下文工程教程，不是本章离线测试数据，但它说明同一件事：模型对工具的语义理解越完整，越不容易传错参数。

P02 的文件工具描述因此没有写成「读文件」这种最小文案，而是把关键决策信息直接放进描述：

- `read_file`：「读取工作区内的 UTF-8 文本文件，可按行数限制输出」；
- `edit_file`：「将工作区 UTF-8 文件中的 old_text 精确替换一次」——「一次」两个字就是行为契约；
- `glob`：「列出工作区内匹配 glob 模式的文件，结果按字母排序」；
- `shell`：「在当前工作目录执行 PowerShell 命令；需要人工批准」——避免模型在文件操作可用时仍去拼 PowerShell 字符串。

官方实践没有规定一成不变的模板，但可以把「模型不需要猜测」拆成四条检查：

- 做什么：工具接受什么输入，产生什么副作用；
- 什么时候用：与同类工具的分工是什么，例如文件操作优先于 shell；
- 返回什么：成功结果的形式是什么，例如字节数、路径列表；
- 失败怎么办：稳定错误码是什么，以及模型下一步可以怎么恢复，例如先读取再编辑。

跨工具协作也值得写进描述：`edit_file` 的描述隐含「先读取、再提供精确文本」的工作流，模型就不会凭记忆拼一段旧文本。描述不是越长越好，而是让模型在决策点不需要猜测。

---

## safe_path：先守住 workspace

四个工具共享同一条路径边界。公开契约是「工作区相对路径」，所以运行时不会猜测或修复危险输入。

下面这些路径直接拒绝：

```text
../secret.txt
nested/../secret.txt
C:\secret.txt
/etc/passwd
NUL.txt
nested/CON.log
file:stream
trailing.
```

逐个说明为什么，顺便解释几个不那么直觉的：

| 被拒路径 | 拒绝理由 | 不拦会怎样 |
| --- | --- | --- |
| `../secret.txt` | 含 `..`，向上跳出工作区 | 读到工作区外的任意文件 |
| `nested/../secret.txt` | 含 `..`，即使最终**结果**在区内也拒绝 | 见下方说明 |
| `C:\secret.txt` | 绝对路径 + 盘符 | 契约是相对路径，绝对路径不在语义内 |
| `/etc/passwd` | 绝对路径（POSIX 风格） | 同上 |
| `NUL.txt` | 首段是 Windows 保留名 `NUL` | 可能被解析成设备而非文件 |
| `nested/CON.log` | 任一段是保留名 `CON` | 同上，保留名在**每一层**都危险 |
| `file:stream` | 冒号属于非法路径字符 | 可能形成 NTFS Alternate Data Stream，数据写进隐藏流 |
| `trailing.` | 尾随点被 Win32 静默去掉 | 你以为写 `trailing.`，实际写了 `trailing` |

关于 `nested/../secret.txt`：它化简之后就是 `secret.txt`，确实在工作区内。**但仍然拒绝。** 理由是「路径化简」这件事一旦允许，就得保证你的化简逻辑和操作系统完全一致——任何一点差异都是可利用的缝隙。直接拒绝 `..` 成本极低，模型改成 `secret.txt` 就行。

Python 实现的保留名检查比直觉更严格（`adapters/filesystem.py` 的 `_is_windows_reserved`）：

```python
if component.endswith((" ", ".")):  # Windows 不允许文件名以空格或点结尾
    return True
if any(ord(char) < 32 or char in '<>:"|*?' for char in component):
    return True
stem = component.split(".", 1)[0].rstrip(" ").upper()
if stem in _WINDOWS_DEVICE_NAMES:  # AUX/CLOCK$/CON/CONIN$/CONOUT$/NUL/PRN
    return True
return len(stem) == 4 and stem[:3] in {"COM", "LPT"} and stem[3] in "123456789"
```

除了设备名，非法字符（`< > : " | * ?` 和控制字符）与尾随空格/点也一并拒绝——Windows 的路径规则是全家桶，只拦一半等于没拦。

`safe_path` 分两步检查（`adapters/filesystem.py`）：

```python
def safe_path(workspace: str, relative_path: str) -> Path:
    root = _workspace_root(workspace)              # ① 验证并解析工作区根目录
    parts = _relative_parts(relative_path, "路径")  # ② 词法检查：拆组件、拒绝危险输入
    target = root.joinpath(*parts)
    # ③ 物理检查：找到已存在的父路径，解析真实位置
    existing = target
    while not existing.exists() and existing != root:
        existing = existing.parent
    physical_parent = existing.resolve(strict=True)
    # ④ 用真实父路径重拼目标，并检查是否仍在工作区内
    candidate = physical_parent.joinpath(*target.relative_to(existing).parts).resolve()
    candidate.relative_to(root)   # 不在 root 内会抛 ValueError，被转成 WorkspacePathError
    return candidate
```

第一关是词法检查（`_relative_parts`），不需要访问磁盘：先拒绝空值、NUL 字符、绝对路径、盘符、`..` 和 Windows 保留组件。

第二关解析磁盘上已经存在的父路径。如果 `workspace/escape` 是一个指向工作区外的 junction 或符号链接，那么 `escape/secret.txt` 的文本看似在 workspace 下，真实路径却在外部。`Path.resolve()` 会顺着链接走到真实位置，解析后的路径不在 root 内时，`relative_to` 抛 `ValueError`，被翻译成 `WorkspacePathError("路径逃逸工作区: ...")`。

### 为什么必须有第二关

第一关只看字符串，第二关才碰磁盘。**很多人会以为第一关就够了**，下面这个例子说明为什么不够。

假设有人（或某个之前的构建脚本）在工作区里留下了一个 junction：

```text
workspace\escape  ──指向──▶  C:\Users\你\.ssh
```

模型请求 `read_file`，参数是 `escape/id_rsa`。走一遍两关：

```text
输入: "escape/id_rsa"

第一关（纯字符串，不碰磁盘）
  ├─ 空值？      不是
  ├─ 含 NUL？    没有
  ├─ 绝对路径？   不是
  ├─ 含 ..？     没有
  ├─ 保留名？     escape / id_rsa 都不是
  └─ 拼成 target = workspace\escape\id_rsa
     在 root 下？ → 是  ✅ 第一关放行！

第二关（解析磁盘真实路径）
  ├─ 找到已存在的父目录: workspace\escape
  ├─ resolve 解析真实位置:  C:\Users\你\.ssh   ← junction 在这里暴露
  ├─ 重新拼:  C:\Users\你\.ssh\id_rsa
  └─ relative_to(root) 抛 ValueError  ❌ 拒绝
```

**第一关看字符串是完全合法的路径，只有真正解析磁盘才能发现它指向工作区外。** 这就是第二关必须存在、且必须访问磁盘的原因。

只解析「已存在的父路径」而不是整条路径，是因为 `write_file` 允许创建还不存在的文件——不存在的部分没有真实路径可解析，但它的父目录有。

这不是完整沙箱。它只保护四个文件工具。`shell` 的 `cwd` 虽然是 workspace，但 PowerShell 仍能使用绝对路径、访问网络或启动子进程。

---

## read_file：限制上下文体积

`read_file` 使用严格 UTF-8 解码。非法字节不会被悄悄替换成 ``，而会返回 `invalid_utf8`。

为什么不宽容一点？因为 `` 一旦进入上下文，模型就看到了**一份错误的文件内容**，而且完全不知道它是错的。它可能基于这段乱码去做 `edit_file`，把文件改坏。明确返回 `invalid_utf8`，模型至少知道「这个文件我读不了，得换办法」。

`adapters/filesystem.py` 里的读取逻辑：

```python
try:
    text = target.read_bytes().decode("utf-8")
except UnicodeDecodeError as error:
    raise InvalidUtf8Error(f"文件不是合法 UTF-8: {relative_path}") from error
lines = text.replace("\r\n", "\n").replace("\r", "\n").splitlines()
if limit is not None and limit < len(lines):
    return "\n".join([*lines[:limit], f"... ({len(lines) - limit} more lines)"])
return "\n".join(lines)
```

Java 对照：`read_bytes().decode("utf-8")` 类似 `Files.readAllBytes(path)` 后用 `StandardCharsets.UTF_8` 严格解码；`splitlines()` 类似按行拆分，但会同时识别 `\n`、`\r\n`、`\r` 等多种换行。

注意这个函数的执行顺序，它和 `prepare` 那条原则一致：

```text
① 校验 limit          ← 参数不合法就别往下走
② safe_path            ← 路径不安全就别碰磁盘
③ 文件存在且是普通文件
④ 严格 UTF-8 解码     ← 解不出来就明确失败
⑤ 按行切分、按 limit 截断
```

**每一步都是「先否决，再前进」。** 前四步任何一步失败，都不会有任何东西被读出来或写出去。

例如文件有三行（`test_files.py` 里的实测值），`limit` 为 1，结果是：

```text
您好 Agent
... (1 more lines)
```

限制读取行数可以避免一个大文件占满模型上下文。`limit` 生效时，尾部会明确标注还剩多少行；不传 `limit` 时则返回完整文件，模型不需要猜测内容是否被截断。

最后那行 `... (1 more lines)` 不是装饰，它是给模型的信号：**「这里被截断了，还有内容」**。如果只返回前两行而不加提示，模型会以为整个文件就这两行，然后基于不完整信息下结论。第 8 章讲上下文压缩时，「截断必须留痕」会升级成一条通用规则。

---

## write_file：字节数以磁盘内容为准

```python
target = safe_path(workspace, relative_path)
target.parent.mkdir(parents=True, exist_ok=True)
data = content.encode("utf-8")
target.write_bytes(data)
return len(data)
```

它会自动创建父目录，并直接写 UTF-8 字节。返回的是 `len(data)`，即编码后的字节数，不是 Python 字符串长度。

例如 `你好` 的 `len("你好")` 是 2，但 UTF-8 实际占 6 字节。工具成功时返回：

```text
已写入 6 个 UTF-8 字节: note.txt
```

这样，结果与真正写入磁盘的数据一致。Java 对照：这和 `content.getBytes(StandardCharsets.UTF_8).length` 的含义一致。

这个区别不是抠细节。看下面几个例子：

| 内容 | 字符数 | UTF-8 字节数 | 差多少 |
| --- | ---: | ---: | --- |
| `alpha` | 5 | 5 | 相同（纯 ASCII） |
| `你好` | 2 | 6 | 3 倍 |
| `café` | 4 | 5 | 差 1（`é` 占 2 字节） |
| `🙂` | 1 | 4 | Python 按码点数，emoji 占 4 字节 |

**如果返回字符数，模型拿到的就是一个和磁盘无关的数字。** 它没法用这个数字校验写入是否完整，也没法据此判断文件大小。工具的返回值应当描述「世界上真实发生了什么」，而不是「程序内部某个变量的值」。

另外 `target.parent.mkdir(parents=True, exist_ok=True)` 这一行值得单独说：它让 `write_file` 能一次写入 `demo/sub/note.txt`，即使 `demo/sub` 还不存在（等价于 `mkdir -p`）。少了它，模型就得先想到「我要建目录」——而它手里根本没有建目录的工具，只能退回去拼 `shell`。**工具设计的一个判断标准：它是否让模型不必绕回 `shell`。**

---

## edit_file：只改第一处精确文本

`edit_file` 不接受行号。行号会随着前面每次编辑而整体偏移，模型很容易拿着旧行号改错位置。

举个具体例子说明「偏移」有多容易出事。模型读到一个文件：

```text
1  import a
2  import b
3
4  def run():
```

它计划做两处编辑：删掉第 2 行，再改第 4 行。第一处编辑执行后，文件变成：

```text
1  import a
2
3  def run():
```

**原来的第 4 行现在是第 3 行。** 模型如果按原计划去改「第 4 行」，就改到了下一行——而且它自己完全不知道改错了。这不是模型笨，是「行号」这个坐标系本身在编辑后就失效了。

精确文本匹配没有这个问题：文本片段是自带定位信息的坐标，前面删了多少行都不影响它。

它要求模型提供旧文本和新文本：

```python
if old_text not in current:
    raise TextNotFoundError(f"文件中找不到精确文本: {relative_path}")
updated = current.replace(old_text, new_text, 1)
target.write_bytes(updated.encode("utf-8"))
```

`str.replace` 的第三个参数 `1` 表示只替换第一次出现——这是 Python 标准库直接支持的行为，一行就实现了「只改第一处」。

这带来三个明确行为：

1. 旧文本必须精确存在；
2. 只替换第一处；
3. 找不到旧文本时不写回文件。

第 3 条是「不做部分修改」原则：`write_bytes` 在 `raise` 之后，所以匹配失败时文件一个字节都没动。模型收到 `text_not_found`，文件仍是它上次读到的样子——这让重试是安全的。

第 2 条「只替换第一处」是有意的保守选择。如果 `old_text` 在文件里出现了 5 次，模型大概根本没意识到，改全部 5 处很可能不是它的意图。只改第一处 + 明确告知，比「猜它想全改」更安全。模型真要全改，可以多调几次。

它读入整段原文本并只替换匹配片段，其余字符原样写回，因此未修改部分不会被顺带改动。测试 `test_reads_writes_edits_and_globs_stably` 会直接验证编辑后的读取结果。

模型正确的工作流应当是：先读文件，再用读到的精确片段调用 `edit_file`。如果文件已经变化，工具会失败，模型需要重新读取，而不是盲目覆盖。

```text
✅ 正确： read_file → 拿到精确片段 → edit_file
❌ 错误： 凭记忆/凭猜测拼一段 old_text → edit_file → text_not_found
```

第二种做法失败率很高，因为模型记忆里的代码往往是「大概长这样」，缩进和空格未必一致。这也是为什么 `edit_file` 的描述明确写了「精确替换一次」——**描述是模型的行为指南，不是给人看的文档。**

---

## glob：先看见项目，再决定读什么

模型通常不知道 workspace 里有哪些文件。`glob` 让它先查找，再选择要读取的目标：

```json
{"pattern":"agent_ch02/**/*.py"}
```

返回值统一使用 `/`，并稳定排序（真实实测值）：

```text
agent_ch02/__init__.py
agent_ch02/bootstrap.py
agent_ch02/cli.py
```

没有匹配时返回 `(无匹配文件)`。

**返回 `(无匹配文件)` 而不是空字符串**，也是一个刻意选择：空字符串在模型看来和「工具坏了」没法区分，而 `(无匹配文件)` 明确表示「工具正常工作，答案是零个」。

三个设计点值得注意：

| 设计 | 为什么 |
| --- | --- |
| 统一用 `/` 分隔（`as_posix()`） | 模型下一步要把这些路径喂给 `read_file`，格式必须能直接用 |
| 稳定字母序排序（`sorted`） | 同样的项目、同样的模式，两次调用结果一致，模型的推理可复现 |
| 一行一个路径 | 不用 JSON 包一层，模型和人都能直接读 |

glob 模式本身也必须是相对路径，不能包含 `..`。这里有三道防线，因为 glob 比其他三个工具更难守：

```text
① 通配符前的字面前缀过 safe_path   escape/*.txt 里的 escape 先被检查
② 遍历时跳过符号链接              避免遍历过程本身走出工作区
③ 每个匹配结果再过一次 safe_path   兜底，防止前两道有遗漏
```

为什么要三道？因为 `read_file` 只处理**一个**确定路径，而 glob 要遍历**一片**目录树（`root.rglob("*")`）——检查点从 1 个变成了成百上千个。攻击面变大，防线就得加厚。

还有一个 Python 特有的细节：标准库 `fnmatch` 不把 `**/` 解释为「零层或多层目录」，所以 `**/*.txt` 匹配不到根目录下的 `a.txt`。适配器为此显式补了一个分支：

```python
if normalized.startswith("**/"):  # 特殊处理 **/ 前缀
    matches = matches or fnmatch.fnmatchcase(relative, normalized[3:])
```

这行代码保证 `**/*.txt` 在 Python 版和 TS 版语义一致：既匹配 `note.txt`，也匹配 `nested/note.txt`。`test_reads_writes_edits_and_globs_stably` 断言了 `("**/*.txt", ("a.txt", "nested/note.txt"))` 这组精确结果。

---

## handler：只在工具边界映射预期错误

适配器先把底层 `OSError`（通过 errno）翻译成 core 定义的领域异常（`adapters/filesystem.py` 的 `_translate_os_error`）：

```python
if getattr(error, "errno", None) == 2:          # ENOENT
    return FileNotFoundError("文件或目录不存在")
if getattr(error, "errno", None) in {20, 21}:   # EISDIR / ENOTDIR
    return InvalidFilePathError("路径指向了错误的文件类型")
return FileSystemOperationError("文件系统操作失败")
```

feature 不读取 Python errno，因此替换文件系统适配器时，工具层的稳定语义不会变化。

handler 位于工具边界，负责把这些预期失败转换成模型能理解的稳定错误码。四个工具共用一个映射函数（`features/builtin_tools.py` 的 `_map_file_error`）：

```python
def _map_file_error(error: Exception, path: str) -> ToolResult:
    if isinstance(error, WorkspacePathError):
        return tool_error("path_escape", str(error))
    if isinstance(error, InvalidUtf8Error):
        return tool_error("invalid_utf8", f"文件不是合法 UTF-8: {path}")
    if isinstance(error, FileNotFoundError):
        return tool_error("file_not_found", f"文件不存在: {path}")
    if isinstance(error, InvalidFilePathError):
        return tool_error("invalid_path", f"路径不是文件: {path}")
    if isinstance(error, FileSystemOperationError):
        return tool_error("filesystem_error", f"文件系统操作失败: {path}")
    raise error   # ← 不认识的错误，继续往上抛
```

`edit_file` 的 handler 在这之上多翻译一个 `TextNotFoundError`（`text_not_found`）。

代码最后那行 `raise error` 是关键，很容易被当成疏漏：

```python
    if isinstance(error, FileSystemOperationError):
        return tool_error("filesystem_error", f"文件系统操作失败: {path}")
    raise error   # ← 不认识的错误，继续往上抛
```

**这不是漏写，是有意的。** 这个函数只翻译**已知的、预期会发生的**失败。如果冒出一个谁都没预料到的错误（比如某个依赖库的内部 bug），把它转成 `filesystem_error` 就等于把真实故障伪装成「正常的文件操作失败」——模型会以为换个路径重试就行，而真正的 bug 被永久掩盖。

对比一下两种写法的后果：

| 写法 | 已知失败 | 未知 bug |
| --- | --- | --- |
| 结尾 `raise error`（本章做法） | 转成稳定错误码，模型可恢复 | 抛到上层（`ToolRegistry.invoke` 的兜底会转成 `tool_execution_error`），明确暴露 |
| 结尾 `return tool_error("unknown", ...)` | 同上 | **被吞掉**，伪装成普通失败 |

这条原则可以推广：**`except` 只处理你能列举出名字的错误。** 列不出名字的，说明你不理解它，也就没资格决定怎么处理它。

四个工具使用的主要错误码如下：

| error_code | 含义 | 模型看到后应该做什么 |
| --- | --- | --- |
| `path_escape` | 路径违反 workspace 边界 | 换成工作区内的相对路径，不要重试同一路径 |
| `file_not_found` | 目标文件不存在 | 先 `glob` 找到真实路径，或改用 `write_file` 创建 |
| `text_not_found` | 精确旧文本不存在 | 重新 `read_file`，用读到的原文再编辑 |
| `invalid_path` | 文件与目录类型不符合操作要求 | 目标是目录，换一个具体文件 |
| `invalid_utf8` | 文件不是合法 UTF-8 | 这是二进制文件，不要再尝试文本读取 |
| `filesystem_error` | 其他受控文件系统失败 | 换路径或换方案；不要原样重试 |

错误码保留英文机器标识，用户和模型可见的说明使用中文，例如 `工具执行错误 [text_not_found]: 文件中找不到精确文本: note.txt`。**第三列才是错误码的真正价值。** 一个好的错误码不只是「告诉模型失败了」，而是「让模型能推出下一步」。对比一下：如果所有失败都返回同一句 `操作失败`，模型只能盲目重试同样的调用，白烧几轮 token。区分成六个稳定错误码，模型每种情况都有明确的下一步。

这也解释了为什么错误码要**稳定**：模型（以及你写的测试）会依赖这些字符串做判断。随手把 `text_not_found` 改成 `not_found` 是一个破坏性变更，哪怕它只是个字符串。

---

## P01 与 P02 只差构建期工具集

到这里四个工具都写完了。但还有一个问题没回答：**第 1 章的智能体和第 2 章的智能体，代码上到底差在哪？**

答案出人意料地小——差一个工具集合。

`agent_ch02/core/profiles.py` 定义固定能力快照：

```python
P01 = ChapterProfile(1, frozenset({"loop", "powershell"}))
P02 = ChapterProfile(2, frozenset({"loop", "powershell", "tool_registry", "files"}))
```

`frozenset` 是不可变集合，Java 可以把它理解成不暴露 `add`/`remove` 的只读集合。但光不可变还不够——bootstrap 还校验**对象身份**：

```python
if profile is not P01 and profile is not P02:
    raise ValueError("必须传入固定的章节配置对象")
```

`is` 比较的是对象身份而不是字段值。这样调用方不能 new 一个内容相同的 `ChapterProfile` 冒充固定章节配置——`test_composition_rejects_a_forged_profile` 专门验证了这一点。Java 中相当于只接受预定义的单例常量。

然后按 profile 选择工具集：

```python
command = command_runner or PowerShellRunner()
tools = (
    create_chapter_one_tools(command)
    if profile.chapter == 1
    else create_chapter_two_tools(command, file_system or LocalWorkspaceFileSystem())
)
```

P02 的工具构建也很直接：

```python
registry = create_chapter_one_tools(command_runner)   # 先复用第 1 章
registry.register(create_read_file_tool(file_system))
registry.register(create_write_file_tool(file_system))
registry.register(create_edit_file_tool(file_system))
registry.register(create_glob_tool(file_system))
return registry
```

读法是「先复用，再追加」：`create_chapter_two_tools` 第一行就调用了 `create_chapter_one_tools`，所以 P02 一定包含 P01 的全部行为。这不是习惯，是结构保证——想删掉 `shell` 你得改代码，不可能因为疏忽而丢掉。

两个 profile 的实际差异：

| | P01 | P02 |
| --- | --- | --- |
| capabilities | `loop`、`powershell` | 再加 `tool_registry`、`files` |
| 模型看到的工具 | `shell` | `shell`、`read_file`、`write_file`、`edit_file`、`glob` |
| Agent Loop 代码 | 同一份 | **同一份** |
| 消息历史处理 | 同一份 | **同一份** |
| 组合根改动 | — | 多一个分支选注册表 |

有一点值得注意：**能力是构建期确定的，不是运行时可切换的开关。** 组合根按 `profile.chapter` 选好注册表，之后这一轮运行里工具集合不再变化。这样每轮发给模型的 `tools` 数组才是稳定的（回到本章开头讲的静态前缀），测试也才能断言「P01 只暴露 `shell`」这种确定性事实（`test_p01_and_p02_tool_sets_are_separate` 就在断言这个）。

运行中动态增减工具是第 19 章 MCP 才需要解决的问题，它引入的复杂度（Registry Snapshot、命名隔离、下一轮才生效）本章一点都不需要。

Agent Loop 每轮仍然从同一个注册表快照生成 tools schema。模型调用、消息历史、最大轮次和结果配对都不需要复制或修改。

---

## 一轮多个工具调用仍然必须配对

工具从 1 个变成 5 个，带来一个第 1 章不太会遇到的情况：**模型可能在一轮里同时要求好几个工具。**

模型可以在同一条 assistant 消息里请求多个工具：

```python
assistant_message(None, (
    tool_call("write-1", "write_file", '{"path":"note.txt","content":"value"}'),
    tool_call("read-1", "read_file", '{"path":"note.txt"}'),
))
```

当前实现按顺序执行。上面的结果历史必须是：

```text
assistant(tool_calls: write-1, read-1)
tool(tool_call_id: write-1, 写入结果)
tool(tool_call_id: read-1, 读取结果)
```

不能漏掉失败调用的结果，也不能在两个 tool 结果之间插入新的 user 或 assistant 消息。即使参数无效，也要生成一个与原 ID 配对的错误结果。

### 违反配对会发生什么

这条规则不是「最好遵守」，是**违反就直接崩**。三种典型错法：

```text
❌ 少一个结果（write-1 校验失败，就跳过不返回了）
   assistant(tool_calls: write-1, read-1)
   tool(read-1, ...)
   → validate_tool_pairing 抛错：以下工具调用缺少返回结果: ['write-1']

❌ 中间插了别的消息
   assistant(tool_calls: write-1, read-1)
   tool(write-1, ...)
   user("等一下，改成别的")      ← 插在配对中间
   tool(read-1, ...)
   → 抛错：以下工具调用缺少返回结果: ['read-1']

❌ 返回了没人要的结果
   assistant(tool_calls: write-1)
   tool(write-1, ...)
   tool(read-1, ...)             ← 模型没请求过 read-1
   → 抛错：工具结果找不到对应的调用 ID: read-1
```

所以「工具执行失败」和「不返回结果」是两件完全不同的事。前者正常，后者是 bug。这也是第 1 章把 `ToolResult` 设计成**数据而不是异常**的原因——handler 里 `raise` 会中断循环、丢掉配对；返回一个 `tool_error(...)` 才能既报告失败又保持结构完整。

### 为什么是串行而不是并发

串行执行是刻意选择。读写同一文件存在顺序依赖；在没有并发安全契约前，不应为了速度擅自并发。

举个具体例子：模型在一轮里同时要求 `write_file("note.txt", "alpha")` 和 `read_file("note.txt")`。

| 执行方式 | 结果 |
| --- | --- |
| 串行（本章） | 先写后读，读到 `alpha`。确定 |
| 并发 | 可能读到 `alpha`，也可能读到旧内容或读到写了一半的文件。**不确定** |

并发不是不能做，而是做之前必须先回答「哪些工具之间可以并发」「冲突怎么检测」。第 13 章引入后台任务时会正面处理它，那时才有对应的契约。现在换来的一点速度，不值得一个不可复现的 bug。

---

## 第 2 章的权限边界

本章有一个**故意留下的不对称**，读到这里应该看得很清楚了：

```text
shell "Remove-Item note.txt"    → 弹出审批，你不敲 y 就不执行
write_file "note.txt" "..."     → 直接执行，你什么都看不到
```

两者都能改文件，但只有一个要审批。为什么？因为第 2 章还没有权限系统，`cli.py` 里只写了一条硬编码规则。但和 TS 版不同，Python 版的 `TerminalAuthorizer` 已经开始读 `effect` 了：

```python
if prepared.definition.effect != "execute":  # 非执行类工具无需批准
    return ToolAuthorizationDecision(True, "该工具类型不需要人工批准")
```

也就是说，`effect` 标签在 Python 版里已经**部分参与了决策**：只有 `execute` 类（目前只有 `shell`）走终端审批，`read`/`write` 类直接放行。这条规则仍然很粗糙——`write_file` 能改文件却免审批——但它是第 3 章完整权限系统的第一块积木。

本章保持以下固定行为：

- `shell` 是 `execute`，真实 CLI 每次调用前要求明确输入 `y` 或 `yes`；
- 四个文件工具依靠 `safe_path` 边界直接执行；
- 无交互输入、默认回车或审批异常都会拒绝 `shell`；
- `cwd` 不是沙箱，不能把文件工具的路径保证外推给 PowerShell。

最后一条最容易被误解，单独说清楚：

| | 有 `safe_path` 保护吗 | 说明 |
| --- | --- | --- |
| `read_file` / `write_file` / `edit_file` / `glob` | 有 | 路径必须落在 workspace 内 |
| `shell` | **没有** | PowerShell 里写 `C:\Windows\...` 完全可以，`cwd` 只是起始目录 |

所以本章的安全模型是**两套并存**的：文件工具靠代码边界，`shell` 靠人工审批。**不要把前者的保证套到后者头上。**这也是为什么第 3 章要做统一的权限系统——两套规则并存迟早会漏。

第 3 章会增加结构化权限规则，让写工具也能进入审批和审计流程，并保证强拒绝不会被弱允许覆盖。

---

## 运行第 2 章

前置条件和第 1 章完全一样：`python/.venv` 已创建、`python/.env` 三项已填好。没做过的回第 1 章「运行第 1 章智能体」照做一遍。本章只需把包安装进共享环境：

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch02_agent'
& ..\.venv\Scripts\python.exe -m pip install -e ".[dev]"
```

### 第 1 步：先跑离线测试

```powershell
& ..\.venv\Scripts\python.exe -m pytest tests
```

预期最后一行：

```text
16 passed in 2.34s
```

这 6 个测试文件、16 个测试全部离线——新增的正是文件工具和路径边界（4+3 个用例）。不需要 API Key，不发网络请求。

### 第 2 步：跑一个完整的四工具链路

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch02.cli --prompt "只使用文件工具：写入 demo/note.txt，内容为 alpha；把第一处 alpha 改成 gamma；读取文件并用 glob 列出 demo/*.txt"
```

**这条命令会真实创建 `python/ch02_agent/demo/note.txt`。** 跑完可以自己确认：

```powershell
Get-Content .\demo\note.txt
```

预期输出 `gamma`。整个过程模型会调用四次工具，对应本章「一个完整例子」那张表。

prompt 里那句「只使用文件工具」是有意加的：不加的话模型可能直接拼一条 PowerShell 命令，那就走回第 1 章了，也会触发 `shell` 的逐次审批。

### 第 3 步：换一个只读任务

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch02.cli --prompt "读取 pyproject.toml，并用 glob 列出 agent_ch02/**/*.py"
```

这条只用 `read_file` 和 `glob`，不产生任何文件改动，适合反复跑着观察。

当前 PowerShell 目录既是 `.env` 向上查找位置，也是 Agent workspace。**换句话说，你在哪个目录敲命令，Agent 的工作区边界就在哪里。** 在 `python/ch02_agent` 里跑，`safe_path` 守的就是这个目录。

### 第 4 步（可选）：运行完整离线验证

```powershell
& ..\.venv\Scripts\python.exe -m compileall -q agent_ch02
& ..\.venv\Scripts\python.exe -m ruff check agent_ch02 tests
& ..\.venv\Scripts\python.exe -m mypy agent_ch02
```

这些检查不需要 API Key，也不会访问网络。真实模型运行只是额外验证，不替代离线契约测试。

### 常见报错排查

第 1 章那张表仍然适用（配置、参数、审批相关的报错）。下面是本章新增的、和文件工具有关的情况：

| 现象 | 原因 | 怎么处理 |
| --- | --- | --- |
| 工具结果里出现 `工具执行错误 [path_escape]: 路径逃逸工作区: ...` | 模型给的路径含 `..`、绝对路径或 Windows 保留名 | 正常行为。看 prompt 是否让模型误以为要访问工作区外的文件 |
| `工具执行错误 [text_not_found]: ...` | 模型凭猜测拼了 `old_text` | 正常行为。模型下一轮应该先 `read_file`；反复失败说明 prompt 里的文本描述不够精确 |
| `工具执行错误 [invalid_utf8]: ...` | 读到了二进制文件（图片、`.exe`、锁文件等） | 正常行为。用 `glob` 限定 `**/*.py` 之类的文本后缀 |
| `工具执行错误 [file_not_found]: ...` | 路径写错，或文件确实不存在 | 让模型先 `glob` 确认路径 |
| 模型仍然去调 `shell` 而不用文件工具 | prompt 没有明确限定 | 在 prompt 里写「只使用文件工具」 |
| 文件写出来是乱码 | 不是本章代码的问题 | `write_file` 固定 UTF-8。检查你用什么打开的——记事本旧版可能按 ANSI 解读 |
| `demo/note.txt` 出现在意外的位置 | 你不在 `python/ch02_agent` 目录下运行 | `Set-Location` 到章节目录再跑 |
| `ModuleNotFoundError: No module named 'agent_ch02'` | 没执行本章的 `pip install -e` | 回到开头执行安装命令 |

---

## 验证与实验

`pytest tests` 会执行 6 个测试文件、16 个测试。它们分工如下：

| 测试文件 | 验证内容 |
| --- | --- |
| `test_ch02_tools.py` | P01/P02 工具集隔离、伪造 profile 被拒、一轮多工具配对、多余字段在副作用前被拒 |
| `test_files.py` | `safe_path` 拒绝 `..`/绝对路径/保留名、读改写查稳定协作、缺文件/坏 UTF-8/找不到文本/读目录的错误映射 |
| `test_loop.py` | 第 1 章循环行为在 P02 下仍然成立 |
| `test_tools.py` | shell 超时结果映射、未知工具、非法 JSON 保持不变 |
| `test_messages.py` | `tool_call_id` 严格配对 |
| `test_config.py` | 共享 `.env` 校验 |

`test_loop.py`、`test_tools.py`、`test_messages.py` 出现在这里不是复制粘贴凑数——它们在验证**累加原则**：第 2 章加了四个工具，第 1 章的行为必须一个字都没变。这套教程每一章都保留前章的测试，就是为了让「不小心改坏了旧行为」立刻暴露。

### 三个建议动手做的小实验

**实验一：给 `read_file` 传一个多余字段**

在测试里让脚本化模型返回 `arguments: '{"path":"note.txt","encoding":"gbk"}'`。`test_extra_fields_are_rejected_before_file_side_effect` 就是现成的例子。

预期：`prepare()` 阶段就失败，返回 `invalid_arguments` 错误结果，**`read_file` 一次都没被调用**。
学到：严格键集合校验 + 校验前置的组合效果。可以在替身文件系统里加个计数器确认它真的没被调。

**实验二：连续两次 `edit_file` 用同一个 `old_text`**

第一次成功替换，第二次会怎样？

预期：第二次返回 `text_not_found`（第一处已经被改掉了），且文件内容不变。
学到「只替换第一处 + 失败不写回」这两条行为的实际后果。

**实验三：在工作区里手工造一个越界 junction**

```powershell
# 需要管理员权限；实验完记得删掉
New-Item -ItemType Junction -Path .\escape -Target C:\Windows\Temp
& ..\.venv\Scripts\python.exe -m agent_ch02.cli --prompt "读取 escape 目录里的任意一个文件"
```

预期：返回 `path_escape`，文件读不到。
学到：`safe_path` 第二关的实际效果——第一关看字符串完全放行，是磁盘解析拦住的。

```powershell
# 清理
Remove-Item .\escape
```

---

## 本章小结

### 三句话版本

1. 加工具的正确姿势是「定义 + 注册」，Agent Loop 一行都不用改。
2. 一份契约（schema 字典 + validator 函数）同时供模型阅读和运行时校验，从根上消除「文档和实现不一致」。
3. 文件工具的安全性来自 `safe_path` 的两道检查：词法拦明显越界，磁盘解析拦符号链接。

### 一定要记住的六条

| # | 结论 | 出现在哪一节 |
| ---: | --- | --- |
| 1 | 校验全在 `prepare()`，副作用全在 `invoke()`，顺序不能反 | ToolRegistry |
| 2 | 契约外的输入明确失败，不做隐式转换、不忽略多余字段 | 用 validator 定义严格输入 |
| 3 | `..`、绝对路径、Windows 保留名一律拒绝，不尝试修复 | safe_path |
| 4 | 只看字符串挡不住 junction，必须解析磁盘真实路径 | 为什么必须有第二关 |
| 5 | `edit_file` 用精确文本而非行号，因为行号编辑一次就失效 | edit_file |
| 6 | `except` 只翻译能列举出名字的错误，其余继续抛 | handler 错误映射 |

### 本章代码边界（明确「还没做什么」）

| 已经有 | 还没有 | 在哪一章补 |
| --- | --- | --- |
| 五个工具（含四个文件工具） | 结构化权限规则、审计日志 | 第 3 章 |
| `shell` 逐次人工审批 | 写工具也进审批流程 | 第 3 章 |
| 文件工具的 `safe_path` 边界 | `shell` 的路径边界（**仍然没有**） | 不在本教程范围；`shell` 始终依赖审批 |
| CLI 按 `effect != "execute"` 放行只读写工具 | `effect` 参与完整权限决策 | 第 3 章 |
| 单轮多工具**串行**执行 | 并发执行与并发安全契约 | 第 13 章（后台任务） |
| 完整读取 + `limit` 截断 | 产物落盘、上下文压缩 | 第 8 章 |
| 启动时固定的五个工具 | 运行中动态增减工具 | 第 19 章 |

### 检查你是否真的读懂了

1. 模型传 `{"path":"note.txt","limit":"10"}`，会发生什么？为什么不自动转成数字？（validator 那节）
2. 模型传 `{"path":"note.txt","limit":true}`，会发生什么？为什么这个值特别？（validator 那节）
3. `nested/../secret.txt` 化简后在工作区内，为什么还是拒绝？（safe_path）
4. 工作区里有个指向外部的 junction，第一关和第二关分别是什么结论？（为什么必须有第二关）
5. `write_file` 写入 `你好`，返回的数字是 2 还是 6？为什么？（write_file）
6. `edit_file` 的 `old_text` 在文件里出现 3 次，会改几处？（edit_file）
7. handler 的映射函数最后为什么要 `raise error` 而不是返回一个兜底错误码？（错误映射）

---

这一章真正增加的不是四个孤立函数，而是一条稳定扩展路径：

```text
validator 函数 + JSON Schema 字典
   ↓
ToolDefinition（name + description + effect + handler + validator）
   ↓
ToolRegistry（schema 快照 + 参数准备 + 分发）
   ↓
Agent Loop（保持不变）
```

以后新增工具，先定义明确输入和副作用，再实现 handler，最后注册一次。循环不需要知道工具细节，模型看到的 schema 和运行时执行的 handler 也不会分家。

下一章要解决的是本章刻意留下的缺口：`effect` 只被 CLI 用了一条硬编码规则，而 `write_file` 不经任何审批就能改文件。第 3 章会把它变成真正的权限系统——规则合并、拒绝优先、完整审计。

---

## 小白阅读路线（Java 开发者考古顺序）

不要从 `cli.py` 开始，也不要一次读完整个项目。建议按下面顺序考古：

1. 先看 `tests/test_files.py`，理解路径、UTF-8、写入、编辑和 glob 的可观察结果；
2. 看 `tests/test_ch02_tools.py`，观察模型如何一次提出四个工具调用；
3. 跳到 `features/builtin_tools.py`，看每个 handler 如何映射错误；
4. 看 `core/tools.py` 的 `prepare()` 和 `invoke()`；
5. 看 `adapters/filesystem.py` 的 `safe_path()`，理解真实路径安全边界；
6. 最后回到 `core/loop.py`，确认循环只新增了工具注册表内容，没有新增文件分支。

Python 与 Java 对照：`Protocol` 是 `interface`，`dataclass(frozen=True, slots=True)` 是不可变 DTO/record，`Path` 是比 `File` 更现代的路径值对象，`with pytest.raises(...)` 类似 JUnit `assertThrows`，`dict` 类似 `Map`，`tuple` 类似不可变 `List`，`frozenset` 类似不可变 `Set`。
