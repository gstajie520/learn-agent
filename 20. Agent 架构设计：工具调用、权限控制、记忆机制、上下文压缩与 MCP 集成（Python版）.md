# 第 20 章：Agent 架构设计：工具调用、权限控制、记忆机制、上下文压缩与 MCP 集成（Python 版）

这是第 20 章的完整 Python 学习版，配套代码位于 `python/ch20_agent`，实现包为 `agent_ch20`，测试位于 `python/ch20_agent/tests`。

前十九章依次加入了 Agent Loop、工具、权限、Hook、TODO、Subagent、Skill、上下文压缩、记忆、动态 Prompt、模型恢复、Task DAG、后台任务、Cron、Teammate、协作协议、工作窃取、Worktree 和 MCP。

第 20 章不再发明一个"终极框架"，也不复制第二套 Loop。它只做一件事：把已经实现的能力接到同一个组合根，并用跨功能场景证明边界同时成立。

---

## 1. 完整 Agent 的核心不是功能数量，而是边界一致

模型只负责提出下一步动作。Harness 必须拥有真正的控制权：

- `ToolRegistry` 决定当前能看到和调用哪些工具；
- `PermissionPolicy` 在 handler 前执行硬权限判断；
- `HookRegistry` 提供生命周期扩展，但不能放宽系统拒绝；
- `MemorySession`、`CompactionManager` 和 `DynamicPromptProvider` 控制上下文；
- `RecoveryManager` 处理 429、529、输出截断和输入过长；
- SQLite Task、Protocol、Mailbox 和 Worktree 保存可恢复状态；
- `JobSupervisor`、`CronRuntime`、`TeammateRuntime` 和 `McpRuntime` 持有长期资源；
- `AgentRunner.close()` 按逆序尝试关闭全部资源。

这意味着 Prompt 可以提示模型"不要写出工作区"，但真正阻止越界写入的必须是运行时权限和路径解析。MCP server 可以在 description 中声称自己只读，但权限只能来自本地可信策略。

### Python 项目结构

```text
python/ch20_agent/
├─ agent_ch20/
│  ├─ core/                  # 领域契约：dataclass 值对象与 Protocol 接口
│  │  ├─ profiles.py          # 能力增量表与 P01-P20 固定档案
│  │  ├─ loop.py              # 唯一 AgentRunner
│  │  ├─ tools.py             # ToolRegistry、快照、ToolContext
│  │  ├─ permissions.py       # 硬权限策略与审批/审计 Port
│  │  ├─ hooks.py             # 生命周期扩展点
│  │  ├─ messages.py          # 消息类型与 validate_tool_pairing
│  │  ├─ model.py             # 供应商无关模型契约与错误类型
│  │  ├─ events.py            # typed EventInbox
│  │  ├─ commands.py          # 命令执行 Port
│  │  └─ filesystem.py        # 工作区文件系统 Port
│  ├─ features/              # 各章能力的领域服务
│  │  ├─ prompting.py         # 固定顺序 Prompt 渲染 + runtime_status
│  │  ├─ memory.py            # 记忆选择与持久化
│  │  ├─ compaction.py        # 四级压缩与 artifact 落盘
│  │  ├─ recovery.py          # 429/529/length/prompt-too-long 恢复
│  │  ├─ tasks.py             # Task DAG 状态机
│  │  ├─ work_stealing.py     # 租约认领
│  │  ├─ worktrees.py         # 可信 Worktree cwd
│  │  ├─ background.py        # JobSupervisor 与 BackgroundDispatcher
│  │  ├─ cron.py              # 定时调度
│  │  ├─ teammates.py         # 队友生命周期
│  │  ├─ mailbox.py           # 持久 Mailbox 与协议消息扩展
│  │  ├─ protocol.py          # 计划审批与优雅关机
│  │  ├─ mcp_tools.py         # MCP 动态工具池
│  │  ├─ skills.py            # Skill 渐进披露
│  │  ├─ subagents.py         # 子 Agent 最小工具面
│  │  ├─ todos.py             # TODO 快照
│  │  └─ builtin_tools.py     # 标准工具
│  ├─ adapters/              # 外部边界实现
│  │  ├─ openai_chat.py       # OpenAI 兼容适配器
│  │  ├─ powershell.py        # PowerShell 命令边界
│  │  ├─ filesystem.py        # 本地文件系统
│  │  ├─ git.py               # Git 子进程
│  │  ├─ mcp_client.py        # stdio MCP 客户端
│  │  ├─ task_sqlite.py       # SQLite 任务库（含 claim token 与 lease）
│  │  ├─ task_json.py         # JSON 任务库
│  │  └─ *_json.py            # 后台、Cron、Mailbox、Protocol 的 JSON 持久化
│  ├─ mcp_servers/demo.py    # 可运行的演示 MCP server
│  ├─ bootstrap.py           # 唯一组合根
│  ├─ config.py              # 四项 OpenAI 配置读取
│  └─ cli.py                 # 真实运行入口
└─ tests/                    # 42 个离线测试文件，共 215 个测试
```

### Java / Spring 概念映射

| Python（本章） | Java / Spring 对应 | 职责 |
| --- | --- | --- |
| `bootstrap.build_agent()` | `@Configuration` 类 | 唯一组合根，装配并校验共享关系 |
| `core/profiles.py` | 枚举常量 + 只读 `Set` | 章节能力白名单，用对象身份校验 |
| `core/loop.AgentRunner` | 应用层 `Service` | 编排模型、工具、权限、Hook |
| `core/tools.ToolRegistry` | `Map<String, Handler>` + 只读视图 | 工具注册与不可变快照 |
| `core/permissions.PermissionPolicy` | Spring Security `AccessDecisionManager` | handler 前的硬授权 |
| `core/hooks.HookRegistry` | `HandlerInterceptor` 链 | 生命周期扩展，不能放宽拒绝 |
| `adapters/*.py` | `@Repository` / 外部 Client | 基础设施实现 |
| `features/*.py` | 领域 `Service` | 各能力的业务规则 |
| `Protocol` 类 | `interface` | 可注入的抽象边界 |
| `@dataclass(frozen=True, slots=True)` | 不可变 `record` | 值对象 |
| `tests/` 中的 Fake | Mockito stub / 手写测试替身 | 离线注入外部边界 |

### Python 语法与 Java 对照

写这一章时会用到的 Python 结构，按第一次出现的顺序列出：

| Python 写法 | 含义 | Java 对照 |
| --- | --- | --- |
| `self` | 实例方法显式接收自身 | Java 隐式 `this`，Python 必须写出来 |
| `@dataclass(frozen=True, slots=True)` | 自动生成构造器、`__eq__`，禁止改字段 | 不可变 `record`，`slots` 类似固定字段布局 |
| `class X(Protocol)` | 结构化子类型接口，无需显式继承 | `interface`，但实现类不必 `implements` |
| `@runtime_checkable` | 允许对 Protocol 使用 `isinstance` | `instanceof` 对接口生效 |
| `T \| None` | 可空类型 | `Optional<T>` 或 `@Nullable T` |
| `frozenset[Capability]` | 不可变集合 | `Set.copyOf(...)` |
| `tuple[str, ...]` | 不可变变长序列 | `List.copyOf(...)` |
| `Callable[[], Mapping[str, object]]` | 零参数返回映射的可调用对象 | `Supplier<Map<String, Object>>` |
| `Literal["read", "write"]` | 只允许固定字面值 | 枚举 |
| `dict` 推导 / 生成器表达式 | 就地构造集合 | Stream `map` / `collect` |
| `with self._lock:` | 上下文管理器，退出自动释放 | `try-with-resources` 或 `synchronized` |
| `*args` / `**kwargs` | 变长位置参数 / 关键字参数 | 可变参数 `T...`；后者 Java 无直接对应 |
| `raise ... from None` | 抛出异常并切断原因链 | `throw new X(msg)`（不传 cause） |

### 建议的测试优先考古顺序

从一个聚焦测试出发，比从 `bootstrap.py` 顶部读起容易得多：

1. `tests/test_ch20_full_harness.py::test_full_harness_combines_dynamic_context_mcp_policy_and_pairing`——一次读完本章要证明的全部跨能力边界；
2. `tests/test_config.py::test_every_profile_is_a_strict_superset_of_the_previous_chapter`——理解能力增量表为什么能保证单调递增；
3. `agent_ch20/core/profiles.py`——看 `_PROFILE_DELTAS` 如何累加成 P20；
4. `agent_ch20/features/prompting.py`——看 `runtime_status` 如何进入缓存键并固定在尾部；
5. `agent_ch20/bootstrap.py`——最后读组合根，此时每个参数的来历都已清楚。

---

## 2. 第 20 章仍然使用同一个 AgentRunner

固定 Profile 位于 `agent_ch20/core/profiles.py`。Python 版把原来逐章手写的能力集合改成"增量表 + 累计推导"：

```python
# 每章只声明"本章新增"的能力；完整档案由前缀累加得到。
_PROFILE_DELTAS: tuple[tuple[Capability, ...], ...] = (
    ("loop", "powershell"),
    # ... P02-P19 的增量，此处省略
    ("mcp",),
    # full_harness 是 P20 的标记能力：不新增具体运行时，只声明前十九章能力全部生效。
    ("full_harness",),
)


def _build_profiles() -> tuple[ChapterProfile, ...]:
    """把增量表累加成每章的完整档案。"""
    profiles: list[ChapterProfile] = []
    accumulated: list[Capability] = []
    for index, delta in enumerate(_PROFILE_DELTAS):
        # extend 就地追加本章增量，因此下一轮天然包含所有历史能力。
        accumulated.extend(delta)
        profiles.append(ChapterProfile(index + 1, frozenset(accumulated)))
    return tuple(profiles)


P20 = _profile_at(20)
```

这段代码里每个名字的作用：

- `_PROFILE_DELTAS`：只记录"这一章新增了什么"的二维常量表，索引即章节序号减一；
- `accumulated`：跨轮次累积的能力列表，`extend` 是就地追加（Java 的 `list.addAll`）；
- `frozenset(accumulated)`：每轮对当前累积状态做一次不可变快照，避免后续轮次改到已发布的档案；
- `ChapterProfile`：`frozen=True` 的 dataclass，只有 `chapter` 和 `capabilities` 两个字段。

这样"第 N 章档案必须是第 N-1 章的严格超集"由数据结构保证，而不是靠人工核对二十份手写集合。测试直接锁定这条不变量：

```python
def test_every_profile_is_a_strict_superset_of_the_previous_chapter():
    """增量推导必须保证章节能力单调递增，避免后一章丢掉已验证能力。"""
    previous = profile_for_chapter(1)
    for chapter in range(2, 21):
        current = profile_for_chapter(chapter)
        assert current.chapter == chapter
        assert previous.capabilities < current.capabilities
        previous = current
```

`<` 在 `frozenset` 上是"真子集"运算符，等价于 Java 的 `a.size() < b.size() && b.containsAll(a)`。

`full_harness` 是整合验收标记，不对应新的业务模块。它表示：P1-P19 的全部 Capability 已经在同一组合根中连接并通过交叉验证。

因此，第 20 章没有新增业务模块，但需要在四个代码位置把完整 Harness 标记、运行态状态、Prompt 尾部段落和固定入口连接起来。

第一处，在 `profiles.py` 的 `_PROFILE_DELTAS` 末尾追加 `full_harness` 增量，并让 `profile_for_chapter(20)` 返回累计出的 P20 固定对象。

第二处，在 `prompting.py` 的渲染器末尾增加可选的 `runtime_status` 段落，并允许 Provider 用 `status_provider` 每轮重新读取状态。

第三处，在 `bootstrap.py` 汇总 MCP 连接与后台 pending work，并只给 P20 安装该状态 Provider。

第四处，`cli.py` 作为固定入口把 `P20` 交给 `build_agent()`，自身不复制工具注册、恢复、调度或关闭逻辑。后续修复公共运行时，所有已引入该能力的章节会一起受益。

Python 版另有一处结构性改动：组合根原先用 `profile in (P17, P18, P19)` 这类章节元组做门禁，每加一章都要改十几处元组。第 20 章统一改成能力判断：

```python
# 以下全部改用 capability 判断而不是逐个列出 profile 单例：P20 是 P19 的严格超集，
# 只要能力集合包含对应标记，同一段共享关系校验就自动对 P20 生效。
if "worktree" in profile.capabilities and worktree_runtime is None:
    raise ValueError("第十八章及以后必须提供 worktree_runtime")
```

`profile` 本身仍用对象身份校验，确保调用方不能临时拼一个同字段对象冒充正式章节：

```python
if profile_for_chapter(profile.chapter) is not profile:
    raise ValueError("必须传入固定的章节配置对象")
```

`is` 比较对象标识（Java 的 `==` 引用比较），`==` 才是值相等（Java 的 `equals`）。这里必须用 `is`。

---

## 3. 组合根如何连接全部能力

`build_agent()` 的关键字参数是第 20 章唯一的外部边界。离线测试可注入模型、时钟、命令执行器和各类持久化运行时；真实 CLI 则创建正式适配器。

关键依赖关系如下：

| 资源 | 必须共享的对象 | 原因 |
|------|----------------|------|
| Cron | `JobSupervisor`、`EventInbox` | 调度和后台完成事件必须进入同一 typed inbox |
| Teammate | Supervisor、Cron、Inbox、Mailbox | 队友生命周期、消息和事件由同一 owner 管理 |
| Protocol | Teammate、Mailbox | 请求状态与实际投递参与方必须一致 |
| Work stealing | SQLite Task store、Worktree claim service | 自动认领与手动认领必须使用同一租约边界 |
| Worktree | SQLite Task store、Git runner | Task、claim、binding 是独立状态线，但必须引用同一 Task |
| MCP | Lead 的 live `ToolRegistry` | 动态工具只进入 Lead，并在下一次模型请求可见 |

`build_agent()` 会检查这些对象身份。依赖虽然类型正确，只要共享关系错误，也会在启动阶段明确失败：

```python
if task_store is not work_stealing_runtime.store:
    raise ValueError("第十七章 task_store 必须和 work_stealing_runtime 共享同一实例")
```

### 区分 Adapter、核心 Service、Registry 与组合根

这四类对象容易混淆，本章的划分是：

- **Adapter**（`adapters/`）：唯一允许接触进程、文件系统、网络和数据库的层。`SubprocessMcpConnection` 启动子进程，`OpenAIChatModel` 发 HTTP，`SqliteTaskStore` 写数据库。它们实现 `core/` 中的 `Protocol`。
- **核心 Service**（`features/`）：只依赖 `Protocol`，不知道具体实现。`McpRuntime` 管理连接状态机，但不知道连接是 stdio 还是 HTTP；测试因此可以注入 `FakeMcpConnection`。
- **Registry**（`core/tools.ToolRegistry`）：工具名到 handler 的映射，并提供 `snapshot()` 生成不可变视图。它是"模型这一轮能调用什么"的唯一权威。
- **组合根**（`bootstrap.py`）：唯一知道"哪个 Adapter 配哪个 Service、哪些对象必须共享"的地方。它不包含业务规则，只做装配和共享关系校验。

判断标准很简单：如果一段代码同时知道 `SqliteTaskStore` 和 `WorktreeRuntime` 两个具体类，它就只应该出现在组合根里。

---

## 4. 单一循环的真实执行顺序

每个用户 turn 都遵守同一顺序：

```text
UserPromptSubmit Hook
-> drain typed events
-> 选择相关 Memory，整理历史并压缩完整消息组
-> 渲染当前 system prompt，取得不可变 ToolRegistry Snapshot
-> RecoveryManager 调用 OpenAI Chat Completions
-> 追加规范化 assistant 消息
-> 对每个 tool call：
     JSON + schema 校验
     -> 解析可信 ToolContext / Worktree cwd
     -> PreToolUse Hook
     -> PermissionPolicy / 人工审批
     -> handler 或后台提交
     -> PostToolUse Hook
     -> 追加同 ID tool result
-> 无工具调用时运行 Stop Hook，最多强制续跑一次
-> 返回最终文本
```

这里有三个不能破坏的不变量。

### 4.1 一次回复只使用一个 Registry Snapshot

模型请求中的 tools 与该回复中全部 tool calls 使用同一个 sealed snapshot。即使回复里的第一个调用连接了 MCP，第二个调用也不能提前使用新工具；新工具只能出现在下一次模型请求。

Python 版的 `ToolRegistry.snapshot()` 返回 `mutable=False` 的新实例，与 live registry 共享版本号但不共享可变映射。

### 4.2 一个 tool call 必须恰好配一个 tool result

未知工具、坏 JSON、schema 错误、权限拒绝、超时、远端失败都必须形成相同 `tool_call_id` 的错误结果。不能因为其中一个调用失败，就丢弃同一 assistant 消息里的其他调用。

`core/messages.validate_tool_pairing()` 是这条规则的可执行版本。本章的 fake 模型在每次 `complete()` 入口都调用它，任何不成对的历史会立即暴露，而不是等到真实 API 报 400。

### 4.3 Prompt 和 Hook 都不是授权边界

工作区外写入由 `PermissionPolicy` 拒绝。Teammate 的 effectful 工具由计划门控规则拒绝。MCP 权限由本地 `McpToolPolicy` 决定。Hook 只能在硬边界允许的范围内修改输入或输出。

---

## 5. 动态上下文：Skill、Memory、工具和工作区

第 20 章的 system prompt 不是固定字符串。`DynamicPromptProvider` 每次请求按固定顺序渲染：

1. identity 与静态 context；
2. 当前工具目录；
3. 当前 workspace；
4. Skill catalog；
5. 本轮选中的 Memory；
6. 可选的 `runtime_status`。

Skill 初始只公开名称和描述，正文通过 `load_skill` 按需读取。Memory 只注入与当前查询相关的记录。大工具结果和旧历史保存到 workspace artefact，再把有界引用放回对话。

动态 MCP 工具也使用同一个 live registry。因此，连接前 system prompt 不含远程工具；连接成功后的下一轮才同时更新 prompt 和 OpenAI tools schema。

P20 新加入的 `runtime_status` 不是普通对话文本，而是组合根在每轮渲染前读取的可同步状态：

```python
def _full_harness_runtime_status(
    background_supervisor: JobSupervisor | None,
    cron_runtime: CronRuntime | None,
    teammate_runtime: TeammateRuntime | None,
    mcp_runtime: McpRuntime | None,
) -> dict[str, object]:
    """汇总 P20 需要让模型看见的两项运行态：MCP 已连接 alias 与是否仍有异步工作。

    这里只读取同步属性，不 await 后台任务，也不向远端发请求：Prompt 渲染发生在
    每次模型请求前，任何阻塞都会拖慢整个回合。

    注意：这份 JSON 只是给模型的决策提示，不是授权依据。真正的边界仍在
    `PermissionPolicy`、计划门控和 `McpToolPolicy`。
    """
    # 三个运行时任一还有未完成工作就算 pending；用 `is not None and ...` 而不是
    # Java 的 `?.` 空安全调用，Python 需要显式判空。
    pending_work = (
        (background_supervisor is not None and background_supervisor.has_pending_work)
        or (cron_runtime is not None and cron_runtime.has_pending_work)
        or (teammate_runtime is not None and teammate_runtime.has_pending_work)
    )
    connections: tuple[str, ...] = (
        () if mcp_runtime is None else tuple(mcp_runtime.connected_aliases)
    )
    return {"mcp_connections": connections, "pending_work": pending_work}
```

`any(...)` 接收一个可迭代对象，任一为真即返回 `True`，等价于 Java 的 `stream().anyMatch(...)`。这里传的是元组而非生成器，因为三个判断都是廉价的属性读取。

组合根只给声明了 `full_harness` 的档案安装这个 Provider：

```python
status_provider = (
    (lambda: _full_harness_runtime_status(
        background_supervisor, cron_runtime, teammate_runtime, mcp_runtime
    ))
    if "full_harness" in profile.capabilities
    else None
)
```

注意注入的是**函数**而不是**快照**。`DynamicPromptProvider` 每轮调用它一次：

```python
def render(self) -> str:
    """转发给 Renderer；每次调用都会重新读取工具、选中记忆和运行态状态。"""
    status = None if self._status_provider is None else self._status_provider()
    return self._renderer.render(..., status=status)
```

如果这里注入的是构建期的 `dict`，MCP 连上之后 Prompt 永远显示 `[]`。这是 Java 里注入 `Supplier<T>` 而不是 `T` 的同一个理由。

这段状态放在全部固定段落之后，例如：

```text
## runtime_status
{"mcp_connections":["fake"],"pending_work":false}
```

`runtime_status` 也参与渲染缓存键，因此连接状态变化会让下一次请求重新渲染；状态不变时仍然命中缓存。测试同时锁定这两点：

```python
def test_live_status_provider_is_reread_and_appended_last(tmp_path: Path) -> None:
    connected = False
    provider = DynamicPromptProvider(
        renderer,
        identity="agent",
        tools=tools,
        workspace=str(tmp_path),
        context={"chapter": 20},
        status_provider=lambda: {"mcp_connections": ["fake"] if connected else []},
    )

    first = provider.render()
    connected = True
    second = provider.render()

    assert first.endswith('## runtime_status\n{"mcp_connections":[]}')
    assert second.endswith('## runtime_status\n{"mcp_connections":["fake"]}')
    assert renderer.cache_hits == 0
    # 状态不变时仍然命中缓存，避免每轮重复拼接整个 Prompt。
    assert provider.render() == second
    assert renderer.cache_hits == 1
```

它不替代 `EventInbox`：typed event 仍在请求前消费，状态只回答"当前是否仍有异步工作、MCP 已连接哪些 alias"这两个模型决策问题。这里只读取同步 getter，不等待后台任务、不查询远端，也不把运行态 JSON 当作授权依据。

---

## 6. Task、Teammate 与 Worktree 是三条状态线

完整 Harness 不把所有状态塞进一个 Task object。

- Task 保存 DAG、状态、owner、claim token 和 lease；
- Protocol 保存计划或关机请求的 typed 状态机；
- Worktree binding 保存 Task 与隔离 Git 工作区的绑定和审计事件。

典型流程是：

```text
Lead 创建 A -> B DAG
-> B 因依赖保持 blocked
-> A completed 后 B 变为 pending
-> Teammate 提交计划
-> Lead 批准计划
-> Teammate 原子 claim B
-> claim token 绑定 Worktree execution scope
-> 每次工具调用重新解析可信 cwd
-> 文件只写入对应 Worktree
```

计划未批准时，effectful handler 和后台提交次数必须为零。批准计划不会绕过其他权限：工作区边界和人工审批仍然生效。

Python 版在迁移中给 Mailbox 的协议扩展补了一个显式 Protocol，替换原来的 `hasattr` 鸭子类型判断：

```python
@runtime_checkable
class ProtocolMailboxStore(MailboxStore, Protocol):
    """支持 typed 协议消息的 Mailbox Repository 扩展协议。

    Java 对照：这相当于让 ProtocolMailboxRepository extends MailboxRepository，
    多出一个只有协议实现才提供的 send_protocol 方法。
    """

    def send_protocol(self, ..., request_id: str, approved: bool | None) -> ProtocolMailboxMessage: ...
```

调用方因此能用 `isinstance` 做一次类型收窄，不支持协议的实现会在投递前得到明确报错，而不是运行时抛 `AttributeError`。

---

## 7. Background、Cron 和 typed event

耗时工具可以由 `JobSupervisor` 接管，当前 tool call 立即得到 job 占位结果。任务完成或失败后，Supervisor 向 `EventInbox` 发布稳定 ID 的 typed event。

`CronRuntime` 使用相同 Supervisor 和 Inbox。调度到期时，它提交后台工作；事件在下一轮由 Runner drain。事件采用至少一次投递，但相同 event ID 只注入一次，确认后再次 poll 必须为空。

测试使用可控时钟和显式释放的 worker，不依赖真实 sleep。关闭时 Supervisor 会等待或取消所有受管任务，不能遗留无主后台任务。

Python 版在这里也调整了一处门禁。原先 `BackgroundDispatcher` 只在 `profile is P13` 时注入，导致 P14 及以后虽然拥有 `background` 能力却拿不到调度器。第 20 章按 TypeScript 契约拆成两条规则：

```python
# query/cancel 两个后台工具只属于 P13 的教学工具面；P14 起改由 typed event 通知。
if "cron" not in profile.capabilities:
    register_background_job_tools(tools, background_supervisor)
# 只要存在 supervisor 就注入 Dispatcher，让 background_eligible 工具真正能转后台。
background_dispatcher = BackgroundDispatcher(background_supervisor)
```

---

## 8. 模型恢复与上下文压缩

`RecoveryManager` 把供应商错误转换为明确恢复路径：

| 情况 | 行为 |
|------|------|
| 429 | 解析 `Retry-After`，在总时限内退避 |
| 连续 529 | 达到阈值后切换显式配置的 fallback model |
| `finish_reason="length"` | 先提升输出预算；仍截断时使用 continuation prompt |
| `ModelPromptTooLongError` | 对完整消息组做一次响应式压缩后重试 |
| 第二次输入过长 | 明确失败，不无限压缩 |

未完成的 `length` 片段不会先写进正式会话历史。压缩不能拆开 assistant tool calls 与对应 tool results。

---

## 9. MCP 是动态能力边界

真实 MCP 客户端使用 stdio 子进程和逐行 JSON-RPC：

```text
子进程 stdin/stdout
-> initialize
-> tools/list / tools/call
```

模型只能提交本地 allowlist 中的 alias，不能提供 command、args 或 cwd。远端 schema 先做安全检查（拒绝外部 `$ref`），再在 handler 前验证参数。

远端业务错误保留健康连接。timeout、进程退出或 transport failure 会原子撤销该 alias 的全部工具。MCP management 工具和远程工具只安装到 Lead，Subagent 与 Teammate 不继承。

---

## 10. 关闭和重建也是业务行为

完整 Harness 的资源注册顺序为：

```text
JobSupervisor -> CronRuntime -> TeammateRuntime -> McpRuntime
```

`AgentRunner.close()` 逆序关闭：

```text
McpRuntime -> TeammateRuntime -> CronRuntime -> JobSupervisor
```

某个资源关闭失败时，Runner 仍会尝试关闭剩余资源，失败状态允许再次调用 close 完成收尾。

迁移中发现 Python 版 `McpRuntime.close()` 与这条契约有三处偏差，本章一并修正：`_closed` 原先在实际关闭前就被置为 `True`；关闭失败的连接被 `_pending.clear()` 丢弃，重试无从下手；因此重复 close 也不具备幂等语义。修正后：

```python
def close(self) -> None:
    """撤销所有动态工具并逆序关闭连接；失败连接保留下来供再次 close 重试。

    _closed 只在没有遗留连接时才置为 True。若先无条件置 True，
    失败连接就会被当成已回收，这正是"重建不猜测"要避免的状态谎言。
    """
    with self._lock:
        if self._closed and not self._pending:
            return
        # 关闭一旦发起就不再接受新连接，即使本轮回收失败。
        self._close_requested = True
        ...
        for connection in targets:
            try:
                connection.close()
            except Exception as error:
                failures.append(error)
                # 保留失败连接，使下一次 close() 能继续重试回收。
                self._pending.append(connection)
        # 只有全部回收成功才对外宣称已关闭。
        self._closed = not self._pending
```

`_close_requested` 与 `_closed` 因此是两个不同状态：前者表示"不再接受新连接"，后者表示"全部资源已确认回收"。新增的 `is_closed` 属性只在两者都满足时为真。

重建 Harness 后，SQLite Task、Memory、Cron pending event、Mailbox/Protocol 和 Worktree binding 从各自权威存储恢复。已经确认消费的事件不能重放，未知外部副作用也不能因进程重启被盲目重试。

---

## 11. 离线整合验收

第 20 章测试从 `build_agent(P20, ...)` 进入，不导入真实凭据，也不访问公网。`tests/test_ch20_full_harness.py` 的核心场景让模型在**同一个回复里**同时做两件事——连接 MCP（应成功）和写工作区外文件（必须被拒），据此一次性验证：

- UserPrompt Hook 执行；
- 初始 prompt 同时包含 Skill 目录项、相关 Memory 与完整 23 个内置工具，且不含 Skill 正文；
- 初始 `runtime_status` 为 `{"mcp_connections":[],"pending_work":false}`；
- 未连接的 MCP 工具不泄漏到 prompt 或 tools schema；
- 越界写入被硬拒绝，`../outside.txt` 确实不存在；
- 两个 tool call 都得到相同 ID 的配对结果，一个失败不影响另一个；
- 下一轮远程 MCP 工具同时进入 prompt 和 tools，且 `runtime_status.mcp_connections` 更新为 `["fake"]`；
- 最终关闭 MCP、Teammate、Cron 和 Supervisor，连接只回收一次，无活跃 worker。

另外两个测试分别覆盖 `pending_work` 的真实来源和 close 的幂等语义。

安装并运行本章全部离线门禁：

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch20_agent'
& ..\.venv\Scripts\python.exe -m pip install -e ".[dev]"
& ..\.venv\Scripts\python.exe -m pytest tests
& ..\.venv\Scripts\python.exe -m compileall -q agent_ch20
& ..\.venv\Scripts\python.exe -m ruff check agent_ch20 tests
& ..\.venv\Scripts\python.exe -m mypy agent_ch20
```

只运行本章聚焦测试：

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch20_agent'
& ..\.venv\Scripts\python.exe -m pytest tests/test_ch20_full_harness.py -v
```

当前结果：215 个测试全部通过，ruff 无告警，mypy strict 在 47 个源文件上无错误。

---

## 12. 第 20 章源码讲解地图

`agent_ch20/` 是 P1-P20 的累计快照，不是只包含 P20 新文件。P1-P19 已经引入的模块沿用其原始章节讲解，第 20 章正文负责新增标记、完整组合、运行态状态、MCP 生命周期和入口收束。

| 源码位置 | 责任 | 本章讲解位置 |
| --- | --- | --- |
| `core/commands.py`、`core/filesystem.py`、`core/events.py` | Command、文件系统与 typed event 的稳定契约 | 第 3 节组合根、第 7 节事件 |
| `core/hooks.py`、`core/permissions.py` | Hook 生命周期与硬权限策略，两个边界不合并 | 第 4 节，尤其 4.3 |
| `core/loop.py`、`core/messages.py`、`core/tools.py` | 唯一 Loop、消息配对和 Registry Snapshot | 第 4 节全部 |
| `core/model.py` | 供应商无关 Model 请求、回复、错误与恢复契约 | 第 8 节 |
| `core/profiles.py` | P1-P20 Capability 增量与 `full_harness` | 第 2 节 |
| `features/builtin_tools.py`、`features/todos.py`、`features/skills.py` | 标准工具、TODO 快照、Skill 渐进披露 | 第 3、4、5 节 |
| `features/subagents.py`、`features/tasks.py` | Subagent 最小工具面与 Task DAG 状态机 | 第 6 节及第 6/12 章 |
| `features/worktrees.py`、`features/work_stealing.py` | 可信 Worktree cwd、claim token 与自动认领 | 第 6 节及第 17/18 章 |
| `features/memory.py`、`features/compaction.py`、`features/recovery.py` | 选中记忆、上下文压缩、供应商故障恢复 | 第 5、8 节 |
| `features/prompting.py` | 固定顺序 Prompt 渲染、缓存键、尾部 `runtime_status` | 第 5 节 |
| `features/background.py`、`features/cron.py` | 受管后台任务与定时调度 | 第 7 节及第 13/14 章 |
| `features/teammates.py`、`features/mailbox.py`、`features/protocol.py` | 队友生命周期、持久 Mailbox、审批/关机协议 | 第 6、7 节及第 15/16 章 |
| `features/mcp_tools.py` | MCP alias、本地策略、动态发布、原子撤销与关闭 | 第 9、10 节及第 19 章 |
| `adapters/mcp_client.py` | stdio 子进程与逐行 JSON-RPC 边界 | 第 9 节及第 19 章 |
| `adapters/background_json.py`、`adapters/cron_json.py`、`adapters/mailbox_json.py`、`adapters/protocol_json.py` | 后台、Cron、Mailbox、Protocol 的 JSON 持久化 | 第 6、7 节 |
| `adapters/task_json.py`、`adapters/task_sqlite.py` | Task JSON/SQLite 持久化，后者持有 claim token 与 lease | 第 6 节及第 12/17 章 |
| `adapters/git.py`、`adapters/filesystem.py`、`adapters/powershell.py` | Git、本地文件系统和 PowerShell 边界实现 | 第 3、6 节 |
| `adapters/openai_chat.py` | OpenAI wire format 与响应规范化 | 第 8 节 |
| `mcp_servers/demo.py` | 可运行的演示 MCP server | 第 9 节 |
| `bootstrap.py`、`cli.py`、`config.py` | 组合根、命令行入口、环境配置装配 | 第 2、3、13 节 |

这个地图也是验收清单：不能只确认文件存在，必须确认每个位置都有对应职责、所属章节说明和直接测试。

---

## 13. 运行第 20 章

`config.py` 只负责把四项显式 OpenAI 配置读取成冻结的配置对象，不会在缺失配置时创建 `.agent_tutorial` 或 MCP 状态。`cli.py` 再校验当前目录是目标 Git 仓库根，构造真实运行时，最后统一交给 `build_agent()`。

`.env` 复用 `python/.env`，需要四个字段：

```text
OPENAI_BASE_URL=https://your-endpoint/v1
OPENAI_API_KEY=your-key
OPENAI_MODEL=your-model
OPENAI_FALLBACK_MODEL=your-fallback-model
```

第 18 章起必须从目标 Git 仓库根目录运行：

```powershell
Set-Location 'E:\workspace\demo-repo'
& E:\cj\study\learn-agent\python\.venv\Scripts\python.exe -m agent_ch20.cli --prompt "分析仓库并先给出计划"
```

CLI 把资源生命周期收在一处：先创建可追踪的运行时，再运行 Agent，最后按统一失败列表关闭。`build_agent()` 成功后由 Runner 接管资源；构造失败时仍按创建顺序逆序兜底回收，避免遗留 stdio 子进程或后台线程。

没有四项 OpenAI 配置时，入口会在模型或 MCP 状态创建前列出缺失字段并返回退出码 2：

```text
配置错误: 缺少或填写错误的必要配置: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL, OPENAI_FALLBACK_MODEL
```

cwd 不是 Git 仓库根目录时，同样在创建 `.agent_tutorial` 状态前失败。这两条边界都由测试锁定。

### 授权与安全边界

真实运行时需要注意：

- **写入边界**：`PermissionPolicy` 用 `write_boundary` 解析真实路径，工作区外写入一律拒绝，不依赖 Prompt 约束；
- **人工审批**：`write_file`/`edit_file` 和 external effect 工具走终端审批；非 TTY 环境默认拒绝，避免无人值守时自动放行；
- **审计**：权限决策写入 stderr，不污染模型最终回答；
- **MCP**：模型只能提交本地 allowlist 中的 alias，不能提供 command 或 args；远端 description 不授予任何权限；
- **子进程**：命令通过参数数组传递，不经 shell 拼接。

离线测试不需要凭据和网络：模型、MCP 连接、命令执行器和时钟全部通过 fake 注入。

---

## 与 Claude Code 的差异

P20 的完整 Harness 在教学简化上与 Claude Code 的真实机制存在以下差异：

**工具面。** CC 的 s20 教学代码列出 27 个内置工具，包含 `compact`、`list_crons`、`cancel_cron`、`check_inbox`、`request_plan` 等。P20 的固定 Lead 工具面是 23 个教学工具。`compact` 不暴露为模型工具，由 `CompactionManager` 和 `RecoveryManager` 在请求前或恢复期自动执行。Cron 只保留 `schedule_cron`。队友消息通过 typed `EventInbox` 自动注入，不提供 `check_inbox`。协议把 `request_plan` 归入 shutdown/plan_approval 状态机，并由 `review_plan` 与 `submit_plan` 表达。额外提供 `disconnect_mcp` 用于显式断开连接。

**循环信号。** CC 教学代码以 `has_tool_use(response.content)` 作为是否继续工具轮的信号，而不是直接信任 `stop_reason`。P20 使用 OpenAI `tool_calls` 字段，并强制一个 assistant tool call 恰好配对一个同 ID 的 tool result。没有工具调用时，由 Stop Hook 做至多一次续跑检查。

**组装位置。** CC 用 `assemble_tool_pool()` 与 `assemble_system_prompt(context)` 分别组装每轮工具和 Prompt。P20 在 `build_agent(P20, ...)` 统一组合，并对各运行时做对象身份检查。类型相同但共享关系错误会在启动阶段失败，而不是到运行时才暴露。

**权限架构。** CC 把权限、日志和审计都挂在 `PreToolUse` hook 上。P20 将 `HookRegistry` 与 `PermissionPolicy` 分离。Hook 只记录、转换和拦截输入输出，不能放宽 `PermissionPolicy`、计划门控或 `McpToolPolicy` 的硬拒绝；这是第 4.3 节不变量在组合根上的具体体现。

**执行者边界。** CC 的 MCP 工具对主 Agent 和子 Agent 都可用。P20 明确 MCP 管理工具和远程工具只安装到 Lead 的 live registry，Subagent 与 Teammate 不继承；Teammate 默认只获得受限文件、消息、协议和任务工具面。

**教学收束。** CC 用"机制很多，循环一个"总结。P20 用"模型提议，Harness 决策"收束：循环结构相同，但每个状态和权限边界都必须在组合根中可验证、可重建、可关闭。

这些简化让本章能聚焦于"分章能力如何收束成同一个 Harness"。真实场景中还应关注 CC 的成熟 harness 细节：工具池排序与缓存断点、MCP 重连/OAuth/反向通知、后台任务通知优先级，以及更细的恢复与压缩策略。

---

## 14. 最后的架构结论

一个可运行的 Agent 不是"模型加几十个工具"。它是一组可以验证的所有权和状态边界：

- 模型提议，Harness 决策；
- Prompt 指导，Policy 授权；
- Hook 扩展，硬边界优先；
- 消息成组，调用与结果严格配对；
- 状态各有权威存储，重建不猜测；
- 长期任务都有 owner，关闭能回收；
- 动态能力下一轮生效，不污染当前快照；
- Subagent 和 Teammate 默认只获得裁剪后的最小工具面。

第 20 章因此没有第二个 Agent。它只有一个被前十九章逐步加固、现在可以整体证明的 Agent Harness。

---

至此，20 章 Python 教程形成完整闭环：每章有固定入口、累进能力、离线行为测试和真实 OpenAI 兼容运行路径；最终章复用同一实现完成整合验收。
