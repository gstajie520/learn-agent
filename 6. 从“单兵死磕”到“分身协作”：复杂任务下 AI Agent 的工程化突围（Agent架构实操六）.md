让一个 Agent 追查深调用链里的 bug，常见的失控是这样：它读文件、搜符号、跑命令，几十轮工具结果很快就塞满历史。即使第 5 章的 TODO 还在，父任务仍然要同时记住最初目标、当前计划和一大段探索轨迹；每多一次工具调用，模型要处理的上下文就更长。

这时不应把所有工作都压进同一条对话。更合适的做法是：把一个自包含子任务交给一次性 Subagent，让它从全新历史开始探索。父 Agent 最后只接收一句有证据的结论，而不接收中间的搜索和读文件记录。

第 6 章新增的正是这个能力：`task`。

![图片](https://mmbiz.qpic.cn/sz_mmbiz_png/1z26fJxNuiaElNML5X1yY2KNZfJl8XyLRDoq2QhYoEAPMHMu02Lkc2LrhZv3OupQUeptQSdExwhKO5ibQ9lkca43Wc93MdEouuxBr080ZRhvM/640?from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

---

## 本章导读

### 读完这一章，你能做到什么

1. 说清楚第 5 章的 TODO 和第 6 章的 `task` 各自解决什么问题，为什么前者不能替代后者。
2. 解释「隔离」在本章的**确切范围**——隔离的是消息历史，不是权限、不是文件、不是进程。
3. 说出父 Agent 调用一次 `task` 后，父 history 里到底多了哪几条消息。
4. 讲清楚为什么禁止递归委派，以及为什么这件事要防两层。
5. 解释子 Agent 跑满 30 轮时，为什么不能把最后一个工具输出当成结论返回。
6. 说出 `task` 的 `try/catch` 为什么不算「业务逻辑里吞异常」。

### 你需要先具备什么

| 需要的东西 | 从哪来 | 检查方式 |
| --- | --- | --- |
| 第 1–5 章的能力累积 | 前五章 | `npm run test:ch05` 是 14 文件 / 109 测试 |
| 理解权限四态与合并优先级 | 第 3 章 | 能说出 `deny > ask > allow > passthrough` 为什么这个顺序 |
| 理解工作区硬边界在规则之前 | 第 3 章 | 能说出为什么越界写入根本不弹审批框 |
| 理解 `AgentRunner` 是可复用的类 | 第 1 章 | 知道 `new AgentRunner({...})` 需要哪些参数 |

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| **`task`** | 本章唯一新增的工具，作用是启动一个子 Agent 并**只**返回它的最终结论 |
| **Subagent / 子 Agent** | 一个新的 `AgentRunner` 实例，全新历史，跑完就丢 |
| **自包含任务** | 不依赖父 history 就能理解的任务描述。因为子 Agent 看不到父 history |
| **`SubagentTool`** | 注册 `task` 工具的类。它**不是**第二套 Agent Loop |
| **`effect: "external"`** | 声明「这个工具会发起一段独立执行」，既不是读也不是写 |
| **`modelFactory` / `toolsFactory`** | 传函数而不是实例，好让每次委派都能拿到独立的工具注册表 |
| **`DEFAULT_SUBAGENT_MAX_TURNS`** | 常量 `30`。可以调低，**不能**调高 |
| **`subagent_turn_limit`** | 跑满轮数还没有最终文本时的结构化错误码 |
| **`subagent_execution_error`** | 其他一切内部异常的脱敏错误码 |
| **`subagent_configuration_error`** | 工厂返回了错东西（不是 `ToolRegistry`、含 `task`、不是 `ModelClient`） |
| **脱敏** | 把内部异常文本换成固定字符串，不让 API Key、路径、栈信息回流给模型 |
| **执行边界** | 允许写 `try/catch` 的地方：任务入口、API 边界、进程边界 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先看一眼一次真实的委派   ← 父看到什么、子看到什么、审计有几条
  ↓
② 这章解决什么问题 + 验收契约 ← 隔离的确切范围
  ↓
③ 一～二：输入契约与执行边界  ← 为什么强调「自包含」
  ↓
④ 三～四：隔离什么、共享什么  ← 本章最核心的一对概念
  ↓
⑤ 五～六：两条硬约束         ← 禁止递归、轮数上限
  ↓
⑥ 七：组合根接线            ← 工厂为什么返回函数
  ↓
⑦ 八～九：验证、运行、排错    ← 动手 + 实验
  ↓
⑧ 十～十一 + 小结            ← 刻意不做的事 + 与官方差异 + 自测
```

---

## 先看一眼一次真实的委派

先不看代码。下面这组数据全部来自 `ch06-subagents.test.ts` 的断言，是运行时的确定事实。

父 Agent 收到任务「delegate the write」，调用了一次 `task`，委派内容是 `" write child.txt with evidence "`（注意首尾有空格）。

**父模型第 1 次请求看到 7 个工具：**

```text
shell  read_file  write_file  edit_file  glob  todo_write  task
```

**子模型第 1 次请求（也就是整体第 2 次请求）看到 6 个工具，`task` 不在里面：**

```text
shell  read_file  write_file  edit_file  glob  todo_write
```

**子模型看到的完整消息数组只有两条：**

```text
[
  { role: "system", content: "You are a focused coding subagent working in the current workspace. ..." },
  { role: "user",   content: "write child.txt with evidence" }
]
```

三个细节：首尾空格被 schema `trim()` 掉了；**没有父 Agent 的任何一条消息**；system prompt 也不是父 Agent 那句，而是子 Agent 专用的那句。

**子 Agent 写了文件，然后给出结论。父 history 最终只有 4 条：**

```text
user:      delegate the write
assistant: tool_calls=[task(parent-task)]
tool:      "child conclusion"          toolCallId=parent-task
assistant: parent final
```

子 Agent 那一轮 `write_file` 调用、那条 tool result、它的最终文本组装过程——**父 history 里一条都没有**。父 Agent 只知道「我委派了，得到一句结论」。

**但是审计记录有两条，审批记录有一条：**

```text
审计：[["task", "allow"], ["write_file", "allow"]]
审批：["write_file"]
```

这一行是本章最需要理解的地方。父 history 干净了，**治理没有变松**：子 Agent 的 `write_file` 照样弹审批框，照样进审计。

### 那如果子 Agent 想越界写呢

同一个测试文件的第二个用例，让子 Agent 写 `../outside.txt`，而且给它配了一个**永远批准**的审批器：

```text
子模型的 tool result：
  Error [permission_denied]: Writing outside the workspace is forbidden

审计：[["task", "allow"], ["write_file", "deny"]]
审批：[]                              ← 审批器一次都没被调用
文件：outside.txt 内容仍是 "sentinel"  ← 没被改
```

审批器写着「一律批准」，却连一次都没被问到。这就是第 3 章的工作区硬边界在规则之前跑的效果——**换到子 Agent 里，一个字都没变。**

---

## 这章解决什么问题

第 5 章的 `todo_write` 解决的是“单次会话如何持续保存计划”。它让最新的完整计划快照不断回到消息流，但不会减少探索过程本身带来的上下文。

第 6 章解决的是“如何隔离一段独立探索”。父 Agent 调用 `task` 后，运行时创建一个新的 `AgentRunner`：

```text
父 Agent
  assistant: task(description=...)
        │
        ▼
一次性 Subagent
  system: 专注完成委派任务，不再委派
  user: description
  history: 全新
  tools: shell/read_file/write_file/edit_file/glob/todo_write
        │
        ▼
父 Agent
  tool: 子 Agent 的 finalText
```

这里的“隔离”只指消息历史隔离：

- 子 Agent 看不到父 Agent 的历史；
- 父 Agent 看不到子 Agent 的中间工具轨迹；
- 子 Agent 在当前 workspace 内写入的文件会保留；
- 父子仍运行在同一个 Node.js 进程中，使用同一组 Hook、权限策略、workspace 和 identity。

因此 Subagent 不是操作系统沙箱。`shell` 仍是 PowerShell 执行，文件路径边界也不是系统级隔离。它解决上下文污染，不会自动获得更少或更多权限。

「隔离什么、共享什么」这件事必须一次说清，否则后面每一节都会读错。逐项列成表：

| 东西 | 父子关系 | 后果 |
| --- | --- | --- |
| 消息历史 | **隔离** | 子看不到父的目标和探索；父看不到子的中间轨迹 |
| system prompt | **隔离** | 子用的是 `DEFAULT_SUBAGENT_SYSTEM_PROMPT`，不是父那句 |
| 工具注册表 | **隔离**（各自 `new`） | 子的 TODO 快照和父的互不影响 |
| `HookRegistry` | **共享同一实例** | 子工具调用同样打 `[Hook] PreToolUse` |
| `PermissionPolicy` | **共享同一实例** | 子的 `write_file` 照样弹审批、照样进审计 |
| workspace 路径 | **共享** | 子写的文件父能读到，不是临时副本 |
| identity | **共享** | 审计里看不出「这是子 Agent 干的」 |
| Node.js 进程 | **共享** | 没有子进程，没有容器 |
| 模型客户端 | 共享（真实接线里是同一个实例） | 见第七节 |

一句话记法：**上下文隔离，治理共享。** 委派不是提权，也不是降权。

那和第 5 章的 TODO 是什么关系？两者解决的是同一个大问题的不同侧面：

| | 第 5 章 `todo_write` | 第 6 章 `task` |
| --- | --- | --- |
| 对付的东西 | 进度在长上下文里丢了 | 探索过程本身把上下文撑爆了 |
| 手段 | 把进度显式化，塞回消息流 | 把探索移出去，只带结论回来 |
| 上下文总量 | **不减少**（还略微增加） | **显著减少** |
| 谁看得见 | 父 Agent 自己 | 父 Agent 只看见结论 |
| 适用场景 | 十步重构，步骤之间互相依赖 | 「查清楚这个项目用什么测试框架」这种自包含子问题 |

两者不冲突，通常一起用：父 Agent 用 TODO 记住十步计划，其中第 3 步用 `task` 委派出去。

本章也不引入并发、后台队列、通知、长期存活 worker 或递归 Agent 树。一个 `task` 调用同步等待一个子 Agent，子 Agent 最多运行 30 轮。

---

## 第 6 章的验收契约

这一章在 `P05` 的能力集合上只增加 `subagent`：

```typescript
export const P06: ChapterProfile = Object.freeze({
  chapter: 6,
  capabilities: new CapabilitySet([
    "loop",
    "powershell",
    "tool_registry",
    "files",
    "policy",
    "hooks",
    "todo",
    "subagent",
  ]),
});
```

可观察结果有五条：

1. 每次 `task` 都从新的模型请求、工具注册表和消息历史开始；
2. 父 history 只获得与原 `task` 调用配对的最终 tool result；
3. 子 Agent 使用父 Agent 的 Hook、权限策略、workspace 和 identity；
4. 子工具集中没有 `task`，递归调用会成为 `unknown_tool`；
5. 子 Agent 30 轮后仍没有最终文本时，返回结构化 `subagent_turn_limit`，绝不把最后一个工具结果冒充答案。

此外，当委派描述为空、全空白，或包含未知字段时，`task` 也和其他工具一样，在进入 handler 之前就返回 `invalid_arguments`。

---

## 一、严格的 `task` 输入

实现位于 `typescript/chapters/ch06/src/features/subagents.ts`。`task` 输入只有一个字段，但依然通过 strict Zod schema 定义：

```typescript
const taskInputSchema = z
  .object({
    description: z
      .string()
      .transform((description) => description.trim())
      .pipe(z.string().min(1))
      .describe("A self-contained task for the subagent to complete."),
  })
  .strict();

export type TaskInput = Readonly<z.output<typeof taskInputSchema>>;
```

它表达了三个约束：

- 描述必须是 JSON string；
- 首尾空白会被去掉，`"   "` 仍然是非法输入；
- `.strict()` 拒绝额外字段。

模型会看到由同一个 schema 导出的 JSON Schema：

```json
{
  "type": "object",
  "additionalProperties": false,
  "required": ["description"],
  "properties": {
    "description": {
      "type": "string",
      "minLength": 1,
      "description": "A self-contained task for the subagent to complete."
    }
  }
}
```

为什么强调“自包含”？因为子 Agent 不会继承父 history。下面这样的委派没有足够信息：

```json
{ "description": "继续处理刚才那个问题" }
```

更好的描述会明确对象、目标和证据要求：

```json
{
  "description": "检查 package.json 和测试目录，确认项目使用的测试框架；只返回能由文件内容支持的结论。"
}
```

「自包含」不是一句原则，它有可操作的检查表。**把描述抄下来，交给一个刚打开这个项目、什么都不知道的人——他能开始干活吗？**

| 描述 | 自包含吗 | 问题在哪 |
| --- | --- | --- |
| `继续处理刚才那个问题` | ❌ | 「刚才」指的是父 history，子 Agent 看不到 |
| `修好这个 bug` | ❌ | 哪个 bug？在哪个文件？什么现象？ |
| `按我们前面说的规范重构` | ❌ | 规范在父 history 里，没传过来 |
| `读一下 login.ts` | ⚠️ | 语法上自包含，但没说**要得出什么结论**，子 Agent 只会读完就说「读完了」 |
| `检查 package.json 和测试目录，确认项目使用的测试框架；只返回能由文件内容支持的结论。` | ✅ | 对象明确、目标明确、证据要求明确 |

第 4 行值得单独看。它不会报错，但会浪费一整次委派——**子 Agent 的最终文本就是父 Agent 唯一能拿到的东西**，所以描述里必须说清「我要的结论是什么形状」。

这也解释了 `DEFAULT_SUBAGENT_SYSTEM_PROMPT` 为什么是这三句：

```typescript
"You are a focused coding subagent working in the current workspace. " +
"Complete only the delegated task, then return a concise, evidence-based final conclusion. " +
"Do not delegate further.";
```

分别对应三件事：**你在哪**（当前 workspace）、**你要交什么**（简洁、有证据的最终结论）、**你不能做什么**（不要继续委派）。第三句是提示层面的约束，第五节会讲运行时层面的两道硬约束——因为提示是可以被忽略的。

---

## 二、`SubagentTool` 只负责执行边界

`SubagentTool` 本身不是第二套 Agent Loop。它注册普通的 `ToolDefinition`，在 handler 中构造现有 `AgentRunner`：

```typescript
export const TASK_TOOL_NAME = "task";
export const DEFAULT_SUBAGENT_MAX_TURNS = 30;

export const DEFAULT_SUBAGENT_SYSTEM_PROMPT =
  "You are a focused coding subagent working in the current workspace. " +
  "Complete only the delegated task, then return a concise, evidence-based final conclusion. " +
  "Do not delegate further.";

export class SubagentTool {
  readonly toolDefinition: ToolDefinition<TaskInput>;

  constructor(options: SubagentToolOptions) {
    // 参数与运行边界校验省略
    this.toolDefinition = Object.freeze({
      name: TASK_TOOL_NAME,
      description: "Launch an isolated subagent and return only its final conclusion.",
      inputSchema: taskInputSchema,
      effect: "external",
      handler: (input, context) => this.#runTask(input, context),
    });
  }
}
```

`effect: "external"` 表示这个工具会发起一段独立执行，而不是直接读文件或写文件。第 3 章的审批策略仍然由工具名和硬边界决定。它不会因为 `task` 被标记为 external，就把所有子工具误判成磁盘写入；子工具是否允许写盘，仍由各自工具名和权限边界决定。

执行时的关键部分如下：

```typescript
const tools: unknown = this.#toolsFactory();
if (!(tools instanceof ToolRegistry)) {
  return toolError(
    "subagent_configuration_error",
    "Subagent tools factory must return ToolRegistry",
  );
}
if (tools.names.includes(TASK_TOOL_NAME)) {
  return toolError(
    "subagent_configuration_error",
    "Subagent tools must not include task",
  );
}

const runner = new AgentRunner({
  model,
  tools,
  systemPrompt: this.#systemPrompt,
  workspace: context.workspace,
  identity: context.identity,
  maxTurns: this.#maxTurns,
  hooks: this.#hooks,
  permissionPolicy: this.#permissionPolicy,
});
const result = await runner.run(input.description);
return toolSuccess(result.finalText);
```

这里没有复制消息循环。父 Agent 和子 Agent 使用同一个 `AgentRunner`，因此消息配对、工具 dispatch、权限、Hook 和 Stop 行为都走同一套实现。

---

## 三、父 history 为什么保持干净

假设父模型调用：

```typescript
toolCall(
  "parent-task",
  "task",
  '{"description":"追踪 login() 的调用链并给出文件证据"}',
);
```

子 Agent 可以进行多轮 `read_file`、`glob` 或 `shell`，但 `SubagentTool` 只返回它的最终文本：

```typescript
return toolSuccess(result.finalText);
```

因此父 history 只会保存四类消息：

```text
user:      父任务
assistant: tool_calls=[task(parent-task)]
tool:      子 Agent 的 finalText, toolCallId=parent-task
assistant: 父 Agent 的最终回答
```

子 Agent 内部历史不会 append 到父 Agent。父 Loop 仍然会正常把 `task` 的返回值写成与 `parent-task` 相同 ID 的 tool message，因此 OpenAI 工具配对契约没有例外。

这也是本章比“让父 Agent 自己继续读文件”更有价值的地方：子 Agent 可以有很长的探索轨迹，但父上下文只留下被委派的动作和可用于后续推理的结论。

把两种做法的消息数量摆在一起，差距很直观。假设这段探索需要 8 次工具调用：

```text
❌ 父 Agent 自己做
   user + assistant + (8 × [assistant tool_calls, tool result]) + assistant
   = 父 history 里 18 条消息，其中 16 条是探索垃圾

✅ 委派给子 Agent
   user + assistant(task) + tool(结论) + assistant
   = 父 history 里 4 条消息，探索的 16 条留在子 Agent 里，随它一起消失
```

**注意「消失」这个词。** 子 Agent 的历史不持久化、不落盘、不回传，`task` 返回后它就被垃圾回收了。这是一把双刃剑：

| 好处 | 代价 |
| --- | --- |
| 父上下文极干净 | 子 Agent 的推理过程**无法审计** |
| 父 Agent 不会被中间错误带偏 | 子 Agent 走错路，父 Agent 只看到一句错结论 |
| 探索多长都不影响父的 token 预算 | 出问题时不好复现——除非你另外记录 |

第二行是这个能力最现实的风险。这也是为什么 `DEFAULT_SUBAGENT_SYSTEM_PROMPT` 里要写 `evidence-based`，以及为什么建议在 description 里明确要求「只返回能由文件内容支持的结论」——**父 Agent 唯一的质量抓手就是那一句结论里有没有可核验的证据**（文件名、行号、命令输出）。

第 8 章的产物落盘和第 9 章的文件记忆会为「子 Agent 的过程需要留痕」提供另一条路径。本章刻意不做。

---

## 四、历史隔离，运行边界共享

子 Agent 使用的是新 `AgentRunner`，但不是一套新权限系统。构造时直接传入父会话组合根创建的同一实例：

```typescript
hooks: this.#hooks,
permissionPolicy: this.#permissionPolicy,
workspace: context.workspace,
identity: context.identity,
```

这意味着子工具调用的顺序仍是：

```text
PreToolUse Hook
-> PermissionPolicy
-> handler
-> PostToolUse Hook
```

例如子 Agent 尝试向 workspace 外写入：

```json
{
  "path": "../outside.txt",
  "content": "changed"
}
```

第 3 章的 workspace 硬边界先产生 deny。即使终端审批提供者会允许写入，它也不会被调用，handler 也不会运行。子模型得到的只是匹配的错误结果：

```text
Error [permission_denied]: Writing outside the workspace is forbidden
```

反过来，合法的子 `write_file` 仍按原策略请求审批并写入同一个 workspace。子 Agent 写出的文件不是临时副本，父 Agent 之后可以继续读取、编辑或验证。

---

## 五、为什么必须禁止递归委派

P06 的父工具有 7 个：

```text
shell
read_file
write_file
edit_file
glob
todo_write
task
```

子工具工厂创建的是独立标准工具集，其中只保留前六个。它不会注册 `task`。

运行时还会做第二层检查：

```typescript
if (tools.names.includes(TASK_TOOL_NAME)) {
  return toolError(
    "subagent_configuration_error",
    "Subagent tools must not include task",
  );
}
```

为什么同时有“工厂不注册”和“运行时拒绝”两层？前者是正常组装路径，后者保护工厂被错误改写时的配置边界。错误配置在模型调用前返回，不会启动一个可能无限嵌套的子会话。

三道防线各自能拦住什么，一次列清：

| 防线 | 在哪 | 拦的是 | 如果只有它会怎样 |
| ---: | --- | --- | --- |
| ① 提示 | `Do not delegate further.` | 模型的**意图** | 模型可以直接忽略提示 |
| ② 工厂 | `createStandardTools()` 不注册 `task` | 子工具集里**没有** `task` | 有人改了工厂就破了 |
| ③ 运行时 | `tools.names.includes(TASK_TOOL_NAME)` | 工厂**被改错**的情况 | —— |

注意第三道防线的返回时机：它在 `new AgentRunner(...)` **之前**。也就是说配置错了的时候，模型请求次数是 **0**：

```text
task handler 开始
  ↓
toolsFactory() → 得到一个含 task 的注册表
  ↓
❌ 立刻返回 Error [subagent_configuration_error]: Subagent tools must not include task
  ↓
（AgentRunner 没被创建，模型一次都没被调用）
```

这个「模型零调用」是有测试断言的（见第八节）。它的意义在于：**配置错误不该花钱，也不该产生任何副作用。**

如果子模型仍然生成 `task(...)`，Registry 会像处理其他未知工具一样返回：

```text
Error [unknown_tool]: Unknown tool: task
```

子模型下一轮可以根据这个结果继续完成自己的工作或给出结论，但不会真的创建孙 Agent。

---

## 六、30 轮上限和脱敏错误

默认上限定义为：

```typescript
export const DEFAULT_SUBAGENT_MAX_TURNS = 30;
```

构造 `SubagentTool` 时允许把上限调低以便测试，但不允许高于 30：

```typescript
if (!Number.isInteger(maxTurns) || maxTurns <= 0) {
  throw new Error("maxTurns must be a positive integer");
}
if (maxTurns > DEFAULT_SUBAGENT_MAX_TURNS) {
  throw new Error("maxTurns must be at most 30");
}
```

构造阶段的校验会把配置错误提前暴露在组合根，而不是等第一次 `task` 调用时才失败。正因为上限在构造时固定，测试可以用更小的 `maxTurns` 验证边界，而正式运行仍统一封顶 30 轮。

当子模型连续 30 轮只返回工具调用时，公共 Loop 抛出 `AgentLimitError`。`task` handler 在自己的执行边界把它转换为稳定错误：

```typescript
if (error instanceof AgentLimitError) {
  return toolError(
    "subagent_turn_limit",
    `Subagent exceeded max_turns=${this.#maxTurns} without a final answer`,
  );
}
```

父模型收到：

```text
Error [subagent_turn_limit]: Subagent exceeded max_turns=30 without a final answer
```

它不会收到第 30 轮最后一个工具输出，也不能把那个输出误认为子任务结论。

这一条值得展开，因为「兜底返回最后一个结果」看起来更友好，实际上更危险：

```text
❌ 如果把第 30 轮的工具输出当结论返回
   子 Agent 跑了 30 轮，最后一轮恰好是 glob 列了 40 个文件名
   → 父 Agent 收到：一串文件名
   → 父 Agent 会把它当成「子任务的结论」继续推理
   → 父 Agent 不知道子任务其实没做完

✅ 现在的做法
   → 父 Agent 收到：Error [subagent_turn_limit]: Subagent exceeded max_turns=30 without a final answer
   → 父 Agent 明确知道：委派失败了，需要拆小任务重试或换策略
```

区别在于**「没有结论」和「有一个不可靠的结论」是两种完全不同的信息**。前者父 Agent 能正确处理，后者会静默污染后续所有推理。这和第 3 章「拒绝也要回填一条明确错误」是同一条原则。

其他异常同样在 `task` 的边界脱敏：

```typescript
return toolError("subagent_execution_error", "Subagent execution failed");
```

本章一共会产生四种结构化错误，都由 `task` 自己的边界产生，父模型能靠 error code 分流：

| 错误码 | 什么时候出现 | 父 Agent 该怎么办 |
| --- | --- | --- |
| `invalid_arguments` | `description` 为空/全空白/有未知字段 | 改描述重试（handler 一次都没跑） |
| `subagent_configuration_error` | 工厂返回的不是 `ToolRegistry`、含 `task`、不是 `ModelClient` | 这是**代码 bug**，重试无用 |
| `subagent_turn_limit` | 跑满轮数还没有最终文本 | 把任务拆小再委派 |
| `subagent_execution_error` | 其他一切内部异常 | 信息不足，通常应报告失败 |

这样模型不会看到内部异常中可能出现的 API Key、文件路径或适配器细节。这里的 `try/catch` 是任务执行边界的受控输出，不是业务逻辑中吞掉失败。父 Agent 能根据结构化 error code 决定重试、改写任务，或直接报告失败。

「不在业务逻辑里 `try/catch`」和「这里可以 `try/catch`」怎么统一？看这个 `catch` 干了什么：

```text
它没有：吞掉异常继续往下跑
        用 ?? 给缺失字段兜个默认值
        假装成功

它做的是：把异常翻译成一个带 error code 的、父模型能识别的失败结果
          并且明确区分「跑超了」和「别的问题」
```

判断标准是**这个 `catch` 之后，调用方还能不能知道失败了**。这里能——父 Agent 收到的是 `isError: true` 的工具结果。所以它属于「执行边界的受控输出」，和 API handler 最外层那个 `catch` 是同一类东西，不是业务逻辑里的失败隐藏。

---

## 七、组合根如何接入 P06

`buildAgent()` 仍是唯一组合根。它先创建前五章的标准工具，再为 P06 创建 `SubagentTool`：

```typescript
const standardTools = createStandardTools(profile, commandRunner, fileSystem);
const tools = standardTools.tools;
const permissionPolicy = permissionPolicyForProfile(profile, fileSystem, dependencies);

const hooks =
  dependencies.hooks === undefined && profile.capabilities.has("hooks")
    ? new HookRegistry()
    : dependencies.hooks;

if (profile.capabilities.has("subagent")) {
  if (hooks === undefined || permissionPolicy === undefined) {
    throw new Error("subagent capability requires hooks and permission policy");
  }

  const subagent = new SubagentTool({
    modelFactory: () => dependencies.model,
    toolsFactory: () => createStandardTools(profile, commandRunner, fileSystem).tools,
    hooks,
    permissionPolicy,
  });
  tools.register(subagent.toolDefinition);
}
```

`createStandardTools()` 是前五章工具组装的唯一出口。它会按章节创建 shell 与文件工具集，并在 P05/P06 上把 `TodoTracker` 同时注册为工具和每轮观察器。父 Agent 与子 Agent 都通过同一个工厂创建独立注册表，因此父级 TODO 状态不会被子级读写，而子级仍复用同一套标准工具契约。

两点值得留意：

- 每一次 `toolsFactory()` 都新建一个 `ToolRegistry`。所以不同子任务不会共享工具注册表，也不会共享 TODO 快照。
- `modelFactory()` 返回组合根注入的模型边界。因此真实 CLI 仍只有一个 OpenAI 配置路径，离线测试仍可注入 `ScriptedModelClient`。

第二点容易读出误解，说清楚：`modelFactory: () => dependencies.model` 每次返回的是**同一个实例**。用工厂而不是直接传实例，图的不是「每次新建一个模型客户端」，而是把「什么时候取」的决定权留给 `SubagentTool`。真正每次新建的只有工具注册表。

```text
modelFactory()  → 每次都返回同一个 dependencies.model
toolsFactory()  → 每次都 new 一个 ToolRegistry（含一个新的 TodoTracker）
```

这也解释了离线测试里一个乍看奇怪的现象：父和子共用一个 `ScriptedModelClient`，所以那个脚本化回复队列是**父子按时间顺序共享**的。测试里的四条回复依次是「父调 task」→「子调 write_file」→「子给结论」→「父给结论」，就是这个原因。

还有一处**刻意的不对称**值得记住，它在 `toolsFactory` 这一行：

```typescript
toolsFactory: () => createStandardTools(profile, commandRunner, fileSystem).tools,
//                                                                        ^^^^^^
//                                                    只取 tools，丢掉 todoTracker
```

后果是：

| | 父 Agent | 子 Agent |
| --- | --- | --- |
| 工具列表里有 `todo_write` | ✅ | ✅ |
| system prompt 里提到 TODO 约定 | ✅（追加了 `TODO_SYSTEM_PROMPT`） | ❌（用的是子 Agent 专用 prompt） |
| 挂了 `toolRoundObserver` | ✅ | ❌ |
| 会收到「计划陈旧」提醒 | ✅ | ❌ |

也就是说，**子 Agent 能调用 `todo_write`，但没人提醒它、也没人读它的快照。** 这是合理的：子 Agent 只做一件自包含小任务，最多 30 轮，本来就不需要长任务的计划追踪机制。

但这确实是一处「工具存在而机制缺席」的不对称。把它明确写出来，比让读者自己在源码里发现要好——**它是取舍，不是遗漏。**

第六章固定入口也没有复制运行时：

```typescript
import { runProfile } from "../cli.js";
import { P06 } from "../core/profiles.js";

process.exitCode = await runProfile(P06, process.argv.slice(2));
```

---

## 八、用离线测试证明行为

`npm run test:ch06` 会执行 **16 个测试文件、118 个测试**。相比第 5 章的 14 文件 / 109 测试，本章新增两个文件：

| 测试文件 | 验证内容 |
| --- | --- |
| `subagents.test.ts` | 单元层：schema、上限、隔离、共享、递归拦截、错误脱敏（本章最大的测试文件，14 KB） |
| `ch06-subagents.test.ts` | 端到端：从 `buildAgent(P06, ...)` 验证真实接线的工具可见性、副作用保留和硬边界 |

第六章的领域测试 `subagents.test.ts` 覆盖：

- strict 参数 schema 和 30 轮上限；
- 父 history 只有 `task` 结果，子 history 从 system + user 重新开始；
- 父子共享 Hook、权限、workspace 与 identity；
- 每次调用重新执行模型/工具工厂，并获得新的工具注册表、Runner 和子 history；
- 递归 `task` 变为 `unknown_tool`；
- workspace 外写在子 handler 前被拒绝；
- 30 轮耗尽与未预期异常都返回脱敏结构化错误；
- 子工具工厂错误地重新注册 `task` 时，模型零调用。

组合根测试 `ch06-subagents.test.ts` 只有两个 `test`，但每一条断言都对应本章的一个设计结论。**「先看一眼一次真实的委派」那一节的全部数据就来自这里。**

第一个测试 `P06 isolates child trajectory while preserving an approved file side effect` 的关键断言：

```typescript
// ① 子 Agent 的写入真的落盘了，而且父子共用同一个 workspace
expect(await readFile(join(workspace, "child.txt"), "utf8")).toBe("child evidence");
// ② 父看 7 个工具
expect(model.requests[0]?.tools.map((t) => t.function.name)).toEqual([
  "shell", "read_file", "write_file", "edit_file", "glob", "todo_write", "task",
]);
// ③ 子看 6 个，没有 task
expect(model.requests[1]?.tools.map((t) => t.function.name)).toEqual([
  "shell", "read_file", "write_file", "edit_file", "glob", "todo_write",
]);
// ④ 子的消息数组「恰好」是这两条 —— 用 toEqual，不是 toContain
expect(model.requests[1]?.messages).toEqual([
  { role: "system", content: DEFAULT_SUBAGENT_SYSTEM_PROMPT },
  { role: "user", content: "write child.txt with evidence" },
]);
// ⑤ 父 history「恰好」是这四条
expect(result.history).toEqual([
  { role: "user", content: "delegate the write" },
  assistantMessage(null, [parentCall]),
  { role: "tool", content: "child conclusion", toolCallId: "parent-task" },
  assistantMessage("parent final"),
]);
// ⑥ 只有 write_file 请求过审批，task 自己没有
expect(approval.requests.map((r) => r.prepared.call.name)).toEqual(["write_file"]);
// ⑦ 审计有两条：task 和子 Agent 的 write_file
expect(audit.records.map((r) => [r.tool, r.decision.behavior])).toEqual([
  ["task", "allow"], ["write_file", "allow"],
]);
validateToolPairing(result.history);
model.assertExhausted();
```

④ 和 ⑤ 用的是 `toEqual` 而不是 `toContain`/`toMatchObject`，这不是风格问题：

| 断言 | 钉住的结论 |
| --- | --- |
| ④ 子消息**恰好** 2 条 | 父 history 一条都没漏进来。用 `toContain` 就证明不了这一点 |
| ④ 里 `user` 内容没有首尾空格 | schema 的 `trim()` 在委派前就生效了 |
| ⑤ 父 history**恰好** 4 条 | 子 Agent 的 `write_file` 调用和 tool result 一条都没回流 |
| ⑥ | `task` 本身不需要审批（它的 effect 是 `external`，不匹配写规则） |
| ⑦ | 但 `task` 仍然过了权限评估并留下审计。**不弹框 ≠ 不治理** |
| `model.assertExhausted()` | 四条脚本回复被用光，父子加起来一次多余的模型请求都没有 |

第二个测试 `child cannot turn approval into a workspace escape` 是本章最重要的一条安全断言。它配了一个**永远返回 allow** 的审批器，然后让子 Agent 写 `../outside.txt`：

```typescript
expect(await readFile(outside, "utf8")).toBe("sentinel");     // 文件没被改
expect(approval.requests).toEqual([]);                        // 审批器一次都没被问
expect(audit.records.map((r) => [r.tool, r.decision.behavior])).toEqual([
  ["task", "allow"], ["write_file", "deny"],                  // 审计里明确记了 deny
]);
expect(model.requests[2]?.messages.at(-1)).toEqual({
  role: "tool",
  content: "Error [permission_denied]: Writing outside the workspace is forbidden",
  toolCallId: "outside-write",
});
```

第 2 行是全章最该记住的断言。**审批器写着「一律批准」，却连一次都没被调用**——因为工作区硬边界在规则之前跑，越界写入根本走不到审批那一步。这条断言证明的不是「拒绝了」，而是**「委派没有创造出任何绕过治理的新路径」**。

先运行第六章最小验收：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run typecheck
npm run test:ch06
```

本章修改了组合根、profile 和共享 Loop 的调用方式，因此提交前还应运行完整门禁：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm test
npm run lint
npm run format:check
npm run build
```

这些测试不读取 `.env`，不访问网络。它们用可脚本化的模型回复证明运行时契约，而不是把一次真实模型调用当作覆盖率。

---

## 九、运行第 6 章

### 第 0 步：准备环境

和前五章完全一样：`typescript/` 里 `npm ci`，`.env` 里填好三个变量。

```powershell
Set-Location 'F:\笔记\Agent实操\code'
```

### 第 1 步：先跑离线测试

```powershell
npm run test:ch06
```

预期最后两行：

```text
 Test Files  16 passed (16)
      Tests  118 passed (118)
```

### 第 2 步：观察一次真实委派

```powershell
npm run ch06 -- --prompt "调用 task 独立检查本项目使用的测试框架，只返回有文件证据的结论"
```

预期在 stderr 上看到父子交错的日志。**Hook 是共享实例，所以子 Agent 那次 `run()` 也会打 `UserPromptSubmit` 和 `Stop`**：

```text
[Hook] UserPromptSubmit                  ← 父 Agent 的 run()
[Hook] PreToolUse: task                  ← 父 Agent 的委派
[Permission] task: allow (default) - No permission rule blocked the request
[Hook] UserPromptSubmit                  ← ★ 子 Agent 的 run() 开始了
[Hook] PreToolUse: read_file             ← 子 Agent 在干活
[Permission] read_file: allow (default) - No permission rule blocked the request
[Hook] PostToolUse: read_file -> ok
[Hook] PreToolUse: glob                  ← 子 Agent 继续
[Permission] glob: allow (default) - No permission rule blocked the request
[Hook] PostToolUse: glob -> ok
[Hook] Stop                              ← ★ 子 Agent 给出最终文本，它的 run() 结束
[Hook] PostToolUse: task -> ok            ← 子 Agent 结束了，task 才返回
[Hook] Stop                              ← 父 Agent 的 run() 结束
```

请特别注意三件事：

1. **子 Agent 的工具调用同样打 `[Hook]` 和 `[Permission]` 日志**——Hook 和权限策略是共享实例，子 Agent 没有任何豁免。
2. **打了两次 `UserPromptSubmit`、两次 `Stop`。** 因为父子各调用了一次 `run()`，而 Hook 注册表是同一个。日志里没有「这是子 Agent」的标记，得靠位置判断。
3. **`PostToolUse: task -> ok` 出现在子 Agent 的 `Stop` 之后**，它包住了子 Agent 的全部日志。这就是「`task` 同步等待子 Agent 完成」的直观体现。

第 2 点是本章一个真实的观测局限：**共享 `HookRegistry` 的代价是日志里分不清父子。** 想区分的话，Hook 回调可以自己维护一个嵌套深度计数——但那是读者的扩展，本章不做。

### 第 3 步：观察委派会不会被审批拦住

```powershell
npm run ch06 -- --prompt "调用 task 让子 Agent 把一句话写入 sub-demo.txt"
```

预期顺序：

```text
[Hook] PreToolUse: task
[Permission] task: allow (default) - ...      ← task 本身不需要审批
[Hook] PreToolUse: write_file

工具调用需要批准: write_file                  ← 子 Agent 触发了审批
原因: File writes require explicit approval from chapter 3 onward
允许本次调用? [y/N] y

[Permission] write_file: allow (terminal-approval) - User approved this tool call
[Hook] PostToolUse: write_file -> ok
[Hook] PostToolUse: task -> ok
```

这一步能同时看到两件事：`task` 自己是 `allow`；**子 Agent 的写入照样弹到你面前**。审批框上并没有标注「这是子 Agent 发起的」——因为父子共享 identity，见第四节的表。

### 第 4 步：统一入口

```powershell
npm run agent-tutorial -- run --chapter 6 --prompt "调用 task 总结 chapters/ch06/src/core 目录职责，再由父 Agent 给出结论"
```

### 第 5 步（可选）：完整离线门禁

```powershell
npm run typecheck
npm test
npm run lint
npm run format:check
npm run build
```

真实模型是否在某次任务中主动使用 `task` 仍有随机性，所以更可靠的验证是观察运行日志和离线测试。一次运行里，应该看到父模型先调用 `task`，随后子模型获得新 history。子 Agent 的中间读写不会回填父 history，父模型只接收最终结论或结构化错误。

### 常见报错排查

| 现象 | 原因 | 怎么处理 |
| --- | --- | --- |
| 模型压根不调用 `task` | 工具描述和系统提示都只是引导 | 把 prompt 写明「调用 task 独立完成 X」；或者接受这个结果 |
| `Error [invalid_arguments]` | `description` 是空串、全空白或带了未知字段 | 正确行为。handler 一次都没跑 |
| `Error [subagent_turn_limit]` | 子 Agent 跑满 30 轮还没给最终文本 | 把委派任务拆小。**不要**去调高上限——构造时会拒绝 >30 |
| `Error [subagent_execution_error]` | 内部异常被脱敏了 | 这个错误码故意不带细节。要排查得看 stderr 或加日志 |
| `Error [subagent_configuration_error]` | 工厂返回了错东西 | 这是代码 bug。检查 `toolsFactory` 有没有误注册 `task` |
| `Error [unknown_tool]: Unknown tool: task` | 子模型试图递归委派 | 正确行为。子工具集里没有 `task` |
| 启动报 `subagent capability requires hooks and permission policy` | P06 需要 hooks 和 policy 同时具备 | 组合根会自动补 `HookRegistry`；检查 `approvalProvider`/`auditSink` 是否传了 |
| 子 Agent 写的文件找不到 | 找错目录了 | 父子共用同一个 workspace。文件就在那儿，不是临时副本 |
| 子 Agent 越界写入没弹审批框 | 这是**预期行为** | 硬边界在规则之前，越界写入直接 `deny`，压根到不了审批 |
| 想看子 Agent 的中间推理 | 本章不留痕 | 设计如此。子 history 随 `task` 返回一起消失，见第三节 |

### 四个建议动手做的小实验

**实验一：把 `maxTurns` 调到 31，看构造就失败**

在 `bootstrap.ts` 的 `new SubagentTool({...})` 里加一行 `maxTurns: 31`，然后跑任何 `npm run ch06`。

预期：**启动就抛** `maxTurns must be at most 30`，模型一次都没被调用。
学到：上限是构造期校验，不是运行期。配置错误应该在组合根就炸，而不是等第一次 `task` 调用。**实验完记得改回去。**

**实验二：故意让子工具集包含 `task`**

把 `toolsFactory` 改成也注册一个 `task`（最简单的办法是让它返回父级的 `tools`）：

```typescript
toolsFactory: () => tools,     // ❌ 故意犯错：返回含 task 的父注册表
```

预期：`task` 调用返回 `Error [subagent_configuration_error]: Subagent tools must not include task`，而且**模型请求次数只有父那一次**（子 Agent 压根没被创建）。
学到：第五节那三道防线里的第 ③ 道。**实验完记得改回去。**

**实验三：验证「子 Agent 看不到父 history」**

在 `subagents.test.ts` 里加一个测试，让父 Agent 先读一个文件，再委派：

```text
父 Agent：read_file note.txt  →  拿到内容 "SECRET"
父 Agent：task("告诉我 note.txt 里写了什么，只凭你自己读到的")
子 Agent 的 messages[1].content 里有 "SECRET" 吗？
```

预期：**没有**。子 Agent 只看到 description 那一句，它必须自己再读一次文件。
学到：隔离是真的隔离。这也是为什么 description 必须自包含。

**实验四：观察子 Agent 的 `todo_write` 有没有提醒**

让子 Agent 连续调用四次 `read_file`（用 `ScriptedModelClient` 脚本化），然后检查子模型的第 4 次请求里有没有 `TODO_STALE_REMINDER`。

预期：**没有**。子 Agent 没挂 `toolRoundObserver`。
学到：第七节那张「刻意的不对称」表的最后一行。子 Agent 有工具但没机制。

---

## 十、这章没有做什么

新手读完一章最容易产生的误解是「这个能力已经完整了」。所以这里把边界摊开：左边是 P06 真有的，中间是它没有的，右边是这个缺口在哪一章补上（或者为什么永远不补）。

| 已经有 | 还没有 | 什么时候补 |
| --- | --- | --- |
| 一层同步委派 | 递归 / 嵌套 Agent 树 | **不补**。运行时硬拦，见第五节 |
| 全新历史（Normal 模式） | Fork 模式（共享父 prompt 前缀以复用 cache） | **不补**。会模糊「子任务上下文独立」这个教学边界 |
| 上下文隔离 | 进程 / 容器隔离 | **不补**。Subagent 从来不是安全沙箱 |
| 共享 `HookRegistry` | Hook 日志里区分父子 | **不补**。想要的话在回调里自己数深度 |
| 单个子任务串行执行 | 并行多个子任务 | 第 13 章（后台任务） |
| 子 history 用完即弃 | 子 history 落盘、可审计 | 第 8、9 章（压缩产物与文件记忆） |
| 同步等待返回 | 后台执行 + 完成通知 | 第 13、15 章 |
| 30 轮硬上限 | 动态预算、按 token 计费的上限 | 第 11 章（API 恢复与总时限） |
| 四种结构化错误码 | 「子 Agent 部分完成」这种中间态 | 要么有 `finalText`，要么是错误，没有第三种 |

本章只增加了一个受控的上下文切分点：父 Agent 把独立工作委派出去，保留最终结论与所有既有运行边界。

---

## 十一、与 Claude Code 的差异

本章的 `task` 是教学子集。Claude Code 内置的 Subagent 也有同样的设计目标：避免主上下文被搜索、日志和文件内容淹没。官方文档见：https://code.claude.com/docs/en/sub-agents。两者的主要差异如下：

- 消息历史创建方式不同。Claude Code 有三种 Subagent 模式：Normal 使用全新历史，Fork 保留父消息前缀以共享 prompt cache，General-purpose 可读写并支持后台与异步任务。本章只实现最简的 Normal 模式，刻意不引入共享历史，以免破坏“子任务上下文独立”这个教学边界。
- 递归委派不同。真实 Claude Code 允许嵌套 Subagent，但由 depth limit 和 Agent 工具管理。本章为教学安全，禁止子 Agent 再调用 `task`，只保留一层委派。
- 后台与通知不同。Claude Code 支持后台 Subagent、权限冒泡和完成通知。本章的 `task` 同步等待子 Agent 完成，不引入并发 Job、事件队列或持久化结果。
- 权限语义一致。真实 Claude Code 中，父级使用 `bypassPermissions` 或 `acceptEdits` 时，子 Agent 不能覆盖。本章的父子共享同一 `PermissionPolicy` 和 `HookRegistry`，上下文隔离不会自动降低或提高权限。
- 停止条件一致。Anthropic 在《Building Effective Agents》中把 Agent 描述为“LLM + 工具循环”，需要明确的停止条件（https://www.anthropic.com/engineering/building-effective-agents）。P06 的 `maxTurns` 和 `finalText` 就是这个原则的可测试落点：子 Agent 要么给出最终文本，要么返回结构化 `subagent_turn_limit`。

---

## 本章小结

### 三句话版本

1. `task` 把一段自包含探索搬到一个全新历史里跑，父 Agent 只拿回一句结论。
2. 隔离的**只有**消息历史和工具注册表；Hook、权限、workspace、identity、进程全部共享。
3. 委派不创造新路径：子 Agent 该弹的审批照样弹，该拦的越界照样拦。

### 一定要记住的八条

| # | 结论 | 出现在哪一节 |
| ---: | --- | --- |
| 1 | 「隔离」= 上下文隔离，**不是**沙箱、不是提权、不是降权 | 这章解决什么问题 |
| 2 | 子 Agent 看不到父 history，所以 description 必须自包含 | 一 |
| 3 | `SubagentTool` 不是第二套 Loop，它复用同一个 `AgentRunner` | 二 |
| 4 | 父 history 只多三条：`assistant(task)`、`tool(结论)`、`assistant(最终回答)` | 三 |
| 5 | 代价是子 Agent 的推理过程无法审计——所以要求 evidence-based 结论 | 三 |
| 6 | 禁止递归防三层：提示、工厂、运行时；第三层保证「模型零调用」 | 五 |
| 7 | 跑满轮数返回结构化错误，**绝不**把最后一个工具输出冒充结论 | 六 |
| 8 | 子 Agent 有 `todo_write` 但没有 `toolRoundObserver`——刻意的不对称 | 七 |

### 本章代码边界

一句话：**P06 只加了「一层同步委派」，没加并行、没加持久化、没加沙箱。** 完整的「有什么 / 没什么 / 哪一章补」对照表在第十节，读完小结请回去扫一眼那张表。

### 检查你是否真的读懂了

1. 子 Agent 在 workspace 里写的文件，`task` 返回后还在吗？为什么？（这章解决什么问题）
2. 父 Agent 调用一次 `task`，父 history 里多了几条消息？分别是什么？（三）
3. 子 Agent 看到的 system prompt 是父 Agent 那句吗？（先看一眼一次真实的委派 / 八）
4. 子 Agent 的 `write_file` 会弹审批框吗？审批框上能看出这是子 Agent 吗？（这章解决什么问题 / 九）
5. 给子 Agent 配一个「永远批准」的审批器，它能写出 workspace 吗？审批器被调用了几次？（八）
6. 子模型自己生成了 `task(...)` 调用，它会收到什么？（五）
7. 子 Agent 跑满 30 轮，父 Agent 收到的是第 30 轮的工具输出，还是别的东西？为什么这样设计更好？（六）
8. `task` handler 里的 `try/catch` 为什么不违反「不在业务逻辑里 catch」这条规则？（六）
9. 子 Agent 的工具列表里有 `todo_write`。它会收到「计划陈旧」提醒吗？（七）

---

下一章将处理另一种上下文膨胀：不同任务需要不同规范和背景知识，但不应该把所有知识常驻在 system prompt。第 7 章会引入按需加载的 Skill。
