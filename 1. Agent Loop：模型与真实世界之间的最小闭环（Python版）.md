# Agent Loop：一个循环，就是模型与真实世界之间的全部距离（Python 版）

这篇文章是原第 1 章 TypeScript 文章的 Python 完整迁移版。设计讨论、章节结构和边界说明全部保留；只有代码、文件路径、依赖安装命令和语言写法改为 Python。

配套项目位于 `python/ch01_agent`。阅读时请把 Python 看成另一种表达方式，先理解对象之间的协作，再补语言语法。

我见过很多人第一次接触 Agent 框架时的反应：打开架构图，看到 Orchestrator、Planner、Memory、Tool Registry，每个框之间都连着箭头。看的时候觉得懂了，但问一句“Agent 最核心的代码是什么”，往往答不上来。

这篇文章不画框，只看代码，而且只讲一个核心：Agent Loop。

它的概念是一个循环：模型提出工具调用，程序执行工具，再把结果放回消息历史，然后继续问模型，直到模型不再调用工具。为了让这个循环一定结束，Python 实现把它写成一个带最大轮次的 `for` 循环，避免模型一直调工具导致程序永不退出。

---

## 先说你现在是什么角色

在还没有 Agent 之前，你用大模型的方式大概是这样的：

你在对话框输入“帮我看看当前目录有哪些 Python 文件，然后分析一下哪个文件最大”。模型输出一条命令：

```powershell
Get-ChildItem -File -Filter '*.py' |
    Sort-Object Length -Descending |
    Select-Object Name, Length
```

但它自己不跑。你切到终端，粘贴进去，回车，看到输出，再切回对话框，把输出复制进去。模型接着给出下一条命令：

```powershell
(Get-Content .\agent_ch01\core\loop.py).Count
Get-Content .\agent_ch01\core\loop.py -TotalCount 80
```

每一次来回，你都在做同一件事：把工具结果手动传回给模型。这个时候，你其实是一个“人工 API”，也是模型和真实世界之间的中间层。

把这个中间层自动化，就是 Agent Loop 要做的唯一一件事。

---

## 两个信号，一个循环

Agent Loop 的逻辑极其简单，整个决策树只有两个分支：

```text
模型返回 tool_calls 了？
├── 是 -> 执行工具，把结果喂回去，再问一次
└── 否 -> 模型觉得做完了，退出
```

| 信号 | 含义 | 动作 |
| --- | --- | --- |
| `assistant.tool_calls` 非空 | 模型说“我要用工具” | 为每个调用执行或生成错误结果，回填后继续循环 |
| `assistant.tool_calls` 为空 | 模型没有再请求工具 | 返回最终文本；没有文本则明确失败 |

这里的循环是机械的，没有任何智能。真正的智能全在模型里，循环只是模型的手脚。

从 Agent 架构看，这个循环对应模型与真实世界之间的两个接口：

- 模型能看到什么，由上下文决定，也就是观察空间；
- 模型能做什么，由工具决定，也就是动作空间。

在 Python 代码里，观察空间是 system prompt、消息历史和工具 schema 共同组成的请求快照；动作空间是 `ToolRegistry` 注册的全部工具。工具执行完以后，结果必须重新放回观察空间，循环才能继续。

---

## 五步拆解：代码怎么跑起来的

### 第一步：用户问题进消息列表

Python 代码位于 `agent_ch01/core/loop.py`：

```python
self._history.append(user_message(prompt))
```

这没什么神秘的，就是把你说的话放进列表。Java 开发者可以把 `self._history` 理解为一个成员变量 `List<ChatMessage> history`，`append` 就是 `history.add(...)`。

`ChatMessage` 不是普通字典，而是四种消息对象的联合：`SystemMessage`、`UserMessage`、`AssistantMessage` 和 `ToolMessage`。每种对象都有明确字段，类似 Java 中不同的 record 类型。

消息对象使用 `@dataclass(frozen=True, slots=True)`：

- `dataclass` 自动生成构造方法、比较方法，类似 Java record 或 Lombok；
- `frozen=True` 表示创建后不允许修改，类似不可变对象；
- `slots=True` 表示只允许声明过的字段，减少意外动态加字段。

构造函数和 `validate_tool_pairing()` 会拒绝重复调用 ID、孤立工具结果和缺失结果。后面每一轮的模型输出、工具结果，都会追加到这份会话历史里，成为模型下一次请求能看到的内容。

### 第二步：把消息和工具定义一起发给模型

```python
snapshot = self._tools.snapshot()
request = ModelRequest(
    messages=(system_message(self._system_prompt), *self._history),
    tools=snapshot.openai_tools(),
)
reply = self._model.complete(request)
```

`*self._history` 是 Python 的序列展开语法。Java 可以把它理解为新建一个列表后执行 `addAll(history)`。

`ToolRegistry` 会生成 JSON Schema，告诉模型“你现在有哪些手”：每个工具叫什么、作用是什么、接收什么参数。`snapshot()` 冻结本次请求可见的工具集合，保证模型看到的 schema 和随后执行调用时使用的 registry 是同一份快照。

第 1 章只有一个名为 `shell` 的 PowerShell 工具。模型适配器位于 `agent_ch01/adapters/openai_chat.py`，它使用 Chat Completions 发出请求，再把 SDK 响应转成内部的 `ModelReply`。核心循环本身不保存 SDK 对象。

shell 的输入 schema、handler 和副作用类别来自同一个 `ToolDefinition`：

```python
def create_shell_tool(command_runner: CommandRunner) -> ToolDefinition:
    def handler(arguments: dict[str, object], context: ToolContext) -> ToolResult:
        command = arguments["command"]
        try:
            result = command_runner.run(str(command), context.workspace)
        except Exception:
            return tool_error("shell_start_failed", "PowerShell process could not be started")

        output = result.output or "(no output)"
        if result.truncated:
            output += "\n[output truncated]"
        if result.timed_out:
            return tool_error("shell_timeout", output)
        if result.exit_code != 0:
            return tool_error(
                "shell_failed",
                f"PowerShell exited with code {result.exit_code}\n{output}",
            )
        return tool_success(output)

    return ToolDefinition(
        name="shell",
        description="Run a PowerShell command in the current workspace.",
        parameters={
            "type": "object",
            "properties": {"command": {"type": "string", "minLength": 1}},
            "required": ["command"],
            "additionalProperties": False,
        },
        effect="execute",
        handler=handler,
    )
```

Java 角度看，`handler` 就是一个实现了 `ToolHandler` 接口的函数对象。Python 函数可以像普通值一样传给 `ToolDefinition`，这比 Java 的匿名类更短，但职责完全一样。

`PowerShellRunner` 合并 stdout 和 stderr，默认 120 秒超时，最多保留 50000 个字符；超时、非零退出码和截断都有明确的可观察结果。它不在 feature 内直接实例化：`bootstrap.py` 创建 adapter，再把它作为 `CommandRunner` 传给工具工厂。这样测试可以注入 Fake 命令执行器，不必启动真实 PowerShell。

### 第三步：追加模型回答，检查是否调了工具

```python
assistant = reply.message
self._history.append(assistant)

if not assistant.tool_calls:
    if assistant.content is None:
        raise AgentRunError("Model stopped without final text or tool calls")
    return RunResult(assistant.content, tuple(self._history), turn)
```

`assistant` 是一个 `AssistantMessage` 对象。它的字段在 Java 中可以理解为：

```java
record AssistantMessage(
    String role,
    String content,
    List<ToolCall> toolCalls
) {}
```

循环退出的条件不是“模型说完了”，而是“模型没有调工具”。模型可以一边输出文本，一边调工具；也可以只调工具不说话；也可以只说话不调工具。我们只关心 `tool_calls` 是否为空。

`finish_reason` 仍然要校验：第 1 章遇到 `length` 或 `content_filter` 会给出明确的类型化失败，不能把不完整响应冒充最终答案。自动恢复会在后续章节加入。

### 第四步：执行工具，收集结果

```python
for call in assistant.tool_calls:
    prepared = snapshot.prepare(call)
    result = snapshot.invoke(prepared, context)
    self._history.append(tool_message(result.content, call.id))
```

`prepare()` 先查找工具，再解析 JSON，再校验参数；`invoke()` 才真正调用 handler。未知工具、非法 JSON、schema 错误、审批拒绝、超时和执行异常都会变成 `ToolResult`，而不是让消息链断掉。

这里每个工具调用结果都对应一个独立的 `role="tool"` 消息，而且要通过 `tool_call_id` 和前面的调用对应上。这相当于数据库外键：assistant 的调用 ID 是主键，tool 结果必须引用它。

### 第五步：把结果追加，回到第二步

上一段代码已经把工具消息追加到 `_history`。Python 的 `for` 循环还没有结束，于是自动回到下一轮，再次调用模型。

这一轮模型看到的历史里，包含了它上一次的输出和工具执行结果，于是可以继续推理：“好，文件列表我拿到了，现在我需要……”。

---

## 完整的 agent_loop 函数

把上面五步组装起来，等价的精简 Python 版如下：

```python
def agent_loop(options: AgentOptions) -> RunResult:
    max_turns = options.max_turns or 20
    if max_turns <= 0:
        raise ValueError("max_turns must be a positive integer")

    history: list[ChatMessage] = [user_message(options.prompt)]
    context = ToolContext(str(Path(options.workspace).resolve()), "user")

    for turn in range(1, max_turns + 1):
        request = ModelRequest(
            messages=(system_message(options.system_prompt), *history),
            tools=options.tools.openai_tools(),
        )
        reply = options.model.complete(request)

        if reply.finish_reason == "length":
            raise IncompleteModelReplyError("Model output reached the token limit")
        if reply.finish_reason == "content_filter":
            raise AgentRunError("Model response was blocked by the content filter")

        assistant = reply.message
        history.append(assistant)
        if not assistant.tool_calls:
            if assistant.content is None:
                raise AgentRunError("Model stopped without final text or tool calls")
            return RunResult(assistant.content, tuple(history), turn)

        for call in assistant.tool_calls:
            prepared = options.tools.prepare(call)
            result = options.tools.invoke(prepared, context)
            history.append(tool_message(result.content, call.id))

    raise AgentLimitError(f"Agent exceeded max_turns={max_turns}")
```

真实版本 `AgentRunner` 还增加了授权、消息配对、工具快照和错误边界，但主干没有变化。这就是一个能跑起来的 Agent Harness 的最小内核。

---

## 一个容易被忽略的差异：Anthropic 格式 vs OpenAI 格式

这份代码使用 OpenAI 兼容格式。如果你去读 Anthropic 官方 API 文档，会发现消息结构稍有不同：

| 概念 | Anthropic 格式 | OpenAI 格式 |
| --- | --- | --- |
| 判断模型是否调工具 | `response.stop_reason == "tool_use"` | `message.tool_calls` 是否非空 |
| 工具调用 ID 字段 | `tool_use_id` | `tool_call_id` |
| 工具结果消息 role | `user`，把 tool_result 嵌在 user 消息里 | `tool` |
| 工具调用块 | content 中的 `tool_use` 块 | `message.tool_calls` 数组 |

形式不同，但本质信号是一样的：“模型有没有举手要工具”。这也是 Agent Loop 唯一需要感知的供应商差异，适配器会把它转换成统一的 `ModelReply`。

---

## 必须说清楚：shell 不是沙箱

配套实现运行在 Windows，工具名是 `shell`，里面执行的是 PowerShell。它没有字符串黑名单，因为换一种写法就能绕过黑名单，反而容易制造“已经安全”的错觉。

真实 CLI 每次执行前显示工具名、原因和参数，并询问：

```text
允许本次调用? [y/N]
```

只有明确输入 `y` 或 `yes` 才会执行；回车、其他输入、无交互 stdin 或审批异常都默认拒绝。拒绝仍会生成同一个 `tool_call_id` 的工具错误结果，让模型可以换方案。

但审批也不等于隔离。`PowerShellRunner` 的 `cwd` 只是子进程的起始工作目录，命令仍可能切换目录、访问绝对路径、启动其他进程或访问网络。真正的规则合并、审计和拒绝优先级会在后续权限章节展开，绝不能把 `cwd` 描述成 sandbox。

---

## 几十行 vs 1729 行：生产代码的差距在哪

核心循环逻辑只是上面这几十行。生产系统剩下的大量代码主要是在做保护、恢复和性能优化。

### 差异一：循环继续的判断方式

最小代码靠 `tool_calls` 是否非空判断是否继续。流式系统可能在完整响应结束前就收到工具调用块，所以生产实现通常在流式接收过程中设置 `needs_follow_up` 标志，而不是只看最后的结束字段。

这不是理论差异：如果只看滞后的 `stop_reason`，可能漏掉已经到达的工具调用，导致循环提前退出。

### 差异二：State 对象扛了很多字段

第 1 章核心只需要 history、轮次和工具上下文。生产 Agent 还可能保存：

| 字段 | 用途 |
| --- | --- |
| messages | 当前消息数组 |
| toolUseContext | 工具、信号、权限上下文 |
| autoCompactTracking | 上下文压缩状态 |
| maxOutputTokensRecoveryCount | token 截断恢复次数 |
| hasAttemptedReactiveCompact | 本轮是否尝试过压缩 |
| maxOutputTokensOverride | 单轮输出上限覆盖 |
| pendingToolUseSummary | 工具调用历史摘要 |
| stopHookActive | 停止钩子是否产生阻塞错误 |
| turnCount | 轮次计数 |
| transition | 上一次循环继续的原因 |

这些字段不是 Agent Loop 的本质，而是长任务可靠性机制。

### 差异三：退出路径

本章有最大轮次、`length`、`content_filter`、无最终文本等失败路径，但不自动恢复。生产系统还要处理：上下文超长、用户中断、模型网络错误、Stop Hook、token budget 耗尽和压缩重试。

每种路径都应该有独立处理逻辑和恢复策略，不是简单地 `return` 或 `throw`。

### 差异四：流式并行工具执行

生产系统可能在模型还在生成响应时就开始执行已到达的工具调用。是否并行，要看工具是否 concurrency-safe；有状态冲突风险的工具必须串行。这是性能优化，不改变循环的基本正确性。

---

## 把上面的东西放在一起

```text
Agent = 模型决策 + Harness 执行 + 消息历史传递
```

- 模型决策：要不要用工具、用哪个、传什么参数；
- Harness 执行：工具调了就跑，结果喂回去；
- 消息历史传递：每一轮输入输出都追加进列表。

换成更常见的公式，就是 **Agent = LLM + 上下文 + 工具**。

在第 1 章代码里：

- 观察空间由 system prompt、消息历史和工具 schema 组成；
- 动作空间由 `ToolRegistry` 中的 `shell` 组成；
- config、人工审批、错误分类和 `max_turns` 是最早的 Harness 保护。

---

## 源码设计地图：每个文件各管一件事

| Python 文件 | Java 角度的职责 | 正文对应 |
| --- | --- | --- |
| `agent_ch01/core/loop.py` | `AgentRunner` 应用服务 | 五步拆解 |
| `agent_ch01/core/messages.py` | 消息 record 和配对验证 | 第一步 |
| `agent_ch01/core/tools.py` | ToolRegistry、schema、prepare/invoke | 第二步、第四步 |
| `agent_ch01/core/model.py` | ModelClient 接口和 DTO | 第二步 |
| `agent_ch01/core/commands.py` | CommandRunner 进程契约 | shell 工具 |
| `agent_ch01/core/profiles.py` | 章节能力白名单 | 启动链 |
| `agent_ch01/features/builtin_tools.py` | shell Handler 和工具注册 | 第二步 |
| `agent_ch01/adapters/openai_chat.py` | OpenAI/DeepSeek SDK 适配器 | 第三步 |
| `agent_ch01/adapters/powershell.py` | ProcessBuilder 类比实现 | shell 工具 |
| `agent_ch01/config.py` | `.env` 和 URL 校验 | 运行 |
| `agent_ch01/bootstrap.py` | 组合根、依赖注入 | 启动链 |
| `agent_ch01/cli.py` | 参数解析、审批、错误出口 | 审批 |

启动顺序是：入口解析参数 -> `config.py` 校验配置 -> `bootstrap.py` 组装依赖 -> `AgentRunner` 跑循环。核心循环完全不碰 SDK、子进程和 `.env`。

---

## 手里只有 shell 的问题

目前这个 Agent 手里只有一个工具：`shell`，也就是 Windows PowerShell 命令入口。这意味着：

- 读文件要 `Get-Content .\path\to\file`；
- 写文件要 `Set-Content .\path\to\file -Value "..."`；
- 找文件要 `Get-ChildItem -Recurse -Filter '*.py'`；
- 移动文件要 `Move-Item`。

用起来又丑又脆：路径和转义容易写错；文件内容里有引号、换行或特殊字符时尤其明显；多步操作也没有原子性。

下一章可以在本章能力上增加 `read_file`、`write_file`、`edit_file`、`glob` 等专用工具。`shell` 仍然保留，但工具边界会更清晰。

---

## 运行第 1 章智能体

所有 Python 章节共享 `python/.venv` 和 `python/.env`，只需要创建一次：

```powershell
Set-Location 'E:\cj\study\learn-agent\python'
python -m venv .venv
Copy-Item '.env.example' '.env'
& .\.venv\Scripts\python.exe -m pip install -e '.\ch01_agent[dev]'
```

编辑 `python/.env`：

```dotenv
OPENAI_BASE_URL=https://api.deepseek.com
OPENAI_API_KEY=你的密钥
OPENAI_MODEL=deepseek-v4-flash
```

运行：

```powershell
Set-Location '.\ch01_agent'
& ..\.venv\Scripts\python.exe -m agent_ch01.cli --prompt "运行 Write-Output 42，并告诉我结果"
```

命令从父目录查找共享 `.env`，当前章节目录作为 Agent workspace。模型请求 `shell` 时，终端会要求逐次审批；只批准你检查过的命令。

---

## 验证与实验：测试就是可重复的对照

运行离线测试：

```powershell
& ..\.venv\Scripts\python.exe -m pytest tests
& ..\.venv\Scripts\python.exe -m ruff check agent_ch01 tests
& ..\.venv\Scripts\python.exe -m mypy agent_ch01
```

测试重点不是网络，而是 Agent 行为：

| 测试文件 | 验证内容 |
| --- | --- |
| `tests/test_loop.py` | 完整 Agent 循环、工具回填和最大轮次 |
| `tests/test_tools.py` | 未知工具、非法 JSON、超时结果 |
| `tests/test_messages.py` | tool 调用与结果的严格配对 |
| `tests/test_config.py` | 缺失配置和非法 URL |

这些测试是“消融实验”的工程化版本：通过脚本化 `FakeModel` 或 `FakeCommandRunner`，一次只替换一个外部边界，观察 `AgentRunner` 是否仍能保持消息配对、错误回填和停止条件。

---

## Java 开发者的考古顺序

第一次不要从 CLI 或 OpenAI SDK 开始，按下面顺序阅读：

```text
tests/test_loop.py
  -> bootstrap.py
  -> core/loop.py
  -> core/model.py
  -> core/tools.py
  -> features/builtin_tools.py
  -> adapters/powershell.py
  -> adapters/openai_chat.py
```

每到一个文件，只问四个问题：

1. 这个文件在 Java 项目中相当于哪一层？
2. 字段保存什么状态？
3. 方法接收什么输入、返回什么输出？
4. 哪些失败会被转换成结构化结果？

重点 Python 语法对照：

| Python 写法 | Java 大白话 |
| --- | --- |
| `self` | 显式写出来的 `this` |
| `@dataclass` | 自动生成构造器和样板方法的 record/Lombok |
| `Protocol` | `interface` |
| `str | None` | 可能为 null 的 String |
| `tuple` | 不可变 List |
| `dict` | Map |
| `*items` | addAll 展开集合 |
| `**mapping` | 把 Map 展开为关键字参数 |
| `with pytest.raises` | JUnit `assertThrows` |
| `def` | 方法或函数声明 |
| 缩进 | Python 用缩进代替 Java 的大括号 |

等你能用 Java 或 Python 手写出“模型请求 `current_time`，程序执行工具，再返回最终文本”的循环，第 1 章就真正学会了。后面的权限、记忆、任务 DAG 和多 Agent，都是在这条循环上继续增加能力。
