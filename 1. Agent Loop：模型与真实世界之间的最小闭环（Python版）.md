# Agent Loop：模型与真实世界之间的最小闭环（Python版）

这是一份与 `python/ch01_agent` 配套的第 1 章文档。

原教程使用 TypeScript 说明 Agent 架构；本版本把同一套行为迁移为 Python，并尽量使用 Java 后端开发者熟悉的表达方式：接口、领域对象、适配器、应用服务、注册表和组合根。

本章只解决一个问题：

> 模型如何请求工具，程序如何执行工具，再如何把工具结果交回模型？

先不要考虑 Memory、Planner、MCP、Cron 或多 Agent。它们都建立在这个闭环之上。

---

## 一、先用 Java 的方式理解 Agent

普通聊天接口通常只有一次调用：

```text
用户问题 -> 模型回答
```

Agent 多了一条分支：模型可能不直接回答，而是要求程序调用工具：

```text
用户问题
  -> 模型请求调用工具
  -> 程序执行工具
  -> 工具结果写回消息历史
  -> 再次请求模型
  -> 模型返回最终文本
```

用 Java 服务层的语言描述，就是：

```java
public String run(String prompt) {
    history.add(userMessage(prompt));

    while (true) {
        ModelResponse response = modelClient.complete(history, toolDefinitions);

        if (response.toolCalls().isEmpty()) {
            return response.text();
        }

        for (ToolCall call : response.toolCalls()) {
            ToolResult result = toolRegistry.execute(call, context);
            history.add(result.toMessage());
        }
    }
}
```

这里的 `while` 不负责思考。真正的决策由模型完成，循环只负责把模型和外部世界连接起来。

---

## 二、Python 项目结构

```text
python/ch01_agent/
├─ agent_ch01/
│  ├─ core/
│  │  ├─ messages.py       # system/user/assistant/tool 消息
│  │  ├─ model.py          # ModelClient 接口和请求响应对象
│  │  ├─ commands.py       # 命令执行接口和结果对象
│  │  ├─ tools.py          # ToolRegistry、参数校验、工具结果
│  │  ├─ loop.py           # AgentRunner 核心循环
│  │  └─ profiles.py       # 第 1 章能力白名单
│  ├─ adapters/
│  │  ├─ openai_chat.py    # OpenAI/DeepSeek 兼容接口适配器
│  │  └─ powershell.py     # PowerShell 进程适配器
│  ├─ features/
│  │  └─ builtin_tools.py  # shell 工具定义
│  ├─ bootstrap.py         # 组合根，负责装配对象
│  ├─ config.py            # .env 配置校验
│  └─ cli.py               # 命令行入口
└─ tests/                  # 离线测试，不访问真实模型
```

如果你熟悉 Spring Boot，可以这样类比：

| Python 文件 | Java/Spring 中的对应概念 |
| --- | --- |
| `core/model.py` | `ModelClient` 接口、DTO/record |
| `adapters/openai_chat.py` | 基础设施适配器 |
| `core/loop.py` | 应用服务 `AgentRunner` |
| `core/tools.py` | 工具注册表和命令分发器 |
| `features/builtin_tools.py` | 具体业务 Handler |
| `bootstrap.py` | `@Configuration` 或 Composition Root |
| `tests/` | 单元测试和 Fake 实现 |

---

## 三、消息为什么不能只用字典

Agent 需要区分四类消息：

| 角色 | 含义 |
| --- | --- |
| `system` | 系统约束，每次请求前注入 |
| `user` | 用户输入 |
| `assistant` | 模型回答，也可能包含工具调用 |
| `tool` | 程序执行工具后的结果 |

`agent_ch01/core/messages.py` 使用不可变 dataclass 表示这些对象。最重要的约束是：

```text
assistant 发出一个 tool call
  -> 必须有一个相同 tool_call_id 的 tool result
```

代码中的 `validate_tool_pairing()` 会拒绝：

- 孤立的 `tool` 消息
- assistant 调用了工具但没有结果
- 重复的工具调用 ID
- 结果 ID 与调用 ID 不匹配

这和后端接口校验是同一个思想：不要让非法状态进入下游供应商。

---

## 四、模型接口只暴露核心契约

`core/model.py` 中的 `ModelClient` 是核心循环唯一需要知道的模型接口：

```python
class ModelClient(Protocol):
    def complete(self, request: ModelRequest) -> ModelReply:
        """向模型发送一轮规范化请求。"""
```

`AgentRunner` 不知道 OpenAI SDK，也不关心 DeepSeek 的 HTTP 细节。真实实现是 `adapters/openai_chat.py`，测试时则注入 FakeModel。

这就是 Java 中面向接口编程的价值：

```java
private final ModelClient modelClient;
```

应用服务依赖接口，适配器负责外部协议转换。

---

## 五、工具注册表是 Agent 的“手”

模型只能提出调用请求，不能直接执行 PowerShell。真正执行动作的是 `ToolRegistry`。

工具定义同时包含四部分：

```python
ToolDefinition(
    name="shell",
    description="Run a PowerShell command in the current workspace.",
    parameters={...},
    effect="execute",
    handler=handler,
)
```

这相当于 Java 中把命令名、参数 DTO、权限分类和 Handler 绑定在同一个 Bean 中。

工具调用分为两个阶段：

```text
prepare(call)
  -> 查找工具
  -> 解析 JSON
  -> 校验参数结构
  -> 生成可执行的 PreparedToolCall

invoke(prepared, context)
  -> 调用真正的 handler
  -> 把异常转换为 ToolResult
```

未知工具、非法 JSON、参数校验失败和执行异常都会转换成工具结果，而不是直接打断整个 Agent：

```text
Error [unknown_tool]: Unknown tool: xxx
Error [invalid_json]: Tool arguments must be valid JSON
Error [tool_execution_error]: Tool execution failed
```

模型可以读取这些结果，并决定下一步如何调整。

---

## 六、PowerShell 工具不是沙箱

本章的 `shell` 工具执行 PowerShell。`PowerShellRunner` 负责：

- 使用指定工作目录 `cwd`
- 合并 stdout 和 stderr
- 使用 UTF-8 解码
- 默认 120 秒超时
- 默认最多保留 50000 个字符
- 区分超时、非零退出码和输出截断

这只是进程适配器，不等于完整安全隔离。第 1 章通过人工授权降低误执行风险，真正的权限策略会在后续章节展开。

工具执行结果统一转换为：

```python
ToolResult(
    content="...",
    is_error=False,
    error_code=None,
)
```

例如 PowerShell 超时：

```text
Error [shell_timeout]: started
```

---

## 七、AgentRunner 的完整流程

打开 `agent_ch01/core/loop.py`，按下面顺序考古：

```text
1. 保存用户消息
2. 校验历史中的工具调用配对
3. 获取工具注册表快照
4. 把 system prompt、历史消息和工具 schema 发给模型
5. 处理模型结束原因
6. 追加 assistant 消息
7. 如果没有 tool call，返回最终文本
8. 如果有 tool call，prepare -> 授权 -> invoke
9. 把每个结果写成 tool 消息
10. 回到第 2 步
```

核心代码可以缩写成：

```python
for turn in range(1, self._max_turns + 1):
    validate_tool_pairing(self._history)

    snapshot = self._tools.snapshot()
    request = ModelRequest(
        messages=(system_message(self._system_prompt), *self._history),
        tools=snapshot.openai_tools(),
    )
    reply = self._model.complete(request)

    assistant = reply.message
    self._history.append(assistant)

    if not assistant.tool_calls:
        return RunResult(assistant.content, tuple(self._history), turn)

    for call in assistant.tool_calls:
        prepared = snapshot.prepare(call)
        result = snapshot.invoke(prepared, context)
        self._history.append(tool_message(result.content, call.id))
```

这里的 `max_turns` 很重要。模型如果一直要求调用工具，程序不能无限循环；达到上限后会抛出 `AgentLimitError`。

模型返回 `length` 时会抛出 `IncompleteModelReplyError`，因为被截断的内容不能冒充最终答案；返回 `content_filter` 时会抛出 `AgentRunError`。

---

## 八、为什么要有组合根

`bootstrap.py` 负责创建并连接具体实现：

```python
def build_agent(profile, model, workspace, command_runner=None, authorizer=None):
    return AgentRunner(
        model=model,
        tools=create_chapter_one_tools(command_runner or PowerShellRunner()),
        system_prompt=SYSTEM_PROMPT,
        workspace=workspace,
        authorizer=authorizer,
    )
```

这和 Spring 的配置类很像。核心循环不自己 `new OpenAI()`，也不自己创建 PowerShell 进程，因此测试可以注入 Fake：

```python
commands = FakeCommandRunner(CommandResult("42", 0, False, False))
runner = build_agent(P01, fake_model, workspace, command_runner=commands)
```

如果一个类既负责业务流程，又负责创建 HTTP 客户端和操作系统进程，测试会变得困难，职责也会混乱。

---

## 九、先运行离线测试

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch01_agent'
python -m pytest
```

测试覆盖的重点不是网络，而是 Agent 行为：

- 工具调用后能继续下一轮模型请求
- 工具结果正确写回历史
- 工具调用 ID 能够配对
- 非法 JSON 不会执行 PowerShell
- 未知工具会返回结构化错误
- 超过最大轮数会失败
- 缺少配置时会一次性报告所有字段

真实运行前安装依赖并配置 `.env`：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -e ".[dev]"
Copy-Item '.env.example' '.env'
```

DeepSeek 兼容配置示例：

```dotenv
OPENAI_BASE_URL=https://api.deepseek.com
OPENAI_API_KEY=你的密钥
OPENAI_MODEL=deepseek-v4-flash
```

启动：

```powershell
python -m agent_ch01.cli --prompt "列出当前目录"
```

执行 `shell` 工具时，程序会要求输入 `y` 或 `yes`。直接回车、非交互终端或授权异常都会拒绝执行。

---

## 十、Java 开发者的考古顺序

不要从 CLI 开始，也不要先读 OpenAI 适配器。建议顺序是：

```text
tests/test_loop.py
  -> bootstrap.py
  -> core/loop.py
  -> core/model.py
  -> core/tools.py
  -> features/builtin_tools.py
  -> adapters/powershell.py
  -> adapters/openai_chat.py
```

每到一个文件，只问四个问题：

1. 输入对象是什么？
2. 输出对象是什么？
3. 它依赖哪个接口？
4. 哪些失败会被转换成结构化结果？

等你能用 Java 或 Python 手写出“模型请求 `current_time`，程序执行工具，再返回最终文本”的循环，第 1 章就真正学会了。后面的权限、记忆、任务 DAG 和多 Agent，都是在这条循环上继续加能力。

