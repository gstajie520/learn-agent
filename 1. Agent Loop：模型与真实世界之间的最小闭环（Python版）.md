# Agent Loop：一个循环，就是模型与真实世界之间的全部距离（Python 版）

这篇文章是原第 1 章 TypeScript 文章的 Python 完整迁移版。设计讨论、章节结构和边界说明全部保留；只有代码、文件路径、依赖安装命令和语言写法改为 Python，并面向有 Java 后端背景的读者补了语言对照。

配套项目位于 `python/ch01_agent`。阅读时请把 Python 看成另一种表达方式，先理解对象之间的协作，再补语言语法。

我见过很多人第一次接触 Agent 框架时的反应：打开架构图，看到 Orchestrator、Planner、Memory、Tool Registry，每个框之间都连着箭头。看的时候觉得懂了，但问一句「Agent 最核心的代码是什么」，往往答不上来。

这篇文章不画框，只看代码，而且只讲一个核心：Agent Loop。

它的概念是一个循环：模型提出工具调用，程序执行工具，再把结果放回消息历史，然后继续问模型，直到模型不再调用工具。为了让这个循环一定结束，Python 实现把它写成一个带最大轮次的 `for` 循环，避免模型一直调工具导致程序永不退出。

---

## 本章导读

### 读完这一章，你能做到什么

1. 用一句话说清 Agent 和「调用一次模型 API」的区别。
2. 看懂一轮 Agent 对话在网络上真正传了哪些 JSON 字段。
3. 自己手写一个不到 60 行、能跑起来的 Agent Loop。
4. 说出这个循环在什么条件下结束，以及为什么必须有轮次上限。
5. 在本机跑通第 1 章配套代码，并让 Agent 帮你执行一条 PowerShell 命令。

### 你需要先具备什么

| 需要 | 程度 |
| --- | --- |
| Python | 能看懂 `class`、函数、类型标注，`dataclass` 不熟也没关系（正文有 Java 对照） |
| 命令行 | 能在 PowerShell 里进入目录、运行 `python` 和 `pip` 命令 |
| 大模型 API | 知道「把消息发给模型、模型返回文本」这件事就够了 |
| 上一章 | 无。这是第 1 章 |

不需要读过任何 Agent 框架的文档。本章出现的每个自造概念都会在下面的术语表里先解释一遍。

### 术语速查（本章第一次出现的词）

先扫一眼，正文里遇到不认识的词回来查即可。**不用背**，读完正文自然就懂了。

| 术语 | 一句话解释 |
| --- | --- |
| **Agent Loop** | 「问模型 → 执行工具 → 把结果喂回去 → 再问模型」的循环。本章唯一主题 |
| **Harness**（挽具） | 循环之外的所有代码：配置校验、工具注册、参数校验、审批、错误分类、轮次上限。模型负责决定，Harness 负责安全地落地 |
| **轮次（turn）** | 一次「发请求给模型」算一轮。注意不是一次工具调用算一轮 |
| **tool_calls** | 模型回复里的一个数组字段。非空表示「我要用工具」，空表示「我说完了」 |
| **tool_call_id** | 每次工具调用的唯一编号。工具结果靠它对应回是哪次调用 |
| **ToolRegistry** | 工具注册表。存放所有工具的名字、说明、参数格式和执行函数 |
| **snapshot（快照）** | 把注册表在本轮「拍照定格」。保证模型看到的工具清单和随后真正执行的工具清单是同一份 |
| **ToolResult** | 工具执行的统一结果类型。成功和失败都是 `ToolResult`，不抛异常打断循环 |
| **`@dataclass(frozen=True)`** | Python 写法：自动生成构造器、字段只读的数据对象，类似 Java record |
| **Protocol** | Python 的结构化接口：只声明方法签名，类似 Java `interface` |
| **观察空间 / 动作空间** | 模型「能看到什么」（系统提示 + 消息历史 + 工具说明）和「能做什么」（注册表里的工具） |
| **组合根（composition root）** | 唯一负责创建真实依赖并注入的文件，本章是 `bootstrap.py`。核心循环自己不 `import` SDK 或启动子进程 |
| **profile** | 章节能力白名单。第 1 章的 `P01` 只允许装 `loop` 和 `powershell` 两项能力 |
| **effect（副作用类别）** | 工具声明自己会不会改变世界。`shell` 标记为 `execute`，因此默认逐次要人工批准 |
| **fail-closed** | 出错时选择拒绝而不是放行。审批环节抛异常也当作「拒绝」处理 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先说你现在是什么角色      ← 建立直觉：Agent 替代的是「你」
  ↓
② 两个信号，一个循环        ← 决策树只有两个分支
  ↓
③ 先看一眼真实的数据流      ← 两轮 JSON 实例，最关键的一节
  ↓
④ 五步拆解 + 完整函数       ← 代码怎么跑起来
  ↓
⑤ 边界与差距               ← 格式差异、shell 不是沙箱、CC 的四点差距
  ↓
⑥ 结论与源码地图            ← Agent = 模型 + Harness + 历史；12 个文件各管什么
  ↓
⑦ 运行第 1 章智能体         ← 动手，六步 + 排错表
  ↓
⑧ 验证、实验与小结          ← 三个动手实验 + 自测题
  ↓
⑨ Java 开发者的考古顺序     ← 从测试出发的源码阅读路线
```

**想先跑起来再读原理？** 直接跳到「运行第 1 章智能体」，那一节是完全自包含的步骤清单，跑通后再回头读 ③。

---

## 先说你现在是什么角色

在还没有 Agent 之前，你用大模型的方式大概是这样的：

你在对话框输入「帮我看看当前目录有哪些 Python 文件，然后分析一下哪个文件最大」。模型输出一条命令：

```powershell
Get-ChildItem -File -Filter '*.py' |
    Sort-Object Length -Descending |
    Select-Object Name, Length
```

但它自己不跑。你切到终端，粘贴进去，回车，看到输出，再切回对话框，把输出复制进去。模型接着给出下一条命令：

```powershell
(Get-Content .\agent_ch01\core\loop.py).Count
Get-Content .\agent_ch01\core\loop.py -TotalCount 80
```

你再去终端跑，再把输出粘回来。

每一次来回，你都在做同一件事：把工具结果手动传回给模型。这个时候，你其实是一个「人工 API」，也是模型和真实世界之间的中间层。

把这个中间层自动化，就是 Agent Loop 要做的唯一一件事。

---

## 两个信号，一个循环

Agent Loop 的逻辑极其简单，整个决策树只有两个分支：

```text
模型返回 tool_calls 了？
├── 是 → 执行工具，把结果喂回去，再问一次
└── 否 → 模型觉得做完了，退出
```

翻译成代码能识别的信号：

| 信号 | 含义 | 动作 |
| --- | --- | --- |
| `assistant.tool_calls` 非空 | 模型举手说「我要用工具」 | 为每个调用执行或生成错误结果 → 回填 → 继续循环 |
| `assistant.tool_calls` 为空 | 模型没有再请求工具 | 返回最终文本；没有文本则明确失败 |

注意：这里的「循环」是机械的，没有任何智能。真正的智能全在模型里，循环只是模型的手脚。

从 Agent 架构的概念框架看，这个循环正好对应模型与真实世界之间的两个接口：

- 模型能看到什么，由上下文决定，也就是**观察空间**。
- 模型能做什么，由工具决定，也就是**动作空间**。

在这份代码里，观察空间是 `system prompt`、消息历史和工具 schema 共同组成的请求快照；动作空间是 `ToolRegistry` 注册的全部工具。工具执行完以后，结果必须重新放回观察空间，循环才能继续。

---

## 先看一眼真实的数据流

这一节是整章最关键的部分。很多人读 Agent 教程卡住，不是卡在代码，而是卡在「不知道网络上到底传了什么」。所以先不看代码，只看数据。

假设用户输入：**「运行 Write-Output 42，并告诉我结果」**。下面是完整两轮请求响应的实际字段。

### 第 1 轮 · 我们发出的请求

发给模型的是一个 JSON。它只有两个我们关心的部分：`messages`（模型能看到什么）和 `tools`（模型能做什么）。

```json
{
  "model": "deepseek-v4-flash",
  "messages": [
    { "role": "system", "content": "You are a coding agent. Use tools when needed, inspect their results, and answer accurately." },
    { "role": "user",   "content": "运行 Write-Output 42，并告诉我结果" }
  ],
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "shell",
        "description": "Run a PowerShell command in the current workspace.",
        "parameters": {
          "type": "object",
          "properties": { "command": { "type": "string", "minLength": 1 } },
          "required": ["command"],
          "additionalProperties": false
        }
      }
    }
  ]
}
```

这段 `system` 内容就是 `bootstrap.py` 里写死的那一行 `SYSTEM_PROMPT`，第 1 章不做任何动态拼装（第 10 章才引入按运行状态生成系统提示的 Provider）。

注意 `tools` 里那段 `parameters`：它就是「工具说明书」。在 Python 版里，它是 `ToolDefinition.parameters` 字段里手写的一个 JSON Schema 字典（TS 版用 zod 自动生成，效果等价）。**同一份字典既作为说明书发给模型，又对应着 `prepare()` 阶段的参数校验**——说明书说要 `command`，校验时就查 `command`，不会错位。

### 第 1 轮 · 模型返回的响应

```json
{
  "choices": [
    {
      "finish_reason": "tool_calls",
      "message": {
        "role": "assistant",
        "content": null,
        "tool_calls": [
          {
            "id": "call_abc123",
            "type": "function",
            "function": {
              "name": "shell",
              "arguments": "{\"command\":\"Write-Output 42\"}"
            }
          }
        ]
      }
    }
  ]
}
```

三个必须看清的细节：

1. **`content` 是 `null`。** 模型这一轮一个字都没说，只举手要工具。所以「模型有没有说完」不能用有没有文本来判断。
2. **`arguments` 是字符串，不是对象。** 里面是 JSON 文本 `{"command":"Write-Output 42"}`，需要我们自己 `json.loads`。而且模型完全可能吐出一段语法错误的 JSON——这是必须处理的真实情况，不是理论风险。
3. **`id` 是 `"call_abc123"`。** 记住它，下一步要用。

看到 `tool_calls` 非空，循环就知道：不能结束，得干活。

### 中间步骤 · 我们在本机执行

Harness 做四件事，顺序不能颠倒：

```text
① 按名字查工具       shell → 找到定义（找不到就生成 unknown_tool 错误结果）
② 解析 arguments     json.loads('{"command":"Write-Output 42"}')
③ 参数校验           必须有 command、必须是非空字符串、不能有多余字段
④ 人工审批           终端打印命令，等你输入 y
   ↓ 你批准了
⑤ 真正执行           启动 PowerShell 子进程，拿到 stdout："42"
```

**第 ③ 步在第 ⑤ 步之前，这个顺序是安全性的核心。** 参数校验必须发生在副作用之前——一旦命令已经跑了，再发现参数不合法就没有意义了。

### 第 2 轮 · 我们把结果喂回去

关键在于：**上一轮的 assistant 消息要原样留在历史里**，然后紧跟一条 `role: "tool"` 的新消息。

```json
{
  "model": "deepseek-v4-flash",
  "messages": [
    { "role": "system", "content": "You are a coding agent. Use tools when needed, inspect their results, and answer accurately." },
    { "role": "user",   "content": "运行 Write-Output 42，并告诉我结果" },
    {
      "role": "assistant",
      "content": null,
      "tool_calls": [
        { "id": "call_abc123", "type": "function",
          "function": { "name": "shell", "arguments": "{\"command\":\"Write-Output 42\"}" } }
      ]
    },
    { "role": "tool", "tool_call_id": "call_abc123", "content": "42" }
  ],
  "tools": [ "…和第 1 轮完全一样…" ]
}
```

`tool_call_id: "call_abc123"` 和上一轮的 `id` 对上了。这是模型唯一的线索——它靠这个编号知道「`42` 是我那次 `Write-Output 42` 调用的结果」。如果模型一轮请求了三个工具，就得有三条 `role: "tool"` 消息，ID 各自对应。

### 第 2 轮 · 模型返回最终答案

```json
{
  "choices": [
    {
      "finish_reason": "stop",
      "message": {
        "role": "assistant",
        "content": "命令执行成功，输出是 42。",
        "tool_calls": []
      }
    }
  ]
}
```

`tool_calls` 空了。循环退出，把 `content` 作为最终答案返回给用户。

### 把两轮压缩成一张图

```text
        我们                                模型
         │                                   │
 第1轮   │  messages(2条) + tools(1个)  ───▶  │
         │                                   │ 「我要 shell」
         │  ◀───  tool_calls=[call_abc123]   │
         │                                   │
  执行   │ 查工具→解析参数→校验→审批→跑       │
         │ 得到 "42"                          │
         │                                   │
 第2轮   │  messages(4条) + tools(1个)  ───▶  │
         │   ↑ 多了 assistant + tool 两条     │ 「够了，我能回答了」
         │  ◀───  tool_calls=[] content="…"  │
         │                                   │
  结束   │ 返回 content 给用户                │
```

每一轮 `messages` 都在变长——这就是 Agent 的「记忆」。没有别的机制，就是一个列表不断 `append`。也正因为它只增不减，第 8 章才必须专门讲上下文压缩。

**一句话总结这一节**：Agent Loop 就是在「`tool_calls` 非空」和「`tool_calls` 为空」之间做选择，然后往一个列表里追加消息。剩下的所有代码，都是为了让这件事在出错时不崩、在失控时能停。

---

## 五步拆解：代码怎么跑起来的

上一节看的是数据，这一节看代码。把 Agent Loop 拆成五步，每一步都对应一段很小的代码。

先看整体对应关系，避免读细节时迷路：

| 步 | 做什么 | 在循环内还是循环外 | 对应上一节的哪个环节 |
| ---: | --- | --- | --- |
| 1 | 用户问题进消息列表 | 循环**外**，只做一次 | 第 1 轮请求里的 `user` 消息 |
| 2 | 消息 + 工具定义发给模型 | 循环内，每轮都做 | 第 1 轮/第 2 轮的请求 |
| 3 | 追加模型回答，检查 `tool_calls` | 循环内 | 判断「继续」还是「结束」 |
| 4 | 执行工具，收集结果 | 循环内，只在有工具调用时做 | 中间步骤那五个子步骤 |
| 5 | 结果追加，回到第 2 步 | 由 `for` 循环本身完成 | 第 2 轮请求变长的那两条消息 |

**注意第 1 步在循环外。** 用户的问题只需要进历史一次；之后每一轮模型都能重新看到它，不需要重复添加。

### 第一步：用户问题进消息列表

**这一步解决什么**：给循环一个起点。

```python
self._history.append(user_message(prompt))
```

就是把你说的话放进一个列表，没有任何魔法。`self._history` 前面的下划线是 Python 的「私有」约定（类似 Java 的 `private`，但靠自觉不靠编译器）；外部代码通过 `history` 属性读到的只是一份 `tuple` 副本，改不了内部状态。

真正值得学的是**消息为什么要定义类型**。配套实现没有用 `dict` 到处传字典，而是把 `ChatMessage` 定义成四种消息的联合：

```python
ChatMessage = SystemMessage | UserMessage | AssistantMessage | ToolMessage
```

每种消息的 `role` 是一个固定字面量，字段各不相同：

| role | 必有字段 | 特有约束 |
| --- | --- | --- |
| `system` | `content` | 不进历史，每轮请求时临时前置 |
| `user` | `content` | 一次 run 只在开头出现一条 |
| `assistant` | `content`、`tool_calls` | `content` 可以是 `None`，但那时 `tool_calls` 必须非空 |
| `tool` | `content`、`tool_call_id` | 必须能对应到前面某个 `tool_calls` 的 `id` |

所有消息都是 `@dataclass(frozen=True, slots=True)`：`dataclass` 自动生成构造器和比较方法，类似 Java record 或 Lombok；`frozen=True` 表示创建后不允许修改；`slots=True` 表示只允许声明过的字段。Java 视角下，这四种消息就是四个 record 类型，`isinstance(message, ToolMessage)` 之后的代码就能放心读 `tool_call_id`。

另外三道保险，分别由不同的函数负责：

| 保险 | 由谁执行 | 拦住什么 |
| --- | --- | --- |
| 冻结消息（`frozen=True`） | 每个消息构造器 | 消息创建后被悄悄改写 |
| 同一回复内调用 ID 唯一 | `AssistantMessage.__post_init__` | 两个调用用同一个 `id`，结果无法一一对应 |
| 全历史配对校验 | `validate_tool_pairing()` | 孤立结果（orphan）、缺失结果（missing）、意外的重复结果（unexpected） |

`validate_tool_pairing()` 在每轮请求前跑一次（`AgentRunner.run()` 里、适配器发请求前各有一道）。这三种坏历史发给模型都会报错，提前拦住比让远端拒绝更省事、错误信息也更准——它会直接告你是哪个 ID 出了问题。

后面每一轮的模型输出、工具结果，都会追加到这份会话历史里，成为模型下一次请求能看到的内容。

### 第二步：把消息和工具定义一起发给模型

**这一步解决什么**：构造模型的观察空间——它能看到什么、能做什么，全在这个请求里。

```python
# AgentRunner.run() 内的片段
snapshot = self._tools.snapshot()
request = ModelRequest(
    messages=(system_message(self._system_prompt), *self._history),
    tools=snapshot.openai_tools(),
)
reply = self._model.complete(request)
```

四行代码，逐行看：

| 行 | 在做什么 | 为什么这么写 |
| --- | --- | --- |
| `snapshot()` | 把注册表定格成本轮只读的工具集合 | 见下方说明 |
| `system_message(...)` + `*self._history` | 临时拼出完整消息元组 | system prompt 每轮都一样，存进历史就是重复存 N 份。`*` 展开类似 Java 先 new List 再 `addAll(history)` |
| `openai_tools()` | 把工具定义转成 OpenAI 格式 schema | 说明书和校验规则同一个来源，不会错位 |
| `complete(request)` | 发请求 | `self._model` 是 `ModelClient` 协议（接口）不是 SDK，测试里可以换成脚本化假模型 |

**`snapshot()` 为什么必要**：模型看到的 schema 和随后执行调用时使用的注册表必须是同一份。否则可能出现「模型看到工具 A，真正执行时却是工具 B」这种不一致——在第 19 章接入 MCP 动态工具池后，注册表会在运行中变化，这个约束就从「好习惯」变成「必需」。第 1 章提前建立它，后面就不用改循环。快照的实现很简单：`ToolRegistry(self._definitions, mutable=False)`，只复制一份定义并把 `mutable` 关掉，再 `register()` 会直接抛「工具注册表快照不可修改」。

`ToolRegistry` 生成的 JSON Schema 告诉模型「你现在有哪些手」：每个工具叫什么、作用是什么、接收什么参数。第 1 章只有一个名为 `shell` 的 PowerShell 工具。

模型适配器位于 `agent_ch01/adapters/openai_chat.py`，它使用 Chat Completions 发出请求，再把 SDK 响应规范化成内部的 `ModelReply`。**核心循环本身不保存 SDK 对象**——这条边界是整套代码能长期维护的原因之一。

shell 的输入 schema、handler 和副作用类别来自同一个 `ToolDefinition`。下面是与配套实现对应的核心定义，实际源码位于 `agent_ch01/features/builtin_tools.py`：

```python
from ..core.commands import CommandRunner
from ..core.tools import ToolDefinition, ToolResult, tool_error, tool_success


def create_shell_tool(command_runner: CommandRunner) -> ToolDefinition:
    def handler(arguments: dict[str, object], context: ToolContext) -> ToolResult:
        command = arguments["command"]
        try:
            result = command_runner.run(str(command), context.workspace)
        except Exception:
            return tool_error("shell_start_failed", "无法启动 PowerShell 进程")

        output = result.output or "(no output)"
        if result.truncated:
            output += "\n[output truncated]"
        if result.timed_out:
            return tool_error("shell_timeout", output)
        if result.exit_code != 0:
            return tool_error("shell_failed", f"PowerShell 退出码为 {result.exit_code}\n{output}")
        return tool_success(output)

    return ToolDefinition(
        name="shell",
        description="Run a PowerShell command in the current workspace.",
        parameters={
            "type": "object",
            "properties": {"command": {"type": "string", "minLength": 1}},
            "required": ["command"],
            "additionalProperties": False,
        },
        effect="execute",
        handler=handler,
    )
```

Java 角度看，`handler` 就是一个实现了 `ToolHandler` 接口的函数对象（Python 里接口叫 `Protocol`）。Python 函数可以像普通值一样传给 `ToolDefinition`，这比 Java 的匿名类更短，但职责完全一样。

这段代码里 `ToolDefinition` 的五个字段，每一个都有明确分工：

| 字段 | 值 | 谁在用它 |
| --- | --- | --- |
| `name` | `"shell"` | 模型用它发起调用；注册表用它查找定义 |
| `description` | `"Run a PowerShell command..."` | 只给模型看。写得含糊模型就会用错 |
| `parameters` | 那个 JSON Schema 字典 | 既作为发给模型的说明书，又对应 `prepare()` 的校验规则 |
| `effect` | `"execute"` | 声明「我会改变世界」，因此审批环节默认要求人工确认 |
| `handler` | 那个内部函数 | 真正干活的地方，返回 `ToolResult` |

再看 `handler` 内部的错误处理，它体现了一条贯穿全书的原则：**工具的失败是数据，不是异常**。

```python
if result.timed_out:
    return tool_error("shell_timeout", output)
if result.exit_code != 0:
    return tool_error("shell_failed", f"PowerShell 退出码为 {result.exit_code}\n{output}")
return tool_success(output)
```

四种情况——进程起不来、超时、退出码非零、正常成功——全部返回 `ToolResult`，没有一个 `raise`。为什么？因为异常会打断循环，模型永远看不到「命令失败了」这个信息，也就没机会换个写法重试。返回 `tool_error` 则会变成一条正常的 `role: "tool"` 消息喂回给模型（内容形如 `工具执行错误 [shell_failed]: PowerShell 退出码为 1\n...`），模型看到 `shell_failed` 和错误输出，下一轮可能就改用正确的命令了。

**这就是 Agent 能自我纠错的机制**——不是模型有什么神秘能力，而是我们把错误信息老实地喂回去了。

`PowerShellRunner` 是执行侧的实际实现，它的三条硬限制：

| 限制 | 默认值 | 目的 |
| --- | --- | --- |
| 超时 | 120 秒 | 单条命令不能永久挂住整个 Agent |
| 输出上限 | 50000 字符 | 一条 `Get-ChildItem -Recurse` 不能淹没上下文 |
| stdout/stderr | 合并为一份 | 错误信息和正常输出一起给模型，它才能诊断 |

超时、非零退出码和截断都有明确的可观察结果，不会静默丢失。它还会在每条命令前加一句 `$OutputEncoding = [System.Text.UTF8Encoding]::new($false)`，强制 PowerShell 用 UTF-8 输出，避免中文在不同 Windows 环境下乱码。

`PowerShellRunner` 不在 feature 内直接实例化：组合根 `bootstrap.py` 创建 adapter，再把它作为 `CommandRunner` 传给 `create_chapter_one_tools`。这样 feature 只依赖 `core/commands.py` 的契约，离线测试可以注入 fake 命令执行器，不必启动真实 PowerShell。

这个「谁创建、谁注入」的分层，用一张图就清楚了：

```text
bootstrap.py（组合根，唯一创建真实实现的地方）
  ├─ PowerShellRunner()      ──注入──▶ create_chapter_one_tools(command_runner)
  ├─ OpenAIChatModel(...)    ──注入──▶ AgentRunner(model=...)
  └─ create_chapter_one_tools(...) ──注入──▶ AgentRunner(tools=...)

core/loop.py（核心循环）
  只认识 ModelClient / ToolRegistry / CommandRunner 这些接口
  从不 import SDK、启动子进程或读 .env
```

好处是测试可以只替换一个边界：测循环时换掉模型，测 shell 时换掉命令执行器，其他部分保持真实。文末「验证与实验」那一节讲的就是这件事。

### 第三步：追加模型回答，检查是否调了工具

**这一步解决什么**：判断循环该继续还是该结束。整个 Agent 的分叉点只有这里。

```python
# AgentRunner.run() 内的片段
assistant = reply.message
self._history.append(assistant)

if not assistant.tool_calls:
    if assistant.content is None:
        raise AgentRunError("模型已停止，但没有返回最终文本或工具调用")
    return RunResult(assistant.content, tuple(self._history), turn)
```

这里有个细节值得注意：SDK 回复对象不能直接混进长期历史。`OpenAIChatModel` 已经在适配器边界把它规范化成内部不可变消息（`_normalize_response()` 里做完了 role、finish_reason、content、tool_calls 的全部校验）；只有再次发请求时，`_to_openai_message()` 才重新生成 Chat Completions 所需的字典。这样业务代码看到的是稳定的内部类型，而不是某个 SDK 版本的响应形状。

另一个细节：循环退出的条件不是「模型说完了」，而是「模型没有调工具」。这两件事不等价——模型可以一边输出文本，一边调工具；也可以只调工具不说话；也可以只说话不调工具。我们只关心它有没有举手要工具。

把 `content` 和 `tool_calls` 的四种组合列出来，就能看清判断依据：

| `content` | `tool_calls` | 含义 | 代码怎么处理 |
| --- | --- | --- | --- |
| `None` | 非空 | 只调工具，不说话（最常见） | 执行工具，继续循环 |
| 有文本 | 非空 | 边解释边调工具 | 执行工具，继续循环（文本保留在历史里） |
| 有文本 | 空 | 正常给出最终答案 | **返回 `final_text`，退出** |
| `None` | 空 | 既没说话又没调工具 | 抛 `AgentRunError`：这是坏响应，不能当成答案 |

第四种情况是必须显式处理的：如果不管它，`final_text` 就会是 `None`，用户看到一个空回答，还以为 Agent「成功了」。**空结果不能冒充成功**——这条原则后面每一章都会再出现。

`finish_reason` 也要校验。它和 `tool_calls` 是两个独立的信号：

| `finish_reason` | 含义 | 第 1 章怎么做 |
| --- | --- | --- |
| `stop` | 模型自然说完 | 正常 |
| `tool_calls` | 模型停下来是为了调工具 | 正常 |
| `length` | 输出撞到 token 上限，被**截断**了 | 抛 `IncompleteModelReplyError` |
| `content_filter` | 内容被安全过滤拦下 | 抛 `AgentRunError` |

为什么 `length` 必须报错而不能继续？因为截断的输出可能是半句话、半个 JSON。把它当成最终答案返回给用户，等于把残缺内容伪装成完整结论。第 1 章选择明确失败，第 11 章才加入自动续写和恢复——**先能正确地失败，再谈优雅地恢复**，顺序不能反。

顺带一提：适配器对 `finish_reason` 还有第二道闸。供应商返回的结束原因如果不在 `{"stop", "length", "tool_calls", "content_filter", "function_call"}` 里，`openai_chat.py` 会抛「不支持的 finish_reason: ...」；旧版 `function_call` 结束原因也会被明确拒绝。

### 第四步：执行工具，收集结果

**这一步解决什么**：把模型的「意图」变成本机真实发生的事，并保证每个调用都有回音。

```python
# AgentRunner.run() 内的片段
for call in assistant.tool_calls:
    prepared = snapshot.prepare(call)
    result = snapshot.invoke(prepared, context)
    self._history.append(tool_message(result.content, call.id))
```

`prepare()` 先让 `ToolRegistry` 查找工具，再把参数 `json.loads` 成 dict、校验必须是对象、校验 shell 只允许 `command` 字段；`invoke()` 才真正调用 handler。未知工具、非法 JSON、schema 错误、审批拒绝、超时和执行异常都会变成 `ToolResult`，而不是让消息链断掉。为了突出 Loop，上面的片段省略了审批分支；实际 CLI 中 `shell` 默认要求逐次批准。

`prepare` 和 `invoke` 分成两个方法，是有意的设计：**`prepare` 绝不产生副作用，`invoke` 才真正执行。** 所以所有校验都能安全地跑在 `prepare` 里，确认无误后才让世界发生改变。

下面这张表列出 `prepare` + `invoke` 可能得到的每种结果，注意最后一列全是「回填」：

| 情况 | 谁发现的 | 产生什么 | 消息链断了吗 |
| --- | --- | --- | --- |
| 工具名不存在 | `prepare` 查注册表 | `unknown_tool`（「找不到工具: ...」） | 没断，正常回填 |
| `arguments` 不是合法 JSON | `prepare` 的 `json.loads` | `invalid_json` | 没断 |
| 参数缺字段 / 多字段 / 类型错 | `prepare` 的校验 | `invalid_arguments` | 没断 |
| 用户在终端输入 `n` | 审批边界 | `permission_denied`（原因是用户拒绝） | 没断 |
| 审批环节自己抛异常 | 审批边界的 `except Exception` | `permission_denied`（fail-closed） | 没断 |
| handler 执行超时 | `invoke` → PowerShellRunner | `shell_timeout` | 没断 |
| 命令退出码非零 | handler | `shell_failed` | 没断 |
| 一切正常 | handler | `tool_success(output)` | 没断 |

**八种情况，零次断链。** 这是循环健壮性的全部秘密：不管发生什么，模型都会收到一条格式正确的 `role: "tool"` 消息，循环就能继续。

其中「审批环节自己抛异常也算拒绝」这一条叫 fail-closed，源码写得很直白：

```python
# 授权器属于外部边界，无论抛出哪种异常都必须默认拒绝
except Exception:
    result = tool_error("permission_denied", "工具授权过程发生异常，已按默认拒绝处理")
```

如果反过来写成「审批出错就放行」，那么任何让审批崩掉的方式都变成了绕过权限的手段。**安全边界出错时必须选择更严格的那一侧**，第 3 章讲权限系统时会把这条原则展开成完整的决定模型。

最后是配对要求：每个工具调用结果都对应一个独立的 `role: "tool"` 消息，而且要通过 `tool_call_id` 和前面的调用对应上，这是 OpenAI 格式的要求，让模型知道哪个结果对应哪次调用。这相当于数据库外键：assistant 的调用 ID 是主键，tool 结果必须引用它。一个 assistant 消息中的每个调用都必须得到且只得到一个同 ID 结果，包括失败和拒绝。

用一个模型同时调两个工具的例子说明：

```text
assistant  content=None  tool_calls=[ {id:"call_1",...}, {id:"call_2",...} ]
tool       tool_call_id="call_1"  content="42"
tool       tool_call_id="call_2"  content="工具执行错误 [shell_failed]: ..."
             ↑ call_2 失败了，但依然必须回填一条消息
```

漏掉 `call_2` 会怎样？`validate_tool_pairing()` 在下一轮请求前就会抛 `MessageContractError`，消息是：

```python
MessageContractError("以下工具调用缺少返回结果: ['call_2']")
```

而不是等模型或供应商报一个含糊的错误。

### 第五步：把结果追加，回到第二步

**这一步解决什么**：让模型带着新信息重新决策——这就是「循环」两个字的全部含义。

```python
# 上一步已经 append 了 tool 消息
# for 循环还没有结束，自动进入下一轮，再次调用模型
```

循环结构本身就实现了「回到第二步」。这一轮模型看到的消息历史里，包含了它上一次的输出和工具执行结果，于是可以继续推理：「好，文件列表我拿到了，现在我需要……」。

这里唯一需要额外解释的是**为什么必须有 `max_turns`**。

`for turn in range(1, self._max_turns + 1)` 这个写法看起来像是防御性编程，但它挡的是真实会发生的事：模型可能陷入「调工具 → 结果不满意 → 换个参数再调 → 还是不满意 → 再调」的死循环。没有上限的话，程序永不退出，token 一直烧。

第 1 章默认 20 轮。注意计数单位：

```text
一轮 = 一次模型请求，不是一次工具调用。

模型一轮请求了 3 个工具 → 执行 3 次工具，但只算 1 轮。
```

轮次用完仍没拿到最终文本，抛 `AgentLimitError`，消息里带上具体的上限值：

```python
raise AgentLimitError(f"Agent 已达到最大模型调用轮数 max_turns={self._max_turns}")
```

用**独立的错误类型**而不是返回 `None` 或普通 `Exception`，是为了让调用方能区分「模型卡住了」和「模型报错了」——这两种情况的恢复策略完全不同。注意 `AgentLimitError` 和 `IncompleteModelReplyError` 都是 `AgentRunError` 的子类：调用方可以只捕基类，也可以精确分流。

---

## 完整的 agent_loop 函数

把上面五步组装起来，就是下面这个可运行的等价精简版。`execute_tool` 是一个返回 `ToolResult` 的窄函数：循环只关心「这次调用得到什么结果」，不关心工具内部如何执行。真实 `AgentRunner` 使用 `ToolRegistry.prepare()`、审批边界和 `ToolRegistry.invoke()` 完成执行。

```python
from pathlib import Path

from agent_ch01.core.loop import AgentLimitError, AgentRunError, IncompleteModelReplyError
from agent_ch01.core.messages import system_message, tool_message, user_message
from agent_ch01.core.model import ModelRequest
from agent_ch01.core.tools import ToolContext, ToolResult


def agent_loop(
    prompt: str,
    model,
    tools,
    execute_tool,
    workspace: str,
    system_prompt: str,
    max_turns: int | None = None,
):
    max_turns = max_turns if max_turns is not None else 20
    if not isinstance(max_turns, int) or max_turns <= 0:
        raise ValueError("max_turns 必须是正整数")

    history = [user_message(prompt)]
    context = ToolContext(str(Path(workspace).resolve()), "user")

    for turn in range(1, max_turns + 1):
        request = ModelRequest(
            messages=(system_message(system_prompt), *history),
            tools=tools.openai_tools(),
        )
        reply = model.complete(request)
        if reply.finish_reason == "length":
            raise IncompleteModelReplyError("模型输出达到 token 上限，回答不完整")
        if reply.finish_reason == "content_filter":
            raise AgentRunError("模型回答被内容过滤器拦截")

        assistant = reply.message
        history.append(assistant)
        if not assistant.tool_calls:
            if assistant.content is None:
                raise AgentRunError("模型已停止，但没有返回最终文本或工具调用")
            return assistant.content, history, turn

        for call in assistant.tool_calls:
            result = execute_tool(call, context)
            history.append(tool_message(result.content, call.id))

    raise AgentLimitError(f"Agent 已达到最大模型调用轮数 max_turns={max_turns}")
```

这就是一个能跑起来的 Agent Harness 的最小内核。

第一次读这段代码，建议按下面的顺序看，跳过所有校验分支：

```text
① for turn in range(1, ...)      ← 骨架：有界循环
② reply = model.complete(...)     ← 问模型
③ history.append(assistant)       ← 记住模型说了什么
④ if not assistant.tool_calls:    ← 唯一的分叉点
⑤     return content              ←   出口 A：正常结束
⑥ for call in assistant.tool_calls ← 出口 B：干活然后继续
⑦     history.append(tool_message(...)) ← 把结果喂回去
⑧ raise AgentLimitError           ← 出口 C：轮次耗尽
```

**三个出口，一个分叉点。** 把这八行认出来，剩下的代码全是围绕它们加的保护：

| 代码片段 | 保护什么 | 不写会怎样 |
| --- | --- | --- |
| `max_turns` 正整数校验 | 配置错误 | 传 `0` 进来循环一次都不跑，静默返回错误 |
| `finish_reason == "length"` | 截断响应 | 半句话被当成最终答案 |
| `finish_reason == "content_filter"` | 被过滤的响应 | 空内容被当成答案 |
| `assistant.content is None` 检查 | 坏响应 | `final_text` 是 `None`，用户看到空白 |
| `Path(workspace).resolve()` | 路径不规范 | 相对路径在不同 cwd 下指向不同目录 |

和真实 `AgentRunner` 的差别只有四处，都不影响你理解循环：

| 精简版 | 真实版 | 为什么 |
| --- | --- | --- |
| `execute_tool` 一个窄函数 | `snapshot.prepare()` + 审批 + `snapshot.invoke()` | 精简版把三段合成一个接口，突出循环主线 |
| 没有 `validate_tool_pairing` | 每轮请求前校验一次 | 真实版要在发请求前拦住损坏历史 |
| `history` 直接返回 | `tuple(self._history)` | 真实版不让调用方改到内部状态 |
| 异常文案随意 | 固定中文文案（见正文各处引用） | 真实版的错误文本是契约的一部分，测试逐字断言 |

完整版本在 `python/ch01_agent/agent_ch01/core/loop.py`，行为测试在 `python/ch01_agent/tests/`。

---

## 一个容易被忽略的差异：Anthropic 格式 vs OpenAI 格式

这份代码用的是 OpenAI 兼容格式（DeepSeek 等供应商也实现了同一协议）。如果你去读 Anthropic 官方的 API 文档，会发现消息结构稍有不同。把两种格式放在一起看，能更清楚地理解「模型有没有请求工具」这个信号本身：

| 概念 | Anthropic 格式 | OpenAI 格式 |
| --- | --- | --- |
| 判断模型是否调工具 | `response.stop_reason == "tool_use"` | `message.tool_calls` 是否非空 |
| 工具调用 ID 字段 | `tool_use_id` | `tool_call_id` |
| 工具结果消息的 role | `user`（把 tool_result 嵌在 user 消息里） | `tool` |
| 工具调用块的格式 | content 里的 `type: "tool_use"` 块 | `message.tool_calls` 数组 |

形式不同，但本质信号是一样的：「模型有没有举手要工具」。这是 Agent Loop 唯一需要感知的信号，和用哪家 SDK 无关。适配器（`_normalize_response()`）会把它转换成统一的 `ModelReply`，循环代码一个供应商名词都不出现。

---

## 必须说清楚：shell 不是沙箱

配套实现运行在 Windows，工具名是 `shell`，里面执行的是 PowerShell。它没有字符串黑名单，因为换一种写法就能绕过黑名单，反而容易制造「已经安全」的错觉。

真实 CLI 把 shell 标记为 `execute` 副作用，每次执行前都会显示工具名和参数，并询问：

```text
工具调用需要批准: shell
参数: {'command': 'Write-Output 42'}
允许本次调用? [y/N]
```

只有明确输入 `y` 或 `yes` 才会执行；回车、其他输入、无交互 stdin 或审批异常都默认拒绝。拒绝仍会生成同一个 `tool_call_id` 的工具错误结果，让模型可以换方案。

但审批也不等于隔离。`PowerShellRunner` 的 `cwd` 只是子进程的起始工作目录，命令仍可能切换目录、访问绝对路径、启动其他进程或访问网络。后面的文件工具有单独的工作区路径边界，`shell` 没有。真正的规则合并、审计和拒绝优先级会在第 3 章展开；在那之前，绝不能把 `cwd` 描述成 sandbox。

---

## 几十行 vs 1729 行：CC 源码的差距在哪

外部仓库 `shareAI-lab/learn-claude-code` 曾对 Claude Code 旧版核心文件 `query.ts` 做过逐行核查，记录显示该文件约 1729 行。Claude Code 本身不开源，公开仓库也不包含这个文件，所以下面的行号只是参考快照，不代表当前接口。

核心循环逻辑只是上面这几十行。那剩下的代码干什么的？

全是保护机制。

### 差异一：循环继续的判断方式

上面的代码靠 `tool_calls` 是否非空来判断是否继续。CC 的做法不同：

```ts
// 参考核查快照（非当前源码摘录）：
// stop_reason === 'tool_use' is unreliable.
// Set during streaming whenever a tool_use block arrives.
let needsFollowUp = false
```

CC 在参考快照中用一个 `needsFollowUp` 标志位，在流式接收响应时，只要检测到 `tool_use` 块就设为 `true`，而不是等整个响应接收完再看 `stop_reason`。

为什么？因为流式响应里 `stop_reason` 可能滞后：内容块已经到了，元数据字段还没更新。如果只看 `stop_reason`，在流式场景下可能漏判，导致工具没被执行，循环提前退出。这是生产级代码对流式传输的具体适配，不是理论层面的差异。

官方 Agent SDK 文档现在也把循环描述为「一直重复，直到模型产生没有工具调用的响应」；`stop_reason` 仍然用来判断最终轮为什么结束，例如 `end_turn`、`max_tokens` 或 `refusal`。

### 差异二：State 对象扛了 10 个字段

第 1 章的核心只需要 history、轮次和工具上下文。CC 的循环 State 对象有 10 个字段：

| 字段 | 用途 |
| --- | --- |
| messages | 当前迭代的消息数组 |
| toolUseContext | 工具、信号、权限上下文 |
| autoCompactTracking | context 压缩状态追踪 |
| maxOutputTokensRecoveryCount | token 恢复尝试次数，上限 3 次 |
| hasAttemptedReactiveCompact | 本轮是否已尝试响应式压缩 |
| maxOutputTokensOverride | 输出 token 限制从 8K 升至 64K 的覆盖 |
| pendingToolUseSummary | 后台用 Haiku 生成的工具调用摘要 |
| stopHookActive | 停止钩子是否产生阻塞错误 |
| turnCount | 轮次计数，用于 maxTurns 检查 |
| transition | 上一次循环继续的原因 |

举两个有意思的：

`maxOutputTokensRecoveryCount` 最多重试 3 次。因为模型有时候会被 token 上限截断，输出还没说完就停了。生产系统必须检测这种情况并恢复，不能直接当成「模型做完了」退出。

`pendingToolUseSummary` 是在后台用更便宜的模型（Haiku）给工具调用历史生成摘要，压缩 context 长度省 token。这是 Agent 长时间跑任务时做 context 管理的手段之一。

另外注意：`taskBudgetRemaining`（任务预算剩余）不在 State 上。源码注释明确写了 `Loop-local (not on State)`：它只是循环内部的局部变量，每次进入循环重置。

### 差异三：退出路径

最小概念代码只有 1 条正常退出路径：模型不调工具。配套实现从第 1 章起还增加了最大轮次，以及 `length`、`content_filter`、无最终文本等明确失败；它们只负责阻止错误完成，不在本章自动恢复。

CC 的退出路径覆盖了：

- 达到最大轮次（maxTurns）
- Context 超长（prompt too long），尝试压缩后继续
- 模型输出被 token 上限截断，恢复后继续
- 模型报错
- 用户主动中断（abort）
- Stop Hook 拦截并产生阻塞错误
- token budget 耗尽后的继续策略
- 响应式压缩重试

每种路径都有独立的处理逻辑和恢复策略，不是简单的 `return` 或 `raise`。第 1 章仍接近单行道；后续章节会逐步补齐这些恢复路径。

### 差异四：流式并行工具执行

CC 还有一个 `StreamingToolExecutor`，在模型还在生成响应内容的时候，对已经到达的工具调用就开始执行了，不等模型说完。

具体怎么决定并发还是串行？看工具的 `concurrency-safe` 标记。安全的工具可以并行跑，有状态冲突风险的工具独占执行。这是纯性能优化，和循环本身的正确性无关，但在有大量工具调用的 Agent 任务里能显著减少总延迟。

---

## 把上面的东西放在一起

现在可以给出一个清晰的结论：

```text
Agent = 模型决策 + Harness 执行 + 消息历史传递
```

这三件事加在一起就是 Agent。不需要 Planner、不需要 Memory、不需要 Orchestrator；这些都只是在这三件事的基础上叠加出来的能力。

- 模型决策：「要不要用工具、用哪个、传什么参数」。这是智能，由模型完成，循环代码控制不了。
- Harness 执行：「工具调了就跑，结果喂回去」。这是机械动作，就是一段很短的循环。
- 消息历史传递：「每一轮的输入输出都追加进列表」。这是记忆，也就是内部 `history` 列表。

换一种更常见的概念公式，就是 **Agent = LLM + 上下文 + 工具**：

- LLM 是决策内核，决定「要不要用工具、用哪个、传什么参数」；
- 上下文是模型每个决策点能看到的观察空间，由工具定义、系统提示和会话历史共同构成；
- 工具是模型改变世界的动作空间，Harness 执行则把工具接入这个上下文。

围绕这三个部分搭起来的约束、验证、恢复和权限机制，就是 Harness。最小循环只保留这三个可运行的部分，Planner、Memory、Orchestrator 都是在这之上叠加的能力。

三件事里，Harness 执行就是我们今天写的这段最小循环。剩下的生产代码，是围绕「Harness 执行」加的保护、恢复和优化；核心循环本身始终没变。

在第 1 章代码里，观察空间由 `system prompt`、消息历史、工具 schema 组成；动作空间由 `ToolRegistry` 中的 `shell` 组成；而 config、人工审批、错误分类和 `max_turns`，正是最早的 Harness。

---

## 源码设计地图：12 个文件各管一件事

把第 1 章的源码文件放到一起看，核心循环之外的大部分代码都在做「启动、边界和约束」：

| 文件 | Java 角度的职责 | 正文对应 |
| --- | --- | --- |
| `agent_ch01/core/loop.py` | `AgentRunner` 应用服务：消息历史、有界循环、工具回填、停止条件 | 五步拆解 |
| `agent_ch01/core/messages.py` | 四种消息 record 和配对验证 | 第一步 |
| `agent_ch01/core/tools.py` | `ToolRegistry`：schema、prepare/invoke、错误结果 | 第二步、第四步 |
| `agent_ch01/core/model.py` | `ModelClient` / `ModelReply` 模型边界 | 第二步 |
| `agent_ch01/core/commands.py` | `CommandRunner` / `CommandResult` 进程执行契约 | shell 工具 |
| `agent_ch01/core/profiles.py` | `ChapterProfile` / `P01` 章节能力白名单 | 启动链 |
| `agent_ch01/features/builtin_tools.py` | 注册 `shell` 工具 | 第二步 |
| `agent_ch01/adapters/openai_chat.py` | OpenAI SDK 适配器与响应规范化 | 第三步 |
| `agent_ch01/adapters/powershell.py` | `PowerShellRunner` 超时、截断、UTF-8 | shell 工具 |
| `agent_ch01/config.py` | 启动前校验 `.env` 与 URL | 运行 |
| `agent_ch01/bootstrap.py` | 组合根：注入模型、命令、工具和 profile | 启动链 |
| `agent_ch01/cli.py` | 参数解析、`TerminalAuthorizer`、错误出口 | 审批 |

`profiles.py` 看起来很小，但它是章节能力边界：`P01 = ChapterProfile(1, frozenset({"loop", "powershell"}))`，`build_agent` 只接受第 1 章 profile，防止把后续章节能力装进第 1 章。`commands.py` 则是 `PowerShellRunner` 后面的窄接口（`Protocol`），让测试可以用 fake 替换真实子进程。

启动顺序是：入口解析参数 → `config.py` 校验配置 → `bootstrap.py` 组装依赖 → `AgentRunner` 跑循环；核心循环完全不碰 SDK、子进程和 `.env`。

---

## 手里只有 shell 的问题

目前这个 Agent 手里只有一个工具：`shell`，也就是 Windows PowerShell 命令入口。

这意味着：

- 读文件要 `Get-Content .\path\to\file`
- 写文件要 `Set-Content .\path\to\file -Value "..."`
- 找文件要 `Get-ChildItem -Recurse -Filter '*.py'`
- 移动文件要 `Move-Item`

用起来又丑又脆：路径和转义容易写错；文件内容里有引号、换行或特殊字符时尤其明显；多步操作也没有原子性。

下一篇会给它四个专用工具：`read_file`、`write_file`、`edit_file`、`glob`。`shell` 仍然保留，后章在本章能力上继续累加。

到时候会出现新问题：模型会不会一次调用多个工具？两个工具同时写同一个文件会怎样？并行执行的边界在哪？

---

## 运行第 1 章智能体

这一节完全自包含：从零开始，六步跑通。每步都给出预期结果，对不上就看下面的排错表。

### 第 0 步：确认 Python 版本

```powershell
python --version
```

预期输出 `3.11` 或更高（配套代码用了 `X | None` 联合类型标注和 `slots=True`，依赖这个版本）。

### 第 1 步：创建共享虚拟环境（只做一次）

所有 Python 章节共用 `python/.venv` 和 `python/.env`，第一次使用时创建：

```powershell
Set-Location 'E:\cj\study\learn-agent\python'
python -m venv .venv
& .\.venv\Scripts\python.exe -m pip install --upgrade pip
Copy-Item '.env.example' '.env'
```

预期结果：`.venv` 目录出现，`python/.env` 从模板复制好（内容见第 3 步）。

之后每个章节只需要把当前章节安装到这一个环境里，不要重新创建 `.venv` 或 `.env`。章节运行时会从当前目录向父目录查找 `.env`，因此 `python/.env` 可以被所有章节复用。

### 第 2 步：安装本章并先跑离线测试（不需要任何密钥）

**建议在配 API 之前先做这一步。** 它能验证代码本身没问题，把「环境问题」和「配置问题」分开：

```powershell
& ..\.venv\Scripts\python.exe -m pip install -e '.\ch01_agent[dev]'
Set-Location '.\ch01_agent'
& ..\.venv\Scripts\python.exe -m pytest tests
```

预期输出的最后一行类似：

```text
9 passed in 1.23s
```

这 9 个测试全部离线：模型和 PowerShell 都被替身注入（`FakeModel`、`FakeCommandRunner`），不发一个网络请求。所以它们能通过，说明循环逻辑、消息配对、错误分类都是好的。

### 第 3 步：编辑 .env

编辑 `python/.env`，填写下面三项，**每一项都不能留空**：

```dotenv
OPENAI_BASE_URL=https://api.deepseek.com
OPENAI_API_KEY=你的密钥
OPENAI_MODEL=deepseek-v4-flash
```

`OPENAI_BASE_URL` 有两条硬性要求，写错的人非常多：

| 要求 | 正确 | 错误 |
| --- | --- | --- |
| 不要带 `/chat/completions` | `https://api.deepseek.com` | `https://api.deepseek.com/chat/completions` |
| 兼容网关通常要以 `/v1` 结尾 | `https://your-provider.com/v1` | `https://your-provider.com` |

原因很简单：OpenAI SDK 会**自己**在 base URL 后面拼 `/chat/completions`。你要是已经写上了，最终请求路径就变成 `/v1/chat/completions/chat/completions`。`config.py` 的 `settings_from_mapping()` 会在发请求前就检测出这种写法并直接报错，不会让你对着一个 404 猜半天。

任何一项为空，程序都会在网络请求前列出缺失字段，不会用默认模型或默认地址掩盖配置错误。**没有隐式默认值**是有意的设计：宁可启动失败，也不要让你以为在用 A 模型、实际在用 B 模型。

### 第 4 步：跑真实 Agent

```powershell
Set-Location '.\ch01_agent'
& ..\.venv\Scripts\python.exe -m agent_ch01.cli --prompt "运行 Write-Output 42，并告诉我结果"
```

`--prompt` 是必填参数，漏掉时 argparse 会打印用法并退出。命令从当前目录向上查找共享 `.env`，当前章节目录作为 Agent workspace。

### 第 5 步：看懂审批提示并批准

模型决定用 `shell` 时，终端会停下来问你：

```text
工具调用需要批准: shell
参数: {'command': 'Write-Output 42'}
允许本次调用? [y/N]
```

这两行信息对应审批的两个判断依据：**哪个工具、具体参数是什么**。看清 `参数` 那一行再决定——这是你唯一能拦住危险命令的地方。

输入 `y` 回车，预期最终输出类似：

```text
命令执行成功，输出是 42。
```

这是模型的最终答案（`cli.py` 只打印 `result.final_text`；PowerShell 的原始输出被子进程捕获后作为工具结果喂给了模型，不会直接打到终端）。

只有明确输入 `y` 或 `yes` 才会执行。**回车、其他输入、无交互 stdin 或审批异常都默认拒绝。** 拒绝仍会生成同一个 `tool_call_id` 的工具错误结果，让模型可以换方案——所以你输 `n` 之后 Agent 不会崩，它会告诉你「我没能执行命令」。只批准你已经检查过的真实命令。

### 常见报错排查

按终端显示的错误文本查表：

| 你看到的错误 | 原因 | 怎么修 |
| --- | --- | --- |
| `配置错误: 缺少或填写错误的必要配置: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL` | `.env` 缺字段或字段为空，或根本没找到 `.env` 文件 | 补齐报错里列出的那几项。注意等号后面有空格也算空 |
| `配置错误: 缺少或填写错误的必要配置: OPENAI_BASE_URL`（只报这一项） | URL 协议不是 http/https，或结尾带了 `/chat/completions` | 按第 3 步的表改 URL |
| `usage: ... error: the following arguments are required: --prompt` | 漏了 `--prompt` 参数 | 补上 `--prompt "..."` |
| `运行失败: Agent 已达到最大模型调用轮数 max_turns=20` | 模型反复调工具但拿不到满意结果 | 换更明确的 prompt；或先用简单命令确认链路通 |
| `运行失败: 模型输出达到 token 上限，回答不完整` | 模型输出被截断（`finish_reason=length`） | 让任务输出更短。第 11 章才会自动恢复这种情况 |
| `运行失败: 不支持的 finish_reason: ...` | 供应商返回了非标准 `finish_reason` | 该第三方接口未完全兼容 Chat Completions，换供应商或换模型 |
| 打印 `无交互输入，默认拒绝。` | 没有交互终端（管道、CI、某些 IDE 内置终端） | 在真实 PowerShell 窗口里跑 |
| `ModuleNotFoundError: No module named 'agent_ch01'` | 没在共享 `.venv` 里安装本章包，或用了系统 Python | 回到第 2 步执行 `pip install -e '.\ch01_agent[dev]'`，并确认命令用的是 `..\.venv\Scripts\python.exe` |
| 401 / 403 类网络错误 | API Key 无效或无权限 | 检查 `OPENAI_API_KEY`，确认该 key 能访问 `OPENAI_MODEL` |
| 404 类网络错误 | base URL 路径不对 | 多半是漏了 `/v1`，见第 3 步 |

退出码也有约定：`0` 正常结束；`2` 配置错误；`1` 其他运行失败。排查脚本化调用时可以直接看退出码分流。

---

## 验证与实验：测试就是可重复的对照

配套测试把本章最关键的边界变成了可重复的离线实验。在 `python/ch01_agent` 下运行 `pytest tests` 会执行 4 个测试文件、9 个测试：

| 测试文件 | 验证内容 |
| --- | --- |
| `test_loop.py` | 完整 Agent 循环、工具回填、最大轮次（`test_loop_enforces_max_turns` 用 `max_turns=2` 验证上限生效） |
| `test_tools.py` | shell 超时结果映射、未知工具、非法 JSON |
| `test_messages.py` | tool 调用与结果的严格配对：乱序接受、重复 ID/孤立结果拒绝、缺结果拒绝 |
| `test_config.py` | 缺失配置、带 `/chat/completions` 的完整端点拒绝 |

这些测试是「消融实验」思维的工程化版本：通过脚本化 `ModelClient` 或 fake `CommandRunner`，一次只替换一个外部边界，观察 `AgentRunner` 是否仍能保持消息配对、错误回填和停止条件。真实模型实验需要密钥，离线验证由这些可重复测试覆盖。

### 三个建议动手做的小实验

看懂不等于会写。下面三个改动都只要几分钟，但能把本章的核心约束从「知道」变成「验证过」。改完记得改回去。

**实验一：把轮次上限调到 1，看它怎么失败**

在 `python/ch01_agent/tests/test_loop.py` 里找到构造 Agent 的地方，把 `max_turns` 传成 `1`，然后让 `FakeModel` 第一轮就请求工具。

预期：抛 `AgentLimitError`，消息是 `Agent 已达到最大模型调用轮数 max_turns=1`。
学到：轮次上限拦的是「模型第一轮调了工具，但没机会给出最终文本」这种情况——工具执行完了，循环却没有下一轮可用。

**实验二：故意漏掉一个工具结果**

手工构造一段历史：一条 assistant 消息带两个 `tool_calls`，但只跟一条 `tool` 消息。然后调用 `validate_tool_pairing(history)`。

预期：抛 `MessageContractError`，消息形如 `以下工具调用缺少返回结果: ['call_2']`。
学到：配对校验的价值在于**提前**报错。少了这层校验，你会在供应商返回一个含糊的 400 时才发现问题，而且不知道是哪个 ID 漏了。

**实验三：让 fake 命令执行器返回非零退出码**

用 `FakeCommandRunner` 返回 `CommandResult("partial", 1, True, False)`（`test_tools.py` 的 `test_shell_result_mapping` 就是这么做的），观察循环有没有中断。

预期：循环不中断，历史里出现一条内容包含 `shell_timeout` 或 `shell_failed` 的 `role: "tool"` 消息，模型还能继续下一轮。
学到：这就是第四步那张表想说的事——**工具失败是数据，不是异常**。也正是 Agent 能自己换方案重试的原因。

---

## 本章小结

### 三句话版本

1. Agent Loop 就是「问模型 → 看 `tool_calls` 空不空 → 执行工具 → 把结果 append 回历史 → 再问模型」，代码不到 60 行。
2. 循环只有一个分叉点（`tool_calls` 空不空）和三个出口（正常返回、轮次耗尽、不可恢复错误）。
3. 生产级 Agent 的代码量差距不在循环，全在循环外围的保护：校验、审批、错误分类、恢复、上限。

### 一定要记住的五条

| # | 结论 | 出现在哪一节 |
| ---: | --- | --- |
| 1 | 退出条件是「模型没调工具」，不是「模型说完了」 | 第三步 |
| 2 | 每个 `tool_call` 必须有且只有一个同 ID 的结果，失败和拒绝也算 | 第四步 |
| 3 | 参数校验必须在副作用之前；`prepare` 不执行，`invoke` 才执行 | 第四步 |
| 4 | 工具失败返回 `ToolResult`，不 `raise`——否则模型没机会纠错 | shell 工具 |
| 5 | 安全边界出错时选择拒绝（fail-closed），不选择放行 | 第四步 |

### 本章代码边界（明确「还没做什么」）

| 已经有 | 还没有 | 在哪一章补 |
| --- | --- | --- |
| 一个 `shell` 工具 | 专用文件工具（读/写/改/找） | 第 2 章 |
| 逐次人工审批 | 规则化权限策略、审计、四态决定 | 第 3 章 |
| `cwd` 起始目录 | 真正的路径边界（`shell` 目前**没有**） | 第 2、3 章 |
| 明确失败（`length`/`content_filter`） | 截断续写、限流重试、fallback 模型 | 第 11 章 |
| 只增不减的 `history` | 上下文压缩与产物落盘 | 第 8 章 |
| 单个 Agent | 子 Agent、多 Agent 协作 | 第 6、15–18 章 |
| 启动时固定的工具集 | MCP 动态工具池 | 第 19 章 |

**别把第 1 章的 `cwd` 当成沙箱。** 这是本章最需要强调的边界，前面「shell 不是沙箱」那一节已经解释过原因。

### 检查你是否真的读懂了

试着不看文章回答这五个问题。答不上来的，回对应章节再看一遍：

1. 模型返回 `content: "好的"` 且 `tool_calls: []`，循环该继续还是结束？（第三步）
2. 模型返回 `content: null` 且 `tool_calls: []`，代码该怎么做？为什么不能直接返回？（第三步）
3. 模型一轮请求了 3 个工具，其中 1 个执行失败。要往历史里 append 几条 `tool` 消息？（第四步）
4. `snapshot()` 到底防的是什么问题？（第二步）
5. 为什么 `max_turns=20` 指的是 20 次模型请求，而不是 20 次工具调用？（第五步）

---

## Java 开发者的考古顺序

第一次不要从 CLI 或 OpenAI SDK 开始，按下面顺序阅读：

```text
tests/test_loop.py
  -> bootstrap.py
  -> core/loop.py
  -> core/model.py
  -> core/tools.py
  -> features/builtin_tools.py
  -> adapters/powershell.py
  -> adapters/openai_chat.py
```

每到一个文件，只问四个问题：

1. 这个文件在 Java 项目中相当于哪一层？
2. 字段保存什么状态？
3. 方法接收什么输入、返回什么输出？
4. 哪些失败会被转换成结构化结果？

重点 Python 语法对照：

| Python 写法 | Java 大白话 |
| --- | --- |
| `self` | 显式写出来的 `this` |
| `@dataclass(frozen=True, slots=True)` | 不可变 record / Lombok `@Value` |
| `Protocol` | `interface` |
| `str | None` | 可能为 null 的 String |
| `tuple` | 不可变 List（`List.copyOf`） |
| `dict` | Map |
| `*items` | addAll 展开集合 |
| `**mapping` | 把 Map 展开为关键字参数 |
| `with pytest.raises` | JUnit `assertThrows` |
| `def` | 方法或函数声明 |
| `_name` | 约定的 private 字段 |
| 缩进 | Python 用缩进代替 Java 的大括号 |

等你能用 Java 或 Python 手写出「模型请求 `current_time`，程序执行工具，再返回最终文本」的循环，第 1 章就真正学会了。后面的权限、记忆、任务 DAG 和多 Agent，都是在这条循环上继续增加能力。
