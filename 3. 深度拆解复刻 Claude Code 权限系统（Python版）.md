# 深度拆解复刻 Claude Code 权限系统：如何实现生产级的 Agent 安全策略？（Python 版）

这篇是原第 3 章 TypeScript 文章的 Python 完整迁移版。设计讨论、章节结构和边界说明全部保留；代码、文件路径和运行命令改为 Python，并面向有 Java 后端背景的读者补了语言对照。

配套项目位于 `python/ch03_agent`，继续共享 `python/.venv` 和 `python/.env`。

前两章的 Agent 已经能执行 PowerShell，也能读、写、编辑和查找文件。

能力够用了，风险也随之出现。

假设用户只说「清理项目里的临时文件」，模型却生成了：

```powershell
Remove-Item -LiteralPath 'C:\temp' -Recurse -Force
```

如果运行时没有权限边界，Agent Loop 会照常把命令交给 PowerShell。模型写错一个路径，影响范围就可能完全不同。

问题不在于模型「坏」。模型只是在预测下一步。真正的问题是：我们把安全完全委托给了概率模型，却没有在副作用发生前设置确定性边界。

这一章把结构化权限策略插进工具准备和 handler 执行之间。

---

## 本章导读

### 读完这一章，你能做到什么

1. 说清楚为什么权限判断不能写在 system prompt 里，只能写在代码的固定位置。
2. 手画出 `prepare → 权限 → invoke` 这条顺序，并解释顺序反过来会失去什么。
3. 解释 `allow` / `deny` / `ask` / `passthrough` 四种行为各自的用途，以及为什么 boolean 不够。
4. 讲清 `deny > ask > allow > passthrough` 这条优先级，并举出一个「规则说允许但仍然被拒绝」的例子。
5. 列出所有会导致 `ask` 变成 `deny` 的情况（一共七种），并说明这叫 fail closed。
6. 判断一次调用会走哪条路径：直接放行、弹审批、还是硬拒绝。

### 你需要先具备什么

| 需要 | 说明 |
| --- | --- |
| 读完第 1、2 章 | 本章直接建立在 `ToolRegistry.prepare()` / `invoke()` 的分离之上 |
| 理解 `effect` 标签 | 第 2 章给每个工具贴了 `read` / `write` / `execute`；本章开始**真正用它** |
| 理解 `safe_path` | 本章的工作区硬边界就是复用第 2 章的 `safe_path` |
| 会跑第 2 章 | 本章运行方式完全一样，只是多了审批交互 |

不需要任何安全或密码学背景。本章讲的全是「在哪一行代码上做判断」这种工程问题。

### 术语速查（本章第一次出现的词）

| 术语 | 一句话解释 |
| --- | --- |
| **PermissionDecision** | 一个不可变的权限结论，三件事：behavior + reason + source |
| **behavior** | 结论本身：`allow` / `deny` / `ask` / `passthrough` |
| **reason** | 人能读懂的原因。会进审计，也会进模型视野 |
| **source** | 谁做的决定。规则名、`shell-default`、`terminal-approval`、`workspace-boundary`、`default` |
| **PermissionRequest** | 权限系统的输入。**只接受已通过 validator 校验的调用** |
| **PermissionRule** | 一条规则 = 名字 + 行为 + 原因 + 匹配函数 |
| **PermissionPolicy** | 合并所有参与方、产出唯一最终决定的地方 |
| **参与方** | 能提出候选决定的角色：硬边界、Shell 默认、规则、Hook 建议 |
| **ApprovalProvider** | 审批边界。`ask` 只能由它收敛成 `allow` 或 `deny` |
| **AuditSink** | 审计边界。记录**最终**决定；它失败则不执行 |
| **WorkspaceWriteBoundary** | 系统硬边界的窄接口。判断写入路径是否在 workspace 内 |
| **fail closed** | 判断依赖出故障时选择拒绝，而不是把「不知道」当成「可以」 |
| **recommendations** | 上游参与方（第 4 章的 Hook）传入的候选决定列表。本章预留不使用 |
| **permission_denied** | 权限拒绝回填给模型的稳定错误码 |
| **permission_evaluation_error** | 权限系统自身出故障时的稳定错误码 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先看一眼真实的决策过程    ← 三条调用走出三种结果，先有画面
  ↓
② 验收结果 + 为什么不是 Prompt ← 本章要证明什么、为什么必须在代码里
  ↓
③ 四态决定                  ← 为什么不是 boolean，三个字段各自的作用
  ↓
④ 请求与规则                ← 权限层的输入契约、一条规则长什么样
  ↓
⑤ 三道闸门如何合并          ← 本章最核心的一节：优先级
  ↓
⑥ 硬边界 / Shell / 审批 / 审计 ← 四个参与方逐个看
  ↓
⑦ 插入 Agent Loop + P02/P03 差异 ← 只加了十几行，能力边界变了多少
  ↓
⑧ 四个场景 + effect 驱动     ← 把前面所有规则合到具体调用上
  ↓
⑨ 边界、运行、验证与小结     ← 刻意不做的事 + 动手 + 排错 + 自测
  ↓
⑩ 小白阅读路线              ← Java 视角的源码考古顺序
```

---

## 先看一眼真实的决策过程

先不看代码。假设模型在同一次会话里先后提出三个调用，看看它们分别走到哪一步。

### 调用 A：`glob`（read）

```json
{"pattern":"agent_ch03/**/*.py"}
```

```text
① 工作区硬边界   effect 不是 write，不表态 → 无候选
② Shell 默认     effect 不是 execute，不表态 → 无候选
③ 规则           confirm-file-write 不匹配 → 无候选
④ 合并           一个候选都没有 → passthrough
⑤ 收敛           passthrough → allow（default）
⑥ 审批           不需要
⑦ 审计           [权限审计] glob: allow (default) - 没有权限规则阻止请求
⑧ 执行           handler 跑起来
```

**默认允许，但仍然留了记录。**

### 调用 B：`write_file`（write，路径在工作区内）

```json
{"path":"chapter3-note.txt","content":"..."}
```

```text
① 工作区硬边界   effect 是 write，path 在 workspace 内 → 不表态
② Shell 默认     不表态
③ 规则           confirm-file-write 匹配 → ask
④ 合并           最强的是 ask
⑤ 收敛           调用 ApprovalProvider
                 ↓
                 工具调用需要批准: write_file
                 原因: 第三章的文件写入需要明确审批
                 参数: {'path': 'chapter3-note.txt', 'content': '...'}
                 允许本次调用? [y/N] y
                 ↓
                 allow（terminal-approval）
⑥ 审计           [权限审计] write_file: allow (terminal-approval) - 用户批准了本次工具调用
⑦ 执行           handler 跑起来
```

**这里有个关键点：审批只授权这一次。** 下一轮再写文件，还会再问一遍。

### 调用 C：`write_file`（write，路径在工作区外）

```json
{"path":"../outside.txt","content":"x"}
```

```text
① 工作区硬边界   effect 是 write，safe_path 拒绝 → deny
② Shell 默认     不表态
③ 规则           confirm-file-write 匹配 → ask
④ 合并           deny 和 ask 都在 → deny 优先
⑤ 收敛           deny 不需要审批，直接就是最终结论
⑥ 审批           ❌ 根本没调用，用户看不到任何提示框
⑦ 审计           [权限审计] write_file: deny (workspace-boundary) - 禁止写入工作区之外
⑧ 执行           ❌ handler 一次都没跑
⑨ 回填模型       工具执行错误 [permission_denied]: 禁止写入工作区之外
```

调用 C 是本章最重要的一个例子。请注意 ④：**规则明明说了 `ask`，也就是「问一下就能过」，但硬边界的 `deny` 直接压过它。** 这就是 `deny > ask` 的含义——不给用户「批准一个不该被批准的操作」的机会。

### 三条调用压成一张表

| | 调用 A `glob` | 调用 B 区内 write | 调用 C 区外 write |
| --- | --- | --- | --- |
| 候选决定 | 无 | `ask`（规则） | `deny`（硬边界）+ `ask`（规则） |
| 合并结果 | `passthrough` | `ask` | **`deny`** |
| 弹审批框？ | 否 | **是** | **否** |
| handler 执行？ | 是 | 看用户输入 | **否** |
| 进审计？ | 是 | 是 | 是 |

最后一行值得单独看：**三种情况都进审计。** 审计记的不是「危险操作」，是「每一个最终决定」。

---

## 先定义验收结果

P03 必须满足以下可观察行为：

1. `deny` 的优先级高于 `ask`、`allow` 和 `passthrough`；
2. 命中 `deny` 时，不询问用户，也不执行 handler；
3. `ask` 只有得到明确的 `allow` 决定后才能执行；
4. 没有审批器、审批异常、非法审批结果、直接回车和无交互输入都默认拒绝；
5. 所有 `shell` 调用默认进入审批，不按命令文本猜测风险；
6. 普通读取在没有规则反对时直接放行；
7. P02 的文件写入不审批，P03 的 `write_file` 和 `edit_file` 才新增审批；
8. 工作区外写入属于系统硬拒绝，规则、未来 Hook 建议和人工批准都不能放宽；
9. 权限拒绝和权限评估异常仍要生成与原调用 ID 配对的 tool result；
10. P03 的每个最终权限决定都进入审计。

直接测试位于 `python/ch03_agent/tests/test_permissions.py` 和 `python/ch03_agent/tests/test_ch03_integration.py`。

---

## 为什么 Prompt 不是安全边界

可以在 system prompt 里写「不要删除系统文件」，但这只是软约束：

- 模型可能理解错误；
- 用户输入可能与系统指令发生冲突；
- 多轮上下文可能弱化早期提醒；
- 不可能枚举所有危险命令和路径组合；
- Prompt 无法证明副作用发生前一定执行了检查。

权限策略必须在代码里，并且位置固定：

```text
模型返回 tool_calls
        ↓
ToolRegistry.prepare：查找 + JSON 解析 + validator 校验
        ↓
PermissionPolicy.decide：硬边界 + 规则 + 审批 + 审计
        ↓
ToolRegistry.invoke：执行 handler
        ↓
回填相同 tool_call_id
```

模型只能提出调用请求。是否执行由 Harness 决定。

Java 对照：Prompt 类似业务提示或操作说明，权限策略才类似 Spring Security Filter、方法鉴权或领域 Policy Service。不能因为 Controller 文档写了「禁止越权」就省略真正的鉴权代码。

---

## 权限不是 boolean，而是四态决定

如果权限函数只返回 `True` 或 `False`，运行时不知道「需要询问用户」和「这条规则不发表意见」的区别，也无法解释决定来源。

用一个具体例子说明 boolean 为什么不够。假设权限函数签名是 `can_run(call) -> bool`，现在有两条规则：

- 规则甲：`write_file` 需要人工确认；
- 规则乙：只关心 `shell`，对 `write_file` 没意见。

模型调用 `write_file` 时，规则甲返回什么？返回 `False` 会变成直接拒绝——但它想要的是「问一下」。返回 `True` 就直接执行了——审批环节消失。**boolean 表达不出「暂缓」。**

规则乙又返回什么？返回 `True` 表示放行，可它其实什么都不想说。如果合并逻辑是「所有规则都返回 True 才执行」，那一条不表态的规则就变成了投赞成票——这在权限系统里是不能接受的语义混淆。

本章定义四种行为：

| behavior | 含义 |
| --- | --- |
| `allow` | 明确允许 |
| `deny` | 明确拒绝 |
| `ask` | 必须取得一次显式审批 |
| `passthrough` | 当前参与方不表态，交给其他规则或默认值 |

`passthrough` 是这四个里最容易被忽略、但设计上最必要的一个。它的作用是**把「弃权」和「赞成」区分开**。在源码里，规则不匹配时返回的其实是 `None`（连候选都不产生），`passthrough` 出现在另一处：所有参与方都没意见时，`_strongest()` 兜底返回它，再由策略转成默认 `allow`。这样「没有任何规则反对」和「有规则明确允许」在审计里是两条不同的记录。

`agent_ch03/core/permissions.py` 用固定 tuple 和 `Literal` 类型同时提供运行时集合和静态类型：

```python
PermissionBehavior = Literal["allow", "deny", "ask", "passthrough"]
PERMISSION_BEHAVIORS: tuple[PermissionBehavior, ...] = (
    "allow", "deny", "ask", "passthrough"
)
```

`Literal` 可以理解成 Java enum 的类型层约束，但 Python 运行时仍要主动校验（`__post_init__` 里逐字段检查）。每个决定还必须携带原因和来源：

```python
decision = PermissionDecision(
    "ask",
    "第三章的文件写入需要明确审批",
    "confirm-file-write",
)
```

`PermissionDecision` 是 `frozen=True, slots=True` 的 dataclass，类似不可变 Java record。三个字段各有明确的消费者，缺一个就有人看不懂：

| 字段 | 谁在用 | 用来做什么 |
| --- | --- | --- |
| `behavior` | Agent Loop | 决定执行、拒绝还是弹审批 |
| `reason` | 用户 + 模型 + 审计日志 | 审批框里显示的「原因」；被拒时回填给模型的文本 |
| `source` | 你（排查时） | 回答「到底是谁拒的」——硬边界？某条规则？还是用户？ |

`source` 的价值在出问题时才显现。设想审批框弹出来了，但你觉得它不该弹——只看 `behavior: "ask"` 你无从下手，看到 `source: "confirm-file-write"` 就知道该去改哪条规则。同理，看到 `source: "workspace-boundary"` 说明是路径问题，改规则是没用的。

`PermissionDecision` 在构造时拒绝非法 behavior、空原因和空来源，并冻结实例。最终拒绝可以直接转换成稳定工具结果：

```python
def to_tool_result(self) -> ToolResult:
    if self.behavior != "deny":
        raise PermissionContractError("只有最终 deny 决策才能转换成工具结果")
    return tool_error("permission_denied", self.reason)
```

因此模型看到的不是含混的「权限拒绝」，而是：

```text
工具执行错误 [permission_denied]: 禁止写入工作区之外
```

这里那个 `raise` 值得多看一眼：`to_tool_result()` **只允许 `deny` 调用**。为什么要限制？

因为 `ask` 和 `passthrough` 都是**中间状态**，它们出现在最终结果里一定意味着有 bug。如果允许 `ask` 转成工具结果，模型就会收到一条「需要审批」的消息——可审批环节根本没跑过，谁都不知道该不该执行。宁可让程序在这里崩掉暴露问题，也不要产生一条语义不明的历史。这是「不隐藏错误」的一个具体应用：**契约违反要立刻失败，不要向下游传递一个说不清的值。**

### ToolResult 与工具错误码体系

每一条工具调用的返回值不是普通字符串，而是带语义的结构化契约：

```python
@dataclass(frozen=True, slots=True)
class ToolResult:
    content: str
    is_error: bool
    error_code: str | None = None
```

`is_error` 让 Agent Loop 不需要解析文本就知道这次调用是否失败。`error_code` 是稳定错误键，例如 `permission_denied`、`unknown_tool`、`invalid_json`、`invalid_arguments`、`shell_timeout` 和 `path_escape`。

模型看到可读中文错误，测试和上游逻辑读取结构化字段。

工具 handler 的返回值也被视为不可信边界。`ToolRegistry.invoke()` 会检查 handler 是否真的返回了 `ToolResult`（还校验 `is_error` 和 `error_code` 的配对一致性），防止扩展工具用畸形对象污染会话历史。

因此 `PermissionDecision.to_tool_result()` 能稳定回填给模型：它通过 `tool_error("permission_denied", self.reason)` 生成一个满足 `ToolResult` 契约的对象，再让 Agent Loop 继续完成与原始调用 ID 配对的回填消息。

---

## PermissionRequest 只接收准备完成的调用

权限规则不应该重新解析模型参数，也不应该处理未知工具。

`PermissionRequest` 只接受已经通过 `ToolRegistry.prepare()` 的调用：

```python
if (
    self.prepared.error is not None
    or self.prepared.definition is None
    or self.prepared.arguments is None
):
    raise PermissionContractError("权限请求必须包含准备完成的工具调用")
```

此时运行时已经知道：

- 工具定义确实存在；
- arguments 是合法 JSON object；
- validator 已通过；
- 工具的 `effect` 和 handler 来自受信任的本地 `ToolDefinition`；
- `ToolContext` 已带入 workspace 和 identity。

非法 JSON、额外字段或错误类型在权限系统之前就失败。这一点很重要：权限层只回答「一个合法调用能否执行」，不重复工具注册表的职责。

`recommendations` 是不可变的结构化建议 tuple。第 3 章暂时不产生它；第 4 章的 PreToolUse Hook 会使用这条通道。建议仍然参与统一优先级，不能绕过系统拒绝。另一个字段 `proposed_decision` 用于把 `ask` 决策传给审批器（构造时校验它必须是 ask 行为），让审批器知道自己在确认什么。

### ToolRegistry 内部流程

权限层看到的 `prepared` 来自 `ToolRegistry.prepare()`。注册表在运行前已经完成三件事：查找工具名、`json.loads` 解析参数、调用工具自己的 validator。错误转换成带稳定错误码的 `ToolResult`，不会进入权限策略。

每轮 Agent Loop 使用 `snapshot()` 拿不可变注册表快照，防止模型请求与执行之间注册表被篡改。`openai_tools()` 只把 name、description 和 JSON Schema 暴露给模型；handler、effect 和 validator 不会进入模型可见的工具定义。

权限拒绝不会漏掉消息配对：`ToolRegistry.invoke()` 还会校验 handler 返回值，畸形结果被转换成 `invalid_tool_result`。

---

## PermissionRule：条件、行为、原因放在一起

一条规则包含四个字段：

```python
PermissionRule(
    name="confirm-file-write",
    behavior="ask",
    reason="第三章的文件写入需要明确审批",
    matches=lambda request: (
        request.prepared.definition is not None
        and request.prepared.definition.name in {"write_file", "edit_file"}
    ),
)
```

`matches` 是 Python `Callable`，Java 可以理解成 `Predicate<PermissionRequest>`。四项各自的用途：

| 字段 | 用途 | 会出现在哪里 |
| --- | --- | --- |
| `name` | 规则的稳定标识 | 决定的 `source`、审计日志 |
| `behavior` | 匹配后提出什么 | 决定的 `behavior` |
| `reason` | 为什么 | 审批框的「原因」行、审计日志、被拒时给模型的文本 |
| `matches` | 什么时候适用 | 只在策略内部调用 |

把这四项放在同一个 dataclass 里是有意的。**如果 `reason` 和判断条件分开写（比如条件在规则里、文案在 UI 层），两者迟早会不一致**——改了条件忘了改文案，用户看到的解释就是错的。规则匹配时，`evaluate()` 自动生成以规则名为 source 的 `PermissionDecision`；不匹配时返回 `None`，也就是不参与本次合并。

本章只装了这一条规则。规则数量少不代表机制简单——机制的价值在于**新增一条规则不需要动 `PermissionPolicy`、不需要动 Agent Loop**，和第 2 章「加工具只需要加一行」是同一个思路。

规则 matcher 如果抛出异常，策略不会继续假设它允许：

```python
try:
    decision = rule.evaluate(request)
except Exception:
    decision = PermissionDecision(
        "deny", f"权限规则执行失败: {rule.name}", rule.name
    )
```

这是 fail closed：权限依赖失败时拒绝，不把「无法判断」解释成「可以执行」。

OWASP AI Agent Security Cheat Sheet 同样把「权限评估失败时默认拒绝」列为工具安全基础原则（https://cheatsheetseries.owasp.org/cheatsheets/AI_Agent_Security_Cheat_Sheet.html）。P03 的规则异常、审批异常和审计异常都按同一原则处理，而不是让策略故障降级成放行。

### PermissionContractError：权限契约错误

本章多处抛出 `PermissionContractError`：构造 `PermissionDecision` 时传入了非法 behavior、创建 `PermissionRequest` 时传入了未准备好的调用、`to_tool_result()` 在非 deny 决定上调用，等等。

它的设计含义是：权限系统的调用方违反了使用契约，而非运行时的外部故障。Agent Loop 的权限边界会把它捕获，并统一转换成 `permission_evaluation_error` 回填给模型，不会向上传播到 CLI 层。

它的价值在语义层：它专门表示权限系统的调用方违反了使用契约。单元测试可以据此把「调用方用错 API」和「权限评估故障」区分开（`test_contract_rejects_invalid_requests` 同时验证了这两类用法）。

---

## 三道闸门如何合并

这一节是本章的核心。前面所有概念在这里汇合。

一次权限判断有三层输入。

第一层是系统硬边界：工作区外写入直接 `deny`。

第二层是结构化参与方：Shell 默认策略、规则，以及后续章节的 Hook 建议。

第三层是显式审批：只有合并结果为 `ask` 时才调用。

### 先看数据是怎么流的

```text
                    ┌─────────────────────────────────┐
                    │ candidates: list[PermissionDecision] │
                    └─────────────────────────────────┘
                                  ↑
    ① 工作区硬边界 ────────────────┤  effect=write 且路径越界 → append deny
    ② Shell 默认 ─────────────────┤  effect=execute → append ask
    ③ recommendations ───────────┤  第 4 章 Hook 建议（本章为空）
    ④ 规则逐条 evaluate ──────────┘  匹配则 append；不匹配返回 None，不 append
                                  ↓
                    _strongest(candidates)
                      找第一个 deny → 有就返回
                      再找第一个 ask → 有就返回
                      再找第一个 allow → 有就返回
                      都没有 → passthrough
                                  ↓
                 ┌────────────────┼────────────────┐
              ask │            deny/allow │   passthrough │
                 ↓                ↓                ↓
        调用 ApprovalProvider   原样作为最终结论   转成 allow(default)
        只接受 allow/deny
                 └────────────────┼────────────────┘
                                  ↓
                        audit.record(最终结论)
                                  ↓
                     is_allowed ? invoke() : to_tool_result()
```

注意 ①–④ 是**全部都跑**的，不是「命中一个就短路」。四层各自产出候选，最后统一比较。这样审计里能看到完整的冲突情况，而不是「碰巧第一个匹配的规则说了什么」。

### 合并优先级

合并优先级固定为：

```text
deny > ask > allow > passthrough
```

用一句话记住它：**越保守的越赢。** 再具体一点：

| 候选集合 | 最终 | 为什么 |
| --- | --- | --- |
| `[]` | `allow`（default） | 没人反对，默认放行读操作 |
| `[allow]` | `allow` | 唯一意见 |
| `[ask]` | 走审批 | 需要一次人工确认 |
| `[allow, ask]` | 走审批 | **`ask` 赢**。一条宽松规则不能取消审批 |
| `[deny, ask]` | `deny` | **`deny` 赢**。连问都不问（调用 C 就是这种） |
| `[deny, allow]` | `deny` | `deny` 赢。这是本章最重要的不变量 |

反过来说，这套优先级**保证了一件事**：只要有任何一个参与方说 `deny`，无论后面加多少条 `allow` 规则、无论 Hook 怎么建议、无论用户敲多少次 `y`，结果都是拒绝。第 4 章加 Hook、第 19 章接 MCP 时，这条保证一个字都不会改。

核心代码会按这个顺序寻找第一个决定：

```python
def _strongest(decisions: Sequence[PermissionDecision]) -> PermissionDecision:
    for behavior in ("deny", "ask", "allow"):
        for decision in decisions:
            if decision.behavior == behavior:
                return decision
    return PermissionDecision("passthrough", "没有权限参与方阻止请求", "default")
```

只有 `passthrough` 或完全没有参与方时，最终才使用默认允许：

```python
final = PermissionDecision("allow", "没有权限规则阻止请求", "default")
```

这不是「所有操作默认安全」。`shell` 会自动产生 `ask`；P03 中带路径的 write 会先经过工作区硬边界，两个文件写工具还有显式 `ask` 规则。默认 `allow` 只落在没有规则反对、也没有更高优先级决定的读取操作上。

Claude Code 官方权限文档使用同样的评估顺序：deny、ask、allow 依次判断，规则的具体程度不会改变顺序；任何层级上的 deny 都不能被其他层级的 allow 覆盖（https://code.claude.com/docs/en/permissions）。

---

## 工作区边界为什么必须在规则之前

考虑下面的调用：

```json
{"path":"../outside.txt","content":"x"}
```

项目规则可能说允许写入，未来 Hook 也可能建议允许，用户甚至可能输入 `y`。但工作区外写入仍必须拒绝，而且不能先弹审批框。

「不能先弹审批框」这一条容易被当成体验优化，其实是安全要求。想象一下如果先弹框：

```text
工具调用需要批准: write_file
原因: 第三章的文件写入需要明确审批
参数: {'path': '../outside.txt', 'content': 'x'}
允许本次调用? [y/N]
```

用户在赶时间、连着敲了几个 `y`，这一个也敲下去了。系统随后还是拒绝——**那这个框存在的意义是什么？** 它只教会用户「批准了也可能不执行」，从而降低对后面每一个审批框的重视程度。审批框应该只在「你的回答真的能决定结果」时出现。

`PermissionPolicy` 因此先询问受信任的 `WorkspaceWriteBoundary`：

```python
allowed = self._write_boundary.is_path_within_workspace(
    request.context.workspace, raw_path
)
if not allowed:
    return PermissionDecision("deny", "禁止写入工作区之外", "workspace-boundary")
```

core 只依赖 `Protocol`，不导入具体文件适配器。组合根注入的 `LocalWorkspaceFileSystem` 实现真实路径判断——`WorkspaceFileSystem` 本身就继承了 `WorkspaceWriteBoundary` 这个窄接口，`is_path_within_workspace` 内部调用第 2 章的 `safe_path()`：拒绝绝对路径、`..`、Windows 保留名、非法字符，以及 junction 或符号链接逃逸。

所以这条边界继承第 2 章的全部路径保证。

P03 的组合根总会注入 write boundary。如果真实路径解析抛出异常，带 `path` 的 write 会得到：

```text
禁止写入工作区之外/无法安全解析写入路径
```

不会降级成允许。P01、P02 不安装权限策略，文件路径继续由第 2 章的 handler 边界校验；这样既保留旧错误语义（`path_escape`），也不会削弱 P03。

顺带一提一个细节：如果 write 调用的 `path` 参数根本不是字符串（validator 都放过了的畸形输入），边界返回 `deny`（`写入路径无效`）而不是崩溃——权限层的输入也不能全信。

---

## Shell 为什么一律 ask

不能用关键词黑名单判断 PowerShell 是否安全。

`Get-Content` 看起来只读，但参数可以指向工作区外；一个普通命令还可以调用脚本、启动子进程或访问网络。只匹配 `Remove-Item`、`Format-Volume` 等词既不完整，也容易绕过。

因此，只要工具 effect 是 `execute`，系统就产生默认 `ask`：

```python
if definition.effect == "execute":
    candidates.append(
        PermissionDecision("ask", "执行 PowerShell 命令需要审批", "shell-default")
    )
```

P01、P02 和 P03 的真实 CLI 都保留这条行为。离线测试可以显式注入审批、审计和文件系统替身，不依赖真实终端。

---

## ask 必须得到显式 allow

`ApprovalProvider` 是可注入边界，类似 Java interface：

```python
class ApprovalProvider(Protocol):
    def decide(self, request: PermissionRequest) -> PermissionDecision: ...
```

只有一个真正的 `PermissionDecision("allow", ...)` 能通过。

以下情况都变成 `deny`：

| 情况 | 最终 source | 为什么不能放行 |
| --- | --- | --- |
| 没有审批器 | `approval` | 没人可问，就等于没批准 |
| 审批器抛出异常 | `approval` | 故障不是同意 |
| 审批器返回普通 object 或 boolean | `approval` | 类型不对说明实现有问题，不猜它的意思 |
| 审批器再次返回 `ask` | `approval` | 没有收敛，等于没回答 |
| 审批器返回 `passthrough` | `approval` | 弃权不是同意 |
| 终端没有交互 stdin | `terminal-approval` | CI / 管道环境里没有真人，不能默认放行 |
| 用户直接回车或输入其他内容 | `terminal-approval` | 提示是 `[y/N]`，默认值是 N |

七种情况有一个共同点：**它们都是「没有得到明确的 yes」，而不是「得到了明确的 no」。** 系统把这两者视为等价——这就是 fail closed。

把它反过来想更清楚：如果任何一种情况被当成放行，那么攻击者（或者只是一个写错的自动化脚本）只需要让审批器出故障，就能拿到执行权限。**安全机制的失效必须导向更严的结果，不能导向更松的结果。**

代码里这个收敛过程分两层。前五种在 `PermissionPolicy._resolve_approval()` 里：

```python
try:
    decision = self._approval.decide(approval_request)
except Exception:
    return PermissionDecision("deny", "审批器发生异常，已拒绝请求", "approval")
if not isinstance(decision, PermissionDecision):
    return PermissionDecision("deny", "审批器返回了无效决定", "approval")
if decision.behavior in {"allow", "deny"}:
    return decision
return _approval_denial(proposed)   # "审批没有明确同意: ..."
```

注意 `decision` 走的是 `isinstance` 检查而不是类型标注。这是刻意的：审批器是外部实现，**不能因为接口声明了返回类型就相信它真的返回了那个类型**（Python 的类型标注在运行时不强制）。`isinstance` 检查才是真正的边界。

后两种在 CLI 的终端实现里。真实终端实现只接受 `y` 和 `yes`：

```python
answer = input("允许本次调用? [y/N] ").strip().lower()
allowed = answer in {"y", "yes"}
return PermissionDecision(
    "allow" if allowed else "deny",
    "用户批准了本次工具调用" if allowed else "用户拒绝了本次工具调用",
    "terminal-approval",
)
```

审批只授权当前一次调用。策略不保存「永远允许」，也不偷偷修改后续规则。

这一点在真实使用时会让人觉得烦：同一个任务里改三个文件，就要敲三次 `y`。这是本章刻意的取舍——「永久允许」需要回答一串新问题（记在哪？范围多大？什么时候失效？谁能撤销？），而这些问题一个都不在 P03 的验收契约里。教学阶段选择最严格的默认值，比选择一个半成品的记忆机制更清晰。

---

## 审计记录最终决定

审计发生在规则合并和审批完成之后，所以记录的是最终结果：

```python
if self._audit is not None:
    self._audit.record(request, final)
```

P03 的终端审计格式为：

```text
[权限审计] write_file: allow (terminal-approval) - 用户批准了本次工具调用
```

它包含工具名、最终行为、决定来源和原因。

一个容易被忽略的细节：**审计写的是 stderr，最终答案写的是 stdout。** 这样 `... > answer.txt` 只会拿到模型的回答，审计日志留在屏幕上；反过来做日志收集时也不会把答案混进去。

审计位置的选择同样是有意的。它在**合并和审批之后、执行之前**：

```text
❌ 放在合并之后、审批之前
   → 记下的是 ask，你永远不知道用户最后批没批

❌ 放在 handler 执行之后
   → 执行失败就没记录了；而「尝试执行过」本身就是要留痕的事

✅ 放在最终结论确定之后、invoke 之前
   → 每一个决定都有记录，且记录一定先于副作用
```

如果审计器本身失败，运行时不能执行一个未留下要求记录的副作用。异常会被 Agent Loop 转换成：

```text
工具执行错误 [permission_evaluation_error]: 权限评估失败
```

handler 调用次数保持为零，消息仍然配对。`test_audit_failure_returns_paired_error_and_skips_handler` 就在逐字断言这条错误文案，同时断言 `note.txt` 根本没被创建。

### 消息契约：每个工具调用必须配对

权限决定和 handler 执行完成后，Agent Loop 使用 `tool_message(result.content, call.id)` 回填。回填不是可选的：每个 assistant 工具调用必须有且只有一个相同 ID 的 tool 结果。

`validate_tool_pairing()` 在每一轮开始时检查消息历史：工具调用后必须跟随对应数量的 tool 消息，ID 必须严格匹配，多余的 tool 消息和孤儿结果都会被拒绝。这保证发送给模型的会话始终满足 Chat Completions 协议，也保证被权限拒绝的调用仍然以 `permission_denied` 结果回到模型视野，模型可以看到它提出的调用确实被拒绝了。

---

## 插入 Agent Loop

第 2 章已经把工具准备和执行分开。第 3 章只在中间增加权限步骤：

```python
if prepared.error is not None:
    result = prepared.error
elif self._permission_policy is not None:
    try:
        permission_decision = self._permission_policy.decide(PermissionRequest(prepared, context))
        result = snapshot.invoke(prepared, context) if permission_decision.is_allowed else permission_decision.to_tool_result()
    except Exception:
        # 权限评估或审计失败时也必须生成配对 tool 消息，且不能执行 handler。
        result = tool_error("permission_evaluation_error", "权限评估失败")
elif self._authorizer is not None:
    ...  # 第 1-2 章的旧授权器路径，P01/P02 继续走这里
else:
    result = snapshot.invoke(prepared, context)
self._history.append(tool_message(result.content, call.id))
```

顺序不能反过来：

```text
prepare → permission → handler
```

如果先执行 handler 再判断，审批就只剩日志价值，无法阻止副作用。

### 和第 2 章的循环比，到底改了什么

这是本章最值得欣慰的部分——**改动小得出奇**：

| | 第 2 章 | 第 3 章 |
| --- | --- | --- |
| 轮次循环、`max_turns`、终止判断 | — | **一行未改** |
| `validate_tool_pairing()` 调用位置 | — | **一行未改** |
| `snapshot()` / `prepare()` / `invoke()` | — | **一行未改** |
| 工具回填 `tool_message(result.content, call.id)` | — | **一行未改** |
| 工具 handler 实现 | — | **一行未改** |
| 中间那段 `if / elif / else` | 只有 `prepared.error`、旧授权器和直接 `invoke` | 中间插入一个 `permission_policy` 分支 |

只加了一个分支，能力边界就完全变了。这正是第 2 章把 `prepare()` 和 `invoke()` 拆开的回报——**当时那个拆分看起来只是「代码整洁」，现在才看出它是为了留出这个插入点。**

再看那个 `except`：

```python
except Exception:
    result = tool_error("permission_evaluation_error", "权限评估失败")
```

这是一个**边界 except**，不是业务逻辑里的 catch。它抓的是权限系统自身的故障（规则实现有 bug、审计器写盘失败、`PermissionContractError`），并把它转成一个配对完整的工具结果。

它必须存在的理由是消息契约：如果异常向上抛，这一轮的 `tool` 消息就少了一条，`validate_tool_pairing()` 下一轮开始时会直接抛「以下工具调用缺少返回结果: [...]」——整个会话就废了。**「权限系统出故障」和「会话结构损坏」不该是同一种严重程度。**

同时注意：这个 except 是 fail closed 的。异常时 `result` 是错误结果，`invoke()` 那一支根本没走到，handler 调用次数保持为零。

P03 的组合根要求显式提供 `approval_provider` 和 `audit_sink`，并由固定 profile 自己组装规则与工作区边界。调用方不能换成一个宽松的 `PermissionPolicy` 来跳过「写入必审批、决定必审计」。缺少任一边界时，Agent 在启动构建阶段失败（`test_p03_requires_approval_and_audit` 断言了这两条错误）。P01、P02 的离线单元测试仍可不注入审批器；真实 CLI 会注入审批器，让 Shell 保持 `ask`。

### Agent Loop 的三种终止状态

Agent Runner 不是无限循环。它有三种可交付终止状态：

- `RunResult`：模型在 `max_turns` 内返回非空 `final_text`，`turns` 记录实际轮数；
- `AgentLimitError`：达到 `max_turns` 仍未结束，调用方不能把中间历史当作最终答案；
- `AgentRunError` / `IncompleteModelReplyError`：模型输出被截断、被内容过滤，或者停止时既没有文本也没有工具调用。

权限错误不是这三类运行错误之一：权限拒绝会作为 tool result 回到模型，权限评估异常会作为 `permission_evaluation_error` 回到模型，Loop 仍可继续。

---

## P02 与 P03 的准确差异

P03 只增加一个 capability：

```python
P03 = ChapterProfile(
    3,
    frozenset({"loop", "powershell", "tool_registry", "files", "policy"}),
)
```

两个 profile 使用相同的五工具注册表和同一个 Agent Loop。组合根根据固定 profile 构建策略，而不是接收调用方拼好的策略：

```python
if profile is P03:
    if approval_provider is None:
        raise ValueError("第三章必须提供 approval_provider")
    if audit_sink is None:
        raise ValueError("第三章必须提供 audit_sink")
    policy = PermissionPolicy(...)
```

因此：

| 操作 | P02 | P03 |
| --- | --- | --- |
| `read_file` / `glob` | 直接允许 | 直接允许并审计 |
| 工作区内 `write_file` / `edit_file` | 直接允许 | `ask`，最终决定审计 |
| 工作区外 write | 文件 handler 返回 `path_escape` | 权限硬边界拒绝并审计 |
| `shell` | 旧授权器 `ask` | 权限策略 `ask` 并审计 |

「直接允许」仍然要先经过文件工具自己的 validator 和 `safe_path`。P02 仍会拒绝工作区外路径，只是保留第 2 章可观察到的 `path_escape`；P03 才把这类写入提前到 handler 之前，以 `permission_denied` 拒绝。

### 能力集为什么不能是可变集合

Python 使用 `frozenset` 保存能力集合。普通 `set` 可以调用 `add()` 和 `remove()`，如果 profile 构造后还能被修改，组合根就不能信任 `"policy" in capabilities` 的结果。

`frozenset` 没有写入 API，类似 Java `Set.copyOf()` 或不可变集合。组合根还使用 `profile is P03` 的对象身份检查，拒绝调用方伪造一个字段相同的 `ChapterProfile`——和第 2 章 P02 的做法一脉相承。

---

## 四个具体场景

### 1. 列出文件

模型调用：

```json
{"pattern":"agent_ch03/**/*.py"}
```

`glob` 是 read，没有规则反对时默认允许，最终 allow 决定会进入审计。

### 2. 执行 PowerShell

模型调用：

```json
{"command":"Get-ChildItem -Force"}
```

即使命令看起来只读，`shell` 仍进入审批：

```text
工具调用需要批准: shell
原因: 执行 PowerShell 命令需要审批
参数: {'command': 'Get-ChildItem -Force'}
允许本次调用? [y/N]
```

### 3. 修改工作区文件

P03 中的 `write_file` 和 `edit_file` 命中 `confirm-file-write`。输入 `y` 后只执行这一次；拒绝或回车时，文件不变化。

### 4. 写入工作区外

```json
{"path":"../outside.txt","content":"x"}
```

系统返回工作区硬拒绝，不调用审批器，也不创建外部文件。

---

## 工具实现：effect 如何驱动权限边界

`features/builtin_tools.py` 是 P02、P03 共用的五工具实现；P01 只注册其中的 `shell`。五个工具都在注册时携带 `effect` 标签。

这个标签不发给模型。它是权限策略判断默认行为的语义依据。

| 工具 | effect | 权限路径 | 主要稳定错误码 |
| --- | --- | --- | --- |
| `shell` | `execute` | Shell 默认 `ask` | `shell_start_failed`、`shell_timeout`、`shell_failed` |
| `read_file` | `read` | 默认 `allow` | `path_escape`、`invalid_utf8`、`file_not_found`、`invalid_path` |
| `write_file` | `write` | 工作区硬边界，再走写入审批 | `path_escape`、`invalid_path`、`filesystem_error` |
| `edit_file` | `write` | 工作区硬边界，再走写入审批 | `text_not_found`、`invalid_utf8`、`file_not_found` |
| `glob` | `read` | 默认 `allow` | `path_escape`、`filesystem_error` |

`write_file` 和 `edit_file` 标记为 `write` 后，`PermissionPolicy` 会在规则之前读取 `path` 并调用 `WorkspaceWriteBoundary`。只有工作区内路径才会继续进入人工审批。

`read_file` 和 `glob` 是 `read`，没有规则反对时由默认策略放行，但仍会经过 `safe_path` 与错误归一。

工具 handler 对已知领域错误返回稳定错误码，对未知错误继续抛出。`ToolRegistry.invoke()` 会把未知故障统一转换为 `tool_execution_error`，模型收到的是可继续规划的失败原因，而不是运行时堆栈。

---

## 当前边界与刻意不做的事

本章没有实现配置文件分层、组织角色、规则热更新或「永久允许」。这些能力没有出现在 P03 验收契约中。

真实产品可能用 Managed、Project、User 多级设置承载权限规则，但任何层级的 deny 都不能被其他层级的 allow 覆盖。P03 不实现完整分层，但保留最关键的 deny 优先语义。

本章也不调用另一个模型判断命令风险。概率分类器可以辅助提示，不能替代硬权限语义。

Shell 仍然不是 sandbox。审批降低误执行风险，不提供操作系统隔离。真正的强隔离需要受限账户、容器、虚拟机或操作系统沙箱。

P03 的 deny 都在执行前生效，没有实现「从模型可见工具列表中移除」的优化；对本章验收目标而言，阻止副作用已经成立。

第 4 章才加入 Hook。`recommendations` 已预留结构化通道，但 P03 不假装已经有 Hook 生命周期。未来 Hook 即使建议 allow，也不能绕过系统 deny。

### 组合根：权限策略在哪里被拼装

`bootstrap.py` 是组合根。它把模型、文件系统、Shell、工具、权限策略组装成一个 `AgentRunner`。Java 可以把它理解为 Spring `@Configuration`。

关键约束是：组合根不接收调用方拼好的 `PermissionPolicy`，而是根据固定 `ChapterProfile` 自己构造策略。P03 缺少审批器或审计器时，`build_agent()` 直接抛错，而不是带着宽松策略启动。

CLI 在 `cli.py` 实现 `TerminalApprovalProvider` 和 `TerminalAuditSink`。审批器先检查 `sys.stdin.isatty()`：没有交互输入时直接拒绝，避免自动化环境默认放行。配置错误退出码 2，运行错误退出码 1。

### 从 ai-agent-book 学到什么

生产级安全是层次化防护：

```text
输入验证
    ↓
权限控制
    ↓
提议者-审核者（事前审批 + 事后验证）
    ↓
Sidecar 并行校验
    ↓
沙盒隔离
```

这个设计方式值得借鉴：每一项机制都要有可观察的输入、行为和输出，而不是只停留在架构描述。

本教程 P03 覆盖了最前面两层（输入验证 + 权限控制），并实现了其中的拒绝优先和 fail closed。更完整的生产系统还需要：

1. **风险分级而不是一刀切**：低风险、可逆操作可以自动放行；高风险、不可逆操作才要求人工审批。P03 保持 Shell 一律 `ask`，是教学上更严格的默认值，真实产品需要按风险等级分层。
2. **提议者-审核者**：独立模型或人工在操作前审批「计划」，在操作后验证「结果」。P03 只做人工单次调用审批，没有独立模型审查。
3. **执行-验证-反馈**：工具执行后自动运行验证（例如写代码后自动跑 lint），把结构化错误返回给 Agent。P03 没有自动验证器，权限系统只管「执行前」。
4. **Sidecar**：轻量安全检查与主模型流式输出并行，避免提示注入通过自由文本影响判断。P03 是同步决策，没有并行 Sidecar。
5. **长输出截断与持久化**：超长输出只保留头尾，完整结果写入临时文件并引导 `read_file` 继续读取。P03 的 PowerShell 适配器已经限制输出上限，但还没有「完整结果落盘 + 文件引导」。
6. **可观测性**：每次调用的时间、参数、结果、耗时和告警都要可追踪。P03 的审计覆盖了最终权限决定，但还没有性能指标和告警。
7. **幂等性与取消语义**：超时或取消后必须知道副作用是否发生。P03 还没有实现幂等去重。
8. **沙盒隔离**：审批不是隔离。ai-agent-book 明确把沙盒作为独立防线，P03 也明确声明 Shell 不是 sandbox。

### 配套代码的文件结构

`python/ch03_agent/agent_ch03/` 沿用「core 契约 + adapters 边界 + features 工具集」的分层。逐文件责任如下：

| 路径 | 责任 |
| --- | --- |
| `bootstrap.py` | 按 profile 组装模型、文件系统、工具和权限策略 |
| `cli.py` | 终端审批、审计和错误退出码 |
| `config.py` | 共享 `.env` 解析、必需字段校验 |
| `core/commands.py` | `CommandRunner` 与 `CommandResult` 命令契约 |
| `core/filesystem.py` | 文件接口、写路径窄接口和稳定领域错误 |
| `core/loop.py` | prepare、permission、invoke、回填的主循环 |
| `core/messages.py` | assistant 工具调用与 tool 结果严格配对 |
| `core/model.py` | `ModelClient`、`FinishReason`、`ModelReply` 模型契约 |
| `core/permissions.py` | 四态决定、规则、审批、审计和工作区硬边界 |
| `core/profiles.py` | 固定 P01/P02/P03 能力快照 |
| `core/tools.py` | 工具注册、解析、校验、快照和调用 |
| `features/builtin_tools.py` | 五个工具实现与 `effect` 标签 |
| `adapters/filesystem.py` | `safe_path` 和真实路径边界 |
| `adapters/openai_chat.py` | OpenAI/DeepSeek 模型适配器 |
| `adapters/powershell.py` | PowerShell 进程超时与输出限制 |

`adapters/filesystem.py` 把 `OSError`（按 errno）归一为 `FileNotFoundError`、`InvalidFilePathError` 等稳定领域错误，工具层再映射成 `file_not_found`、`invalid_path` 等错误码。这些边界都是权限系统之外的独立防线。

---

## 运行第 3 章

### 第 0 步：准备环境

继续使用共享环境，安装本章包：

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch03_agent'
& ..\.venv\Scripts\python.exe -m pip install -e ".[dev]"
```

`python/.env` 的三个变量和第 1 章一样。没配过的回第 1 章「运行第 1 章智能体」。

### 第 1 步：先跑离线测试（不需要 API Key）

```powershell
& ..\.venv\Scripts\python.exe -m pytest tests
```

预期最后一行：

```text
24 passed in 2.9s
```

这 8 个测试文件、24 个测试全部离线——本章新增的是 `test_permissions.py`（5 个）和 `test_ch03_integration.py`（3 个）。不需要 API Key，不发网络请求。

### 第 2 步：跑一次会弹审批的真实任务

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch03.cli --prompt "读取 README.md，然后把一句摘要写入 chapter3-note.txt"
```

这条 prompt 是专门挑的：它先 `read_file`（默认放行）再 `write_file`（必须审批），一次跑完就能看到两条路径。

预期你会依次看到：

```text
[权限审计] read_file: allow (default) - 没有权限规则阻止请求

工具调用需要批准: write_file
原因: 第三章的文件写入需要明确审批
参数: {'path': 'chapter3-note.txt', 'content': '...'}
允许本次调用? [y/N]
```

敲 `y` 回车，然后会看到：

```text
[权限审计] write_file: allow (terminal-approval) - 用户批准了本次工具调用
```

确认文件真的写了：

```powershell
Get-Content .\chapter3-note.txt
```

### 第 3 步：这次故意拒绝

再跑一次同样的命令，在审批框那里**直接回车**（不输入任何东西）。

预期：

```text
[权限审计] write_file: deny (terminal-approval) - 用户拒绝了本次工具调用
```

然后模型会收到 `工具执行错误 [permission_denied]: 用户拒绝了本次工具调用`，通常它会在最终回答里告诉你写入被拒绝了。**文件内容不会有任何变化。**

这一步是本章最值得亲手做的验证：`[y/N]` 里大写的 `N` 是默认值，回车等于拒绝。

### 第 4 步：看硬边界拦下一次越界写入

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch03.cli --prompt "把 hello 写入 ../outside.txt"
```

预期：

```text
[权限审计] write_file: deny (workspace-boundary) - 禁止写入工作区之外
```

**注意这次没有弹审批框**——你根本没有机会批准它。`python/ch03_agent` 的上一级目录里不会出现 `outside.txt`。

### 第 5 步（可选）：完整离线门禁

```powershell
& ..\.venv\Scripts\python.exe -m compileall -q agent_ch03
& ..\.venv\Scripts\python.exe -m ruff check agent_ch03 tests
& ..\.venv\Scripts\python.exe -m mypy agent_ch03
```

测试不读取真实 API Key，也不访问网络。真实模型运行只能作为额外冒烟证据。

### 常见报错排查

| 现象 | 原因 | 怎么处理 |
| --- | --- | --- |
| 审批框一闪而过，直接看到 `无交互输入，默认拒绝。` | 当前不是交互终端（在 CI、管道或某些 IDE 的输出面板里） | 在真实 PowerShell 窗口里跑。这是 fail closed 的正确行为，不是 bug |
| 输入 `Y`（大写）能过吗 | 能。代码做了 `.strip().lower()` | — |
| 输入 `yes` 能过吗 | 能。只接受 `y` 和 `yes` 两种 | 输入 `ok`、`sure`、`是` 都会被判为拒绝 |
| 每写一个文件都要批一次 | 本章刻意不做「永久允许」 | 正常行为。见「ask 必须得到显式 allow」末尾 |
| `read_file` 也进审计了，看着很吵 | 审计记录的是**每一个**最终决定 | 正常行为。审计输出在 stderr，`2>$null` 可以临时屏蔽 |
| `工具执行错误 [permission_evaluation_error]: 权限评估失败` | 权限系统自身故障（规则实现有 bug、审计器写失败） | 这是 fail closed 的兜底。handler 没有执行；去查你新加的规则或审计实现 |
| 启动就报 `第三章必须提供 approval_provider` | 你自己调 `build_agent()` 时没注入审批器 | 补上审批器和审计器。P03 不允许无边界启动 |
| 明明是只读命令，`shell` 还是要批准 | `effect == "execute"` 一律 `ask`，不看命令内容 | 正常行为。见「Shell 为什么一律 ask」 |
| `ModuleNotFoundError: No module named 'agent_ch03'` | 没执行本章的 `pip install -e` | 回到第 0 步执行安装命令 |

---

## 验证与实验

`pytest tests` 会执行 8 个测试文件、24 个测试。本章新增的两个是：

| 测试文件 | 验证内容 |
| --- | --- |
| `test_permissions.py` | `PermissionDecision` 构造校验、deny 压过 allow、ask 无审批器时 fail closed、显式审批放行且 `proposed_decision` 传递正确、工作区 deny 不可被 allow 规则 + allow 审批 + allow 建议三重覆盖、shell 默认 ask 与读取默认 allow、契约错误 |
| `test_ch03_integration.py` | 端到端：P03 缺审批器/审计器时启动失败、P02 不审批而 P03 审批、审计失败时回填 `permission_evaluation_error` 且文件未被创建 |

其余 6 个是前两章的累计测试（`test_loop`、`test_ch02_tools`、`test_files`、`test_tools`、`test_messages`、`test_config`），继续保证第 3 章加权限没有改坏旧行为。

这里最值得学的是断言的写法。**只断言「返回了错误」是不够的**——一个实现可能先执行了副作用再返回错误，测试照样通过。所以测试用了三类更硬的断言：

```python
assert approval.requests == []                      # 审批器一次都没被调用
assert not (tmp_path / "note.txt").exists()        # 文件确实没被创建
validate_tool_pairing(result.history)              # 消息结构仍然完整
```

第一条证明「硬边界拒绝时不弹审批框」，第二条证明「副作用真的没发生」，第三条证明「拒绝不会破坏会话」。三条缺一不可——这就是「验证真实行为，不是看起来做完了」的具体样子。

顺带说一句：测试断言的回填内容是**逐字精确**的完整字符串，比如 `"工具执行错误 [permission_evaluation_error]: 权限评估失败"`。所以改动任何一条 `reason` 文案都会让测试变红——这是有意的，`reason` 会进入模型上下文，改了就等于改了模型看到的信息。

### 四个建议动手做的小实验

**实验一：让规则的 matcher 抛异常**

在 `bootstrap.py` 的 `confirm-file-write` 规则里，把 `matches` 改成 `lambda _request: (_ for _ in ()).throw(RuntimeError("boom"))`（或任何会抛异常的写法），然后跑一次 `write_file`。

预期：得到 `deny`，`source` 是 `confirm-file-write`，`reason` 是 `权限规则执行失败: confirm-file-write`。
学到：规则故障导向拒绝，而不是「这条规则不算」。**实验完记得改回去。**

**实验二：加一条 `allow` 规则，看它能不能推翻硬边界**

在 `bootstrap.py` 的规则 tuple 里追加一条：

```python
PermissionRule(
    "allow-everything",
    "allow",
    "测试 allow 能否覆盖 deny",
    lambda _request: True,
),
```

然后跑 `--prompt "把 hello 写入 ../outside.txt"`。

预期：仍然是 `deny (workspace-boundary)`。`allow` 无论加多少条都赢不了 `deny`。`test_workspace_denial_cannot_be_overridden` 已经用「allow 规则 + allow 审批 + allow 建议」三重包围验证过同一件事。
学到：这是本章最核心的不变量，值得亲手确认一次。**实验完记得改回去。**

**实验三：让审批器返回 `passthrough`**

写一个假审批器 `decide` 返回 `PermissionDecision("passthrough", "不表态", "fake")`（参考 `test_permissions.py` 的 `RecordingApproval` 写法）。

预期：最终 `deny`，`source` 是 `approval`，`reason` 以 `审批没有明确同意:` 开头。
学到：`ask` 只能被 `allow` 或 `deny` 收敛，弃权算拒绝。

**实验四：让审计器抛异常**

把 `TerminalAuditSink.record()` 改成直接 `raise RuntimeError("审计不可用")`，然后跑一次 `read_file`。

预期：得到 `工具执行错误 [permission_evaluation_error]: 权限评估失败`，**并且内容没有被读**（哪怕这只是个读操作，模型收到的是错误结果而不是文件内容）。
学到：审计不是「顺便记一下」，它是执行前置条件。留不下记录就不执行。**实验完记得改回去。**

---

## 本章小结

### 三句话版本

1. 权限判断必须在代码的固定位置——`prepare` 之后、`invoke` 之前，Prompt 做不到这个保证。
2. 决定不是 boolean 而是四态，因为「需要问一下」和「我不表态」都必须能表达出来。
3. `deny > ask > allow > passthrough`：任何一个参与方的拒绝，都不能被其他人的允许推翻。

### 一定要记住的七条

| # | 结论 | 出现在哪一节 |
| ---: | --- | --- |
| 1 | 顺序是 `prepare → permission → handler`，反了就只剩日志价值 | 插入 Agent Loop |
| 2 | 硬边界的 `deny` 出现时，审批框根本不弹 | 工作区边界为什么必须在规则之前 |
| 3 | 「没有得到明确的 yes」和「得到明确的 no」等价——七种情况都是 `deny` | ask 必须得到显式 allow |
| 4 | 审批器的返回值用 `isinstance` 验证，不信类型标注 | ask 必须得到显式 allow |
| 5 | 审批只授权这一次，不存在「永久允许」 | ask 必须得到显式 allow |
| 6 | 审计记录的是**最终**决定，且发生在执行之前；审计失败则不执行 | 审计记录最终决定 |
| 7 | `execute` 一律 `ask`，不按命令文本猜风险 | Shell 为什么一律 ask |

### 本章代码边界（明确「还没做什么」）

| 已经有 | 还没有 | 在哪一章补 |
| --- | --- | --- |
| 四态决定 + 优先级合并 | Hook 提出建议（`recommendations` 通道已预留） | 第 4 章 |
| 一条内置规则 | 配置文件分层（Managed / Project / User） | **不做**，见「刻意不做的事」 |
| 单次调用审批 | 永久允许、规则热更新 | **不做** |
| 审批 + 审计 | 独立模型审查、执行后自动验证 | **不做**，见 ai-agent-book 对比 |
| `shell` 一律 `ask` | 按风险分级的差异化默认值 | **不做**（教学上取更严的默认值） |
| 最终决定进审计 | 性能指标、告警、幂等去重 | 部分见第 11 章 |
| `execute` 需审批 | 操作系统级沙箱隔离 | **不做**。审批不等于隔离 |
| deny 在执行前生效 | 从模型可见工具列表中移除工具 | **不做**（阻止副作用已经足够） |

### 检查你是否真的读懂了

1. 一次 `glob` 调用，四个参与方分别产出什么候选？最终 source 是什么？（先看一眼真实的决策过程）
2. 为什么 boolean 表达不了「需要问一下」？举出规则乙那种「不表态」的问题。（权限不是 boolean）
3. 候选集合是 `[deny, ask]` 时，用户会看到审批框吗？为什么？（三道闸门 / 工作区边界）
4. 审批器抛异常，最终 behavior 是什么？如果换成「默认允许」会有什么风险？（ask 必须得到显式 allow）
5. 审计器抛异常，handler 执行了吗？模型收到什么？（审计记录最终决定）
6. 第 3 章的 Agent Loop 比第 2 章多了几处改动？（插入 Agent Loop）
7. `to_tool_result()` 为什么只允许 `deny` 调用？（四态决定）

---

这一章建立了一个不会被弱允许覆盖的副作用边界：

```text
ToolRegistry.prepare
        ↓
workspace hard boundary
        ↓
rules / recommendations
        ↓
deny > ask > allow > passthrough
        ↓
explicit approval when needed
        ↓
final audit
        ↓
ToolRegistry.invoke
```

下一章会加入 Hooks，把工具执行前后、用户输入和停止阶段的扩展逻辑从循环中拆出去。无论 Hook 给出什么建议，系统 `deny` 仍然拥有最终优先级。

---

## 小白阅读路线（Java 开发者考古顺序）

1. 先运行并阅读 `tests/test_permissions.py`；
2. 看 `PermissionDecision` 三个字段和 `to_tool_result()`；
3. 看 `PermissionRule.evaluate()`；
4. 看 `PermissionPolicy.decide()` 如何合并候选；
5. 看 `tests/test_ch03_integration.py` 的审计失败场景；
6. 最后看 `core/loop.py` 中权限策略插入的位置。

Python 与 Java 对照：dataclass 是 record/DTO，Protocol 是 interface，Callable 是函数式接口，tuple 是不可变 List，`None` 类似 null，`is` 是对象身份比较，`@property` 类似只读 getter，lambda 类似 Java Lambda 表达式。
