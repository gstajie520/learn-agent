# 12. 实战干货：5 个工具、3 个状态，带你撸出一个生产级 Agent 任务引擎（Python版）

盖房子不能先盖屋顶再打地基。对 Agent 来说，真正麻烦的不是列出“建表、写接口、加测试”，而是回答三个问题：

- 哪些任务现在可以开始？
- 谁已经认领了任务，谁有权完成它？
- 进程退出后，任务图能否原样恢复？

第五章的 `todo_write` 解决的是当前会话里的执行清单，没有依赖边，也不跨会话保存。第十二章增加的是 workspace 级 JSON Task DAG：五个任务工具、三个状态、一张加锁的持久化任务图。

后台执行、Cron、长期 teammate 和工作窃取留到后续章节。本章只把“什么可以做、谁可以做、做完以后谁被解锁”建模清楚。

---

## 配套代码入口

Python 快照位于 `python/ch12_agent`。本章新增或改动的核心文件如下：

| Python 文件 | Java/Spring 类比 | 职责 |
| --- | --- | --- |
| `agent_ch12/features/tasks.py` | domain model + command handlers | Task、异常、TaskStore interface、五个工具 |
| `agent_ch12/adapters/task_json.py` | Repository adapter | JSON 读写、整图锁、DAG 校验、原子替换 |
| `agent_ch12/core/profiles.py` | feature profile | P12 在 P11 上增加 `task_dag_json` |
| `agent_ch12/bootstrap.py` | Spring `@Configuration` | 强制注入 TaskStore，主/子 Agent 共享任务图 |
| `tests/test_tasks.py` | Repository 单测 | 持久化、错误、状态、并发行为 |
| `tests/test_ch12_integration.py` | `@SpringBootTest` | P12 工具列表和组合根契约 |

前 11 章的 shell、文件、权限、Hook、TODO、subagent、Skill、压缩、记忆、动态 Prompt 和恢复层仍然存在，但本章只把注意力放在任务 DAG 以及它如何接入既有组合根。

---

## TODO 与 Task 不是一回事

| 维度 | `todo_write` | JSON Task DAG |
| --- | --- | --- |
| 粒度 | 当前工作的步骤快照 | 可独立认领和完成的项目任务 |
| 所有者 | 单个 Agent session | 当前 workspace |
| 存储 | 进程内存 | `.agent_tutorial/.tasks/{uuid}.json` |
| 依赖 | 无 | `blocked_by` 有向边 |
| 状态 | 提醒 Agent 更新清单 | 锁内条件迁移 |
| 重建 | 新会话为空 | 从磁盘严格恢复 |

P12 仍保留 `todo_write`。模型可以用 TODO 拆解“怎样完成当前请求”，再用 Task DAG 管理“整个项目哪些任务先做”。两套状态不会自动同步，也不会互相覆盖。

第六章的 `task` 是一次性委派子 Agent，也不等于项目 Task。P12 新增的五个工具都使用完整动词名称，避免三个概念混在一起。

---

## 一个 Task 的严格模型

Python 用不可变 `dataclass` 保存领域对象：

```python
task = Task(
    id="9d59f5ec-7c94-49a7-b728-f36007fd912c",
    subject="endpoints",
    description="create API endpoints",
    status="pending",
    owner=None,
    blocked_by=(),
)
```

Java 开发者可以把它看成：

```java
record Task(
    UUID id,
    String subject,
    String description,
    TaskStatus status,
    String owner,
    List<UUID> blockedBy
) {}
```

但 Python 的 `Task.__post_init__()` 还承担构造校验：

- `id` 必须是小写 canonical UUID，文件名必须和 payload ID 完全相同。
- `subject` 去掉首尾空白后不能为空。
- `blocked_by` 中的 ID 必须是 UUID，不能重复。
- `pending` 必须满足 `owner is None`。
- `in_progress` 和 `completed` 必须有非空 owner。
- 未知状态、坏字段类型、额外 JSON 字段都明确失败。

这里没有第四个 `blocked` 状态。“阻塞”是派生事实：任务仍是 pending，并且至少一个依赖未完成。

状态机只有单向推进：

```text
pending --claim--> in_progress --complete--> completed
```

当前章节没有 update、delete、unclaim 或自动回退。进程重建后，`in_progress` 仍保留原 owner；系统不会猜测未知外部副作用能否重放。

---

## TaskStore 是窄接口

```python
class TaskStore(Protocol):
    def create_task(self, value: CreateTaskInput) -> Task: ...
    def get_task(self, task_id: str) -> Task: ...
    def list_tasks(self) -> tuple[Task, ...]: ...
    def claim_task(self, task_id: str, owner: str) -> Task: ...
    def complete_task(self, task_id: str, owner: str) -> TaskCompletion: ...
```

`Protocol` 就是 Python 对 Java `interface` 的对应形式。工具层只知道这些方法，不知道底层是 JSON 文件还是未来的 SQLite。这样 P17 替换存储实现时，不需要改工具名字和 Agent Loop。

---

## 路径属于 workspace，但不散落在根目录

所有运行数据都位于 `workspace/.agent_tutorial/`：

```text
workspace/
  .agent_tutorial/
    .tasks.lock
    .tasks/
      1b73c624-7ae6-4e78-83d5-7bcf91dd5f13.json
      9d59f5ec-7c94-49a7-b728-f36007fd912c.json
```

一个文件只保存一个 Task，字段使用稳定顺序和 UTF-8：

```json
{
  "blocked_by": ["1b73c624-7ae6-4e78-83d5-7bcf91dd5f13"],
  "description": "create API endpoints",
  "id": "9d59f5ec-7c94-49a7-b728-f36007fd912c",
  "owner": null,
  "status": "pending",
  "subject": "endpoints"
}
```

外部传入的 Task ID 先通过 `canonical_task_id()` 校验，再参与文件名拼接。`../outside`、绝对路径和 UUID 的替代拼写无法到达文件系统。

`JsonTaskStore` 每次创建或读取前都会重新解析 workspace、`.agent_tutorial` 和 `.tasks`，验证真实路径仍位于 workspace 内。目录 symlink 或 Windows junction 指向外部时，操作会失败，不会先写出任务文件。

---

## 为什么锁的是整张图

`claim_task` 的判断不仅依赖目标任务，还依赖所有 `blocked_by` 任务；`create_task` 还要验证新节点加入后全图没有缺边和环。因此不能只锁一个 JSON 文件。

Python 版使用两层保护：

1. 进程内 `threading.RLock`，让同一 Python 进程中的线程排队。
2. Windows 使用 `msvcrt.locking`，Unix 使用 `fcntl.flock`，让不同进程共享同一把 `.tasks.lock`。

每个操作都遵循同一顺序：

```python
with self._locked(paths):
    graph = self._load_graph(paths)
    # 在锁内重新读取完整 DAG，再判断 claim/create/complete。
```

关键不是锁对象叫什么，而是“锁内重新读取”。两个进程可以同时在锁外决定“我要认领 A”，但第二个进程进临界区后会看到 A 已经是 `in_progress`，因此只有一个成功。

`get_task` 和 `list_tasks` 也使用同一把锁，返回的是一致的整图快照，不会一半来自迁移前、一半来自迁移后。

---

## 原子 JSON 替换

持有锁不等于文件不会写坏。Python 版保存流程是：

```python
def _atomic_replace(path: Path, content: bytes) -> None:
    descriptor, temporary_name = tempfile.mkstemp(dir=path.parent)
    temporary = Path(temporary_name)
    try:
        with os.fdopen(descriptor, "wb") as handle:
            handle.write(content)
            handle.flush()
            os.fsync(handle.fileno())
        os.replace(temporary, path)
    finally:
        temporary.unlink(missing_ok=True)
```

临时文件和目标文件在同一个目录，写入并 `fsync()` 后才用 `os.replace()` 发布。若写入、同步或替换失败，旧 JSON 字节保持不变，临时文件被清理，调用方收到 `task_storage_error`。

这不是数据库事务。它保证单个 Task 文件不会出现半文件；整图锁保证条件判断和原子替换之间没有另一个合规 writer 插入。P17 才会把任务认领和 lease 放进 SQLite 事务。

---

## 创建时先证明它仍是 DAG

`create_task` 的输入只有三个字段：

```json
{
  "subject": "endpoints",
  "description": "create API endpoints",
  "blocked_by": ["1b73c624-7ae6-4e78-83d5-7bcf91dd5f13"]
}
```

UUID 由可注入生成器产生，不让模型指定。锁内创建按顺序验证：

1. 新 ID 没有碰撞。
2. 所有依赖已经存在。
3. Task 不依赖自己。
4. 新节点加入候选图后，DFS 检查全图没有环。
5. 全部通过后才原子写入。

缺失依赖和环都显式失败。把拼错的 ID 永远留成 pending，会造成静默故障，模型也无法知道该如何修复。

合法 API 只允许新 Task 指向已经存在的 Task，所以正常 API 很难创建环；但磁盘被手工破坏或旧版本写入坏数据时，`list_tasks()` 仍会执行整图校验并报告 `task_graph_error`，不会返回半张图。

---

## 五个工具：schema 与 handler 同源

五个工具由 `register_task_tools()` 从同一组 `ToolDefinition` 注册：

| 工具 | Effect | 输入 | 成功结果 |
| --- | --- | --- | --- |
| `create_task` | write | subject、description、blocked_by | 新 Task JSON |
| `get_task` | read | task_id | 完整 Task JSON |
| `list_tasks` | read | 无 | 完整稳定排序的任务集合 |
| `claim_task` | write | task_id | 带 owner 的 in_progress Task |
| `complete_task` | write | task_id | completed Task 与本次直接解锁集合 |

`ToolRegistry.prepare()` 会先解析 JSON 并拒绝额外字段，然后才冻结参数和允许 handler 执行。这样模型传入 `owner`、路径片段或未知字段时，副作用还没有发生。

稳定错误码如下：

| 错误码 | 含义 |
| --- | --- |
| `task_not_found` | canonical UUID 不存在 |
| `task_graph_error` | 缺依赖、自依赖、环或 ID 碰撞 |
| `task_blocked` | 至少一个依赖未完成 |
| `task_invalid_state` | 状态不允许本次迁移 |
| `task_owner_mismatch` | 完成者不是当前 owner |
| `task_storage_error` | JSON、锁或目录边界损坏 |

机器读取错误码，学习者和模型读取后面的中文说明；这和前面章节的 `工具执行错误 [shell_timeout]: 命令超时` 是同一原则。

---

## owner 不能由模型参数伪造

`claim_task` 的 schema 只有 `task_id`。owner 来自可信的 `ToolContext.identity`：

```python
task = store.claim_task(
    str(arguments["task_id"]),
    normalize_owner(context.identity),
)
```

`complete_task` 也从 `ToolContext` 获取 owner。只有状态为 `in_progress` 且 owner 完全匹配时才允许完成。

模型不能通过 `{"task_id":"...","owner":"someone-else"}` 冒充另一身份，因为额外字段会在任何副作用前被 `invalid_arguments` 拒绝。

---

## 完成上游时只报告本次直接解锁

看一个四节点图：

```text
schema ---> endpoints ---> tests
   |
   +------> docs
```

初始时只有 schema ready。提前认领 endpoints、tests 或 docs 都会返回 `task_blocked`，并保持状态、owner 和文件字节不变。

schema 完成后：

- endpoints 和 docs 都直接依赖 schema，且全部依赖已完成，所以它们是本次报告的 ready 任务。
- tests 仍依赖尚未完成的 endpoints，不会被报告。
- 一个从一开始就没有依赖的其他 pending Task 已经 ready，也不会被误报为“本次解锁”。

实现只返回新满足条件的 Task 快照，不改写下游文件。下游仍是 pending，下一步必须由某个 identity 显式 claim。

---

## 重建不是“扫到几个算几个”

新建 `JsonTaskStore(workspace)` 时不依赖旧进程内存。第一次 `get_task/list_tasks/claim_task/complete_task` 会在锁内读取全部 JSON，并验证：

- 每个文件是普通 UTF-8 JSON 文件。
- filename、payload ID 和 UUID 规范一致。
- 字段 schema 与 owner/status 不变量成立。
- 每条依赖都指向当前集合中的 Task。
- 全图无自依赖和环。

任何一个文件损坏都会让整张图明确失败。跳过坏文件会让其他任务的依赖语义悄悄改变，不能称为恢复。

重建成功后，Task、status、owner 和 `blocked_by` 与上一次一致。当前章节不猜测 `in_progress` 的 handler 是否已经产生外部副作用，因此不会自动重置为 pending。

---

## 接入累进 Bootstrap

P12 不复制 Loop。固定 profile 只在 P11 能力集合上增加 `task_dag_json`：

```python
P12 = ChapterProfile(12, frozenset((*P11.capabilities, "task_dag_json")))
```

组合根有两个强制约束：

- P12 缺少 `task_store`，构建立即失败。
- P11 及以前传入 `task_store`，也立即失败，避免“配置了但实际上没有使用”。

CLI 在配置校验通过后创建：

```python
task_store = JsonTaskStore(str(Path.cwd()))
runner = build_agent(
    P12,
    model,
    workspace,
    recovery_config=recovery_config,
    task_store=task_store,
)
```

P12 主工具序列保留 P11 前缀，最后追加五个 Task 工具。一次性子 Agent 共享 workspace、权限、Hook 和同一个 TaskStore，因此看到的是同一张项目图；它仍然不能递归调用第六章的 `task` 子 Agent 工具。

---

## 与 Claude Code 的差异

参照 Claude Code 的任务系统，可以看到教学版 P12 的边界：

**锁粒度。** Claude Code 可以对不同任务文件使用更细粒度的锁；P12 使用 `.tasks.lock` 全局锁保护整张图，因此所有任务操作串行化，推理更简单但并行度更低。

**TaskUpdate 分离。** Claude Code 有独立的更新工具管理依赖、activeForm 和 metadata。P12 把依赖集中在 `create_task(blocked_by=...)` 中，没有 update、delete 或 release。

**ID 分配。** Claude Code 可以用高水位标防止序号重用；P12 使用 UUID v4，因此不依赖序号，也没有删除后重用风险。

**字段数量。** Claude Code 的任务记录还可以包含 activeForm、blocks、metadata。P12 只有 `id`、`subject`、`description`、`owner`、`status`、`blocked_by` 六个字段，刻意保持教学边界。

**释放路径。** Claude Code 在 teammate 关闭时可以 release，让任务回到 pending。P12 没有 unclaim/release，`in_progress` 只能向前完成。

**响应式 UI。** Claude Code 可以监听文件系统刷新任务列表。P12 不实现 watcher；调用 `list_tasks` 时重新读取整张图。

---

## 运行与验证

真实入口：

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch12_agent'
& ..\.venv\Scripts\python.exe -m pip install -e ".[dev]"
& ..\.venv\Scripts\python.exe -m agent_ch12.cli --prompt "建立 schema、endpoints、tests 和 docs 的任务依赖"
```

离线验证：

```powershell
& ..\.venv\Scripts\python.exe -m pytest tests
& ..\.venv\Scripts\python.exe -m compileall -q agent_ch12
& ..\.venv\Scripts\python.exe -m ruff check agent_ch12 tests
& ..\.venv\Scripts\python.exe -m mypy agent_ch12 --no-incremental
```

测试覆盖坏 JSON、非法状态、缺边、环、ID 碰撞、原子写失败、workspace 目录逃逸、无效迁移、owner 校验、直接解锁、两个 Store 并发认领和五个严格工具。全程不需要 API Key，也不会访问网络。

---

## 本章刻意没有做什么

P12 的边界到“持久、可原子认领的 JSON DAG”为止：

- 不实现慢工具后台化，那是 P13。
- 不实现 Cron，那是 P14。
- 不实现持续 teammate 和 mailbox，那是 P15。
- 不实现 request-response 计划门控，那是 P16。
- 不实现 lease、claim token 和工作窃取，那是 P17 的 SQLite TaskStore。

JSON 加排他文件锁足以证明本章的状态和持久化契约，但不会把外部副作用变成数据库事务。Agent 认领后执行文件、Shell 或网络操作，仍然必须经过已有权限、Hook、取消和恢复边界。

---

## 小结

第十二章把“任务清单”升级成了可验证的项目任务图：

- TODO 与 Task 分层，当前会话步骤和跨会话项目状态各自有清晰 owner。
- Task 只允许 pending、in_progress、completed 三态，阻塞由依赖派生。
- 五个工具从同一 `ToolDefinition` 生成 schema 与 handler。
- 全图锁包住加载、DAG 校验、条件迁移和原子 JSON 替换。
- claim 只有一个并发赢家，complete 只能由 owner 执行。
- 重建严格恢复整张图，损坏、缺边和环不会被静默跳过。

下一篇进入后台任务。Task DAG 已经知道“什么可以做”，但慢操作仍会占住当前 Agent Loop。P13 会增加受管后台 job 和 typed event inbox，让 Agent 在等待期间继续处理其他工作。
