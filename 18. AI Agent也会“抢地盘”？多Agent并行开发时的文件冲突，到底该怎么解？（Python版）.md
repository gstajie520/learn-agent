# 第 18 章：Git Worktree 隔离与任务绑定（Python 版）

本文是原第 18 章的 Python 迁移版。原文的验收场景、状态机、事务边界和安全删除规则全部保留；代码示例改成 `python/ch18_agent` 中可以运行的 Python 实现。

## 先固定验收场景

我们要验证的不是“目录是否创建”，而是一条完整链路：Lead 为 pending Task 创建 `alice`、`bob` 两个 Worktree；队友认领后，工具调用分别写入自己的目录；主目录不被污染；任务完成并合入 integration ref 后才允许删除；任何无法证明安全的情况都转为 `needs_review`，不强制丢弃现场。

## 为什么文件锁不是答案

文件锁只能把写入排队：Alice 写完后 Bob 仍然可能覆盖同一个文件。Git Worktree 复用对象库，却为每个分支提供独立物理目录，解决的是“并行改动分别保存和审查”。

## 不能把 worktree 塞进 Task

Task 的 `pending -> in_progress -> completed`、claim 的租约和 Worktree 的生命周期是三条状态线。Python 版使用 SQLite 独立表 `worktree_bindings`，通过 `task_id` 关联 Task；创建 binding 不会偷偷认领任务。

## Binding 是独立状态机

```text
reserved -> active -> kept ---------> removed
                   |                  ^
                   +-> needs_review --+
```

`WorktreeBinding` 是 `@dataclass(frozen=True, slots=True)`，可以按 Java `record` 理解。`branch` 固定为 `wt/{name}`，路径固定为 `.agent_tutorial/worktrees/{name}`，模型不能自由拼接目录。

## Lead 恰好增加三项工具

Lead 新增 `create_worktree`、`keep_worktree`、`remove_worktree`。队友和子 Agent 不获得管理工具，只能在 claim 后使用解析出的执行上下文。

## 创建为什么先 reserve，再调用 Git

Git 不是 SQLite 事务的一部分。实现先写入 `reserved` 意图和 `reserve` 事件，再执行 `git worktree add`；只有回读 HEAD 和当前分支都符合预期，才在同一 SQLite 事务中迁移到 `active`。Git 失败时保留 `reserved`，便于人工处理，不假装“创建失败从未发生”。

## 手动认领和自动窃取必须走同一条路

`WorktreeRuntime.claim_task` 与 `claim_next` 最终都调用同一个 SQLite store。自动认领只扫描 `active` binding 的 ready Task，因此没有 Worktree 的任务不会被队友误领。

## cwd 不是线程字典，而是每次执行的可信上下文

`AgentRunner` 每次 `run()` 创建新的 `execution_scope`。每次工具调用都按以下顺序处理：schema 校验 -> `ToolContextProvider.resolve` -> 校验 identity、幂等键和 scope 未被修改 -> 校验路径仍在仓库根内 -> Hook、权限和 handler。先 claim、后写文件时，第二个工具会自动使用 Worktree cwd。

## 失效 claim 不能回落主目录

未知、过期、已完成或 owner 不匹配的 `claim_token` 都返回 `worktree_context_error`。系统不会因为 Worktree 不可用就悄悄回到主目录，否则一次失效租约就可能把改动写错位置。

## Keep 是显式保留，不是完成 Task

`keep_worktree` 只接受 `completed + active`。它把 binding 迁移为 `kept`，表示目录留给 review 或后续集成；它不改变 Task 的完成逻辑。

## Remove 必须先证明“可以删”

删除顺序是：Task 已完成；路径位于受管根；Git 注册路径和分支匹配；`git status --porcelain=v1 --untracked-files=all` 为空；branch tip 和 integration ref 可解析；branch tip 已是 integration tip 的祖先；最后 detach、删除分支、移除 Worktree。任一步失败都保留目录并转 `needs_review`。

## 审计必须和状态迁移同事务

`SqliteTaskStore._transition_worktree` 在一个 `BEGIN IMMEDIATE` 中更新 binding 并插入 `worktree_events`。事件表由 SQLite trigger 拒绝 `UPDATE/DELETE`，因此它是 append-only 审计链。事件失败时状态更新一起回滚。

## 非 Git workspace 要在状态创建前失败

CLI 先调用 `worktree_runtime.validate_repository()`，确认当前目录是 Git 仓库根，再创建任何 `.agent_tutorial` 运行状态。非 Git 目录不会留下半成品数据库或目录。

## 两个运行入口

直接运行章节入口：

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch18_agent'
& ..\.venv\Scripts\python.exe -m agent_ch18.cli --prompt "为任务创建 alice Worktree 并查看状态"
```

统一入口（在 `code/` TypeScript 项目中）仍使用 `npm run agent-tutorial -- run --chapter 18 ...`。Python 章节复用 `python/.venv` 和 `python/.env`，不会重复创建环境或密钥文件。

## 如何验证

```powershell
& ..\.venv\Scripts\python.exe -m pytest tests
& ..\.venv\Scripts\python.exe -m compileall -q agent_ch18
& ..\.venv\Scripts\python.exe -m ruff check agent_ch18 tests
& ..\.venv\Scripts\python.exe -m mypy agent_ch18 --no-incremental
```

重点阅读 `tests/test_ch18_worktrees.py`，再追踪 `features/worktrees.py -> adapters/task_sqlite.py -> core/loop.py`。这条路径分别对应 Java 的 Service、JDBC Repository 和请求拦截器。

## 从 ai-agent-book 学到什么

Agent 的边界不只在模型 prompt，还在身份、租约、文件系统和外部命令。把这些边界都建模成可测试的端口和状态迁移，才能让“并行开发”从演示变成可恢复的工程流程。

## 前后版本的变化

第 17 章解决“谁认领任务”；第 18 章新增 `worktree` capability，解决“认领后写到哪里”。Task DAG、claim token、mailbox、protocol 和 teammate 逻辑继续复用，没有把 Worktree 偷塞进 Task 字段。

## 本章明确不做什么

本章不自动 merge 冲突，不替人工决定 review 结果，不删除无法证明干净或未集成的提交，也不允许模型自定义任意 branch、path 或 shell 命令。

## 与 Claude Code 的差异

示例使用 Python 标准库 `sqlite3`、`subprocess` 和显式 `ToolContextProvider`，便于 Java 开发者观察事务与请求上下文。生产系统可以替换为更强的 Git 服务或数据库，但不能删除“先证明、再删除”的约束。

## 小结

Worktree 是文件隔离，claim token 是执行身份，SQLite binding 是两者之间的可审计关系。`reserved -> active -> kept/needs_review -> removed` 让跨 Git 和数据库的非原子流程变得可恢复；每个工具调用重新解析 cwd，则保证模型的每一步副作用都发生在正确的隔离目录。
