# 从串行到异步：AI Agent 架构演进中的「慢操作」填坑指南（Python 版）

> 本文是第十三章的完整 Python 迁移版，配套项目位于 `python/ch13_agent`。
> 原文的架构、状态机、边界和对照分析完整保留；代码入口改为 Python。
> 你是 Java 后端开发，可以先把 `BackgroundJob` 看成不可变 DTO，把 `BackgroundJobStore` 看成 Repository，把 `JobSupervisor` 看成受管 `ExecutorService`，把 `EventInbox` 看成 `BlockingQueue<RuntimeEvent>`。

你让 Agent 干一件正经事：「装好依赖，然后跑测试，顺手把 README 读一下告诉我怎么启动」。

它很听话，第一步就调了 `shell` 跑 `npm install`。

然后你盯着屏幕看了两分钟。

什么都没发生。README 就在那儿，几毫秒就能读完的一个文件，但没人去读它。Agent 卡在那一行上，整个 Loop 停在那儿，等一个跟读 README 毫无关系的子进程退出。

问题不是「装依赖慢」——装依赖本来就慢，这是事实，改不了。问题是**这两分钟里 Agent 什么也没干**。它明明还有活可以干，而且那些活跟正在等的东西一点关系都没有。

第 12 章的 Task DAG 已经能回答「什么可以做」了：哪些任务就绪、谁认领了、依赖满不满足。但它管不了「怎么做」——认领一个任务去跑五分钟的构建，那五分钟照样是死等。DAG 描述工作，不执行工作。

第 13 章补的就是这一层：一个工具调用在通过参数校验、Hook、权限和审批之后，可以**先返回一个占位结果**，让当前这一轮工具调用立刻闭合；真实结果稍后作为一条 **typed runtime event** 回到**同一个 Agent Loop**。

本章只增加一个能力，它由四部分组成：**一个三态参数、一个受管 Supervisor、一个 typed 事件队列、一份持久化 job 状态**。

先把范围钉死。本章只后台化 **P13 主 Agent 的 `shell` 工具**。Cron 定时、常驻 teammate、后台 subagent、输出流式文件、交互式提示看门狗，全都不在本章。

---

## 本章导读

### 读完这一章，你能做到什么

1. 说清 `run_in_background` 为什么是**三态**（`True` / `False` / `None`）而不是布尔值，以及每一态各自把决策权交给了谁。
2. 解释后台提交为什么必须排在权限判决**之后**，并说出提前提交会留下什么具体的竞态。
3. 描述占位结果的作用：它闭合了什么、没有承诺什么、以及为什么它不能是错误结果。
4. 说出后台 job 的六个状态，以及「`running` 没有结果、每个终态都必须有结果」这条不变量在哪几处被强制。
5. 解释为什么一个完成事件只会被发布一次——即使完成、超时、取消三条路同时到达终点。
6. 说明事件为什么必须是 typed 对象而不能是普通字典，以及塞字典进去会在哪一行失败。
7. 描述一条后台结果回到 Loop 的完整路径，并说出历史里那条消息的 `role` 是什么、为什么不是 `tool`。
8. 判断进程在后台命令跑到一半时被 `Ctrl+C`，下次启动磁盘上那个 job 会变成什么状态、会不会重跑。

### 你需要先具备什么

| 前置 | 出自哪一章 | 本章怎么用它 |
| --- | --- | --- |
| Agent Loop 的一轮：模型 -> 工具 -> 结果回填 | 第 1 章 | 后台化只改「谁等结果」，这条链路一步不动 |
| `ToolDefinition` / `ToolRegistry` / validator 校验 | 第 2 章 | 三态参数和两个新工具全靠精确键集合守边界 |
| OpenAI 工具消息必须严格配对 | 第 1、2 章 | **本章最关键的前置**，占位结果的存在理由就是它 |
| `PermissionPolicy` 的判决位置 | 第 3 章 | 后台提交必须排在它后面，本章有一节专讲 |
| Pre/PostToolUse Hook 的顺序 | 第 4 章 | 后台不改变 Hook 顺序，只改变中间那一步 |
| 文件锁 + 原子替换 + fail closed | 第 12 章 | 后台 job 的持久化直接沿用同一套纪律 |
| Profile 能力位与组合根按能力注入 | 第 5～12 章 | P13 靠一个 `background` 能力位接上整套后台运行时 |

只来得及补一章的话，补第 1 章里「一条 assistant 消息里有几个 `tool_calls`，就必须回填几条 `tool` 消息」。占位结果这个设计的全部动机都在那条协议约束上。

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| 后台 job | **一次已经获批的执行尝试**，有独立生命周期，跨进程持久化 |
| 三态参数 | `True` / `False` / `None` 三个取值各代表一种决策归属，不是布尔值 |
| `concurrency` | 工具定义上的并发声明，只有 `background_eligible` 才可能进后台 |
| 慢命令启发式 | 一张关键词表，只在模型没表态（`None`）时兜底，不预测任意命令耗时 |
| 占位结果 | 后台提交成功后立刻返回的普通成功 `ToolResult`，只带 `job_id` 和 `running` |
| `BackgroundDispatcher` | 分流器：判断这次调用走同步还是后台，本身不做权限判断 |
| `JobSupervisor` | 后台 worker 的**唯一 owner**，管容量、超时、取消、关闭、发事件 |
| `RuntimeEvent` | 外部异步结果进入 Loop 的**唯一接口**，必须有 `event_id` 和 `to_payload()` |
| `EventInbox` | typed 事件的 FIFO 队列，只收实现了 `RuntimeEvent` 的对象 |
| 事件泵（`RuntimeEventPump`） | Loop 侧看到的抽象：drain 一批、必要时 wait、然后 acknowledge |
| 条件迁移 | 「只有磁盘上还是 `running` 才写终态」的原子写入，输的一方返回 `None` |
| 终态 | `completed` / `failed` / `timed_out` / `cancelled` / `interrupted` 五个 |
| `interrupted` | 进程上次异常退出留下的 `running`，下次启动时被迁移成这个状态 |
| batch 位置 | 注入事件时附带的 `{ index, total }`，告诉模型这是本批的第几条 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 三种「等待」要分清             ← 先划概念，否则后面全是混乱
  ↓
② 三态参数与两道门              ← 谁决定进不进后台
  ↓
③ 为什么提交必须在权限之后      ← 本章最重要的顺序约束
  ↓
④ 占位结果：闭合而不撒谎        ← 协议配对是怎么保住的
  ↓
⑤ 六个状态与一条不变量          ← job 的生命周期和它的强制点
  ↓
⑥ Supervisor 是唯一 owner       ← 容量、超时、取消、关闭
  ↓
⑦ 只发一次：条件迁移            ← 三条路竞争终点时怎么办
  ↓
⑧ 事件怎样回到同一个 Loop       ← typed-only、批量、去重、role=user
  ↓
⑨ 查询与取消两个工具            ← 模型主动看进度的手段
  ↓
⑩ 崩溃之后：interrupted         ← 重启不重放
  ↓
⑪ 接进组合根                    ← 能力位、15 个工具、子 Agent 边界
  ↓
⑫ 运行 + 实验 + 排错            ← 动手把上面每条结论都验一遍
  ↓
⑬ 小结 + 自测                   ← 记住六条 + 自测题
```

---

## 三种「等待」要分清

进入实现之前必须先把三个概念分开，否则后面每一节都会打结。这三样东西在日常说话时都叫「任务」或者「等待」，但在代码里是三种完全不同的对象。

| | TODO（第 5 章） | 项目 Task（第 12 章） | 后台 job（本章） |
| --- | --- | --- | --- |
| 它是什么 | 会话内的步骤清单 | 可跨会话认领的工作单元 | **一次已获批的执行尝试** |
| 活多久 | 一个会话，进程退出即消失 | 跨进程、跨会话，存在磁盘 | 一次执行，但状态持久化 |
| 谁改它 | 模型整表覆盖 | 认领者单向推进状态 | **只有 Supervisor** |
| 状态 | `pending` / `in_progress` / `completed` | 同左，三个 | **六个**，见后文 |
| 谁在等它 | 没人等，它只是提醒 | 认领者自己 | **没人阻塞地等** |
| 存哪 | 内存 | `.agent_tutorial/.tasks/` | `.agent_tutorial/background/` |

三者不互通，也不该互通。最容易犯的错是把后台 job 当成「Task 的第四种状态」——比如给 Task 加一个 `running` 表示「正在后台跑」。

**为什么不能这么干？** 因为一个 Task 可以对应零次、一次或多次执行尝试。你认领了「跑测试」这个任务，第一次跑超时了，改了配置再跑一次，这是两个后台 job、一个 Task。把执行尝试塞进 Task 的状态机，等于把「做了几次」压成「现在是什么状态」，信息直接丢掉。

反过来也一样：后台 job 完成不代表 Task 完成。`npm test` 跑完退出码是 1，job 状态是 `failed`——但这个 Task 该不该标记 `completed`，那是模型看到结果之后的判断，不是 Supervisor 能替它做的决定。

**一句话记住分工**：Task 说「这件事该做」，后台 job 说「这次执行怎么样了」。

还有一个更细的区分，跟本章的实现直接相关：

```text
「等待」的两种含义

含义 A：谁需要这个结果？
  → 模型。它迟早要看到 npm install 的输出。
     这个「等」是无法消除的，结果没出来就是没出来。

含义 B：谁在占着执行流？
  → 本章要消除的正是这个。
     同步执行占着 Loop，是实现选择，不是必然。
```

后台化只动含义 B，完全不动含义 A。模型该看到的结果一条都不会少，只是**看到的时机**从「同一轮的 tool 消息」变成了「后续某一轮的 runtime event」。

---

## 三态参数与两道门

### 只有 P13 主 Agent 的 shell 变了

先看一个容易被忽略的事实：`run_in_background` 这个参数**不是全局加上去的**。

真实探测出来的两个 schema，放在一起对比：

```json
// P13 主 Agent 的 shell
{
  "name": "shell",
  "description": "在当前工作目录执行 PowerShell 命令；需要人工批准。",
  "parameters": {
    "type": "object",
    "properties": {
      "command": { "type": "string", "minLength": 1 },
      "run_in_background": { "type": "boolean" }
    },
    "required": ["command", "run_in_background"],
    "additionalProperties": false
  }
}

// P12（以及 P01-P12、以及所有一次性 subagent）的 shell
{
  "name": "shell",
  "properties": {
    "command": { "type": "string", "minLength": 1 }
  },
  "required": ["command"],
  "additionalProperties": false
}
```

同一个工具名、同一个 description、同一个 handler，**但模型看到的参数不一样**。这靠 `create_shell_tool(command_runner, background)` 的布尔参数实现——一个布尔值从 bootstrap 走到工具工厂，没有全局状态、没有环境变量、没有单例。

注意 handler 里**没有任何后台逻辑**。它拿到 `command` 就跑，跑完返回 `ToolResult`。后台化发生在它之外——这是本章能保持简单的关键：**被后台化的代码本身不需要改**。

`test_p13_profile_and_main_tools` 直接断言了 schema 差异：P13 的 shell 有 `run_in_background` 属性，P12 没有。

### 三态：每一态代表一个决策归属

`run_in_background` 有三个取值，它们的区别不是「真/假/没填」，而是**决策权在谁手里**：

| 取值 | 决策归属 | 含义 |
| --- | --- | --- |
| `true` | 模型 | 「我要它进后台」——哪怕是个瞬间返回的命令 |
| `false` | 模型 | 「我要同步等」——哪怕命中了慢命令关键词 |
| `None` / 省略 | 运行时 | 「我不表态，你按规则判」 |

判断函数必须**先区分显式布尔值**，再考虑启发式（Python 版签名是 `should_run_in_background(command, requested)`）：

```python
def should_run_in_background(command: str, requested: bool | None) -> bool:
    if requested is not None:
        return requested          # 显式指定时直接返回

    lowered = command.lower()     # 启发式识别
    return any(marker in lowered for marker in BACKGROUND_MARKERS)
```

`test_three_state_background_decision` 把四条关键路径全部钉住：

```python
assert should_run_in_background("npm install", None)        # 启发式命中 → 后台
assert not should_run_in_background("Get-ChildItem", None)  # 启发式不中 → 同步
assert should_run_in_background("Get-ChildItem", True)      # 显式覆盖 → 后台
assert not should_run_in_background("npm install", False)   # 显式覆盖 → 同步
```

第 4 行是关键：`npm install` 命中了关键词表，但因为模型明确说了 `False`，它还是走同步。**显式布尔值是命令，不是建议。**

关键词表就七条，硬编码，没有配置项：

```python
BACKGROUND_MARKERS = ("cargo build", "compile", "deploy", "docker build", "npm install", "pip install", "pytest")
```

注意 `npm ci` 和 `npm test` 都**漏了**。这不是 bug，是这个设计的诚实之处：**关键词表不可能预测任意命令的耗时**。那为什么还留着这张表？因为它是**确定性兜底**。同样的输入永远得到同样的判定，可测试、可解释、不需要模型配合。

这里的取舍值得说清楚：另一条路是让运行时「智能判断」，比如先跑 500ms 看有没有结束、或者查历史耗时统计。那需要引入计时、采样、历史存储三样新东西，而且判断结果不稳定。**本章选了「笨但确定」。**

### 两道门，缺一不可

一次调用要真的进后台，必须**同时**通过两道门：

```text
门 1：definition.concurrency == "background_eligible"
      ↓ 不满足 → 同步执行，结束
门 2：should_run_in_background(command, requested) == True
      ↓ 不满足 → 同步执行，结束
      ↓ 都满足
   supervisor.submit(...)
```

`BackgroundDispatcher.dispatch()` 的返回值语义是关键：**返回 `None` 表示「不处理，让 Loop 走默认同步路径」；返回 `ToolResult` 表示「已处理，短路后续 invoke」**：

```python
def dispatch(self, prepared, context, tools):
    definition = prepared.definition
    if definition is None or definition.concurrency != "background_eligible" or prepared.arguments is None:
        return None                      # 门 1 不满足
    arguments = dict(prepared.arguments)
    requested = arguments.pop("run_in_background", None)   # 从参数中摘掉，真实工具不需要
    command = str(arguments.get("command", ""))
    if not should_run_in_background(command, requested):
        return None                      # 门 2 不满足
    job_id = self.supervisor.submit(...)
    return tool_success(f"后台任务已提交: job_id={job_id}; status=running")
```

门 1 缺省是 `inline`。这是个刻意的默认值选择：**新加一个工具，默认不能后台**。想要后台能力必须显式写 `concurrency="background_eligible"`。

为什么门 1 不能省？如果只看参数不看工具声明，那么任何工具只要参数里带了 `run_in_background: true` 就能进后台。但绝大多数工具的 handler 根本没为「延迟返回」设计过——`write_file` 后台化之后，模型拿到占位结果就以为写完了，接着去读那个文件，读到的是旧内容。**门 1 是把「哪些工具敢后台」这个决定留在代码里，而不是留给模型的参数。**

---

## 为什么提交必须在权限之后

### 一次工具执行的完整顺序

后台化是插在 `_execute_tool` 里的，看这个方法的完整骨架（`core/loop.py`）：

```python
async def _execute_tool(self, call, context, tools):
    try:
        prepared = tools.prepare(call)          # 1. 解析并校验参数
    except Exception:
        return ToolExecution(tool_error("tool_preparation_error", "工具准备失败"))
    if prepared.error is not None:
        return ToolExecution(prepared.error)

    pre_hook = await self._hooks.run_pre_tool(prepared)   # 2. PreToolUse 钩子
    effective = pre_hook.updated_input or prepared
    if pre_hook.blocking_error is not None:
        return ToolExecution(pre_hook.blocking_error, pre_hook.additional_context)

    permission_error = self._check_permission(effective, context, pre_hook)  # 3. 权限判定
    if permission_error is not None:
        return ToolExecution(permission_error, pre_hook.additional_context)

    result = None                                           # 4. 这里才执行——后台派发插在这一步
    if self._tool_dispatcher is not None:
        try:
            result = self._tool_dispatcher.dispatch(effective, context, tools)
        except Exception:
            result = tool_error("background_submission_error", "后台任务提交失败")
    if result is None:
        result = tools.invoke(effective, context)

    post_hook = await self._hooks.run_post_tool(effective, result)   # 5. PostToolUse 钩子
    ...
```

四道关卡，`dispatch` 排在最后。**这个顺序不是随便排的。** Dispatcher 拿到的是 Hook 改写后、权限实际审查过的 `PreparedToolCall`。

### 拒绝的时候，磁盘上必须是干净的

把顺序反过来，先提交后判权限，会发生什么：

```text
错误顺序：submit() → 权限判定 → 拒绝

t=0ms    创建 job 记录，状态 running，写入磁盘
t=0ms    Supervisor 启动 worker 线程，npm install 开始下载
t=1ms    权限判定返回 deny
t=1ms    ??? 现在怎么办
```

到 `t=1ms` 已经晚了。命令在跑，进程在动网络，磁盘上有一条 `running` 记录。你现在有三个选择，全都是坏的：

| 补救方式 | 问题 |
| --- | --- |
| 调 `cancel()` | 竞态：命令可能已经跑完了；而且 `npm install` 已经改了 `node_modules` |
| 直接删 job 文件 | job 历史里出现空洞，审计断了；worker 还在跑，收束时会写回一个刚被删的 id |
| 留下 `cancelled` 记录 | 一条被权限拒绝的操作在历史里长得像一次被取消的正常操作 |

**没有一种补救能回到「什么都没发生」。** 所以顺序不能靠「出错了再修」——它必须一开始就对。

### 一个更普适的规律

这条规律不止适用于后台任务：

```text
凡是「一旦开始就无法完全撤销」的操作，
它的授权检查必须在它开始之前完成，
而不是在它开始之后补救。
```

启动一个 worker 是不可撤销的（网络请求发出去了）；写一条 `running` 记录是半可撤销的。两者组合起来就是完全不可撤销。所以四道关卡的顺序是：**所有能拒绝的判断排在所有不可撤销的动作之前。**

---

## 占位结果：闭合而不撒谎

### 协议逼出来的设计

OpenAI 的消息协议有一条硬规则：

```text
一条 assistant 消息里有 N 个 tool_call
  ↓
下一次请求前，必须有恰好 N 条 tool 消息
  ↓
每条 tool 消息的 tool_call_id 必须对上其中一个
  ↓
少一条 / 多一条 / id 对不上 → 400，请求根本发不出去
```

现在把后台化这件事放进这条规则里：`shell` 进后台了，命令要跑两分钟，但下一次模型请求现在就要发。**那条 `tool` 消息从哪来？**

三个选择：

| 方案 | 结果 |
| --- | --- |
| 不发 tool 消息 | 400。协议不允许。 |
| 等命令跑完再发 | 那就没有后台化，回到同步。 |
| **现在就发一条，说明「已提交」** | 本章的选择。 |

第三个方案的核心一行（`BackgroundDispatcher.dispatch` 的返回）：

```python
return tool_success(f"后台任务已提交: job_id={job_id}; status=running")
```

**「闭合」和「撒谎」的区别**：占位结果必须诚实。

```text
❌ 撒谎：返回 "" 或假输出，模型会以为命令跑完了、成功了
✅ 诚实：明确说这是 running，并给出可查询的 job_id
```

撒谎的写法能让协议闭合，但它向模型传达了「这件事做完了」。模型会基于这个假设继续——比如接着去 `node_modules` 里读文件。诚实的写法闭合了协议，同时明确告诉模型：**这件事在跑，结果还没到，你可以用这个 id 查。**

### 为什么它是 `tool_success` 而不是 `tool_error`

占位结果用的是 `tool_success`，`is_error` 为 `False`。可能有人觉得该用 `tool_error`——毕竟结果没拿到。

不对。`is_error` 回答的问题是「**这次工具调用本身有没有出问题**」，不是「任务成功了吗」。提交进后台这件事是成功的：job 建了、worker 起了、id 拿到了。真正的执行结果稍后单独送达，那时候才有 `is_error` 可谈。

这个区分后面会再次出现：一个 `failed` 的 job（命令退出码非 0），它事件里的 `is_error` 是 `True`；但当初提交它的那条占位结果，`is_error` 仍然是 `False`。**两条结果，两个含义，不冲突。**

---

## 六个状态与一条不变量

### 一活五终

后台 job 的状态机只有一层，没有嵌套，没有子状态：

```text
                    ┌──────────────┐
     submit() ────► │   running    │  ← 唯一的活跃态
                    └──────┬───────┘
                           │
     ┌─────────┬───────────┼───────────┬─────────────┐
     ▼         ▼           ▼           ▼             ▼
 completed  failed    timed_out   cancelled    interrupted
     │         │           │           │             │
     └─────────┴───────────┴───────────┴─────────────┘
                  五个终态，进去就不再出来
```

五个终态各自对应一个明确的原因：

| 状态 | 什么时候进入 | error_code |
| --- | --- | --- |
| `completed` | 执行返回了 `is_error == False` 的结果 | `None` |
| `failed` | 执行返回了 `is_error == True` 的结果 | 原样保留，如 `shell_failed` |
| `failed` | 执行函数自己抛异常 | `background_execution_error` |
| `failed` | 执行返回了不是 `ToolResult` 的东西 | `background_contract_error` |
| `timed_out` | 超过 120 秒 | `background_timeout` |
| `cancelled` | 收到 `cancel_background_job` | `background_cancelled` |
| `interrupted` | 进程崩溃，下次启动时发现 | `background_execution_error` |

注意 `failed` 占了三行。**「命令跑失败」「代码抛异常」「契约被违反」在状态上都是 `failed`，靠 `error_code` 区分。** 这是个刻意的取舍：状态机保持六个值不膨胀，细粒度原因交给错误码。

为什么要分这么细？因为**模型的下一步动作取决于原因**。`failed` 说明命令跑了但退出码不对，该看输出；`timed_out` 说明命令可能没问题只是慢，该考虑拆分或者延长超时；`cancelled` 是有人主动停的，不该重试；`interrupted` 是环境问题，重试是合理的。**如果这五个都塞成一个 `failed`，模型只能瞎猜。**

### 构造时就校验的四条状态不变量

`BackgroundJob` 是 `@dataclass(frozen=True, slots=True)`，`__post_init__` 不接受任何不一致的组合（`test_background_job_enforces_state_invariants` 逐条钉住）：

```python
# 状态一致性规则 1：running 状态不能携带 result
if self.status == "running" and self.result is not None:
    raise BackgroundError("background_contract_error", "running 状态不能携带 result")

# 状态一致性规则 2：终态必须携带 result
if self.status != "running" and self.result is None:
    raise BackgroundError("background_contract_error", "终态必须携带 result")

# 状态一致性规则 3：completed 必须是成功结果
if self.status == "completed" and self.result is not None and self.result.is_error:
    raise BackgroundError("background_contract_error", "completed 必须是成功结果")

# 状态一致性规则 4：失败终态必须是错误结果
if self.status != "completed" and self.status != "running" and ... not self.result.is_error:
    raise BackgroundError("background_contract_error", "失败终态必须是错误结果")
```

用表格看，加上 id 和字段的校验：

| 不变量 | 违反的例子 | 抛出的消息 |
| --- | --- | --- |
| id 必须是 canonical UUID | `A1B2C3D4-...` | `job_id 必须是 canonical UUID` |
| `source_tool_call_id` / `tool_name` 非空 | `""` | `工具调用 id 和工具名不能为空` |
| `status` 必须是六个字面量之一 | `"pending"` | `后台任务状态无效` |
| `running` ⇒ `result is None` | `running` + result | `running 状态不能携带 result` |
| 终态 ⇒ 有 result | `failed` + None | `终态必须携带 result` |
| `completed` ⇒ 成功结果 | `completed` + `is_error=True` | `completed 必须是成功结果` |
| 失败终态 ⇒ 错误结果 | `failed` + `is_error=False` | `失败终态必须是错误结果` |

最后两条是一对。它们保证**状态和结果不会互相矛盾**。冻结实例让改状态只能构造新实例，和第 12 章 `Task` 的处理是同一思路：**不可变对象没有「中间状态」，读到的一定是完整的。**

---

## Supervisor 是唯一 owner

### 没有裸线程

本章有一条硬规矩：**代码里不允许出现「起了一个线程然后不管它」**。

反面教材长这样：

```python
# ❌ 绝对不要这么写
threading.Thread(target=run_command).start()     # 谁在等它？daemon 之外退出即杀
some_future.add_done_callback(lambda f: None)    # 异常吞掉了
```

这些写法创造的都是**孤儿线程**：没人知道它存在、没人知道它什么时候结束、它抛异常时没人接。

`JobSupervisor` 存在的意义就是接管这件事。它同时是四个角色：

```text
┌─────────────────────── JobSupervisor ───────────────────────┐
│                                                              │
│  ① 唯一的提交入口     submit()                                │
│     容量上限 4，满了抛 background_capacity                    │
│                                                              │
│  ② 唯一的状态写入者   _run_worker → store.finish_running()     │
│     Dispatcher、工具、Loop 都不直接写 store                    │
│                                                              │
│  ③ 唯一的线程持有者   _controls: dict[job_id, (Thread, Event)] │
│     每个 job 一个 cancel_event + 一个超时检查                  │
│                                                              │
│  ④ RuntimeEventPump 实现  has_pending_work / drain_events... │
│     Loop 通过这个接口跟它打交道，不认识 JobSupervisor 这个类     │
└──────────────────────────────────────────────────────────────┘
```

三个数字都在构造函数里给缺省值，测试可以覆盖：

| 参数 | 缺省 | 含义 |
| --- | --- | --- |
| `capacity` | `4` | 同时最多 4 个 running job |
| `timeout` | `120.0` | 单个 job 最多 120 秒 |
| `close_timeout` | `10.0` | `close()` 最多等 10 秒 |

三个都做了范围校验。**参数校验放在构造函数里，坏配置在启动时就炸，而不是等到第一个 job 卡住才发现。**

### 先落盘，再启动 worker

`submit()` 的顺序是刻意的：

```python
with self._lock:
    if self._closed:
        raise BackgroundError("background_closed", "后台任务服务已关闭")
    if len(self._controls) >= self.capacity:
        raise BackgroundError("background_capacity", "后台任务容量已满")
    job_id = _uuid(self._id_generator())
    self.store.create_running(job_id, source_tool_call_id, tool_name)   # 先持久化
    cancel_event = threading.Event()
    worker = threading.Thread(target=self._run_worker, ..., daemon=True)
    self._controls[job_id] = (worker, cancel_event)
    worker.start()                                                       # 再启动
    return job_id
```

源码注释写得很直白：「先持久化为 running 状态（类似 WAL，确保不丢失任务记录）」。如果线程先启动、落盘失败，你就有一个在跑但没有记录的 worker——进程一崩它就彻底消失了，连 `interrupted` 都不会留下。

`test_supervisor_persists_before_worker_and_publishes_one_event` 把这条顺序**实测钉住**了：operation 里读自己的 job 状态，断言读到的是 `running`——说明 worker 开始执行时磁盘记录已经存在：

```python
def operation(_: threading.Event) -> ToolResult:
    observed.append(store.get_job(JOB_ID).status)   # worker 里读自己的状态
    return tool_success("404 passed")

assert supervisor.submit("slow-call", "shell", operation) == JOB_ID
assert supervisor.wait_idle(1)
assert observed == ["running"]                        # ← 落盘先于 worker
```

### 容量满了是错误，不是排队

第 5 个 job 提交时会拿到 `background_capacity` 错误，**而不是被放进队列等着**。

为什么不排队？排队意味着要引入一个等待队列、一个调度器、一套「队列里的 job 是什么状态」的语义。而且模型拿到「已排队」这个反馈之后，它并不知道要等多久。**直接失败反而更有用**：模型立刻知道现在跑不了，它可以选择先做别的、或者取消一个正在跑的。

对比一下「排队」方案：模型提交后拿到一个 job id，看起来成功了，但那个任务可能在队列里躺 5 分钟。**一个假装成功的排队，比一个诚实的失败更难调试。**

### worker 的状态判定

`_run_worker` 是线程的主逻辑（对应 TS 版的三方竞速，Python 版因同步线程模型而更直接）：

```python
status, result = "completed", None
started = time.monotonic()          # 单调时钟，不受系统时间调整影响
try:
    result = operation(cancel_event)
    if not isinstance(result, ToolResult):
        result = tool_error("background_contract_error", "后台操作返回了无效结果")
    if result.is_error:
        status = "failed"
except Exception:
    status, result = "failed", tool_error("background_execution_error", "后台任务执行失败")

# 状态修正 1：如果外部请求取消，优先标记为 cancelled
if cancel_event.is_set() and status == "completed":
    status, result = "cancelled", tool_error("background_cancelled", "后台任务已取消")

# 状态修正 2：如果执行时间超过超时阈值，标记为 timed_out
if time.monotonic() - started > self.timeout and status == "completed":
    status, result = "timed_out", tool_error("background_timeout", "后台任务执行超时")

job = self.store.finish_running(job_id, status, result)   # 条件迁移
with self._lock:
    self._controls.pop(job_id, None)                       # 释放容量
if job is not None:
    self.inbox.publish(self._event(job))                   # 只有胜者才发事件
```

注意超时检测的写法：**Python 版是「事后判定」而不是「定时器打断」**——命令跑完后对比耗时，超过 120 秒就把状态改写为 `timed_out`。这和 TS 版的 `Promise.race` + 定时器不同：一个卡死的命令会一直占着线程，直到它自己结束才被标记。代价是线程资源被占用；收益是不需要向同步线程发信号的机制。这是两个版本因并发模型不同产生的**真实差异**。

### 取消：只对 running 有效

`cancel()` 的三种情况，三种反馈：

| 情况 | 反馈 |
| --- | --- |
| job 在跑 | 触发 cancel_event，等线程收束（`control[0].join(self.close_timeout)`），返回终态 |
| job 已经收束了 | `background_job_state`（「任务已经是终态」） |
| job 根本不存在 | `background_job_not_found`（由 `store.get_job` 抛出） |

`test_supervisor_cancel_returns_terminal_state` 实测：operation 里 `cancel.wait(1)` 模拟响应取消信号的 worker，`supervisor.cancel(JOB_ID).status == "cancelled"`。

**`cancel()` 会等到 worker 真的收束才返回。** 所以取消工具在 `cancel()` 之后回读，读到的一定是已经迁移过的终态，不会读到一个还是 `running` 的中间快照。

**取消是协作式的。** cancel_event 是 `threading.Event`——worker 想响应就检查 `cancel_event.is_set()`，不理它就继续跑。`BackgroundOperation` 的签名就是 `def __call__(self, cancel_event: threading.Event) -> ToolResult`。P13 的取消语义是：Supervisor 不再等这个 job，job 状态迁移到 `cancelled`，但底层 PowerShell 进程会继续跑到自己结束。这个限制必须说清楚。

### close 有超时

```python
def close(self) -> None:
    with self._lock:
        self._closed = True
        controls = tuple(self._controls.values())    # 快照
    for _, event in controls:
        event.set()                                   # 主动取消所有还在跑的 job
    for thread, _ in controls:
        thread.join(self.close_timeout)               # 每个线程最多等 close_timeout
```

**它主动取消，不是被动等待。** 那个循环给每个还在跑的 job 发取消信号。**磁盘上留下的是明确的 `cancelled`，而不是悬空的 `running`**。

---

## 只发一次：条件迁移

### 同一个 job，两条路想收束它

假设一个 job 刚好在超时的瞬间跑完了，或者 cancel 和完成撞上。如果各自都去写状态、各自都去发事件，模型会收到两条关于同一个 job 的完成通知。

### 解法：让 store 做仲裁

关键在于 `finish_running` 的返回类型是 `BackgroundJob | None`：

```python
def finish_running(self, job_id: str, status: str, result: ToolResult) -> BackgroundJob | None:
    """仅允许 running -> 终态；竞争写者拿不到已完成 Job。"""
    with self._locked():
        job = self._read(directory / f"{job_id}.json")
        if job.status != "running":
            return None                            # ← 后到的写者
        finished = BackgroundJob(...)
        self._write(...)
        return finished                            # ← 胜出的写者
```

「读当前状态 → 判断是否 running → 写入」这三步整个包在 `_locked()` 里，是原子的。**两个并发调用不可能都读到 `running`。**

于是：

```text
     ┌──────────────┐
     │  两条路径    │
     │ 同时想收束   │
     └──────┬───────┘
            │
    ┌───────┴────────┐
    ▼                ▼
finish_running    finish_running
    │                │
  返回 job       返回 None
    │                │
    ▼                ▼
 publish 事件      直接跳过
```

**恰好一条事件。** 不需要在 Supervisor 里维护「这个 job 我发过事件了吗」的标记，也不需要加锁——判断被下推到了本来就有锁的那一层。

`test_json_store_conditionally_finishes_and_recovers_once` 实测：

```python
first = store.finish_running(JOB_ID, "completed", tool_success("完成"))
second = store.finish_running(JOB_ID, "completed", tool_success("重复"))
assert first is not None and first.status == "completed"
assert second is None                       # ← 第二次收束被拒绝
```

**第二次返回 `None`，而且没有抛错。** 这个「安静地什么都不做」是刻意设计的。用返回值表达「你来晚了」比用异常更合适，因为**这不是错误，是正常的竞速结果**。

### 为什么不在内存里记一个 Set

有个看起来更简单的做法：在 Supervisor 里放一个 `published: set[str]`，发过就记下来。

它有两个问题：

1. **进程重启后 Set 是空的。** 崩溃恢复时把一个 `running` 改成 `interrupted`，Set 里没记录，于是又发一次事件。
2. **Set 和磁盘可能不一致。**

把判断放在 store 里就没这个问题：**「状态是否已经迁移」这个事实本身就存在磁盘上**，它是唯一的真相。这是第 12 章那条原则的延续——**一个问题只有一个当前有效的真相来源**。

---

## 事件怎样回到同一个 Loop

### 只接受 typed 事件

`EventInbox` 是个线程安全的 FIFO 队列（`threading.Condition` 实现，对应 Java 的 `BlockingQueue`），但它的 `publish` 有一道类型闸门：

```python
def publish(self, event: RuntimeEvent) -> None:
    if not is_runtime_event(event):
        raise TypeError("EventInbox 只接受 RuntimeEvent 对象")
    with self._condition:
        self._events.append(event)
        self._condition.notify_all()      # 唤醒所有等待者
```

为什么不允许随便塞个字典进来？`test_event_inbox_only_accepts_runtime_event` 实测：

```python
with pytest.raises(TypeError):
    inbox.publish({"event_id": "bad"})    # 普通字典被拒
```

一个裸对象没有 `to_payload()`，塞进去要到注入那一刻才会炸——那时候已经在 Loop 深处，栈上看不出是谁发的。**在 publish 处炸掉，错误离原因最近。**

`notify_all()` 唤醒全部等待者，不是只唤醒一个。被唤醒的 `wait()` 会重新检查队列（条件变量标准写法），先跑的 drain 走事件，后跑的发现队列又空了，回去继续等。

### 事件是 user 消息，不是第二条 tool 结果

这是本章最容易做错的地方，也是最不能妥协的地方。

后台命令跑完了，结果要送给模型。直觉上它应该是一条 `tool` 消息——毕竟它是工具的执行结果。**但不行。**

回到协议规则：每条 `tool` 消息必须带一个 `tool_call_id`，而且这个 id 要对应一个尚未被回复的 `tool_call`。而那个 `slow-call`，**在提交的时候就已经被占位结果回复过了**。

```text
第 2 条 assistant:  tool_calls = [slow-call, fast-call]
第 3 条 tool:       tool_call_id = slow-call   ← 已经用掉了
第 4 条 tool:       tool_call_id = fast-call   ← 已经用掉了

现在后台结果来了，它能用哪个 tool_call_id？
  用 slow-call → 同一个 id 出现两条 tool 消息 → 400
  用一个新 id  → 这个 id 没有对应的 tool_call → 400
  不带 id      → tool 消息缺 tool_call_id → 400
```

**三条路全是死的。** 所以后台结果只能作为一条 `user` 消息注入。`runtime_event_message` 的实现（`core/events.py`）：

```python
def runtime_event_message(event: RuntimeEvent, index: int = 0, total: int = 1) -> ChatMessage:
    """把事件包装成普通 user 消息，绝不伪装成 tool result。"""
    if not is_runtime_event(event):
        raise TypeError("event 必须是 RuntimeEvent")
    if index < 0 or total <= 0 or index >= total:
        raise ValueError("batch 位置必须满足 0 <= index < total")
    payload = {"runtime_event": dict(event.to_payload()), "batch": {"index": index, "total": total}}
    return user_message(json.dumps(payload, ensure_ascii=False, separators=(",", ":")))
```

返回的是 `user_message(...)`——**写死了「事件只能变成 user 消息」**。真实注入的内容（`test_event_inbox_only_accepts_runtime_event` 断言）：

```python
message = runtime_event_message(inbox.drain()[0])
payload = json.loads(message.content or "")
assert message.role == "user"
assert payload["batch"] == {"index": 0, "total": 1}
assert payload["runtime_event"]["job_id"] == JOB_ID
```

**这不是妥协，这是唯一正确的映射。** `tool` 消息的语义是「你这次调用的返回值」，它和 `tool_call` 一一对应、同一轮内闭合。后台结果的语义是「之前那件事有进展了」，它天然是个跨轮次的通知——`user` 消息正是承载外部输入的角色。

### 两个注入点

Loop 里有两处调用 `_inject_runtime_events`，参数不同：

```python
# 位置一：模型给了没有工具调用的回答之后（core/loop.py:282）
if self._event_pump is not None and self._event_pump.has_pending_work:
    self._inject_runtime_events(True)
    continue                       # ← 注入后继续下一轮

# 位置二：每次模型请求前（组装请求那段）
```

那个布尔参数是 `wait_for_pending`：

| 位置 | 参数 | 行为 |
| --- | --- | --- |
| 请求前 | `False` | 队列里有就拿，没有就算了，**绝不阻塞** |
| 无工具回答后 | `True` | 队列空但还有 running job，**等** |

这个设计解决的是「什么时候可以真的结束」：

```text
情况 A：模型还在调工具
  Loop 本来就要转下一圈，不需要等。
  → 后台结果搭便车，零额外延迟

情况 B：模型说完了，不调工具了
  这时候如果直接 return，那些还在跑的后台 job 就白跑了。
  → 必须等，等到它们收束，把结果给模型看
```

`has_pending_work` 就是回答「还有没有值得等的东西」——只看 `active_count > 0`（**还有没有正在跑的 job**），不看队列里有没有事件。

### 完整的注入流程

`_inject_runtime_events` 做五件事：

```python
def _inject_runtime_events(self, wait_for_pending: bool) -> None:
    if self._event_pump is None:
        return
    events = self._event_pump.drain_events()                        # 1. 先非阻塞取
    if not events and wait_for_pending and self._event_pump.has_pending_work:
        events = self._event_pump.wait_for_events()                 # 2. 空了才考虑等
    fresh = [event for event in events if event.event_id not in self._seen_event_ids]  # 3. 去重
    self._seen_event_ids.update(event.event_id for event in fresh)
    total = len(fresh)
    self._history.extend(runtime_event_message(event, index, total) for index, event in enumerate(fresh))  # 4. 带位置批量注入
```

去重靠 `event_id`。没有它，同一个事件被注入两次，模型会看到两遍同样的完成通知。

第 4 步的 `{ index, total }` 是给模型的定位信息。三个 job 同时完成时，模型会连着收到三条 `user` 消息。有了 `total: 3`，模型看到第一条就知道后面还有两条，不会急着基于不完整的信息下结论。

省略位置时默认 `{ index: 0, total: 1 }`——单个事件就是「一批里的第一个，总共一个」，语义一致，不需要特例分支。

### RuntimeEventPump：Loop 不认识 Supervisor

Loop 依赖的是一个四成员接口（`Protocol`），不是 `JobSupervisor` 这个类：

```python
class RuntimeEventPump(Protocol):
    has_pending_work: bool
    def drain_events(self, limit=None): ...
    def wait_for_events(self, limit=None): ...
```

`JobSupervisor` 实现它，实现得很薄——几乎全是转发给 `EventInbox`：

```python
def drain_events(self, limit=None):
    return self.inbox.drain(limit)

def wait_for_events(self, limit=None):
    return self.inbox.wait(limit)
```

这一层抽象的实际收益是：**Loop 完全不知道后台任务这件事。** 它只知道「有个东西会产生 RuntimeEvent，我要在合适的时机把它们注入 history」。第 13 章接的是后台 job，第 14 章接 Cron、第 15 章接 Inbox，Loop 这段代码一行不动。

### 一条完整的轨迹

把上面的机制串起来（用注入的 prompt 构造），一次「后台安装 + 同时读 README」的真实历史形状：

```text
 1. user      后台安装依赖，同时读 README
 2. assistant calls=[shell#slow-call, read_file#fast-call]  content=None
 3. tool      [tool_call_id=slow-call] 后台任务已提交: job_id=...; status=running
 4. tool      [tool_call_id=fast-call] # demo readme        ← 它没有等 npm install
 5. assistant content="已提交后台安装，README 已读取，等待安装结果。"
 6. user      {"runtime_event":{...,"status":"completed","result":{...}},"batch":{...}}
 7. assistant content="安装完成。"
```

第 3 条是本章的核心：`slow-call` 这个 `tool_call_id` 立刻拿到了一条 `tool` 消息，协议闭合。第 4 条证明了后台化真的有用——`read_file` 紧跟着执行完了。第 6 条的 `role` 是 **`user`**，不是 `tool`。

对比一下，如果 `shell` 是同步的：

```text
 2. assistant calls=[shell#slow-call, read_file#fast-call]
    ⏳ 这里卡两分钟 —— fast-call 一行代码都没执行
 3. tool      [tool_call_id=slow-call] 404 passed
 4. tool      [tool_call_id=fast-call] # demo readme
```

**但第 2 条到第 3 条之间那两分钟里，整个进程在空转。**这就是本章要解决的全部问题：**把「谁在等结果」和「谁在占着 Loop」这两件事拆开。**

---

## 查询与取消：两个工具

模型光有占位结果不够用——它需要能主动问「那个 job 现在怎么样了」，也需要能说「别跑了」。所以本章注册了两个新工具（`register_background_job_tools`），**只给主 Agent**：

```python
registry.register(ToolDefinition("query_background_job", "查询后台任务当前状态", schema, "read", query, validate))
registry.register(ToolDefinition("cancel_background_job", "取消正在运行的后台任务", schema, "write", cancel, validate))
```

那句注释点出了一个设计决定：**没有 `create_background_job` 工具。** 模型不能直接说「给我建个后台任务」，它只能通过 `shell` 的 `run_in_background` 参数间接触发。因为后台 job 必须绑定一个真实的工具调用——它的 `source_tool_call_id` 要能追溯回去，它的 operation 要是一个已经通过权限检查的调用。

### 两个工具的差别在 effect

两个工具共用同一个输入 schema（validator 要求**恰好** `{"job_id"}` 一个字段、canonical UUID 格式）：

| 工具 | effect | 做什么 |
| --- | --- | --- |
| `query_background_job` | `read` | 读一条 job 记录，不改任何状态 |
| `cancel_background_job` | `write` | 触发取消，等收束后返回最终状态 |

`effect` 决定权限策略怎么处理它。`read` 通常可以自动放行，`write` 可能需要确认。

查询的返回是完整的 job 快照（`_job_text` 转成 JSON，`ensure_ascii=False` 保留中文）。查询 running 状态的任务返回 `result: None`——这正是那条不变量在工具返回上的样子。

### 错误码

已知领域错误转成稳定的错误码，未知异常继续向上抛（和第 12 章同一个模式）：

```python
try:
    job = supervisor.get_job(str(arguments["job_id"]))
    return tool_success(_job_text(job))
except BackgroundError as error:
    return tool_error(error.error_code, str(error))
```

**只有 `BackgroundError` 会被转成 `ToolResult`。** 别的异常原样抛出去。**领域错误给模型看，程序缺陷给开发者看，两者不能混。**

### job_id 格式在入口就卡住

`CANONICAL_UUID` 这个正则在三个地方出现：工具 validator、`BackgroundJob.__post_init__`、持久化层的文件名解析。三层用的是**同一个常量**，不存在「一层严一层松」的漂移。

大写和乱码在 `registry.prepare()` 阶段就被拦下（`invalid_arguments`，**连 handler 都没进**）。为什么对大小写这么严？因为 job id 直接用作**文件名**。Windows 的文件系统大小写不敏感，Linux 敏感——只允许一种规范写法，跨平台行为才一致。

---

## 崩溃之后：interrupted 从哪来

### 一个必然存在的空档

进程被 `Ctrl+C` 干掉、被 OOM killer 干掉、机器断电——这些时候磁盘上会留下什么？

```text
t=0s     job 落盘，status = running
t=5s     命令还在跑
t=5s     💥 进程消失
         磁盘上：status = running，但世界上已经没有任何 worker 了
```

这条 `running` 记录**永远不会自己变成终态**。下次启动时，查询它会返回 `running`，模型会以为它还在跑，然后一直等。

### 恢复发生在构造函数里

`JobSupervisor.__init__` 最后一行就是启动恢复：

```python
# 立即执行启动恢复（把遗留 running 标记为 interrupted）
self._recover()
```

注意 Python 版和 TS 版的差异：**TS 版的 `#recover()` 是异步的（构造时存 promise、每个方法先 await）；Python 版是同步的**——构造函数直接跑完恢复才返回，所以不存在「恢复没跑完就被调用」的时序问题，也不需要 `ready()` 屏障。这是同步线程模型带来的简化。

恢复本身很短：

```python
def _recover(self) -> None:
    for job in self.store.interrupt_running():
        if job.result is not None:
            self.inbox.publish(self._event(job))
    self._ready = True
```

store 那一侧（`interrupt_running`）在锁内把所有 `running` 迁移成 `interrupted`，**只返回本次真正迁移的**。`test_json_store_conditionally_finishes_and_recovers_once` 实测幂等性：

```python
store.create_running(other, "call-2", "shell")
assert len(store.interrupt_running()) == 1     # 第一次：迁移 1 条
assert store.interrupt_running() == ()         # 第二次：幂等，无事发生
```

**恢复是幂等的**——连续崩 5 次再起来，模型也只会收到一份中断通知，不会收到 5 份。这一点在实践中很重要：如果恢复不幂等，模型的上下文会被历史垃圾越堆越满。

### 模型看到的恢复事件

上次崩溃留下的 job，在这次启动的第一轮就会被注入。事件里 `source_tool_call_id` 指向的是**上一个进程里的那次工具调用**，在当前会话的历史里根本不存在。这不是 bug——它是审计信息。模型不需要拿它去配对什么，因为事件是 `user` 消息，不需要配对。

**这也再次说明了为什么事件不能是 `tool` 消息。** 如果它是 `tool` 消息，就得有一个对应的 `tool_call`。而上个进程的 `tool_call` 已经随会话历史一起消失了，配都配不上。

---

## 接进组合根

### 能力位是双向配对的

`bootstrap.py` 的检查（`test_p13_profile_and_main_tools` 间接验证——构建必须成功）：

```python
if profile is P13:
    if background_supervisor is None:
        if background_store is None:
            raise ValueError("第十三章必须提供 background_store")
        background_supervisor = JobSupervisor(background_store, EventInbox())
    background_dispatcher = BackgroundDispatcher(background_supervisor)
    register_background_job_tools(tools, background_supervisor)
```

**有能力位没 Supervisor 时自动从 store 构造一个**——这是 Python 版与 TS 版的一处差异：TS 版两个方向都显式报错；Python 版缺 Supervisor 但有 store 时会补全，只有 store 也没有时才报错。两种做法共同保证的是**能力要么完整要么缺席，不能半开**：P13 声明了 `background` 能力，运行时就一定真的有后台执行。

### shell 的 background 开关怎么传下去

```python
create_chapter_one_tools(command, background=profile is P13)
```

一个布尔值从 bootstrap 走到工具工厂。**要判断某个 shell 实例有没有后台能力，只需要看它是被谁用什么参数造出来的。**

### 子 Agent 拿不到后台能力

这是本章一个容易被忽略但很重要的边界：

```python
# 子 Agent 的工具工厂
create_chapter_two_tools(command, actual_file_system, background=profile is P13)
```

等等——这一行**传了** `profile is P13`？看仔细：这是 Python 版与 TS 版的又一处真实差异。**Python 版的子 Agent 工具工厂没有硬编码 `False`**，主 Agent 和子 Agent 用同一个表达式。但子 Agent 仍然拿不到 `query_background_job` / `cancel_background_job`——`register_background_job_tools` 只在主 tools 上调用，子工厂里没有这一行。

| | 主 Agent | 子 Agent（Python 版） |
| --- | --- | --- |
| `shell` 有 `run_in_background` | 有 | **有**（与 TS 版不同） |
| `query_background_job` / `cancel_background_job` | 有 | **没有** |
| `tool_dispatcher` | `BackgroundDispatcher` | 无（直接 `tools.invoke`） |

注意第一行的后果：子 Agent 的 shell schema 声明了 `run_in_background`，但它的执行链上没有 Dispatcher（`tool_dispatcher` 只挂在主 AgentRunner 上）——**传了 `true` 也只会被同步执行**。这比 TS 版的「schema 层就隔离」更宽松，代价是子 Agent 传了参数却悄悄不生效。这是两个版本边界划法不同的真实取舍，如实记录。

### Supervisor 同时是三样东西

组装 `AgentRunner` 时，同一个对象被塞进三个不同的槽位：

```python
return AgentRunner(
    ...,
    tool_dispatcher=background_dispatcher,      # ① Dispatcher 持有（提交 job）
    event_pump=background_supervisor,           # ② eventPump（取事件）
    resources=(background_supervisor,),         # ③ resources（close 时收束）
)
```

**必须是同一个实例。** 如果 Dispatcher 提交到 A、Loop 从 B 取事件，那事件永远到不了 Loop。

`AgentRunner.close()` 按逆序关闭资源。后注册的先关。

### CLI 只在需要时构造

```python
background_store = JsonBackgroundJobStore(str(Path.cwd())) if profile 有 background else None
```

跑第 12 章时这里是 `None`，**不会创建 `.agent_tutorial/background/` 目录**。这是能力位设计的实际收益：**新增能力不改旧章节的运行时行为。**

### P13 的完整工具列表

`test_p13_profile_and_main_tools` 断言了 P13 主 Agent 的完整工具序列（15 个）：P12 的 13 个前缀 + `query_background_job` + `cancel_background_job`。

**只加了两个工具。** 这一章绝大部分工作量不在「加工具」上，而在 Dispatcher、Supervisor 状态机、事件注入通路这些**模型看不见的部分**：

| 改动 | 模型能不能看见 |
| --- | --- |
| `shell` 多了 `run_in_background` 参数 | 能看见（schema 变了） |
| 新增 2 个 job 工具 | 能看见 |
| `shell` 的 `concurrency` 标成 `background_eligible` | **看不见**（工具定义的元数据） |
| `BackgroundDispatcher` 接在 Loop 和 registry 之间 | **看不见** |
| `JobSupervisor` 的容量、超时、恢复 | **看不见** |
| 事件注入通路 | 看不见机制，只看见注入进来的 user 消息 |

**一个能力的「模型可见面」通常远小于它的实现量。** 这也是为什么本章反复强调持久化和不变量——**模型看不见的部分出错时，模型也帮不了你。**

---

## 运行第 13 章

### 第 0 步：准备环境

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch13_agent'
& ..\.venv\Scripts\python.exe -m pip install -e ".[dev]"
```

### 第 1 步：先跑测试（不需要 API Key）

```powershell
& ..\.venv\Scripts\python.exe -m pytest tests
```

期望最后一行：

```text
138 passed
```

（26 个测试文件；本章新增 `test_background.py` 6 个用例 + `test_ch13_integration.py` 1 个。）

### 第 2 步：真跑

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch13.cli --prompt "后台运行 npm install，同时读取 README.md；拿到结果后再总结"
```

这个 prompt 是刻意设计的：一件慢事 + 一件快事 + 一个需要两者结果的总结。

**你应该看到什么**（按时间顺序）：

```text
1. shell 被调用，几乎立刻返回 后台任务已提交: job_id=...; status=running
2. read_file 紧跟着返回 README 内容    ← 关键观察点：它没有等 npm install
3. 模型可能先给一句中间回答
4. 一条 runtime_event 出现，status 是 completed 或 failed
5. 模型给出最终总结
```

**关键观察点是第 2 步。** 如果 `read_file` 的结果是在 `npm install` 跑完之后才出现的，说明后台化没生效——去看排错表。

### 第 3 步：看磁盘上留下了什么

```powershell
Get-ChildItem .agent_tutorial\background\
Get-Content .agent_tutorial\background\*.json
```

每个 job 一个文件，文件名是 job id，五个键一个不多一个不少。

### 常见报错排查

| 现象 | 原因 | 怎么办 |
| --- | --- | --- |
| `配置错误: 缺少或填写错误的必要配置: ...` | `python/.env` 不存在或缺项 | 回第 1 章配置四个变量 |
| `ValueError: 第十三章必须提供 background_store` | 用 P13 但没传 store | 检查是不是绕过 `cli.py` 自己组装了 |
| 命令确实进了后台，但 `read_file` 还是等了很久 | 快命令本身慢（比如读一个巨大文件） | 用 `Measure-Command` 单独测一下 |
| `shell` 的参数里没有 `run_in_background` | 跑的是 ch12 或更早 | 确认命令是 `agent_ch13.cli` |
| `Error [invalid_arguments]` 查询 job | `job_id` 大写或格式不对 | 用占位结果里原样返回的那个 id |
| `Error [background_capacity]: 后台任务容量已满` | 同时超过 4 个 running job | 等一个结束，或者取消一个 |
| `Error [background_job_not_found]` | job_id 不存在 | 检查 id 是否抄对 |
| `Error [background_job_state]: 任务已经是终态` | 取消一个已完成的 job | 正常保护；去查结果而不是取消 |
| `ModuleNotFoundError: No module named 'agent_ch13'` | 没执行本章的 `pip install -e` | 回到第 0 步 |

### 一个坏文件毒化整个目录

在 `.agent_tutorial\background\` 里手工塞一个格式不对的文件，然后去查一个**完全正常的** job：

**预期**：读好文件也失败，报的是那个坏文件的名字。因为 `_read` 会在 `list_jobs` / `interrupt_running` 时把整个目录都解析一遍，任何一个文件坏了就整体抛错（fail closed，第 12 章定的原则）。清理办法就是把那个文件删掉。

### 动手小实验

这几个实验都不调真实模型。在 `tests/test_background.py` 末尾追加即可，固定 ID（`JOB_ID` / `EVENT_ID`）和辅助结构都是现成的。

**实验一：启发式到底认得哪些命令**

```python
for command in ["npm install", "NPM Install openai", "pytest -q", "npm ci", "npm test", "Get-ChildItem"]:
    print("后台" if should_run_in_background(command, None) else "同步", command)
```

**看第 4、5 行。** `npm ci` 和 `npm test` 都是货真价实的慢操作，但关键词表里只有 `npm install`，所以它们走同步。这不是 bug，是**启发式的固有代价**。

**实验二：显式参数一定压过启发式**

参考 `test_three_state_background_decision` 的四条断言。第 4 行是关键：`npm install` 命中关键词，但 `False` 让它走同步。**显式布尔值是命令，不是建议。**

**实验三：先落盘，再启动 worker**

参考 `test_supervisor_persists_before_worker_and_publishes_one_event`：在 operation 里读自己的 job 状态，断言读到 `running`。学到：worker 开始执行时磁盘记录已经存在——崩溃后至少能恢复成 `interrupted`。

**实验四：第二次收束会被拒绝**

参考 `test_json_store_conditionally_finishes_and_recovers_once` 的前半：同一个 job `finish_running` 两次，第二次返回 `None` 且不抛错。**这不是错误，是正常的竞速结果。**用返回值而不是异常表达「你来晚了」。

**实验五：崩溃恢复只发一个事件**

参考 `test_json_store_conditionally_finishes_and_recovers_once` 的后半：`interrupt_running()` 第一次迁移 1 条、第二次返回空元组。**恢复是幂等的**——连续启动两次，第二次没有任何 `running` 可迁移，不发任何事件。

**实验六：取消返回终态**

参考 `test_supervisor_cancel_returns_terminal_state`：operation 里 `cancel.wait(1)` 模拟响应取消，`cancel()` 返回的 job `status == "cancelled"`。学到：**取消不是「发出信号就返回」，而是「等它真的停下来」**。

**实验七：把落盘顺序反过来，看会坏成什么样**

写一个迷你实现：先启动线程再 `create_running`。让 operation 在落盘前就读磁盘——会读到 `background_job_not_found`。**这就是为什么 submit 的顺序是「容量 → 落盘 → 启动」。**

**实验完记得把这些追加的测试删掉或改回去。**

---

## 这章刻意没有做什么

一个能力清单容易让人以为「后台任务」已经做完了。其实 P13 只铺到了地基，有几件事是**明确决定不做**的：

| 没做的事 | 为什么不做 | 现在的替代 |
| --- | --- | --- |
| 抢占式取消 | Python 线程没有安全强杀机制（`Thread.kill()` 不存在） | 协作式：cancel_event + 等待 + 超时放弃 |
| 把 `cancel_event` 传给工具 handler | 工具签名改动牵动所有工具 | 只在 Supervisor 层判定，工具本身不感知 |
| 任务排队 | 排队会让「提交成功」变成谎言 | 容量满就报 `background_capacity` |
| 进度事件（0%→100%） | 需要工具侧协议，超出本章范围 | 只有终态一个事件 |
| 跨进程共享 job | 需要真正的 IPC 或 daemon | 每个进程一个 Supervisor，靠恢复兜底 |
| 子 Agent 的 job 工具 | 会让 job 归属和生命周期变复杂 | 子 Agent 没有查询/取消工具 |
| 自动重试 | 重试策略是业务决策，不是运行时职责 | 终态如实上报，模型自己决定重试 |
| 输出停滞看门狗 | 交互式提示检测超出本章范围 | 只有全局超时 |

### 特别说一下「进度事件」

很多人第一反应是「后台任务当然要能报进度」。但想想那条事件通路：

```text
每次进度更新 → 一个 RuntimeEvent → 一条 user 消息 → 塞进 history
```

一个跑 3 分钟、每秒报一次进度的构建任务，会往上下文里塞 180 条消息。**上下文预算会被进度条吃光。**

真要做，正确的做法不是「多发事件」，而是「让模型主动查」——这正是 `query_background_job` 存在的意义。**拉取比推送更省上下文**，这是 Agent 场景和普通 UI 场景很不一样的地方。

---

## 与 Claude Code 的差异

参照 `learn-claude-code/s13_background_tasks/README.md` 中对 CC 源码的分析，P13 的教学简化与 Claude Code 的后台任务系统存在以下差异：

**后台任务类型。** CC 定义了 7 种后台任务（`Task.ts:7-13`）：`local_bash`、`local_agent`、`remote_agent`、`in_process_teammate`、`local_workflow`、`monitor_mcp`、`dream`，每种有自己的注册、生命周期和通知机制。P13 只后台化了 P13 主 Agent 的 shell 工具。

**线程模型。** CC 运行在 Node.js/Bun 单线程事件循环中，「后台」只是不 await。`ShellCommand.background(taskId)` 把 stdout/stderr 重定向到文件，让进程独立运行而不阻塞主循环。P13 用 `JobSupervisor` 管理受管 worker 线程（`threading.Thread(daemon=True)`）、容量、超时、取消和关闭，通过持久化 `JsonBackgroundJobStore` 保证进程崩溃后可恢复。

**三态 vs 布尔。** CC 的 bash 工具 schema 有 `run_in_background: boolean` 参数（`BashTool.tsx:241`），模型显式指定。P13 的三态语义里 `None` 才走关键词启发式兜底。P13 的启发式没有相应的 CC 实现痕迹。

**通知队列。** CC 后台任务完成后通过 `enqueueTaskNotification` 入队到共享命令队列。通知格式是结构化 XML `<task_notification>`，优先级分 `next`/`later`。P13 用 typed `RuntimeEvent` / `EventInbox` FIFO 队列，事件与 `tool_call_id` 解耦，不区分优先级。

**停滞看门狗。** CC 后台 bash 任务有一个输出停滞看门狗（45 秒无输出增长后检测交互式提示），防止后台任务卡在无人响应的交互式对话框。P13 支持全局超时和显式取消，但没有输出停滞检测。

**并发限制。** CC 前台工具调用有 `CLAUDE_CODE_MAX_TOOL_USE_CONCURRENCY`（默认 10）；后台 bash 任务没有硬性限制，它们是独立子进程。P13 的 `JobSupervisor` 默认容量为 4，超载时返回 `background_capacity` 错误。CC 没有类似的容量拒绝机制。

**状态能力。** CC 的 `LocalShellTaskState` 支持停止运行中的后台任务和读取后续输出（重定向文件按需读取下一段）。P13 的后台 job 支持取消和超时，但无法在 worker 运行中流式读取部分输出；终态结果是一次性完整交付的。

---

## 从 ai-agent-book 的 Flux 实验中学到什么

`ai-agent-book/chapter4/async-agent/README.md` 是配套实验 4-5 的事件驱动异步框架，代号 Flux。它和本章走的是同一条路线：**异步工具先返回一个占位 `task_id`，任务完成后再以新事件注入轨迹。** P13 直接借鉴了两处：按 id 查询/取消后台 job，以及一次批量注入多条已就绪事件。

### Flux 的实验设计

Flux 把事件按紧急度分成三类：

```text
INTERRUPT  → 立即取消当前 turn 和所有后台任务
IMMEDIATE  → 立即回答，但不打断后台任务
DEFERRED   → 进入 pending 缓冲，等异步结果到达时一次性批量追加
```

它把状态检查点保存到 `trajectory + task progress`，恢复时把 running 任务标为 `suspended`，**让上层决定是重跑还是续跑**。

### P13 的对照与取舍

| 维度 | Flux | P13 | P13 这样选的原因 |
| --- | --- | --- | --- |
| 查询/取消 | `query_task` / `cancel_task`，可查流式进度 | `query_background_job` / `cancel_background_job`，running 时 `result: None` | 不保留进度就不用维护进度协议 |
| 批量事件 | 有独立 pending buffer + 紧急度分类 | `drain_events()` 取整批，按 `event_id` 去重，带 `batch` 位置 | 少一个缓冲层，事件统一在下一轮模型请求前可见 |
| 打断 | 用户可 `INTERRUPT` 取消当前 LLM turn | 只有显式取消工具 + close 时统一取消 | 打断当前模型请求需要改 Loop 的核心时序 |
| 持久化 | 完整 trajectory + progress，恢复为 `suspended` | 每个 job 一份终态 JSON，遗留 running → `interrupted` | 终态快照够用，且不需要恢复中间进度 |
| 安全边界 | 只跑白名单 Python 子进程，不调 shell | 后台化的是已过 Hook、权限、审批的真实 PowerShell 工具 | 因为跑真实工具，所以必须有容量、超时、关闭边界 |

### 最值得注意的一条差异：suspended vs interrupted

这两个词看起来像同义词，但它们代表**完全不同的责任划分**：

```text
Flux:  running → suspended   ← 「还没结束，等上层决定重跑还是续跑」（中间态）
P13:   running → interrupted ← 「已经结束了，结论是失败」（终态）
```

`suspended` 意味着框架承诺保留足够信息让任务能续上——代价是持久化格式变复杂，而且「续跑」对很多操作根本不成立（`npm install` 中途被杀，续跑意味着什么？）。

`interrupted` 意味着框架只承诺「告诉你它没跑完」。要重试就让模型重新调一次工具——**重试策略回到模型手里，运行时不猜。**

回想六个状态那一节的不变量：`running` 是唯一的活状态。如果引入 `suspended`，这条不变量就破了。**P13 用「少一个状态」换掉了「续跑能力」**，这个交换在教学场景里是划算的。

### 为什么安全边界那一条最容易被忽略

Flux 只跑白名单 Python 子进程，所以它可以不太在意「后台任务会干什么」。P13 后台化的是**真实的 shell 工具**，一个 `docker build` 可能跑 10 分钟、吃满 CPU、写一堆文件。

所以 P13 必须有 Flux 不需要的三样东西：

```text
容量 4       ← 防止模型一口气提交 50 个构建
超时 120s    ← 防止一个卡死的命令永久占位
close 超时   ← 防止进程退不出去
```

这是一条通用规律：**你的后台任务能做的事越危险，你需要的边界就越多。**

---

## 配套代码索引

| 文件 | 行数 | 本章的角色 |
| --- | --- | --- |
| `agent_ch13/features/background.py` | 792 | 全章核心。`BackgroundJob` 领域模型、六个状态、`JobSupervisor`、`BackgroundDispatcher`、两个 job 工具、`should_run_in_background` |
| `agent_ch13/adapters/background_json.py` | 142 | `JsonBackgroundJobStore`：`finish_running` 条件迁移、`interrupt_running` 批量恢复、原子替换 |
| `agent_ch13/core/events.py` | 83 | `RuntimeEvent` 契约、`EventInbox` typed-only FIFO、`runtime_event_message` 把事件变成 user 消息 |
| `agent_ch13/core/loop.py` | 439 | `RuntimeEventPump` 契约、注入点、`_inject_runtime_events` 去重与批量、`_execute_tool` 的权限先行顺序 |
| `agent_ch13/features/builtin_tools.py` | — | `create_shell_tool(runner, background)` 的双 schema 分支与 `concurrency` 标记 |
| `agent_ch13/bootstrap.py` | — | 组合根。P13 自动构造 Supervisor、Supervisor 一实例三角色 |
| `agent_ch13/cli.py` | — | 真实入口。按能力位决定是否构造 Supervisor 与 store |
| `agent_ch13/core/profiles.py` | — | `P13 = P12 + "background"` 的能力位定义 |
| `tests/test_background.py` | — | 三态判断、typed-only inbox、状态不变量、条件迁移、先落盘后 worker、取消收束（6 个用例） |
| `tests/test_ch13_integration.py` | — | P13 工具序列、shell schema 差异、Supervisor 资源接线（1 个用例） |

读代码的建议顺序：

```text
① core/events.py 全文（83 行）
   → 先建立「事件是什么」的概念，这是最小的一块

② features/background.py 的 BackgroundJob.__post_init__
   → 四条状态不变量，看清一个 job 的合法形状

③ features/background.py 的 should_run_in_background
   → 三态判断，全章最短的一段核心逻辑

④ features/background.py 的 submit
   → 「先落盘，再启动 worker」的顺序

⑤ features/background.py 的 _run_worker
   → 状态判定 + 条件迁移 + 发事件

⑥ adapters/background_json.py 的 finish_running
   → 条件迁移，理解「只发一次」怎么实现的

⑦ core/loop.py 的 _inject_runtime_events
   → 事件怎么回到 history

⑧ tests/test_background.py
   → 用测试反查你有没有漏掉的分支
```

---

## 本章小结

### 三句话版本

第 13 章让 Agent 在等待慢操作时还能继续干活。做法是：慢操作交给一个受管的 `JobSupervisor` 去跑，工具立刻返回一个占位结果保持协议闭合，任务结束后把终态包成 typed 事件、以 user 消息的形式在下一轮模型请求前注入回同一个 Loop。所有「看起来更方便」的做法——排队而不报错、把事件当第二条 tool 结果、先提交再查权限、允许一个中间态 `suspended`——都会在某个具体场景下把正确性弄丢。

### 一定要记住的六条

1. **权限先于提交。** 被拒绝的调用必须做到零副作用、零磁盘痕迹。顺序错了，一次被拒绝的操作会在下次启动时以 `interrupted` 事件冒出来。
2. **占位结果闭合协议，但不撒谎。** 它只说「已受理，id 是 X」，不说「已完成」。
3. **一活五终，终态不可变。** `running` 是唯一活状态。少一个中间态，就少一整类「这到底算不算结束」的判断。
4. **条件迁移仲裁竞速。** 谁先写进磁盘谁定终态，晚到的一方拿到 `None` 并安静跳过发事件。事件恰好发一次，靠的不是内存里的标记位，而是磁盘上的状态。
5. **事件是 user 消息，不是 tool 结果。** 后台结果和原来那次 `tool_call` 已经没有配对关系了，硬塞成第二条 `tool` 消息会直接破坏协议。
6. **协作式取消，等不到就诚实报错。** Python 线程没有安全强杀机制，`close()` 能做的是发信号、等、超时放弃。它不保证关干净，只保证如实告诉你。

### 检查你是否真的读懂了

不看前文回答这八个问题：

1. `run_in_background` 为什么是三态而不是布尔？`None` 和不传有区别吗？
2. 一个工具要走后台，需要同时满足哪两个条件？只满足一个会怎样？
3. 占位结果为什么是 `tool_success` 而不是 `tool_error`？
4. 为什么后台 job 不能做成 Task 的第四个状态？
5. 「超时」和「完成」在同一毫秒到达，谁赢？输的那一方会发生什么？
6. 后台任务的终态为什么不能作为第二条 `tool` 消息注入？
7. 进程被 kill 之后重启，一个残留的 `running` 会变成什么？恢复跑两次会发几个事件？
8. Python 版的超时判定和 TS 版的定时器打断有什么不同？代价是什么？

答不上来的，回去看对应小节：

| 问题 | 回去看 |
| --- | --- |
| 1 | 「三态：每一态代表一个决策归属」 |
| 2 | 「两道门，缺一不可」 |
| 3 | 「为什么它是 `tool_success`」 |
| 4 | 「三种「等待」要分清」 |
| 5 | 「只发一次：条件迁移」+ 实验四 |
| 6 | 「事件是 user 消息，不是第二条 tool 结果」 |
| 7 | 「崩溃之后：interrupted 从哪来」+ 实验五 |
| 8 | 「worker 的状态判定」 |

---

下一篇进入 Cron 调度。后台任务解决的是「慢操作不占住 Loop」，但触发它的仍然是模型的一次工具调用——**没有人说话的时候，Agent 什么也不会做。** P14 会加上时间驱动的调度器：表达式解析、时区、持久化 outbox，让「每天早上九点检查一次依赖更新」这种事不需要人来点一下。事件边界的原则不变，变的是事件从哪来。
