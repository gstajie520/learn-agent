# 从“单兵作战”到“自组织团队”，多 Agent 协同的必经之路是什么？（Python版）

> 本文是第 16 章 TypeScript 原文的 Python 迁移版。重点不是再增加几个工具，而是把队友协作升级为可恢复、可审计的结构化协议。

## 一、消息送达不等于协议完成

第 15 章已经有 Mailbox 和持久队友，但“计划审批”和“优雅关机”如果只发普通文本，就会遇到三个问题：模型无法可靠判断状态、重复响应可能覆盖结论、进程重启后无法知道请求是否已经完成。

Java 类比：`ProtocolRequest` 像持久化的状态聚合，`JsonProtocolStore` 像 Repository，`ProtocolRuntime` 像领域 Service，`PermissionRule` 像执行前的拦截器。

## 二、ProtocolRequest 是状态真相

协议请求保存这些字段：

```text
id / kind / sender / target / status / content
created_at_utc / expires_at_utc / resolution
```

`status` 只有 `pending`、`approved`、`rejected`。`resolution` 记录唯一响应消息 UUID、结构化 `approved` 布尔值、反馈正文和完成时间。状态不能从自然语言推断。

第 16 章有两种请求：

- `shutdown`：Lead 请求队友完成当前消息后优雅关闭；
- `plan_approval`：队友提交计划，Lead 明确批准或拒绝。

## 三、先登记，再发送

创建协议请求时必须先写入 `state.json`，再发送 typed Mailbox 消息：

```text
create pending request
        |
        v
send shutdown_request / plan_approval_request
        |
        v
worker or Lead consumes response
```

如果发送失败，pending 请求不能回滚成“从未发生”，因为它需要保留给诊断和恢复流程。

## 四、typed Mailbox，而不是 content JSON

普通消息只有 `task/message/result`。协议消息使用独立 kind：

```text
shutdown_request
shutdown_response
plan_approval_request
plan_approval_response
```

协议消息还必须携带：

```text
request_id / approved
```

请求的 `approved` 必须是 `null`，响应必须是布尔值。发送方、接收方、正文、kind 和 request_id 都要与 ProtocolRequest 完整匹配，否则消息进入 quarantine，不能阻塞其他合法消息。

## 五、优雅关机的确定性路由

`shutdown_request` 不进入模型。队友 Runtime 直接：

1. 校验请求和当前队友身份；
2. 标记队友进入 shutdown；
3. 发送 `shutdown_response(approved=true)`；
4. ack 原 shutdown 请求；
5. 不再发起新的模型调用。

这和 Java 中收到 `ShutdownCommand` 后走确定性状态机类似，不应该让 LLM 自己决定是否关闭。

## 六、计划审批与 plan gate

队友可以调用 `submit_plan`，Lead 通过 `review_plan(request_id, approve, feedback)` 给出结构化结论。计划审批响应到达后，复用同一个队友 Runner 和历史，生成恢复提示继续工作。

真正重要的是 `plan_gate`：

```text
没有计划                 -> 允许原有行为
最新计划 pending/rejected -> 拒绝 effectful 工具
最新计划 approved        -> 允许 effectful 工具
```

这条规则位于权限策略中，运行在 handler 和后台提交之前。Prompt 不能授予写文件、执行命令等副作用权限；只有持久化的 approved 状态才能放行。

## 七、推荐考古顺序

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch16_agent'
& ..\.venv\Scripts\python.exe -m pytest tests/test_ch16_protocol.py
& ..\.venv\Scripts\python.exe -m pytest tests/test_ch16_bootstrap.py
& ..\.venv\Scripts\python.exe -m ruff check agent_ch16 tests
& ..\.venv\Scripts\python.exe -m mypy agent_ch16 --no-incremental
```

先看 `ProtocolRequest` 字段，再看 `JsonProtocolStore` 的状态迁移，然后看 `ProtocolRuntime.route_teammate_message()`，最后看 `plan_gate_rule` 如何接入 PermissionPolicy。阅读路径仍然是 Java 熟悉的“领域模型 -> Repository -> Service -> 权限拦截 -> 集成测试”。

## 八、本章边界

本章没有实现完全去中心化的 Agent Team，也没有让队友自行审批自己。Lead 仍然是计划审批者，Mailbox 仍然是持久传输边界，ProtocolStore 仍然是请求状态真相。后续章节可以在这些稳定边界上继续加入更复杂的协作拓扑。
