"""外部系统适配器。

这是什么：
    基础设施层的适配器集合，负责与外部系统（模型 API、操作系统、文件系统）交互。

Java 类比：
    类似六边形架构中的 Adapter 层或 Spring 的 Infrastructure 层，实现核心领域定义的接口。

为什么需要：
    - 隔离外部依赖，核心业务不直接依赖 OpenAI SDK 或 subprocess
    - 让测试能够用 Fake 替换真实适配器
    - 符合依赖倒置原则：核心层定义接口，适配器层提供实现
"""
