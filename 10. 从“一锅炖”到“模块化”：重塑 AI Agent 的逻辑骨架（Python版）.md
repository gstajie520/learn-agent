# 从「一锅炖」到「模块化」：重塑 AI Agent 的逻辑骨架（Python 版）

> 本文完整保留原第十章的结构和技术论证，并把配套实现迁移为 Python。配套项目位于 `python/ch10_agent`。
> 面向 Java 后端开发者，文中会用 Renderer、Provider、`Supplier<String>` 和
> Spring `@Configuration` 对照 Python 的 Protocol、组合根与对象引用。

第 9 章做完之后，Agent 已经有了相当完整的能力栈：记忆系统、上下文压缩、技能懒加载、权限管道、子 Agent 调度。每加一个能力，system prompt 就多一段。到第 9 章结束时，那个字符串大概长这样：

```python
system_prompt = " ".join(
    (
        "You are a coding agent.",
        "Use tools when needed and inspect their results.",
        "For complex tasks, keep the TODO snapshot current.",
        # 工具、Skill、workspace 和记忆不在这里硬编码。
    )
)
```

没人敢改它。不知道哪段是必须的，哪段是某次调试留下的，改一处会不会影响其他地方。项目切换时要整个重写，但又不确定哪些该留、哪些该扔。

这不是 prompt 写得好不好的问题，是结构问题。一大段硬编码字符串本质上是把配置藏在代码里，和把数据库连接字符串写死在业务逻辑里是同一类错误。

---

## 本章导读

### 读完这一章，你能做到什么

1. 说清「硬编码 system prompt」具体会带来哪三个可复现的工程问题，而不是笼统地说「不优雅」。
2. 复述一次系统提示从「运行态对象」到「发给模型的字符串」经过的三个角色，并说明每个角色为什么不能省。
3. 背出五个 section 的固定顺序，并解释为什么「顺序」本身就是一份契约。
4. 判断一个 context 值会被接受还是被拒绝，并说出被拒绝时抛的是哪个错误。
5. 解释为什么缓存必须挂在实例上而不是模块全局，以及缓存键少覆盖一个输入会导致什么后果。
6. 说明为什么本章的缓存**不等于**供应商 API 的 prompt cache，不要把两件事混着宣称。
7. 分清哪些运行态变化会在下一轮生效、哪些必须重建 Agent 才生效。
8. 说清第 9 章的记忆注入路径为什么必须在第 10 章关掉，不关会看到什么现象。

### 你需要先具备什么

| 前置 | 出自哪一章 | 本章怎么用它 |
| --- | --- | --- |
| Agent Loop 每轮如何组装模型请求 | 第 1 章 | 系统提示在每轮组装请求前渲染 |
| `ToolRegistry` 与工具快照 | 第 2 章 | `tools` section 直接读注册表当前名称 |
| workspace 绝对路径与硬边界 | 第 2 章 | `workspace` section 输出 `resolve()` 后的绝对路径 |
| Profile 能力位与组合根 | 第 5～9 章 | P10 靠一个 `dynamic_prompt` 能力位切换渲染路径 |
| Skill 目录与正文分离 | 第 7 章 | `skills` section 只放目录，正文仍靠 `load_skill` |
| `MemorySession.selected` 与 `TurnLifecycle` | 第 9 章 | **本章最关键的前置**，记忆注入要从旧路径搬到新路径 |

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| 动态系统提示 | 每轮模型请求前按当前运行态重新渲染出来的 system prompt |
| section | 系统提示里一个带 `## 标题` 的独立段落，本章固定五个 |
| `DynamicPromptRenderer` | 纯渲染器：拿到运行态对象，按固定顺序拼出字符串 |
| `DynamicPromptProvider` | 适配器：把渲染器和运行态对象绑在一起，对外只暴露零参数 `render()` |
| `SystemPromptProvider` | Loop 侧的接口（Python `Protocol`），只有一个零参数 `render() -> str` |
| 运行态取值 | 渲染时才去读数据源当前值，不在构建期把值拷成字符串 |
| 规范 JSON（canonical JSON） | 按键排序、紧凑输出的唯一序列化形式，语义相同必然文本相同 |
| 缓存键 | 覆盖全部模型可见输入的规范 JSON 字符串，用来判断能否复用上次结果 |
| 会话隔离 | 缓存挂在 renderer 实例上，两个 Agent 实例互不影响 |
| API prompt cache | 供应商侧按请求前缀命中的缓存，和本章的进程内缓存**不是一回事** |
| 有界目录 | 只含名称和一行描述的轻量索引，不含正文 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先看一眼渲染结果长什么样     ← 三个阶段，看 section 怎么长出来
  ↓
② 三个真实的痛点               ← 为什么必须拆，不是审美问题
  ↓
③ 三个角色，一条渲染链         ← 本章的架构骨架：Renderer / Provider / Loop
  ↓
④ 五个 section 与固定顺序      ← 顺序是契约，空值也有明确形状
  ↓
⑤ Context 必须是严格 JSON      ← 为什么不能用 default=str 糊过去
  ↓
⑥ 缓存必须属于会话             ← 挂错位置会串会话；缓存键少一项会发过期指令
  ↓
⑦ 接入唯一 Loop                ← 零参数契约，以及失败为什么不回退
  ↓
⑧ 记忆只注入一次               ← 第 9 章的旧路径必须关掉
  ↓
⑨ 运行 + 实验 + 排错           ← 动手验证本章所有结论
  ↓
⑩ 小结 + 自测                  ← 记住五条 + 边界表 + 自测题
  ↓
⑪ Java 开发者先看这里          ← 源码考古顺序
```

---

## 先看一眼渲染结果长什么样

抽象讲「模块化」不如直接看输出。下面是 renderer 在三个阶段渲染出来的真实结果（`test_prompting.py` 断言过的形状，用一个临时 workspace）。

**阶段 1：空注册表，没有 Skill，没有记忆。**

```text
## identity
agent
context: {"chapter":10}

## tools
(none)

## workspace
C:\Users\你的用户名\AppData\Local\Temp\probe1-xxxxx
```

三个 section。注意 `tools` 下面那个 `(none)`——没有工具时不是省略这个 section，而是明确写出「当前没有工具」。后面会讲为什么。

**阶段 2：注册两个工具，其他不变。**

```text
## identity
agent
context: {"chapter":10}

## tools
- read_file
- todo_write

## workspace
C:\Users\你的用户名\AppData\Local\Temp\probe1-xxxxx
```

只有 `tools` section 变了。这里没有任何人手工去改字符串——`ToolRegistry` 里多了两个工具，渲染时读到的就是新列表（`test_zero_argument_provider_reads_live_tool_state` 断言 `## tools\n(none)` 变成 `## tools\n- inspect`）。

**阶段 3：加上一个 Skill 和一条被选中的记忆。**

```text
## identity
主智能体
context: {"flags":[true,null,1.5],"mode":"编码","nested":{"a":1,"b":2}}

## tools
- read_file
- todo_write

## workspace
<绝对路径>

## skills
- **sql-style**: SQL 编写规范

## memory
<relevant_memories>

## database (project)

生产数据库约束

始终使用真实数据库。

</relevant_memories>
```

五个 section 全部出现。两件事值得先记住：

- `skills` section 里只有 `sql-style` 的**名称和一行描述**，Skill 文件里的正文一个字都没进来。正文仍然要靠 `load_skill` 显式加载。
- `memory` section 里只有被选中的那条记忆。同一个 store 里还有一条 `keyboard` 记忆没被选中，它的正文也一个字都没进来。

这两条在 `test_renderer_outputs_fixed_sections_without_private_bodies` 里有逐字断言：整个 prompt 和期望值**完全相等**（不是「包含」），且 `"PRIVATE UNSELECTED MEMORY" not in prompt`。

### 真实运行时它长什么样

上面用的是最小输入。跑真实的 P10 Agent 时（`test_ch10_integration.py`），`identity` section 是组合根拼好的整段基线指令（`SYSTEM_PROMPT` + TODO 提示），`tools` section 是完整的 8 个工具：

```text
## tools
- shell
- read_file
- write_file
- edit_file
- glob
- todo_write
- task
- load_skill
```

这里有个容易看漏的点：**`identity` 参数收到的不是「你是谁」这一句，而是组合根拼好的整段基线指令**。`bootstrap.py` 把基线 prompt 当作 `identity=prompt` 传进去，所以 identity section 里有行为基线加一行 context。这是当前实现的真实形状，不是笔误。

---

## 三个真实的痛点

在拆解解法之前，先把问题说清楚——不是泛泛的「不够优雅」，是三个具体会出问题的地方。

### 痛点一：换项目要重写整个 prompt，但不知道哪些该改

当前这个 Agent 是 coding agent，prompt 里有工作目录、工具列表、行为规范。换一个项目，工作目录变了，工具可能变了，但身份定义和行为规范也许还能复用。

硬编码成一段字符串之后，这些边界完全不可见。你面前只有一坨文本，唯一能做的是全部重写，或者小心翼翼地做字符串替换——而字符串替换在 prompt 上格外危险，因为改错了不会报错，只会让模型行为变得微妙地不对。

拆成 section 之后，边界变成可见的：`identity` 可复用，`workspace` 必须换，`tools` 自己会跟着注册表变。

### 痛点二：改一处可能悄悄影响全局

加一段工具描述，可能和前面某条行为规范在语义上冲突。LLM 会怎么理解这个冲突，完全不可预测。

这类 bug 最难缠的地方在于：**它不报错。** 类型检查过，测试过（如果测试没断言 prompt 内容），CI 全绿，只是 Agent 偶尔做出奇怪的选择。排查时你甚至不会怀疑到 prompt，因为「prompt 又没改过」——但它上周确实被加了一段。

按 section 拆开、并且用测试断言 section 顺序和内容之后，这类改动至少变成可见的：加的东西落在哪个 section、有没有挤到别人前面，都是能断言的。

### 痛点三：每次请求带全部内容，但并不是每次都需要

记忆内容只在本轮真的选中了相关记录时才有意义。如果这一轮什么都没选中，还留一段「当前没有相关记忆」的占位说明，就是纯噪音。

System prompt 每轮都会发送。无关指令不只是浪费 token，更重要的是**稀释注意力**——这是第 5 章讲过的问题，上下文里无关内容越多，关键指令的相对权重越低。

所以本章的规则是：`skills` 和 `memory` 两个 section 在没内容时**整段省略**，不留占位。

### 三个痛点对应三个设计决定

| 痛点 | 本章的对策 | 在哪一节展开 |
| --- | --- | --- |
| 边界不可见 | 拆成五个具名 section，各有唯一数据源 | 五个 section 与固定顺序 |
| 改一处影响全局 | 顺序固定成契约，用测试断言 | 同上 |
| 无关内容占位 | 可选 section 无内容时整段省略 | 同上 |
| 顺带解决：值会过期 | 运行态取值，每轮重新读 | 三个角色，一条渲染链 |

---

## 三个角色，一条渲染链

这一节是本章的架构骨架，值得慢一点看。

系统提示从「运行态对象」变成「发给模型的字符串」，中间经过三个角色。它们的职责严格不重叠：

```text
运行态数据源                     三个角色                        模型
─────────────                 ──────────                     ────

ToolRegistry ─┐
SkillRegistry ─┤
MemorySession ─┼──> DynamicPromptProvider ──> DynamicPromptRenderer ──> system 消息
workspace 路径 ─┤    （绑定 + 零参数适配）      （校验 + 规范化 + 拼装）        ↑
identity/context ┘              ↑                                        │
                                │                                        │
                          AgentRunner 每轮请求前调用 render() ─────────────┘
```

| 角色 | 唯一职责 | 明确不做的事 |
| --- | --- | --- |
| `DynamicPromptRenderer` | 校验输入、规范化 context、按固定顺序拼字符串、维护缓存 | 不发现依赖，不读磁盘，不选记忆 |
| `DynamicPromptProvider` | 在构建期绑定 renderer 和运行态对象，对外暴露零参数 `render()` | 不改渲染逻辑，不加自己的缓存 |
| `AgentRunner` | 每轮模型请求前调一次 `render()`，校验返回值 | 不知道有几个 section，不知道数据从哪来 |

### 为什么要有 Provider 这一层

第一次看这个设计，很自然会问：既然 renderer 已经能渲染了，为什么不让 `AgentRunner` 直接调 `renderer.render(...)`？

因为那样 Loop 就必须知道每一个数据源。它得持有 `ToolRegistry`、`SkillRegistry`、`MemorySession`、workspace、context……然后每加一个 section，Loop 的构造参数就要多一个。这正是本章想消除的耦合，只是从字符串搬到了参数列表。

Provider 的作用就是把这堆绑定关系收在**组合根**里。对 Loop 来说，系统提示只是一个接口：

```python
class SystemPromptProvider(Protocol):
    """零参数提示词提供者，类似 Java 的 Supplier<String>。"""

    def render(self) -> str: ...
```

一个方法，零参数。Loop 不知道背后有五个 section，也不知道记忆是什么。

Provider 的实现只做转发，不做决策：

```python
def render(self) -> str:
    return self._renderer.render(
        identity=self._identity,
        tools=self._tools,
        workspace=self._workspace,
        context=self._context,
        skills=self._skills,
        memory=self._memory,
    )
```

注意 `skills` 和 `memory` 是可选能力，Python 版用 `None` 表示「没有这个能力」——renderer 侧判断 `skills is None` 就跳过对应 section，「没有这个能力」和「这个能力当前是空的」是两种不同状态（`None` 时连 `render_catalog()` 都不会调）。

### 组合根怎么接线

`bootstrap.py` 里 Provider 的构建是这样的：

```python
system_prompt_provider = (
    DynamicPromptProvider(
        DynamicPromptRenderer(),          # 无状态 Renderer，可复用
        identity=prompt,                  # 静态身份部分（包含 TODO 提示）
        tools=tools,                      # 引用传递：下一轮能读取新注册的工具
        workspace=workspace,
        context={"chapter": profile.chapter, "identity": "user"},
        skills=skill_registry,            # 引用传递：下一轮能读取新加载的 Skill
        memory=memory_session,            # 引用传递：下一轮能读取新选中的记忆
    )
    if "dynamic_prompt" in profile.capabilities
    else None  # 旧章节兼容：不启用动态 Prompt
)
```

三个细节：

第一，`DynamicPromptRenderer()` 是**在这里 new 出来的**。每次 `build_agent()` 都得到一个新 renderer，这是后面「缓存必须属于会话」能成立的前提。

第二，传进去的 `tools`、`skill_registry`、`memory_session` 都是**对象引用**，不是它们当前的字符串形态。所以后续这些对象内部状态变了，renderer 下次读到的就是新值。

第三，整个 Provider 只在 `dynamic_prompt` 能力位打开时才创建。P09 及之前没这个能力位，`system_prompt_provider` 是 `None`，Loop 退回用构建期的固定字符串。旧章节行为一行都没改。

---

## 五个 section 与固定顺序

当前实现没有维护一份 `PROMPT_SECTIONS` 字符串字典，也没有手写 `enabledTools` 列表。`DynamicPromptRenderer` 直接读取已经参与运行的对象，输出五个 section：

| Section | 策略 | 数据来源 | 没内容时 |
| --- | --- | --- | --- |
| `identity` | 始终 | 基础行为指令 + 规范化 JSON context | 不可能为空（空 identity 直接抛错） |
| `tools` | 始终 | `ToolRegistry.names` 当前快照 | 输出 `(none)` |
| `workspace` | 始终 | `Path(workspace).resolve()` 的绝对路径 | 不可能为空（空字符串直接抛错） |
| `skills` | 按需 | `SkillRegistry.render_catalog()`，不含正文 | **整段省略** |
| `memory` | 按需 | `MemorySession.render_selected()`，本轮选中的记忆 | **整段省略** |

顺序是契约：`identity -> tools -> workspace -> skills -> memory`。

### 为什么「顺序」要当成契约

一个常见的实现是把 section 放进字典，再遍历键输出。那样顺序就依赖键的插入顺序——一个能被重构悄悄改掉的东西。

本章用列表，顺序写死在代码里：

```python
# 固定顺序组装 section：identity → tools → workspace → skills → memory
sections = [
    f"## identity\n{normalized_identity}\ncontext: {context_json}",
    f"## tools\n{tool_catalog}",
    f"## workspace\n{resolved_workspace}",
]
if skill_catalog:  # 有 Skill 才添加此 section
    sections.append(f"## skills\n{skill_catalog}")
if memory_body:  # 有选中记忆才添加此 section
    sections.append(f"## memory\n{memory_body}")
prompt = "\n\n".join(sections)
```

两条规则从这段代码直接读得出来：

1. **前三个 section 无条件存在**，位置永远是 0、1、2。
2. **可选 section 只能 append 到尾部**，永远不会插到前面三个中间。

测试断言的不是「包含某段文本」，而是相对位置（`test_ch10_integration.py`）：

```python
assert prompt.index("## identity") < prompt.index("## tools")
assert prompt.index("## tools") < prompt.index("## workspace")
assert prompt.index("## workspace") < prompt.index("## skills")
assert prompt.index("## skills") < prompt.index("## memory")
```

这样任何人把 `append` 改成 `insert(0, ...)`，或者把某个 section 挪个位置，测试立刻红。

### 为什么没有工具时要写 `(none)`，而 Skill 为空就省略

这两个决定看起来矛盾，其实针对的是两种不同情况。

```python
# 工具列表为空也输出 "(none)"，让模型明确知道当前没有可用工具。
tool_catalog = "(none)" if not tool_names else "\n".join(f"- {name}" for name in tool_names)
```

**工具是核心能力，「没有工具」是一个需要模型知道的事实。** 如果整段省略，模型看到的是「这里本该有工具列表但什么都没说」，它可能仍然尝试调用工具，然后拿到一堆错误。明确写 `(none)`，模型就知道这一轮只能靠纯文本回答。

**Skill 和记忆是补充信息，「没有」就等于「这件事不存在」。** 写一段「当前没有可用 Skill」没有任何指导价值，只是占位噪音——正好是痛点三要消除的东西。

判断标准可以这样记：**这个 section 为空时，模型的行为应该改变吗？** 工具为空，行为要变（别调工具）；Skill 为空，行为不变（本来就没这个选项）。

### section 之间是空行分隔

`"\n\n".join(sections)` 意味着 section 之间隔一个空行。`test_renderer_outputs_fixed_sections_without_private_bodies` 用整串相等断言钉住了每个 `\n\n` 的确切位置。

---

## Context 必须是严格 JSON

`context` 用来放章节号、identity、运行模式这类小型结构化状态。它的特殊之处在于：**同一份 context 既要渲染成给模型看的文本，又要参与缓存键计算。**

这就是为什么不能用 `json.dumps(context, default=str)` 之类的手法把未知对象糊成字符串。一旦允许「糊过去」，两个语义不同的 context 可能序列化出同一个字符串，缓存就会错误命中；或者同一个 context 两次序列化出不同字符串，缓存永远不命中。

### 允许的类型

```python
JsonScalar: TypeAlias = bool | int | float | str | None
JsonValue: TypeAlias = JsonScalar | tuple["JsonValue", ...] | dict[str, "JsonValue"]
```

类型只是第一道关。运行时还有一道递归校验（`_normalize_json_value`），因为 Python 的类型标注在边界上是可以被绕过的（`cast`、外部输入、鸭子类型）。

### 完整的接受 / 拒绝清单

这张表是各分支的实际行为：

| 输入 | 结果 | 渲染出的 context / 错误信息 |
| --- | --- | --- |
| `{}` | 通过 | `context: {}` |
| `{ n: 1, s: "文本", b: True, nil: None }` | 通过 | `context: {"b":true,"n":1,"nil":null,"s":"文本"}` |
| `{ a: { y: 2, x: 1 }, list: [1, "二", False, None] }` | 通过 | `context: {"a":{"x":1,"y":2},"list":[1,"二",false,null]}` |
| `{ z: 1, a: 2 }` | 通过 | `context: {"a":2,"z":1}` |
| `["不能是数组"]` | 拒绝 | `context 必须是 JSON object` |
| `"不能是字符串"` | 拒绝 | 同上 |
| `{ when: datetime.now() }` | 拒绝 | `context 包含 JSON 不支持的值` |
| `{ m: {} 之外的类实例 }` | 拒绝 | 同上 |
| `{ fn: lambda: 1 }` | 拒绝 | 同上 |
| `{ n: float("nan") }` | 拒绝 | `context 包含非有限 JSON 数字` |
| `{ n: float("inf") }` | 拒绝 | 同上 |
| 非字符串键 | 拒绝 | `context JSON object 的 key 必须是字符串` |
| 自引用对象 | 拒绝 | `context 包含循环 JSON object` |
| 自引用数组 | 拒绝 | `context 包含循环 JSON array` |

有几行值得单独说。

**键顺序不影响结果。** `{ z: 1, a: 2 }` 渲染出 `{"a":2,"z":1}`——按键排序。嵌套对象也一样，`{ y: 2, x: 1 }` 变成 `{"x":1,"y":2}`。所以两份「写的顺序不同、内容相同」的 context 会得到同一个缓存键（`test_cache_uses_semantic_context_and_is_invalidated_by_live_state` 断言键顺序颠倒后仍命中）。

**根必须是普通 dict。** 判断用的是 `type(context) is not dict`——严格类型相等，不是 `isinstance`。所以 `dict` 的子类、任何「看起来像字典」的对象都进不来。这是有意的：这些对象的序列化形式依赖实现细节。

**bool 和 int 分开处理。** Python 里 `bool` 是 `int` 的子类，`isinstance(True, int)` 成立——所以整数分支显式排除了 `bool`，浮点数分支用 `math.isfinite()` 拒绝 `NaN` 和 `Infinity`。

### 实现上的两个关键点

`_normalize_json_value` 做递归转换，递归路径上维护一个 `active` 集合（存对象 `id()`）来发现循环引用：

```python
if type(value) in (list, tuple):
    identity = id(value)
    if identity in active:
        raise PromptContextError("context 包含循环 JSON array")
    active.add(identity)
    try:
        return tuple(_normalize_json_value(item, active) for item in sequence)
    finally:
        active.remove(identity)
```

注意 `finally` 里的 `active.remove(identity)`。如果不删，同一个对象在**兄弟位置**出现两次（合法的 DAG，不是循环）也会被误判成循环。删掉之后，只有真正在**当前递归路径上**的对象才算循环（`test_rejects_cyclic_context` 验证自引用数组被拒）。

列表转成**不可变 tuple**——这是 Python 版的一个内在优势：规范化后的 context 天然只读，TS 版要 `Object.freeze` 递归冻结才能做到同样的事。

### identity 也有校验

顺带说一下 `identity`，它的规则更简单但同样严格：

```python
def _normalize_identity(identity: str) -> str:
    if not isinstance(identity, str):
        raise TypeError("identity 必须是字符串")
    normalized = identity.strip()
    if not normalized:
        raise ValueError("identity 不能为空")
    return normalized
```

实测行为：

| identity 输入 | 结果 |
| --- | --- |
| `"  前后空格  "` | 通过，渲染成 `前后空格`（strip 掉了） |
| `""` | 拒绝：`identity 不能为空` |
| `"   "` | 拒绝：同上 |

strip 是为了让「多打了个空格」不会产生一个新的缓存键。

---

## 缓存必须属于会话

### 挂在模块全局会串会话

一个很容易写出来的版本是这样：

```python
# 反面例子：不要这样写
_last_key: str | None = None      # 模块顶层
_last_prompt: str | None = None
```

单个 Agent 跑起来毫无问题。但同一个进程里跑两个 Agent 实例时，它们共享这两个变量——第一个 Agent 渲染完，第二个 Agent 输入不同却可能读到第一个的结果。表现出来就是「B 会话的 Agent 拿到了 A 会话的 workspace 路径」，而且极难复现，因为它依赖两个会话的渲染时序。

本章把缓存放进实例字段：

```python
class DynamicPromptRenderer:
    # 上一次所有模型可见输入的稳定 JSON 快照。
    _last_key: str | None
    # 与 _last_key 对应的最终 prompt 文本。
    _last_prompt: str | None
    # 仅用于观测缓存复用次数，不影响渲染结果。
    _cache_hits: int
```

每次 `build_agent()` 都 `DynamicPromptRenderer()`，所以缓存天然按会话隔离。换一个 renderer 实例，`cache_hits` 从 0 开始，即使输入完全一样。

### 缓存键必须覆盖全部可见输入

这是本节最要紧的一条。缓存键少覆盖一个输入，后果不是「缓存效率低」，而是**给模型发过期指令**。

```python
key = _stable_json(
    {
        "context": normalized_context,
        "identity": normalized_identity,
        "memory": memory_body,
        "skills": skill_catalog,
        "tools": tool_names,
        "workspace": resolved_workspace,
    }
)
```

六个输入，正好对应五个 section 里所有会变的东西：

| 缓存键里的项 | 对应 section | 少了它会怎样 |
| --- | --- | --- |
| `identity` | identity | 换了行为基线仍发旧指令 |
| `context` | identity | 章节号或运行模式变了没反映 |
| `tools` | tools | **新注册的工具模型不知道，或已移除的工具模型还在调** |
| `workspace` | workspace | 换了工作目录仍报旧路径 |
| `skills` | skills | 新 Skill 不出现在目录里，模型不知道能加载 |
| `memory` | memory | 本轮选中的记忆是上一轮的 |

注意键里用的是 `tool_names`（名称 tuple）,不是注册表对象。工具的描述或 schema 变化不会触发失效，因为那些内容不进 system prompt（它们走请求的 `tools` 参数，是另一条边界）。

### 缓存的实际行为

`test_cache_uses_semantic_context_and_is_invalidated_by_live_state` 跑出来的完整序列：

| 步骤 | cache_hits | 同一字符串 |
| --- | ---: | :---: |
| 第 1 次渲染 | 0 | — |
| 第 2 次（context 键顺序颠倒） | 1 | ✅ |
| 第 3 次（新增工具后） | 1（没涨） | ❌ |
| 第 4 次（换 workspace） | 1 | ❌ |

逐行读一遍：

- 第 1 次必然 miss，没有可比的东西。
- 第 2 次把 context 的键顺序颠倒，**仍然命中**——这就是规范 JSON 的作用。
- 第 3 次注册了一个新工具，`cache_hits` 停在 1（没涨），返回的也是不同字符串（`- write_file` 出现）。失效生效了。
- 第 4 次换 workspace，同样失效（新路径出现在输出里）。

`test_cache_is_invalidated_when_skill_catalog_or_selected_memory_changes` 又补上 skills 和 memory 两个维度的失效：换选中的记忆后缓存同样不命中。

### 失败不污染缓存

一次非法渲染抛错之后，再用之前那份合法输入渲染，**仍然命中缓存**，拿到的还是失败前那个结果。

这是因为所有校验都在更新 `_last_key` / `_last_prompt` **之前**完成：

```python
normalized_context = _normalize_context(context)   # 非法在这里就抛了
...
key = _stable_json({...})
if key == self._last_key and self._last_prompt is not None:
    self._cache_hits += 1
    return self._last_prompt
...拼装...
self._last_key = key      # 只有全部成功才走到这里
self._last_prompt = prompt
```

如果顺序反了——先写缓存再校验——一次失败调用就会把缓存置成半成品状态，之后所有渲染都拿到脏数据。`test_invalid_context_does_not_poison_previous_cache` 专门覆盖了这条。

### 这个缓存不是 API prompt cache

必须说清楚，否则很容易误宣称性能收益。

| 对比项 | 本章的缓存 | 供应商 API 的 prompt cache |
| --- | --- | --- |
| 在哪 | 进程内，renderer 实例字段 | 供应商服务端 |
| 省什么 | 重复拼装字符串的 CPU | 重复处理相同 token 前缀的费用和延迟 |
| 命中条件 | 六个输入的规范快照全部相等 | 断点前的完整请求前缀一致 |
| 谁能观测 | `renderer.cache_hits` | 响应里的 usage 字段 |

当前 OpenAI 适配器**没有**把静态和动态 section 分成不同的 cache-control block。所以本章不能宣称「动态内容变化时仍会命中 API 级静态前缀缓存」——那需要额外的分块设计，本章没做。

`cache_hits` 这个属性存在的意义是**可测试性**，不是性能指标。

---

## 接入唯一 Loop，而不是复制 Loop

本章新增一个能力，但 `core/loop.py` 里的工具执行循环一行都没有复制。动态提示的接入点只有一处：每轮组装模型请求前。

```python
request = ModelRequest(
    messages=(system_message(self._render_system_prompt()), *request_history, ...),
    ...
)
```

工具调用、权限、Hook、整批结果处理、压缩、Stop 行为全部还是原来那套实现。

### 两道校验，两个时机

**构造时**校验 provider 有没有 `render()`：

```python
if system_prompt_provider is not None and not callable(
    getattr(system_prompt_provider, "render", None)
):
    raise TypeError("system_prompt_provider 必须实现 render()")
```

**每轮渲染时**校验返回值：

```python
def _render_system_prompt(self) -> str:
    if self._system_prompt_provider is None:
        return self._system_prompt  # 旧章节兼容：使用静态 Prompt
    rendered = self._system_prompt_provider.render()
    if not isinstance(rendered, str) or not rendered.strip():
        raise AgentRunError("动态 system prompt 必须是非空字符串")
    return rendered
```

两个观察点：

- `TypeError` 是**构造时**抛的，`run()` 根本没开始。契约问题越早暴露越好。
- 四种「返回值不对」（空串、纯空白、非字符串）都归到同一条 `AgentRunError`，因为对 Loop 来说它们是同一类问题：拿不到可用的系统提示。

Loop **不捕获** renderer 内部的异常。这是有意的：渲染失败意味着系统提示的某个数据源坏了，继续跑只会用错误的指令去操作真实环境。让它炸，比静默降级安全。

### 为什么失败不回退到构建期的字符串

`AgentRunner` 手里确实有一个构建期的 `system_prompt`（P10 里它是基线 prompt 那段）。既然有 fallback，为什么 provider 返回空时不用它？

因为那段字符串**不包含 workspace、工具列表、Skill 目录和记忆**。用它去跑，模型会以为自己没有工具、不知道在哪个目录工作。这不是降级，是换了个 Agent。

provider 存在时构建期字符串确实不参与——`test_p10_uses_one_dynamic_prompt_across_tool_rounds` 断言发给模型的 system 消息是动态渲染结果（含全部五个 section）。

### 「下一轮生效」的准确边界

「运行态变化下一轮生效」这句话要按真实实现理解，不能笼统地当成「什么都会自动更新」。

| 变化 | 下一轮生效吗 | 为什么 |
| --- | --- | --- |
| `ToolRegistry` 注册 / 移除工具 | 生效 | 每次渲染重读 `tools.names` |
| `MemorySession` 本轮选中的记忆变了 | 生效 | 每次渲染重读 `memory.selected` |
| workspace 路径 | 不变 | Provider 绑定的是构建期传入的路径 |
| 磁盘上新增 / 删除 Skill 目录 | **不生效** | `SkillRegistry` 是构建期扫描的快照 |

最后一行是最容易误解的。同一个 provider，磁盘上多了一个 Skill，渲染结果一个字都没变。要让它出现，必须重建 Agent——新的构建流程会重新 `SkillRegistry.scan(workspace)`。

**当前实现不会悄悄监视目录。** 这是刻意的选择：每轮都去扫一遍磁盘，会给每次模型请求加上文件系统 I/O，而 Skill 目录在一次会话里几乎不变。要做热更新，应该显式地加一个失效通知机制，而不是靠轮询。

---

## 记忆只注入一次

这一节是本章唯一需要动到前一章行为的地方，也是最容易出 bug 的地方。

### 问题：两条路径同时开着

第 9 章的记忆是通过 `MemorySession.before_model()` 注入的——它返回一条额外的 system 消息，追加在请求历史后面。

第 10 章之后，选中的记忆已经进了动态 Prompt 的 `memory` section。如果旧路径还开着，同一段正文会在一次请求里出现**两次**：一次在 system prompt 里，一次作为独立 system 消息。

重复注入的后果：白烧一份 token，而且给模型一个错误信号——同一件事说两遍，通常意味着它更重要。

### 解法：组合根显式切换所有权

```python
# 第 10 章：有 dynamic_prompt 时不再通过消息注入记忆，而是放入 System Prompt
memory_session = MemorySession(
    ...,
    emit_context_messages="dynamic_prompt" not in profile.capabilities,
)
```

那个 `not in` 是整段逻辑的核心：

| Profile | `dynamic_prompt` | `emit_context_messages` | 记忆从哪注入 |
| --- | --- | --- | --- |
| P09 | 无 | `True` | `MemorySession.before_model()` 的独立 system 消息 |
| P10 | 有 | `False` | 动态 Prompt 的 `memory` section |

这样 P09 行为完全不变，P10 换成新路径，两者都只注入一次。

### 生命周期没有被关掉，只关了它的注入

容易误解的一点：`emit_context_messages=False` **不是**把记忆功能关掉。`MemorySession` 仍然完整地干这些事：

- `begin_turn()` 每轮读取集合、调用 selector 选记忆（失败时降级为关键词匹配）
- `complete()` 回合结束从 canonical history 提取新记忆、达到阈值时整理
- `selected` 属性暴露本轮选中的记录

只有 `before_model()` 的那条独立消息被关掉了。renderer 通过 `selected` 读同一份数据：

```python
memory_body = (
    "" if memory is None or not memory.selected else memory.render_selected()
)
```

注意这里读的是 `selected` 和 `render_selected()`，不是重新去 store 里选一遍。**选择权仍然属于 `MemorySession`，renderer 只负责渲染。** 这是本章反复出现的分工原则：prompt 层不重新实现工具发现、记忆选择或路径校验。

### 注入位置的变化

P09 和 P10 的请求结构差异：

```text
P09 的请求：                      P10 的请求：
┌─────────────────────────┐      ┌─────────────────────────┐
│ [0] system: 固定 prompt  │      │ [0] system: 动态 prompt  │
│                         │      │      ├─ ## identity     │
│                         │      │      ├─ ## tools        │
│                         │      │      ├─ ## workspace    │
│                         │      │      ├─ ## skills       │
│                         │      │      └─ ## memory  ←注入 │
├─────────────────────────┤      ├─────────────────────────┤
│ [1] user: 用户问题       │      │ [1] user: 用户问题       │
├─────────────────────────┤      └─────────────────────────┘
│ [2] system: 记忆 ←注入   │
└─────────────────────────┘
```

P10 少了一条消息，记忆的位置从「用户问题之后」挪到了「系统提示内部」。

### 测试怎么钉住这条

关键是**统计出现次数**，而不是断言「包含某段文本」。后者在重复注入时同样会通过：

```python
assert sum(
    "<relevant_memories>" in (message.content or "")
    for message in first_main.messages
) == 1
```

同一个测试还断言了跨工具轮的稳定性：

```python
assert second_main.messages[0] == first_main.messages[0]
```

一次工具调用（哪怕是 `unknown_tool` 失败的调用）之后，第二次主请求的 system 消息和第一次**完全相同**。工具集合没变、选中记忆没变、Skill 目录没变，所以缓存命中，渲染结果一致。

---

## 运行第 10 章

### 第 0 步：准备环境

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch10_agent'
& ..\.venv\Scripts\python.exe -m pip install -e ".[dev]"
```

### 第 1 步：先跑离线测试（不需要 API Key）

```powershell
& ..\.venv\Scripts\python.exe -m pytest tests
```

期望最后一行：

```text
105 passed in 6.1s
```

（20 个测试文件；本章新增 `test_prompting.py` 6 个用例 + `test_ch10_integration.py` 1 个。）

### 第 2 步：让 Agent 报出自己的工具列表

这一步需要真实模型，先确认 `python/.env` 已配好。

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch10.cli --prompt "列出当前 Agent 的可用工具"
```

这个 prompt 是有讲究的：模型能准确报出 8 个工具名，说明 `tools` section 真的到了它面前。如果它回答「我没有工具」或者报出一堆不存在的工具名，说明渲染链某处断了。

### 第 3 步：换个 prompt 看 workspace 和记忆

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch10.cli --prompt "你当前的工作目录是哪里？有哪些可以加载的 Skill？"
```

模型应该报出 `python/ch10_agent` 目录的绝对路径，以及 `skills/` 下扫描到的 Skill 名称。

如果之前跑过第 9 章、`.memory` 里有内容，还可以试：

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch10.cli --prompt "关于这个项目，你记得什么约束？"
```

### 第 4 步（可选）：完整门禁

```powershell
& ..\.venv\Scripts\python.exe -m compileall -q agent_ch10
& ..\.venv\Scripts\python.exe -m ruff check agent_ch10 tests
& ..\.venv\Scripts\python.exe -m mypy agent_ch10
```

### 常见报错排查

| 你看到的 | 原因 | 怎么办 |
| --- | --- | --- |
| `配置错误: 缺少或填写错误的必要配置: ...` | `python/.env` 没配 | 回第 1 章配置三个值 |
| `TypeError: system_prompt_provider 必须实现 render()` | 传了个没有 `render()` 的对象 | 检查是否误传了 renderer 而不是 provider |
| `AgentRunError: 动态 system prompt 必须是非空字符串` | `render()` 返回空串、空白或非字符串 | 检查 renderer 的数据源，不要试图加 fallback |
| `PromptContextError: context 必须是 JSON object` | context 传了数组或标量 | 根必须是普通 dict（`type(x) is dict`） |
| `PromptContextError: context 包含 JSON 不支持的值` | context 里有日期、函数或类实例 | 先转成 JSON 标量，比如 `date.isoformat()` |
| `PromptContextError: context 包含非有限 JSON 数字` | 有 `NaN` 或 `inf` | 通常是某个计算没做除零保护 |
| `PromptContextError: context JSON object 的 key 必须是字符串` | 用了非字符串做键 | 换成字符串键 |
| `PromptContextError: context 包含循环 JSON object` | context 里有循环引用 | 传之前手工挑出需要的字段，别整个对象丢进来 |
| `TypeError: tools 必须是 ToolRegistry` | 传了普通列表 | renderer 要的是注册表对象，不是名称列表 |
| `ValueError: identity 不能为空` | identity 是空串或纯空白 | 至少给一句身份描述 |
| 模型说「我没有可用工具」 | `tools` section 是 `(none)` | 检查是否用了空的 `ToolRegistry` |
| 磁盘上新增的 Skill 不出现 | `SkillRegistry` 是构建期快照 | 重建 Agent；这是设计边界，不是 bug |
| 记忆正文出现两次 | `emit_context_messages` 没关 | 检查是否绕过 `bootstrap` 手工组装了 session |

### 动手小实验

这几个实验都不调真实模型。在 `tests/test_prompting.py` 末尾追加即可，辅助函数（`register_read_tool`、`write_skill`、`FixedSelector`）都是现成的。

**实验一：五个 section 怎么长出来，正文会不会泄漏**

参考 `test_renderer_outputs_fixed_sections_without_private_bodies`：分三个阶段渲染（空注册表 → 注册工具 → 加 Skill 和记忆），断言整个 prompt 与期望值**完全相等**（不是「包含」）。重点看两个泄漏断言：Skill 正文和未选中记忆的正文都不在 prompt 里。

**实验二：context 的精确边界**

写一个循环，把 `{"when": datetime.now()}`、`{"n": float("nan")}`、自引用对象等逐一传进 `render()`，用 `try/except` 打印每种的通过/拒绝和错误信息。对照上面的接受/拒绝清单。特别留意：键顺序颠倒的两个 context 渲染出**同一个** `context:` 行（排序是递归的）；`datetime` 对象被拒绝而不是被 `default=str` 糊过去。

**实验三：缓存的四种情况**

参考 `test_cache_uses_semantic_context_and_is_invalidated_by_live_state`：同一 renderer 渲染四次（键顺序颠倒 → 命中；新增工具 → 失效；换 workspace → 失效），观察 `cache_hits` 的涨落。最值得琢磨的是最后一点：**缓存失效了，但渲染结果是确定的**——换一个 renderer 实例、输入相同，`cache_hits` 归零但文本相同。这两件事必须同时成立。

**实验四：哪些运行态变化会生效，哪些不会**

用同一个 provider 渲染两次，中间往磁盘写一个新 Skill 目录。断言两次渲染完全相同、新 Skill 名不出现。然后重建一次（重新 `SkillRegistry.scan`），新 Skill 出现。学到：「动态」指**每轮重新读取运行时对象**，不是**运行时重新扫描磁盘**——这两件事很容易混。

**实验五：记忆到底注入几次**

参考 `test_p10_uses_one_dynamic_prompt_across_tool_rounds` 的次数统计：`sum("<relevant_memories>" in ... for message in ...) == 1`。统计出现次数而不是断言「包含」，后者在重复注入时同样会通过。

**实验完记得把这些追加的测试删掉或改回去。**

---

## 这章没有做什么

照着教程宣称不存在的能力，是最容易踩的坑。把边界列清楚：

| 已经有 | 还没有 | 什么时候补 |
| --- | --- | --- |
| 五个固定 section | 静态 / 动态缓存边界分块 | **不补**。需要适配器层配合 cache-control，见「这个缓存不是 API prompt cache」 |
| renderer 实例缓存 | API 级 prompt cache 命中保证 | **不补**。当前 OpenAI 适配器没有分块 |
| 全部内容渲染进 system prompt | 系统上下文 / 用户上下文分离 | **不补**。见「与 Claude Code 的差异」 |
| 构建期扫描 Skill 目录 | 目录变化的运行时热更新 | **不补**。需要显式失效通知，不该靠轮询 |
| Profile 控制能力集合 | 按运行模式整体替换 prompt | **不补**。教学版只需要一套 section |
| `cache_hits` 计数 | 真实 API cache ratio / TTFT 实验 | **不补**。需要真实凭据和压测环境 |
| 严格 JSON context | context 的 schema 声明与版本化 | **不补**。当前 context 只放章节号和 identity |
| 顺序与内容的离线断言 | prompt 质量评测（A/B、行为回归） | 这是下一步该做的事，不是本章范围 |

有两条要特别强调。

**没有静态 / 动态缓存边界。** 本章把 identity、工具、workspace、Skill 目录和选中记忆**全部**渲染进 system prompt。这意味着记忆一变，整个 system prompt 的文本就变了。如果供应商按前缀缓存，这会让前面那些其实没变的 section 也一起失效。真实产品需要把静态部分和动态部分分开成不同的缓存块——本章没做，所以也不宣称有这个收益。

**没有 prompt 质量评测。** 本章的 105 个测试全部在验证「结构是否正确」：顺序对不对、缓存会不会串、正文有没有泄漏、记忆注入几次。**没有一个测试在验证「这套 prompt 是否让模型表现更好」。** 后者需要构造评测集做行为对比，是完全不同的工作。

---

## 与 Claude Code 的差异

Claude Code 的系统提示同样按 section 组装，但规模比本教学版大得多，而且会显式区分静态与动态两类内容。

**静态 / 动态边界。** 标准交互模式下，静态 section（identity、doing_tasks、using_tools、tone_style 等）和动态 section（session_guidance、memory、env_info_simple、output_style 等）由 `SYSTEM_PROMPT_DYNAMIC_BOUNDARY` 分隔。`mcp_instructions` 是唯一的易失 section，用 `DANGEROUS_uncachedSystemPromptSection()` 标记为不参与常规 section 缓存——因为 MCP server 可以在轮次之间连接或断开。

P10 的 `identity -> tools -> workspace -> skills -> memory` 只把可选 section 追加到尾部，没有静态 / 动态边界，也没有必须绕过缓存的 section。对教学版来说这是合理简化，但它意味着 P10 的 system prompt 是「全都会变」的一整块。

**缓存层级。** 官方 API 的 prompt caching 会缓存 `tools`、`system`、`messages` 前缀，只有断点前的完整前缀一致时才命中（[Prompt caching](https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching)）。CC 在 API 级缓存之外还有会话级 `getSystemContext` / `getUserContext` memoize 和动态 section 注册缓存。

P10 只有 renderer 实例缓存这一层。而且当前 OpenAI 适配器没有把静态和动态 section 分成不同缓存块，所以即使同一会话内组装结果被复用，也无法保证供应商侧会命中静态前缀缓存。

**上下文归属。** CC 把 `gitStatus`、`cacheBreaker` 等**系统上下文**追加到 system prompt，把 `CLAUDE.md`、当前日期等**用户上下文**前置为 `<system-reminder>` 用户消息。P10 把所有可见上下文都渲染进 system prompt，不区分归属。

这个差异直接影响静态前缀的稳定性：用户消息变化不应该破坏 system prompt 的缓存前缀，而 P10 当前没有利用这一层。想在自己项目里做前缀缓存，这是第一个该改的地方。

**模式替换。** CC 支持 `CLAUDE_CODE_SIMPLE`、Proactive/KAIROS、Coordinator、Agent 等运行模式，部分模式会用紧凑 prompt 或专用 prompt 整体替换标准 section——`CLAUDE_CODE_SIMPLE` 下核心 prompt 只有约 150 字符。

P10 的 Profile 只控制能力集合，渲染器始终输出同一套 section。真实产品通常要按运行模式切换整套指令，这是教学版和产品的一条明显分界。

四个差异汇总：

| 维度 | Claude Code | P10 |
| --- | --- | --- |
| section 规模 | 大量，显式分静态 / 动态两组 | 五个，全部视为可变 |
| 缓存层级 | API 前缀缓存 + 会话 memoize + section 注册缓存 | 只有 renderer 实例缓存 |
| 易失内容 | `mcp_instructions` 显式标记绕过缓存 | 无此概念 |
| 上下文归属 | 系统上下文进 system，用户上下文进 `<system-reminder>` | 全部进 system prompt |
| 运行模式 | 多模式，可整体替换 prompt | 单一套 section |

---

## 从 ai-agent-book 学到什么：先定缓存边界，再做动态提示

`ai-agent-book` 的 `book/chapter2.md` 是本章最接近的对照：它用 KV Cache 与 Agent 状态栏说明了「什么能放进 system prompt、什么应该追加到末尾」。

P10 把身份、工具、workspace、Skill 目录和选中记忆全部渲染进 system prompt。这是教学上的简化，代价就是不能宣称 API prompt cache 命中。

| ai-agent-book 的关键设计 | P10 对应 | 差异与边界 |
| --- | --- | --- |
| 静态前缀 + 动态轨迹分开 | P10 固定五个 section，运行态只读数据源 | P10 没有 static/dynamic 缓存边界 |
| 动态信息追加末尾 | P10 可选 skills/memory section 追加到 prompt 尾部 | P10 仍全在 system prompt；ai-agent-book 建议动态状态作为末尾 user 消息 |
| Agent 状态栏由代码确定性维护 | P10 的 provider/renderer 每轮重新读取运行时对象 | P10 没有独立状态栏；workspace/skills/memory 都在 system prompt |
| Skills 目录常驻、正文按需加载 | `SkillRegistry.render_catalog()` + `load_skill()` | P10 已与 ai-agent-book 主张一致 |
| 实验先定义指标再实现 | 离线测试 `cache_hits`、顺序断言、context 规范化、记忆单次注入 | P10 没有真实 API cache ratio / TTFT 实验 |

最值得借鉴的一点是**顺序**：先确定缓存边界在哪，再决定动态内容放哪。P10 反过来了——先把 section 拆干净，缓存边界留到以后。这在教学上可以接受（拆分本身就是本章要讲的事），但在真实项目里，如果一开始不想清楚「哪些内容会变、变了会让谁失效」，后面很可能要把整个 prompt 结构再翻一遍。

如果要在 P10 上继续演进，优先级应该是：先把上下文按归属拆到 system / user 两侧，再引入静态 / 动态缓存块，最后才是 prompt 质量评测。反过来做，评测结果会被缓存结构的变化反复推翻。

---

## 配套代码索引

第 10 章实现集中在 `python/ch10_agent/agent_ch10/`，章节入口固定选择 P10 Profile。本章新增 `features/prompting.py`，并通过 `core/loop.py` 的 `SystemPromptProvider` 协议接入 AgentRunner。

- 功能实现：`python/ch10_agent/agent_ch10/features/prompting.py`
- 组合根：`python/ch10_agent/agent_ch10/bootstrap.py`
- Loop 接入点：`python/ch10_agent/agent_ch10/core/loop.py`
- 能力与 profile：`python/ch10_agent/agent_ch10/core/profiles.py`
- 章节入口：`python/ch10_agent/agent_ch10/cli.py`
- 数据源：`python/ch10_agent/agent_ch10/core/tools.py`、`features/skills.py`、`features/memory.py`
- 直接行为测试：`python/ch10_agent/tests/test_prompting.py`
- 接线测试：`python/ch10_agent/tests/test_ch10_integration.py`

从上到下读的话，三个角色的分工是这样的：

```text
ToolRegistry / SkillRegistry / MemorySession   ← 各自持有数据，各自负责校验
     ↑ 只读当前值
DynamicPromptRenderer   ← 校验 + 规范化 + 固定顺序拼装 + 实例缓存
     ↑ 零参数适配
DynamicPromptProvider   ← 构建期绑定，运行期只转发
     ↑ SystemPromptProvider 协议
AgentRunner             ← 每轮请求前调 render()，不知道有几个 section
```

最后一行值得注意：`AgentRunner` 只认 `SystemPromptProvider` 这个协议（一个零参数方法），完全不知道 prompt 里有什么。所以动态提示能力可以整体拔掉，Loop 不需要改一行——P09 就是这么跑的。

| 实现 | 职责与边界 |
| --- | --- |
| `DynamicPromptRenderer.render()` | 唯一渲染入口；校验四类输入类型、规范化 context、生成缓存键、按固定顺序输出五个 section |
| `_normalize_identity()` | 拒绝非字符串和空白 identity；strip 后参与缓存键 |
| `_normalize_context()` / `_normalize_json_value()` | 递归转换合法 JSON（list 转 tuple 保证不可变）；拒绝非普通 dict、非字符串键、非有限数和循环引用 |
| `_stable_json()` | 按键排序的紧凑序列化，使语义相同的 context 得到同一文本和同一缓存键 |
| `DynamicPromptRenderer` 的 `_last_key` / `_last_prompt` | 实例级缓存；只有六个输入的规范快照全部相等才复用，校验失败不写入 |
| `cache_hits` | 只用于测试与观测，证明相同输入不重复组装；不是性能指标 |
| `DynamicPromptProvider` | 构建期绑定 renderer 与运行态对象，对外暴露零参数 `render()` |
| `SystemPromptProvider` | Loop 侧协议，只有 `render() -> str`，使 Loop 与数据源解耦 |
| `AgentRunner._render_system_prompt()` | 每轮请求前渲染；空值或非字符串立即抛 `AgentRunError`，不回退到构建期字符串 |
| `bootstrap.py` 的 `emit_context_messages` 开关 | 用 `"dynamic_prompt" not in profile.capabilities` 决定记忆从哪注入，保证只注入一次 |
| `P10` | 在 P09 能力上追加 `dynamic_prompt`，使组合根切换到运行时渲染路径 |
| `cli.py` | 固定使用 P10，读取共享 `python/.env` 并启动真实运行 |

---

## 本章小结

### 三句话版本

1. 硬编码的 prompt 字符串在构建期就定死了，工具、Skill、记忆一变它就说谎；本章把 prompt 变成每轮请求前重新渲染的**函数**。
2. 渲染分三个角色：`DynamicPromptRenderer` 只负责校验和拼装，`DynamicPromptProvider` 负责绑定数据源并适配成零参数接口，`AgentRunner` 每轮调一次 `render()`。
3. 固定的五段顺序（`identity -> tools -> workspace -> skills -> memory`）、规范 JSON 缓存键和实例级缓存，共同保证「同样的上下文 → 同样的文本」，这是后续做缓存边界的地基。

### 一定要记住的五条

**第一，section 顺序是契约，不是排版。** 可选段落只能追加到尾部，不能插队。一旦顺序随输入变化，缓存键就失去意义，模型看到的结构也不稳定。测试里的顺序断言就是在守这条线。

**第二，缓存键必须覆盖所有模型可见的输入。** P10 的键包含六项：`context`、`identity`、`memory`、`skills`、`tools`、`workspace`。漏掉任何一项，那一项变了却复用旧 prompt，模型就会拿到过期信息——这类 bug 不会报错，只会让 Agent 行为莫名其妙。

**第三，缓存必须是实例级的。** 模块级全局缓存会让两个会话互相污染：A 会话的记忆可能出现在 B 会话的 prompt 里。这不是性能问题，是数据泄漏。

**第四，校验要在写缓存之前。** 非法 context 应该抛错并且**不留下任何痕迹**。如果先写缓存再校验，一次失败就会污染后续所有渲染。

**第五，记忆只能注入一次。** 动态提示启用后，记忆已经作为 `## memory` section 进了 system prompt，`MemorySession` 就必须停止再发自己的 system 消息。`emit_context_messages="dynamic_prompt" not in profile.capabilities` 那个取反就是干这件事的，看着不起眼，去掉就是双份注入。

### 检查你是否真的读懂了

试着回答下面五个问题。答不上来的，回到对应实验重跑一遍。

1. 为什么 `render()` 是零参数的？如果它接受参数，谁会变成 Loop 的依赖？（实验五 / `SystemPromptProvider` 那一节）
2. `{ "b": 1, "a": 2 }` 和 `{ "a": 2, "b": 1 }` 会不会命中同一个缓存？为什么需要规范 JSON？（实验二、实验三）
3. `datetime.now()` 放进 context 会发生什么？为什么不直接接受它？（实验二）
4. 工具列表变了但 identity 没变，缓存会命中吗？说出判断依据。（实验三）
5. 磁盘上新增一个 Skill 目录，正在运行的 Agent 会看到吗？为什么？（实验四、「这章没有做什么」）

第五问最容易答错：不会看到。`SkillRegistry.scan()` 是构建期的一次性快照，不监听目录变化。动态提示的「动态」指的是**每轮重新读取运行时对象**，不是**运行时重新扫描磁盘**。这两件事很容易混。

---

第 10 章把提示词从「构建期的一个字符串」变成了「运行期的一个函数」。到这里，Agent 每轮看到的上下文终于是它真实拥有的能力了。

但真实运行远不止「上下文对不对」。模型会输出到一半被截断，API 会限流，会返回 503，输入会超出上下文窗口，你还可能想在中途换一个模型。这些都不是逻辑错误，而是运行时必然会遇到的状况——第 11 章会把它们逐一处理掉。

---

## Java 开发者先看这里

推荐继续使用测试驱动的「考古法」：

1. 先看 `python/ch10_agent/tests/test_ch10_integration.py`，观察最终 system prompt。
2. 再看 `tests/test_prompting.py`，理解固定 section、缓存和严格 JSON。
3. 然后看 `features/prompting.py` 的 `DynamicPromptRenderer`。
4. 最后看 `core/loop.py` 和 `bootstrap.py`，理解 Provider 怎样接入唯一 Agent Loop。

Python 与 Java 对照：`Protocol` 是 `Supplier<String>` 式的函数式接口；`type(x) is dict` 是比 `instanceof Map` 更严格的类型判断（连子类都拒）；`tuple` 天然等价于递归 `List.copyOf` 的不可变集合（TS 版要 `Object.freeze` 才能做到）；`math.isfinite()` 对应 `Double.isFinite()`；`sorted(mapping, key=str)` 是键排序的规范化（Jackson 的 `SORT_PROPERTIES_ALPHABETICALLY`）。
