# API 韧性即生命：决定 AI Agent 商业化成败的隐藏细节（Python 版）

> 本文是第十一章的完整 Python 迁移版，配套项目位于 `python/ch11_agent`。
> 面向 Java 后端开发者，文中用 Resilience4j/Hystrix、`Supplier`、record 等对照 Python 的 dataclass、Protocol 与装饰器。

凌晨两点，手机响了。

一个跑了三个小时的 Agent 任务挂了。日志最后一行：

```text
openai.APIStatusError: Error code: 529 - {'type': 'error', 'error': {'type': 'overloaded_error', 'message': 'Overloaded'}}
```

没有重试，没有等待，没有任何恢复动作。直接崩。

三个小时的工具调用、文件读写、中间结论全部作废，而供应商侧那次过载可能只持续了两秒。

这不是罕见 bug，而是四类日常情况的必然结果：

- 429 是标准限流信号，配额一紧就出现；
- 一些 OpenAI 兼容端点用 529 表示临时过载；
- 输入超过 context window 时，端点返回结构化的「prompt 太长」错误；
- 输出撞上 `max_tokens` 时，你拿到的是一个**成功**的 HTTP 200，正文却是半句话。

问题从来不是 API 会不会出错，而是 Agent 能否在明确边界内恢复。

第 10 章的 Agent 已经有记忆、上下文压缩、规划工具和动态 Prompt，但模型请求仍然裸着穿过唯一 Loop：`model.complete(request)` 一抛错，整个 `run()` 就结束。第 11 章只增加一层能力——让**同一个逻辑模型请求**自己处理输出截断、输入过长和瞬态 API 故障，同时保证不污染正式历史、不重复执行 Hook、不隐藏失败。

---

## 本章导读

### 读完这一章，你能做到什么

1. 分清三种「模型没给出可用答案」的故障，说出各自的供应商无关信号和恢复动作，而不是笼统地「重试一下」。
2. 解释为什么判断故障类型不能用 `"429" in str(exc)`，以及正确做法只读哪几个字段。
3. 说清 `finish_reason == "length"` 为什么不是异常，以及第一次遇到它时为什么要把整段回复丢掉。
4. 手算默认配置下 10 次瞬态重试的每一次等待时长，并说出总等待为什么正好解释了 300 秒的默认时限。
5. 判断一个 `Retry-After` 头会被接受还是被拒绝，并说出被拒绝时抛的是哪个错误。
6. 说明「连续 3 次 529 才切 fallback」里的**连续**是什么意思，哪些情况会把计数清零。
7. 说出一次续写恢复之后，canonical history 里应该剩几条消息、外层 turn 应该是几。
8. 解释本章为什么只包装主 Loop 请求，而记忆抽取、压缩摘要、子 Agent 仍然走裸模型。

### 你需要先具备什么

| 前置 | 出自哪一章 | 本章怎么用它 |
| --- | --- | --- |
| Agent Loop 每轮如何组装模型请求 | 第 1 章 | 恢复层就插在「组装完请求」和「拿到回复」之间 |
| `ModelClient` / `ModelRequest` / `ModelReply` 契约 | 第 1 章 | 恢复层只认这三个类型，不认供应商对象 |
| canonical history 与请求历史的区别 | 第 8 章 | **本章最关键的前置**，恢复只改请求快照，不改正式历史 |
| `CompactionManager` 与协议组不可拆分 | 第 8 章 | 输入过长时直接复用它做响应式压缩 |
| 动态系统提示每轮重新渲染 | 第 10 章 | 首条 system 消息在压缩时必须留在原位 |
| Profile 能力位与组合根 | 第 5～10 章 | P11 靠一个 `recovery` 能力位切换到恢复路径 |

如果只来得及补一章，补第 8 章的「canonical history 和请求历史不是一回事」。本章一半的设计都是围绕这条边界展开的。

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| 逻辑模型请求 | 外层 Loop 眼里的「一次模型调用」，内部可能真实发出多次 HTTP 请求 |
| 恢复层 | `RecoveryManager`，把一次逻辑请求内的重试、续写、压缩、切换全部收在里面 |
| 归一化错误 | 在适配器边界把供应商异常转成本项目自己的类型，之后只按类型分派 |
| 输出截断 | 模型撞到 `max_tokens` 停下，HTTP 200 但 `finish_reason` 是 `length` |
| 输出预算升级 | 第一次截断时把 `max_tokens` 从 8000 提到 64000，重发同一请求 |
| 续写 | 预算提到上限仍截断时，把已有片段和续写提示追加进**请求快照**再问一次 |
| 请求快照 | `complete()` 内部那个局部 `request_messages`，不可变 tuple，每次重试重建 |
| 响应式压缩 | 因为「输入过长」被动触发的压缩，区别于第 8 章按阈值主动触发的压缩 |
| 瞬态故障 | 429 限流和 529 过载，重试有意义的那一类失败 |
| 指数退避 | 等待时长按 `base × 2^attempt` 增长，封顶 32 秒 |
| 抖动（jitter） | 在退避时长上叠加一小段随机量，避免同批请求同时醒来 |
| `Retry-After` | 服务端明确告诉你「等多久再来」的响应头，优先级高于本地退避 |
| fallback model | 主模型连续过载时切换到的备用模型 |
| 总时限（deadline） | 一个 turn 从 `begin_turn()` 起算的墙钟上限，默认 300 秒 |
| typed failure | 每种耗尽/超时/取消都有专属错误类，调用方按类型判断而不是读文案 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先看三条路径真实跑起来什么样   ← 直接看输出，不先讲抽象
  ↓
② 三种故障，三条路              ← 为什么必须分开处理
  ↓
③ 先归一化错误                  ← 只读结构化字段；200 也要校验
  ↓
④ 路径一：输出截断              ← 升预算 → 续写 → 只有合并结果出门
  ↓
⑤ 路径二：输入过长              ← 只压缩请求快照，只压一次
  ↓
⑥ 路径三：429 / 529             ← 退避曲线、Retry-After、fallback 计数
  ↓
⑦ 取消与总时限                  ← 次数不是唯一边界
  ↓
⑧ 接入唯一 Loop                 ← 内部重试为什么不算新 turn
  ↓
⑨ 运行 + 实验 + 排错            ← 动手把上面每条结论都验一遍
  ↓
⑩ 小结 + 自测                   ← 记住五条 + 自测题
  ↓
⑪ Java 开发者的阅读和调试顺序    ← 源码考古
```

---

## 先看三条路径真实跑起来什么样

抽象讲「重试」不如直接看输出。下面是 `tests/test_recovery.py` 用**脚本化的假模型**跑出来的真实结果：假模型按预定顺序返回回复或抛错，时钟和 sleep 都被注入成可观测的假实现。

**第一段：输出被截断。**

```text
=== 路径一：输出被截断 ===
第 1 次请求  model=primary max_tokens=8000  消息数=2
第 2 次请求  model=primary max_tokens=64000 消息数=2   ← 半截回答被整段丢弃
第 3 次请求  model=primary max_tokens=64000 消息数=4   ← 多了半截回复 + 续写提示
最终 content = "第一段第二段"
最终 finish_reason = stop
调用方原请求消息数 = 2（未被改写）
```

三行请求日志说明了三件事：

- 第 2 次请求的 `max_tokens` 从 8000 变成 64000，但**消息数还是 2**——第一次那段残缺回复被整段丢掉了，没有进请求。
- 第 3 次请求消息数变成 4，多出来的两条是「上一次的半截回复 + 续写提示」，它们只存在于请求快照里。
- 最后一行：调用方手里那个 `ModelRequest` 从头到尾没被改写（`test_second_length_reply_continues_only_in_request_snapshot_and_merges_text` 断言 `request.messages == _request().messages`）。

**第二段：输入过长。**

```text
=== 路径二：输入过长 ===
第 1 次请求  消息数=2  → 抛 ModelPromptTooLongError
第 2 次请求  消息数=3  首条 role = system，第二条是压缩摘要
最终 content = "成功"
```

注意 `max_tokens` 没变：输入过长和输出截断是两件事，不能互相顶替。首条仍然是 system（`test_prompt_too_long_keeps_leading_system_and_compacts_only_once` 断言 `model.requests[1].messages[0] == system_message("规则")`），第二条换成了压缩摘要加保留的尾部。

**第三段：连续过载。**

```text
=== 路径三：连续 529 切 fallback ===
每次请求用的模型 = ["primary","primary","primary","fallback"]
实际 sleep 秒数（jitter 注入为 0）= [0.5,1,2]
```

三次 529、三次退避，第 4 次请求才换模型。`["primary","primary","primary","fallback"]` 就是「连续 3 次 529 后切 fallback」的字面含义（`test_overload_switches_to_fallback_after_threshold` 的精确断言）。

三条路径的外部表现完全不同，但对外层 Loop 来说，它们都只是「`complete()` 返回了一个可用的 `ModelReply`」。

---

## 三种故障，三条路

三种故障看起来都像「模型没给出可用答案」，但触发信号和恢复动作完全不同：

| 故障 | 供应商无关信号 | 恢复动作 |
| --- | --- | --- |
| 输出被截断 | `ModelReply.finish_reason == "length"` | 首次提升输出预算；之后在请求窗口内续写 |
| 输入上下文过长 | `ModelPromptTooLongError` | 保留当前 system prompt，响应式压缩请求快照一次 |
| 临时故障 | `ModelRateLimitError` / `ModelOverloadedError` | 遵守 Retry-After 或指数退避；连续 3 次 529 后切 fallback |

**为什么不能用一条通用重试路径解决全部三种。** 挨个看反例：

- 输出截断时重试原请求毫无意义——预算没变，第二次还是会截在同一个位置。它需要的是**改参数**（提预算）或**改请求内容**（追加续写提示），不是重发。
- 输入过长时重试更糟——请求一个字节都没少，重试一百次都是同一个 400。它需要的是**缩小输入**。
- 429/529 恰恰相反：请求本身完全正确，唯一该做的就是**什么都别改，等一会儿再发**。改内容反而可能引入新问题。

一条通用路径必然在这三者中至少两个上做错事。所以这三条路径统一放在 `RecoveryManager.complete()` 里，但**内部是三个独立分支**。

公共 `AgentRunner` 的职责完全不变：组装当前请求、执行工具、追加最终回复、运行 Hook。恢复层不会重新进入外层 Loop，所以一次 API 重试不会被计成新的 Agent turn。

还有第四种情况需要单独说：**不认识的错误**。500、503、连接重置、程序自己的 `TypeError`——这些统统原样抛出，一次都不重试。

`test_unknown_errors_are_not_wrapped_or_retried` 断言得很硬：`error.value is original`——错误对象被原样传递，没有被包装、没有丢栈，且模型只被调用 1 次。

---

## 先归一化错误，禁止匹配异常文本

很多示例代码这样判断故障：

```python
# 反面教材，不要这么写
if "429" in str(exc):
    retry()
elif "prompt" in str(exc):
    compact()
```

这会在三种情况下判错：

1. 业务错误文案里恰好带 `429`（比如用户输入了一个订单号 `INV-429`）；
2. 反向代理返回的 HTML 错误页里带 `prompt` 字样；
3. 程序自己的异常消息里带这些词——本该立刻暴露的 bug 变成了静默重试。

当前实现把供应商对象严格限制在一个文件里：`agent_ch11/adapters/openai_chat.py`。整个项目只有它 `import` OpenAI SDK。适配器只读取 SDK 的稳定结构：

- `APIStatusError.status_code`
- `APIStatusError.body`
- `response.headers`（读 `retry-after`）
- `request_id`

分类规则是显式的：

```python
PROMPT_TOO_LONG_CODES = frozenset({
    "context_length_exceeded", "max_context_window",
    "prompt_is_too_long", "prompt_too_long", ...
})

if status == 429:
    return ModelRateLimitError("OpenAI 请求被限流", retry_after=retry_after, ...)
if status == 529:
    return ModelOverloadedError("OpenAI 模型暂时过载", ...)
if status == 400 and error_code in PROMPT_TOO_LONG_CODES:
    return ModelPromptTooLongError("OpenAI 输入超过模型上下文窗口", ...)
return None     # 无法映射 → 原样抛出
```

`error_code` 只允许来自平坦的 `code` / `type`，或最多一层嵌套的 `error.code` / `error.type`（`_structured_error_identifier` 只看这两个候选）。实现不读 `message`，不递归搜索任意 JSON，不根据异常字符串猜测。识别前会把 code 统一转小写——不同端点大小写不一致是常见现象。

### 类型和 status 是不变量

三种内部异常定义在 `agent_ch11/core/model.py`，构造期就锁死了 status：

| 错误类 | 允许的 status_code | 携带的额外信息 |
| --- | --- | --- |
| `ModelRateLimitError` | 只能是 429 | 可选 `retry_after` 原始字符串、`error_code`、`request_id` |
| `ModelOverloadedError` | 只能是 529 | `error_code`、`request_id` |
| `ModelPromptTooLongError` | 只能是 400 | `error_code` |

这样恢复层可以按 Python 异常类型分派，不会收到「类型是限流但 status 是 500」这种语义矛盾的对象。构造一个矛盾对象会在构造期就失败（`test_recovery_config_rejects_invalid_budgets_and_error_statuses` 断言 `ModelRateLimitError(..., status_code=500)` 抛 `ValueError`），而不是在恢复逻辑里表现成诡异行为。

### HTTP 200 同样是不可信边界

上面全在讲「请求失败」。但供应商返回 200 不代表结构可信——字段缺失、类型不对、出现未见过的枚举值，都可能发生。`_normalize_response()` 在把响应交给恢复层之前逐字段验证：

- 必须恰好一个 `choice`；
- `message.role` 只能是 `assistant`；
- `finish_reason` 只能落在白名单内（`{"stop", "length", "tool_calls", "content_filter", "function_call"}`）；
- `content` 必须是 `str | None`；`refusal` 是字符串时可以作为最终内容回退；
- 旧版 `function_call` 明确拒绝；
- `tool_calls` 必须逐项带完整的 `id`、`name`、`arguments`；
- `usage` 三个计数必须是非负整数。

三条容易被忽略的边界：

**`finish_reason` 未知值直接失败，不当成 `stop`。** 供应商加了一个新枚举值（比如 `max_output_reached`），宽容处理会让恢复层把一个截断回复当成完整回复接受，答案就少了一截而且没人知道。明确失败比静默错误好。

**`content_filter` 能通过适配器。** 它是 OpenAI 合法的 `finish_reason`，适配器的职责是忠实传递供应商语义。最终要不要接受它由 Loop 决定——后面「接入唯一 Loop」会讲它在哪一层被拒。

**`refusal` 可以当内容。** 模型明确拒答时 `content` 是 `None`，但 `refusal` 里有文本。把它当最终内容，比让 Loop 抛「模型没给文本也没调工具」更贴近真实语义。

一句话总结这一层的价值：**core 不 import OpenAI SDK，所有供应商细节收在 adapter 内。** 恢复层面对的永远是 model.py 里那套稳定契约，换供应商时改 adapter 就够了。Java 对照：这就是异常映射器（`ExceptionMapper`）加 DTO 收窄的组合，Supplier 侧的 Jackson 严模式。

---

## 路径一：输出截断只存在于请求窗口

`finish_reason == "length"` 是一个**正常响应**，HTTP 200，不是异常。它表示模型撞到了 `max_tokens` 上限，话没说完就被截断了。

第一次遇到它时，恢复层做两件事：把输出预算从 8000 提到 64000，然后把那段残缺回复**完全丢弃**，重发同一个请求：

```python
if reply.finish_reason == "length":
    if not state.has_escalated:
        state.current_max_tokens = config.escalated_max_tokens
        state.has_escalated = True
        continue
```

为什么第一次要整段丢掉，而不是直接从半截处续写？因为提预算之后模型可能一次就写完，一份连贯的完整回复比「半截 + 拼接」质量更好。丢掉的那点 token 换来的是更好的输出结构，值得。

`RecoveryConfig` 在构造时就验证 `escalated_max_tokens <= model_max_tokens`（还有 `initial < escalated`、`base_delay <= max_delay` 等一整套）。预算不能靠「先发请求再看供应商是否接受」来试错——那样每次配置错误都要浪费一次真实请求和一次真实等待。`test_recovery_config_rejects_invalid_budgets_and_error_statuses` 把这几条构造期校验全部钉住。

### 提预算之后还截断，才进入续写

这时才追加片段和续写提示，而且只追加到 `request_messages` 这个**局部快照**上：

```python
fragment = reply.message.content
# 续写要求非空纯文本片段，不能有 tool_calls
if reply.message.tool_calls or fragment is None or not fragment:
    raise RecoveryRetriesExhausted("续写恢复要求非空纯文本片段")
if state.recovery_count >= self._config.max_continuations:
    raise RecoveryRetriesExhausted("续写恢复次数已耗尽")

fragments.append(fragment)
request_messages = (
    *request_messages,
    reply.message,
    user_message(CONTINUATION_PROMPT),
)
```

续写提示是固定的一句：

```python
CONTINUATION_PROMPT = (
    "Continue exactly where you left off. Do not repeat any text, no apology, "
    "no recap. Pick up mid-thought."
)
```

三个要求各有原因：`Do not repeat any text` 防止重复正文，`no apology` 防止模型写「抱歉刚才被截断了」这类对最终读者无意义的话，`Pick up mid-thought` 明确允许从句子中间接上，不必凑成一个新段落。

### 请求次数实测

默认 `max_continuations = 3`。如果模型一直截断，实际会发多少次请求？答案是 **5 次**：1 次初始 + 1 次提预算 + 3 次续写。消息数序列 `[2,2,4,6,8]` 很直观：提预算那次消息数不变（残缺回复被丢了），之后每次续写多两条（半截回复 + 续写提示）。

把 `max_continuations` 设成 0 就等于关掉续写——只剩「初始 + 提预算」两次，第二次仍截断就直接失败。这是合法配置，构造期不会报错（校验只拒绝**负数**）。

### 截断的回复带 tool_calls 时立刻失败

```python
if reply.message.tool_calls or fragment is None or not fragment:
    raise RecoveryRetriesExhausted("续写恢复要求非空纯文本片段")
```

注意这时 `recovery_count = 0`：这不是「续写次数用完了」，而是**根本不该续写**。一个被截断的 `tool_calls` 参数可能只有半个 JSON，把它当片段拼接会产出无法解析的工具调用；把它追加进历史更糟，会留下一个永远等不到 `tool` 结果的孤儿调用。所以这里选择立刻失败。

空字符串片段同理。如果允许空片段，一个反复返回空串加 `length` 的端点会让恢复层白跑三轮续写，最后拼出一个空答案。

### 最终成功时才合并

合并只发生在成功返回的那一刻，规则是「按原样拼接，不自动补空格」：

```python
def _merge_fragments(reply, fragments):
    if not fragments:
        return reply
    return ModelReply(
        ModelReply_message_with_content(reply, "".join(fragments) + (reply.message.content or "")),
        reply.finish_reason,
        reply.usage,
    )
```

不补空格是刻意的：中文、代码、URL 中间插一个空格都是错的，而模型被要求「接着上一句写」，本来就不该有额外空白。`test_second_length_reply_continues_only_in_request_snapshot_and_merges_text` 断言最终 content 恰好是 `"第一段第二段"`——两个片段零填充拼接。

只有这一个合并后的消息能返回公共 Loop 并进入 canonical history。这条边界同时解决两个问题：

- 首次残缺回复、续写提示、中间片段都不会污染会话历史；
- 带未完成 `tool_calls` 的截断响应不会被追加，从源头避免孤儿工具调用。

---

## 路径二：输入过长只压缩请求快照一次

输入过长来自适配器抛出的 `ModelPromptTooLongError`。它和 `finish_reason == "length"` 是两件完全不同的事，不能互相代替：

| | 输入过长 | 输出截断 |
| --- | --- | --- |
| HTTP 状态 | 400 | 200 |
| 表现形式 | 抛异常 | 正常返回，`finish_reason` 是 `length` |
| 根因 | 请求太大，模型根本没开始生成 | 请求没问题，但答案写不完 |
| 正确动作 | 缩小输入 | 提高输出预算或续写 |
| 拿错了会怎样 | 一直提预算，请求还是超窗口，白跑 | 一直压缩历史，答案还是被截断，而且丢了上下文 |

恢复层直接复用第 8 章的 `CompactionManager.compact_on_prompt_too_long()`：

```python
# 分离首条 system message（保留）和其余消息（可压缩）
leading, compactable = _split_leading_system(request_messages)
# 调用第 8 章的压缩管理器
outcome = self._compaction.compact_on_prompt_too_long(
    compactable, retry_count=prompt_too_long_retries
)
self._check_boundary()  # 压缩后检查边界
prompt_too_long_retries += 1
state.has_attempted_reactive_compact = True
# 拼接：(system, *压缩后的历史)
request_messages = (*leading, *outcome.history)
validate_tool_pairing(request_messages)  # 校验配对完整性
```

`_split_leading_system` 只做一件小事：如果首条是 system 就把它单独摘出来，压缩只作用于其后的消息，压完再把 system 放回第一位。

为什么必须这样？因为第 10 章的系统提示是**每轮动态渲染**的，里面有当前工具列表、workspace 路径、选中的记忆。如果它被压进摘要，模型下一次就不知道自己有哪些工具、能在哪个目录操作。实测确认了它留在原位：

```python
assert model.requests[1].messages[0] == system_message("规则")
assert model.requests[1].messages[1:] == (
    system_message("压缩摘要"),
    user_message("保留的最新请求"),
)
```

其后的请求快照按**完整消息组**压缩：assistant 的全部 `tool_calls` 和对应的 `tool` 结果仍然是不可拆分单元。这条规则来自第 8 章，本章直接继承。

### 没有修改任何原数据

canonical history、调用方传进来的 `ModelRequest`、以及那份 messages 快照，全都没有被修改。`request_messages` 是 `complete()` 内部的局部变量，每次重建出一个新 tuple（这正是 tuple 的红利：拼接天然产生新对象，不存在共享可变状态）。

响应式 transcript 记录的是「被压缩前的**请求快照**」，不是 canonical history 的别名。完整会话追溯仍由第 8 章的主动压缩 transcript 负责。这两份 transcript 用途不同，不要混。

### 同一个逻辑请求只压缩一次

```text
第 1 次请求 → ModelPromptTooLongError → 压缩
第 2 次请求 → ModelPromptTooLongError → 抛 PromptTooLongRetryError
```

为什么不允许压第二次？因为已经压过一次还是超窗口，说明问题不在「历史太长」，而在别的地方——可能是单条 user 消息本身就超窗口、可能是某个工具返回了巨型结果、也可能是模型的真实窗口比配置里小。这些情况反复压缩只会把有用上下文一点点磨掉，最后拿一个既没上下文又还是失败的请求去撞墙。

需要澄清一个容易误读的状态位。`state.has_attempted_reactive_compact` 在成功返回时会被重置成 `False`。它不是「本回合压过一次」的持久记录。真正拦住第二次压缩的是 `complete()` 内部的局部计数 `prompt_too_long_retries`——状态位只是给外部观测用的瞬时标记。

压缩也处在同一个取消和总时限边界内（`self._check_boundary()` 在压缩后调用），不能借摘要调用绕开 deadline。摘要本身也可能失败，这时第 8 章的 `_commit` 异常清理保证不会留下半份 transcript。

---

## 路径三：429、529、Retry-After 与 fallback

429 和 529 共用同一条瞬态重试路径，区别只有两点：529 会累计连续计数，429 会读 `Retry-After`。

### 退避曲线

没有有效 `Retry-After` 时，默认退避基线是 `0.5 × 2^attempt`，封顶 32 秒。把抖动注入成恒定 0，能看到干净的曲线：

```text
=== 完整退避曲线（jitter 注入为 0，10 次尝试耗尽）===
sleep 序列 = [0.5,1,2,4,8,16,32,32,32]
sleep 总秒数 = 127.5
实际请求次数 = 10
抛出 RecoveryRetriesExhausted: 限流恢复次数已耗尽
```

**注意：10 次请求，只有 9 次 sleep。** 这不是 off-by-one，而是正确行为——第 10 次失败时次数已经用尽，直接抛错，没有理由再睡一觉。

抖动取满（`base × 25%`）时：

```text
sleep 序列 = [0.625,1.25,2.5,5,10,20,40,40,40]
sleep 总秒数 = 159.375
```

每一项都是无抖动版本的 1.25 倍，总等待 159.375 秒。注意封顶后的三项变成了 40 秒——`max_delay_seconds = 32` 封的是**基线**，抖动叠在封顶之后。这是有意的：抖动的目的就是打散同时醒来的请求，如果它也被硬封在 32 秒，一批同时被限流的客户端仍会在同一秒重试。

整理成表：

| 第几次失败 | 基线（jitter=0） | 抖动取满 | 累计（抖动取满） |
| --- | --- | --- | --- |
| 1 | 0.5 | 0.625 | 0.625 |
| 2 | 1 | 1.25 | 1.875 |
| 3 | 2 | 2.5 | 4.375 |
| 4 | 4 | 5 | 9.375 |
| 5 | 8 | 10 | 19.375 |
| 6 | 16 | 20 | 39.375 |
| 7 | 32（封顶） | 40 | 79.375 |
| 8 | 32 | 40 | 119.375 |
| 9 | 32 | 40 | 159.375 |
| 10 | 不 sleep，直接耗尽 | — | — |

最后一列解释了默认 `total_timeout_seconds = 300` 是怎么来的：最坏情况下光等待就要 159 秒，加上 10 次真实请求本身的耗时，300 秒是一个「能跑完全部重试，又不会把用户挂到天亮」的量级。这个值不是拍出来的。

### Retry-After 优先，但必须能被严格解析

429 带 `Retry-After` 时，服务端的指示优先于本地退避：

```python
model = ActionModel(
    [ModelRateLimitError("限流", retry_after="1.5"), ModelReply(assistant_message("好"), "stop")]
)
...
assert sleeps == [1.5]     # 不是退避基线 0.5
```

解析规则（`_parse_retry_after`）很严格。Python 版接受两种形式：

| 输入形式 | 结果 | 原因 |
| --- | --- | --- |
| 非负十进制秒数（含小数） | 接受 | HTTP 规范的 delta-seconds 形式 |
| 两端有空格 | 接受 | 解析前先 `strip()` |
| `0` | 接受，等 0 秒 | 服务端说立刻可以重试，那就立刻 |
| 负数 / `NaN` / `Infinity` / 空串 | 拒绝 `InvalidRetryAfterError` | 不是有效秒数 |
| RFC 2822 HTTP-date 带 `GMT`（`Fri, 28 Aug 2026 12:00:05 GMT`） | 接受，算差值 | `email.utils.parsedate_to_datetime` 是标准库的 HTTP-date 解析器 |
| 已经过去的 HTTP-date | 接受，等 0 秒 | 差值取 `max(0, ...)` |
| 无时区的日期串 | 拒绝（`HTTP-date 必须包含时区`） | 会被按本地时区解释，同一个头在不同机器上算出不同结果 |
| 任意非日期文本 | 拒绝 | 无法解析 |

注意：Python 版和 TS 版在**科学计数法**上有差异——TS 版显式拒绝 `1e2`（不在 delta-seconds 语法里）；Python 版的 `float("1e2")` 会解析成功返回 100.0 秒。这是 Python `float()` 比 JS `Number()` 宽松带来的实现差异，实际影响极小（没有供应商会返回科学计数法），但如果你的端点会，请在 `_parse_retry_after` 里加一层正则。

带时区 HTTP-date 的差值计算有测试（`test_rate_limit_honors_seconds_and_http_date_and_rejects_invalid_value`）：注入 `utc_now` 为固定时刻，让 `Retry-After: <当前+2秒>` 的差值精确等于 2.0 秒。**单调时钟和 UTC 时钟分开**也是有意设计：算 deadline 必须用单调时钟（系统时间被 NTP 回调也不会算错），算 `Retry-After` 日期差值必须用真实 UTC 时钟（要和服务端说的时刻对齐）。用一个时钟兼顾两件事，其中一件必然出错。

`Retry-After` 非法时的行为很明确：**不 sleep，不重试，直接抛 `InvalidRetryAfterError`**。因为端点返回了一个不符合契约的头，说明它的限流语义不可信；这时按自己的退避猜一个时长，很可能撞在同一个限流窗口里。

### 连续 529 才切 fallback

```python
except ModelOverloadedError:
    state.consecutive_529 += 1
    if state.consecutive_529 >= self._config.overload_fallback_threshold:
        state.current_model = self._config.fallback_model
        state.consecutive_529 = 0
    transient_failures += 1
    self._retry_transient(transient_failures, None, "模型过载")
    continue
```

阈值默认 3。第三次 529 时更新状态、完成本次退避，**下一次请求**（也就是第 4 个）才显式携带 fallback model。

「连续」这个词是字面意思。429、输入过长、成功响应都会把计数清零（成功分支里 `state.consecutive_529 = 0`）。为什么要这么严？因为「连续过载」是判断主模型真的不可用的证据，而「偶尔过载」只是正常波动。一次 429 夹在中间，说明请求确实到达了主模型并被正常处理（只是限流），不构成切换理由。

注意退避的 `attempt` 用的是**总瞬态失败次数** `transient_failures`，不区分 429 还是 529。所以 529 计数被清零后，退避并不会重新从 0.5 秒开始。这一点符合直觉：退避对付的是「整体上正在失败」，不该被错误类型切换重置。

---

## 取消与总时限是恢复的一部分

重试次数不是唯一边界。上一节算过：10 次退避在最大抖动下，仅等待就是 159 秒；如果每次真实请求还各花十几秒，一个 turn 就能拖到五分钟以上。所以默认 turn 总时限设为 300 秒，并允许测试或调用方显式缩短。

### 五个可注入边界

`RecoveryManager` 把所有「与真实世界打交道」的东西都做成可注入依赖，这是本章离线测试能精确到小数点的原因：

| 注入点 | 默认实现 | 测试里换成什么 |
| --- | --- | --- |
| `monotonic` | `time.monotonic` | 手动推进的假时钟（`iter([0.0, 2.0])`） |
| `utc_now` | `lambda: datetime.now(UTC)` | 固定时刻，让 `Retry-After` 日期差值可预测 |
| `sleeper` | `threading.Event.wait` 的封装 | `lambda seconds, _: sleeps.append(seconds)`，不真等 |
| `jitter` | `random.uniform(0, upper)` | 恒定 0，拿到确定曲线 |
| `cancellation` | 新建一个 `CancellationToken` | 外部持有，测试里手动 `cancel()` |

### 该失败的时候立刻失败

如果等待会达到或越过当前 turn 的剩余时限，恢复层直接抛 `RecoveryDeadlineExceeded`——不会先睡到超时再报告失败：

```python
if delay >= self._remaining_seconds():
    raise RecoveryDeadlineExceeded("等待时间会达到或超过当前回合 deadline")
```

反面做法是「先睡 30 秒，醒来发现超时了，再报错」——用户白等 30 秒，拿到的还是同一个失败。

`test_deadline_and_pre_cancelled_turn_stop_before_network_request` 验证了两个方向：

- **预取消**：`begin_turn()` 前就 `cancel()` 了，`complete()` 抛 `RecoveryCancelledError`，且 `model.requests == []`——**一次网络请求都没发**。
- **超时**：假时钟第一次返回 0.0、第二次返回 2.0，`total_timeout_seconds=1`，第二次 `_check_boundary()` 时抛 `RecoveryDeadlineExceeded`。

### 取消令牌的契约

`CancellationToken` 的契约是明确的：

```python
token = CancellationToken()
unsubscribe = token.subscribe(lambda: print("收到取消通知"))
token.cancel()       # 幂等，只触发一次
unsubscribe()        # 退订
```

- `cancel()` 幂等，只触发一次，触发后清空监听器（锁内复制、锁外调用，避免死锁）；
- `subscribe()` 拒绝非函数参数（`TypeError`）；
- 如果令牌已经取消，新订阅会**立即执行**并返回一个空操作退订函数。最后一条防的是竞态——订阅晚了一步就永远等不到通知。

默认 `sleeper` 用 `threading.Event.wait`，所以**正在退避等待时可以被取消**。Java 对照：`AtomicBoolean`（状态）+ `CopyOnWriteArrayList<Runnable>`（监听器）的组合，`Event.wait` 类似 `Object.wait()` 可被打断。

### 一个必须说清楚的 Python 差异

当前 `ModelClient.complete()` 是**同步**接口。取消令牌可以让下一次请求或等待及时失败，但不能像 TS 版 `AbortSignal` 那样强制中止一个已经运行中的同步 OpenAI SDK 调用。要做到真正中止，需要把模型边界改成异步客户端（`httpx.AsyncClient` 加 `asyncio.Task`），或额外设计线程/进程取消协议；**本章不伪装已经具备这个能力**。源码 docstring 里明确写了这条限制。

### 五种 typed failure

所有耗尽路径都有专属错误类，调用方按类型判断，不读文案：

| 错误类 | 什么时候抛 | 该怎么处理 |
| --- | --- | --- |
| `RecoveryRetriesExhausted` | 续写次数用完、瞬态尝试用完、片段不合法 | 真的失败了，向用户报告 |
| `RecoveryDeadlineExceeded` | 剩余时间不够，或操作跑过了 deadline | 缩小任务或调大 `total_timeout_seconds` |
| `RecoveryCancelledError` | 外部 `cancel()` 被调用 | 正常路径，不必当错误上报 |
| `InvalidRetryAfterError` | 服务端返回的 `Retry-After` 不合规范 | 查端点实现，或按需放宽解析 |
| `PromptTooLongRetryError` | 压缩过一次仍然输入过长（第 8 章定义，本章复用） | 检查是否有单条巨型消息或工具结果 |

它们都继承 `RecoveryError`，所以想一把接住全部恢复失败可以 `except RecoveryError`，想精确区分就用具体类。

---

## 接入唯一 Loop，而不是复制 Loop

最容易走错的一步是「为了加恢复能力，复制一份 Loop 出来改」。那样会立刻分裂成两套 Hook 顺序、两套历史维护规则，之后每加一个能力都要改两处。

本章的做法是给公共运行时新增**一个窄协议**，只有两个方法：

```python
class ModelRequestExecutor(Protocol):
    def begin_turn(self) -> None: ...
    def complete(self, request: ModelRequest) -> ModelReply: ...
```

`AgentRunner.run()` 在现有 memory turn lifecycle 之后调用一次 `begin_turn()`，然后在模型边界二选一：有执行器走 `executor.complete(request)`，没有就走裸 `model.complete(request)`。

### 为什么必须分成 begin_turn 和 complete 两个方法

`begin_turn()` 重置的是**跨请求的恢复状态**：当前模型（可能已经切到 fallback）、当前输出预算（可能已经升级过）、连续 529 计数、deadline 起点：

```python
def begin_turn(self) -> None:
    started = self._now()
    self._state = RecoveryState(
        self._config.primary_model, self._config.initial_max_tokens
    )
    self._deadline = started + self._config.total_timeout_seconds
```

这些状态的生命周期是「一个 Agent turn」，而 `complete()` 在一个 turn 内会被调用多次（每一轮工具调用之后都要再问一次模型）。如果把重置放进 `complete()`，上一轮切到 fallback、下一轮又回到主模型，deadline 也会每轮重新计时——总时限就形同虚设。

反过来，忘记调 `begin_turn()` 会立刻失败（`_require_state()` 抛 `RecoveryError`），不会静默用默认值跑。

### 内部重试不重跑任何 Hook

这是「一个逻辑请求」这个说法的实际含义。`complete()` 内部无论重试多少次，都不会重新运行：

- UserPrompt Hook
- 请求历史准备（`history_processor.prepare()`）
- 动态系统提示渲染
- 工具 schema 快照
- Stop Hook

外层看到的仍然是一次模型调用。`test_p11_requires_recovery_config_and_keeps_recovery_attempts_out_of_history` 把这条完整钉住了：

```python
result = runner.run("开始工作")
main_requests = [request for request in model.requests if request.tools]
assert result.final_text == "最终回答"
assert [message.content for message in result.history[1:]] == ["最终回答"]
# ↑ canonical history 只有 1 条 assistant——被丢弃的截断回答没留下痕迹
assert [request.max_tokens for request in main_requests] == [8_000, 64_000]
# ↑ 预算升级确实发生了，但只体现在请求上
assert [request.model for request in main_requests] == ["primary", "primary"]
```

**canonical history 只有两条消息**（1 条 user + 1 条 assistant）。那段被丢弃的「截断回答」没有留下任何痕迹。内部发了 2 次 HTTP 请求，外层仍然只算一个 turn。

### `content_filter` 在 Loop 层显式失败

适配器能解析 `content_filter`，因为它是 OpenAI 合法的 `finish_reason`。但被内容过滤的回复既不是最终答案，也不是可重试的瞬态错误。所以 Loop 在写入 history 前就拒掉它：

```python
if reply.finish_reason == "content_filter":
    raise AgentRunError("模型回答被内容过滤器拦截")
```

放在 Loop 层而不是恢复层，是因为这是**语义判断**（这个回复能不能当答案），不是**可靠性判断**（这次调用要不要重来）。恢复层只管后者。

### 拒绝调用方覆盖恢复状态

`complete()` 会检查调用方有没有试图自己指定 `model` 或 `max_tokens`：

```python
if request.model is not None and request.model != self._config.primary_model:
    raise ValueError("request.model 必须匹配 RecoveryConfig.primary_model")
if request.max_tokens is not None and request.max_tokens != self._config.initial_max_tokens:
    raise ValueError("request.max_tokens 必须匹配 initial_max_tokens")
```

规则是「不填，或者填得和恢复层的起点一致」。为什么这么严？因为这两个字段是恢复状态机自己要改的：`model` 会因 fallback 变化，`max_tokens` 会因预算升级变化。允许调用方每轮传入自己的值，就等于允许它随时把状态机重置到一个不确定的位置。与其在运行时表现成「fallback 莫名失效」，不如在接线阶段就报错。

### 组合根：能力位与配置必须成对

P01-P10 不注入执行器，仍直接调用 raw `ModelClient`，原有的 `length` guard 继续作为明确失败（`IncompleteModelReplyError`）。只有 P11 的 bootstrap 才创建 `RecoveryManager`，并且要求显式 `RecoveryConfig`：

```python
if recovery_config is not None and "recovery" not in profile.capabilities:
    raise ValueError("recovery_config 需要第十一章或更高章节")
if "recovery" in profile.capabilities and recovery_config is None:
    raise ValueError("第十一章及以后必须提供 recovery_config")
```

两个方向都拦：给 P10 传恢复配置是配置错误（能力位没开，配置不会生效，静默忽略最坏）；P11 不传恢复配置更危险——如果允许，P11 会**静默退化成 raw ModelClient**，你以为有恢复能力其实没有，直到线上凌晨两点才发现。集成测试把这两个方向都断言了。

### 摘要器必须绑定裸模型

Bootstrap 复用同一个 `CompactionManager` 处理主动和响应式压缩（保证协议组拆分规则一致），但摘要器继续绑定 raw model。

如果把 summarizer 绑到 `RecoveryManager`，路径会变成：

```text
输入过长 → 响应式压缩 → 摘要请求 → 恢复层 → 摘要请求本身输入过长 → 再压缩 → ...
```

每一层都还持有上一层的 deadline 和状态，问题会以「莫名超时」的形式表现出来，而不是一个清晰的栈溢出。

同理，本章只包装**主 Loop 请求**。memory selector/extractor、压缩摘要器、一次性 subagent 仍使用 raw model。`test_p11_prompt_too_long_uses_raw_summary_model_and_retries_once` 把这条直接钉住了：

```python
side_requests = [request for request in action_model.requests if not request.tools]
assert side_requests[0].model is None      # 压缩摘要直接使用原始模型
assert side_requests[0].max_tokens is None
assert side_requests[1].model is None      # 记忆提取也不进入恢复层
```

旁路请求没有携带 `model` 字段——因为它没经过恢复层。给这些旁路调用加恢复能力需要新的会话所有权契约（谁的 deadline？谁的 fallback 状态？），不能共享主 Agent 的 `RecoveryState`。这是本章明确划出的边界，不是遗漏。

---

## 运行第 11 章

### 第 0 步：准备环境

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch11_agent'
& ..\.venv\Scripts\python.exe -m pip install -e ".[dev]"
```

### 第 1 步：先跑离线测试（不需要 API Key）

```powershell
& ..\.venv\Scripts\python.exe -m pytest tests
```

期望最后一行：

```text
121 passed
```

（23 个测试文件；本章新增 `test_recovery.py` 8 个用例 + `test_ch11_integration.py` 3 个，`test_openai_chat.py` 和 `test_config.py` 也随本章扩展到 4 和 6 个。）

### 第 2 步：配置四个环境变量

第 11 章起 `python/.env` 需要**四个**字段，比前十章多一个 `OPENAI_FALLBACK_MODEL`：

```text
OPENAI_BASE_URL=
OPENAI_API_KEY=
OPENAI_MODEL=
OPENAI_FALLBACK_MODEL=
```

如果漏了 fallback，启动时会立刻失败，而不是等到真的过载时才发现：

```text
配置错误: 缺少或填写错误的必要配置: OPENAI_FALLBACK_MODEL
```

四个都没配的话，四个字段会**一次性全部列出**，不用一个一个试。

`OPENAI_FALLBACK_MODEL` 填什么？最有意义的选法是**和主模型不同容量或不同部署的模型**——主模型连续过载时，切到一个同样过载的模型没有任何收益。填成和主模型同名是允许的（配置校验不拦），但那样 fallback 就只是空转。

### 第 3 步：跑一次真实请求

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch11.cli --prompt "只回答一句话：你现在用的是哪个模型名？不要调用任何工具。"
```

实际输出（模型答什么取决于你配的端点）：

```text
[Hook] 触发事件: UserPromptSubmit
[Hook] 触发事件: Stop
（模型的回答）
```

两行 `[Hook]` 各出现**一次**，这是本章一个隐含的正确性信号：恢复层在 Hook 之内，内部重试不会让它们重复打印。

### 第 4 步（可选）：完整门禁

```powershell
& ..\.venv\Scripts\python.exe -m compileall -q agent_ch11
& ..\.venv\Scripts\python.exe -m ruff check agent_ch11 tests
& ..\.venv\Scripts\python.exe -m mypy agent_ch11
```

### 常见报错排查

| 你看到的 | 原因 | 怎么办 |
| --- | --- | --- |
| `配置错误: 缺少或填写错误的必要配置: OPENAI_FALLBACK_MODEL` | 沿用了前十章的三字段 `.env` | 补上第四个字段 |
| `ValueError: recovery_config 需要第十一章或更高章节` | 给 P10 或更早的 profile 传了恢复配置 | 恢复配置只在 P11+ 生效，去掉或换 profile |
| `ValueError: 第十一章及以后必须提供 recovery_config` | 手工调 `build_agent(P11, ...)` 没传配置 | 传一个 `RecoveryConfig`；这条拦的是「静默退化成裸模型」 |
| `RecoveryError: 恢复回合尚未开始` | 直接调 `complete()` 没先 `begin_turn()` | 每个 turn 开始时调一次 `begin_turn()` |
| `ValueError: request.model 必须匹配 RecoveryConfig.primary_model` | 请求里自己指定了别的模型名 | 不要在请求里覆盖 `model`，fallback 由恢复层决定 |
| `ValueError: request.max_tokens 必须匹配 initial_max_tokens` | 请求里自己指定了输出预算 | 预算由恢复层升级，不要在请求里写 |
| `ValueError: escalated_max_tokens 必须大于 initial_max_tokens` | 两档预算写反或写成相等 | 升级档必须严格大于初始档 |
| `ValueError: escalated_max_tokens 不能超过 model_max_tokens` | 升级后预算超过模型上限 | 调小 `escalated_max_tokens` 或调大 `model_max_tokens` |
| `InvalidRetryAfterError: Retry-After 不是秒数或 HTTP-date` | 端点返回了非法值或无时区日期 | 看「Retry-After 解析规则」那张表 |
| `PromptTooLongRetryError` | 压缩一次后仍超窗口 | 查是否有单条巨型 user 消息或巨型工具结果 |
| `RecoveryRetriesExhausted: 续写恢复次数已耗尽` | 续写次数用完还在截断 | 拆小任务，或调大 `max_continuations` |
| `RecoveryRetriesExhausted: 续写恢复要求非空纯文本片段` | 截断的回复带 `tool_calls` 或正文为空 | 这是有意的硬失败，见「路径一」 |
| `RecoveryRetriesExhausted: 限流恢复次数已耗尽` | 10 次都是 429 | 配额确实不够，降并发或换 key |
| `RecoveryDeadlineExceeded: 等待时间会达到或超过当前回合 deadline` | 要等的时间超过剩余时限 | 调大 `total_timeout_seconds`，或接受这次失败 |
| `AgentRunError: 模型回答被内容过滤器拦截` | 供应商内容过滤命中 | 改 prompt；这不是可重试错误 |
| `OpenAIResponseError: 不支持的 finish_reason: ...` | 端点返回了未知结束原因 | 先确认端点兼容性；宽容处理会引入静默截断 |
| 500 / 503 / 连接错误直接抛出 | 本章有意不重试这些 | 见「这章没有做什么」 |

### 动手小实验

这几个实验都不调真实模型。在 `tests/test_recovery.py` 末尾追加即可，辅助函数（`ActionModel`、`_manager`、`_config`、`_request`）都是现成的。

**实验一：三条路径各自的完整轨迹**

参考 `test_first_length_reply_is_discarded_and_uses_escalated_budget` 和 `test_second_length_reply_continues_only_in_request_snapshot_and_merges_text`：脚本化三段回复（length → length → stop），断言 `max_tokens` 序列 `[8_000, 64_000, 64_000]`、消息数序列 `[2, 2, 4]`、最终 content 是两段拼接。

**实验二：续写的四条边界**

构造四个变体：一直截断（数清 5 次请求）、`max_continuations=0`（只剩 2 次）、片段带 `tool_calls`（立刻失败且 `recovery_count=0`）、空字符串片段（同样拒绝）。前两个抛「次数已耗尽」，后两个抛「非空纯文本片段」——错误不同，含义不同：前者是「机会用完」，后者是「根本不该续写」。

**实验三：退避曲线与 Retry-After 解析**

把 `jitter` 注入 `lambda upper: upper`（取满），10 次 429 跑完数 `sleeps` 序列，对照上面那张表（总 159.375 秒）。再用 `Retry-After: "3"`、HTTP-date、`"不是合法头"` 三种输入跑 429，分别得到 3 秒、差值秒、`InvalidRetryAfterError`。

**实验四：时限、取消、配置校验**

参考 `test_deadline_and_pre_cancelled_turn_stop_before_network_request`：预取消后 `model.requests == []`（一次请求都没发）；假时钟推进过 deadline 抛 `RecoveryDeadlineExceeded`。再把 `RecoveryConfig` 的各种非法组合逐个构造，看构造期就抛 `ValueError`（包括两条「看着像错、其实合法」的：`max_continuations=0` 和 `jitter_ratio=0` 都能构造成功）。

**实验五：从完整 Agent 观察恢复层**

参考 `test_p11_requires_recovery_config_and_keeps_recovery_attempts_out_of_history`：第一次回复是截断的，第二次才完整。跑完数 canonical history——只有 user 和最终 assistant 两条；数 `max_tokens`——`[8_000, 64_000]`；数不带 tools 的旁路请求——`model` 字段全是 `None`。**改一个地方再跑一次**：把第一条回复从 `"length"` 改成 `"stop"`，请求次数 2 → 1，`max_tokens` 序列只剩 `[8_000]`，而 canonical history 的**形状**没变——历史结构对「路上重试了几次」完全无感，这正是恢复层想要的效果。

**实验完记得把这些追加的测试删掉或改回去。**

---

## 这章没有做什么

写清楚边界，比列举功能更能说明这一层的定位。

**不重试 500 / 503 / 连接错误。** 本章只对 429 和 529 做退避重试。500、503、连接重置、超时全部原样抛出。原因是这些状态的语义不明确：500 可能是幂等的临时故障，也可能是「请求已经被部分处理」。在不知道服务端是否已产生副作用的情况下重试，风险大于收益。真要加，需要先明确幂等性契约。

**不做流式恢复。** 本章的所有恢复都建立在「拿到一个完整的 `ModelReply` 之后再判断」这个前提上。流式响应在中途断开时，已经吐出的 token 该不该保留、要不要拼进续写，是另一套设计。

**不做同步调用的强制中止。** Python 版的 `CancellationToken` 只能在边界检查点生效，不能中断运行中的同步 SDK 调用（见「取消、deadline 与 Python 的边界」）。TS 版的 `AbortSignal` 做得到，这是两个版本的真实能力差异。

**不做主动 token 预算控制。** 恢复层是**被动**的：等到供应商说「太长了」才压缩。真实生产系统通常还会在发请求**之前**估算 token 数并提前裁剪。那属于成本控制，不属于故障恢复。

**不给旁路请求恢复能力。** memory selector/extractor、压缩摘要器、subagent 都还是裸模型。这不是遗漏，是因为它们需要各自的会话所有权契约：谁的 deadline？谁的 fallback 状态？共享主 Agent 的 `RecoveryState` 会让状态互相污染。

**不做跨 turn 的熔断。** `consecutive_529` 每次 `begin_turn()` 都清零。也就是说，如果连续三个 turn 各遇到两次 529，每个 turn 都会从主模型重新开始，不会记住「这个模型最近一直不行」。跨 turn 的健康度追踪需要一个 turn 之上的生命周期。

**不做多 fallback 链。** 只有一个 `fallback_model`。fallback 自己也过载时，就是重试到耗尽为止，不会再往下切。

---

## 与 Claude Code 的差异

Claude Code 的恢复系统比教学版 P11 覆盖更广的生产场景。CC 的查询引擎定义了 17+ 个 reason/transition，每轮 LLM 调用后根据 stop_reason 和错误类型路由到专门恢复路径；P11 只实现长度升级→续写→耗尽、prompt_too_long→响应式压缩→耗尽、429/529→退避→fallback→耗尽三条路径，其余错误原样失败。上述 CC 实现细节来自外部仓库 `shareAI-lab/learn-claude-code` 的 `s11_error_recovery/README.md`。

**退避公式。** CC 使用 `min(500 × 2^(attempt-1), 32000) + random(0~25%)`（毫秒），attempt 表示第几次重试。P11 应用 `base_delay_seconds × 2^attempt`（默认 0.5s），封顶 `max_delay_seconds`（默认 32s），再叠加可配置的 `jitter_ratio`（默认 0.25）。CC 从 attempt=1 开始（attempt-1 让首次等待 500–625ms），P11 从 attempt=0 开始（`2^0 = 1`），同样得到 0.5s + 抖动。两条曲线实质相同，参数都可配置。

**Fallback 切换。** CC 在切换到备用模型时清空当前轮所有 pending messages，并显示「Switched to {model} due to high demand」。P11 只将 `current_model` 从 primary 改为 fallback，messages 和 histories 原封不动。CC 的清空策略能避免不同模型 prompt 格式不兼容导致的 tokenization 错误；P11 保守地保留上下文，假设两模型 prompt 结构兼容。

**续写收益递减。** CC 的 `tokenBudget.ts` 追踪每次 continuation 的 token 增量，连续 3 次每次不足 500 token 时主动停止续写，避免无限追加无实质进展的片段。P11 只检查固定的 `max_continuations` 计数（默认 3 次），不评估每次 output 的实际质量变化。

**流式恢复。** CC 在流式模式下会将 429/529 等错误缓冲，不中断已有的流式输出，等待流结束再决策重试；用户对后端恢复几乎无感知。P11 当前的 `ModelClient` 和 `RecoveryManager` 都走 request/response 模式，一次 LLM 调用要么返回要么抛错，不支持流式恢复路径。

---

## 配套代码索引

想按文件读代码，从这张表进：

| 文件 | 你能在里面找到什么 |
| --- | --- |
| `agent_ch11/features/recovery.py` | 本章主体。`RecoveryConfig`、`RecoveryState`、`CancellationToken`、五种 typed 失败、`RecoveryManager.complete()` 的三条恢复路径、`_retry_transient`、`_parse_retry_after`、`_merge_fragments`、`_split_leading_system` |
| `agent_ch11/core/model.py` | 稳定契约层。`FinishReason` 白名单、`ModelAPIError` 及三个子类的 status 不变量、`ModelRequest`/`ModelReply`/`TokenUsage`/`ModelClient` |
| `agent_ch11/adapters/openai_chat.py` | 唯一 import OpenAI SDK 的文件。`_map_api_status_error`、`_structured_error_identifier`、`_normalize_response`、`_to_openai_message` |
| `agent_ch11/core/loop.py` | `ModelRequestExecutor` 协议定义、`begin_turn()` 调用点、执行器与裸模型的分派、`length` guard、`content_filter` 显式失败 |
| `agent_ch11/bootstrap.py` | 组合根。能力位与配置的双向成对检查、`RecoveryManager` 构造、摘要器绑定裸模型 |
| `agent_ch11/core/profiles.py` | `P11 = P10 + "recovery"` 的能力位定义 |
| `agent_ch11/config.py` | 四字段校验、`http`/`https` 协议检查 |
| `agent_ch11/features/compaction.py` | 第 8 章的压缩器，本章通过 `compact_on_prompt_too_long()` 复用 |

测试按路径分文件，想看某条路径的全部边界条件，直接读对应测试：

| 测试文件 | 覆盖什么 | 用例数 |
| --- | --- | --- |
| `tests/test_recovery.py` | 三条恢复路径、退避、fallback、deadline、取消、快照隔离 | 8 |
| `tests/test_ch11_integration.py` | 从 `build_agent(P11, ...)` 端到端观察恢复行为（含 P10 的动态 Prompt 回归） | 3 |
| `tests/test_openai_chat.py` | 错误归一化、200 响应逐字段校验 | 4 |
| `tests/test_config.py` | 四字段校验 | 6 |

---

## 本章小结

### 三句话版本

API 韧性不是把每个异常都吞掉，而是让每条恢复路径有可验证的输入、状态和终止条件。三种故障各走各的路：输出截断在请求窗口内升级预算并续写，输入过长只压缩请求快照一次，429/529 按结构化信号退避重试并在连续过载时切 fallback。恢复过程对 canonical history 完全不可见——正式历史只接收最终那条合并后的完整回复。

### 一定要记住的五条

**一、恢复只改请求快照，不改正式历史。** 这是本章一半设计的来源。丢弃的截断片段、压缩掉的中间消息、失败的重试，都只发生在 `complete()` 内部的局部变量上。外层看到的永远是「一次模型调用，一条最终回复」。

**二、只认结构化字段，不匹配异常文本。** `status_code` 加 `error.code`，就这两样。文案会变，status 不会。一个字符串里带 429 的普通 `Error` 不该被当成限流——鸭子类型在这里是漏洞，不是灵活性。

**三、HTTP 200 也是不可信边界。** `finish_reason` 出现未知新值时明确失败，而不是宽容地当成 `stop`。宽容处理会让一个截断回复以「完整答案」的身份进入历史，而且没有任何人会知道。

**四、耗尽必须是 typed 失败，不是静默降级。** 五个 `RecoveryError` 子类各自对应一种耗尽原因。看到 `RecoveryDeadlineExceeded` 就知道是时限，看到 `RecoveryRetriesExhausted` 就知道是次数，看到 `PromptTooLongRetryError` 就知道压缩过一次还是不够。含糊的失败让线上问题不可诊断。

**五、能力位与配置必须成对。** 给 P10 传恢复配置报错，P11 不传恢复配置也报错。后者更重要——如果允许，P11 会静默退化成裸模型，你以为有恢复能力其实没有，直到凌晨两点才发现。

### 检查你是否真的读懂了

答不上来就回去看对应章节：

1. 一次续写恢复之后，canonical history 里有几条消息？外层 `turns` 是几？（「接入唯一 Loop」）
2. 为什么第一次遇到 `finish_reason == "length"` 时要把那段回复**整段丢弃**，而不是留着接续写？（「路径一」）
3. 无时区的日期串 `Retry-After` 会被接受吗？为什么？（「路径三」的解析表）
4. 抖动为什么加在 32 秒封顶**之后**，而不是之前？（「路径三」）
5. 连续两次 529、然后一次 429、再一次 529，会切 fallback 吗？（「路径三」）
6. 一个字符串里写着 `Error code: 429` 的普通 `Error`，会被归一成 `ModelRateLimitError` 吗？（「先归一化错误」）
7. 为什么摘要器必须绑定裸模型而不是 `RecoveryManager`？（「摘要器必须绑定裸模型」）
8. `content_filter` 为什么在 Loop 层拒绝，而不是在恢复层？（「接入唯一 Loop」）
9. 10 次瞬态尝试会产生几次 sleep？为什么默认时限是 300 秒？（「路径三」）
10. Python 版的 `CancellationToken` 和 TS 版的 `AbortSignal` 有什么能力差异？（「取消、deadline 与 Python 的边界」）

---

下一篇进入任务系统。Agent 已经能从 API 故障中恢复，但项目任务仍需要跨进程持久化、依赖解锁和原子认领。第 12 章会把会话 TODO 与真正的 JSON Task DAG 分开建模——前者是一次对话内的待办清单，后者是能被多个进程并发认领的持久化依赖图。

---

## Java 开发者的阅读和调试顺序

1. 先读 `python/ch11_agent/tests/test_recovery.py`。`ActionModel` 就是一个按顺序返回结果的 Mockito Stub。
2. 进入 `agent_ch11/features/recovery.py` 的 `RecoveryManager.complete()`，观察 `request_messages`、`RecoveryState` 和 `fragments`。
3. 回到 `agent_ch11/core/loop.py`，确认正式 history 只追加最终合并后的 assistant 消息。
4. 最后阅读 `agent_ch11/adapters/openai_chat.py`，理解供应商异常为什么必须在边界处归一化。

Python 与 Java 对照：`RecoveryManager` 是包在 HTTP Client 外面的 Resilience4j 装饰器；`CancellationToken` 是 `AtomicBoolean` + `CopyOnWriteArrayList<Runnable>`；`email.utils.parsedate_to_datetime` 是标准库的 HTTP-date 解析器（对应 `java.time.ZonedDateTime.parse` 加 RFC 1123 格式器）；`threading.Event.wait` 是可被打断的 `Object.wait()`；`ModelAPIError` 子类的 status 不变量是「构造器校验的枚举异常类」——`new ModelRateLimitError(..., status_code=500)` 直接在构造期抛 `ValueError`，类似把 `IllegalArgumentException` 放进异常构造器。
