# 11. API 韧性即生命：决定 AI Agent 商业化成败的隐藏细节（Python版）

凌晨两点，一个跑了三个小时的 Agent 任务挂了。日志最后一行：

```text
openai.APIStatusError: Error code: 529 - overloaded_error
```

没有重试，没有等待，也没有备用模型，任务直接失败。

这不是少见异常。429 是标准限流信号，很多 OpenAI 兼容端点会用 529 表示临时过载，输入超过上下文窗口时会返回结构化的 context-length 错误。问题不是 API 会不会出错，而是 Agent 能不能在清晰的边界内恢复。

第十章已经有动态 Prompt、记忆和上下文压缩，但模型请求仍然直接穿过唯一的 Agent Loop。第十一章只增加一层：让一次逻辑模型请求可以处理输出截断、输入过长和瞬态 API 故障，同时保证不污染正式历史、不重复执行工具、不隐藏最终失败。

---

## 三种故障，三条路

| 故障 | Python 内部信号 | 恢复动作 |
| --- | --- | --- |
| 输出被截断 | `ModelReply.finish_reason == "length"` | 第一次提升输出预算，之后在请求快照中续写 |
| 输入上下文过长 | `ModelPromptTooLongError` | 保留首条 system，响应式压缩一次 |
| 临时 API 故障 | `ModelRateLimitError` / `ModelOverloadedError` | 遵守 Retry-After 或指数退避，连续 3 次 529 后切 fallback |

三条路径统一放在 `RecoveryManager.complete()` 中。公共 `AgentRunner` 仍然只负责组装当前请求、执行工具、追加最终回复和运行 Hook。一次 API 重试不会重新进入外层 Loop，因此不会重复执行副作用。

---

## 先归一化错误，不要匹配异常文本

在 Java 里，我们不会写 `if (exception.getMessage().contains("429"))` 作为可靠的业务分支。Python 也一样。`openai_chat.py` 是供应商边界，负责把 SDK 的结构化错误转换成核心层异常：

```python
try:
    response = self._client.chat.completions.create(**payload)
except APIStatusError as error:
    mapped = _map_api_status_error(error)
    if mapped is None:
        raise
    raise mapped from error
```

映射规则固定且可测试：

```python
if status == 429:
    return ModelRateLimitError(
        "OpenAI 请求被限流",
        retry_after=retry_after,
        error_code=error_code,
        request_id=request_id,
    )
if status == 529:
    return ModelOverloadedError("OpenAI 模型暂时过载", error_code=error_code)
if status == 400 and error_code in PROMPT_TOO_LONG_CODES:
    return ModelPromptTooLongError("OpenAI 输入超过模型上下文窗口", error_code=error_code)
```

`error_code` 只从顶层或一层嵌套的 `code/type` 读取，不读取 `message`，也不递归搜索任意 JSON。未知 400、其他 5xx、连接错误、超时和程序错误都原样传播。

三种异常定义在 `agent_ch11/core/model.py`。它们把 HTTP status 作为不变量：限流只能是 429，过载只能是 529，输入过长只能是 400。这样 `RecoveryManager` 可以按异常类型分派，而不会收到语义矛盾的对象。

成功的 HTTP 200 同样是不可信边界。适配器还会检查 choices 数量、assistant role、finish reason、tool call 字段和 usage 字段。响应结构不正确时抛出 `OpenAIResponseError`，不会把脏数据送进恢复层或正式 history。

---

## 路径一：输出截断只存在于请求窗口

`length` 是合法响应，不是网络异常。第一次遇到它时，丢弃半截回答并升级预算：

```python
if reply.finish_reason == "length":
    if not state.has_escalated:
        state.current_max_tokens = config.escalated_max_tokens
        state.has_escalated = True
        continue
```

`RecoveryConfig` 会在创建时校验 `escalated_max_tokens <= model_max_tokens`。预算不能靠“先请求一次，再看供应商是否接受”来试错。

如果升级后仍然截断，才进入续写。续写片段和提示只追加到本地 `request_messages`：

```python
fragment = reply.message.content
if reply.message.tool_calls or not fragment:
    raise RecoveryRetriesExhausted("续写恢复要求非空纯文本片段")
fragments.append(fragment)
request_messages = (
    *request_messages,
    reply.message,
    user_message(CONTINUATION_PROMPT),
)
```

这里的关键是“本地快照”。`AgentRunner` 的 canonical history 不会看到中间半截回答，也不会看到续写提示。最终成功后，恢复层把所有片段拼起来，只返回一个 `ModelReply`：

```python
return _merge_fragments(reply, fragments)
```

默认最多续写 3 次。带未完成 `tool_calls` 的截断响应直接失败，避免产生没有对应 tool result 的孤儿调用。

---

## 路径二：输入过长时响应式压缩

输入过长来自 `ModelPromptTooLongError`，不能拿 `finish_reason == "length"` 代替。恢复层保留首条 system prompt，把剩余消息交给第八章的 `CompactionManager`：

```python
leading, compactable = _split_leading_system(request_messages)
outcome = self._compaction.compact_on_prompt_too_long(
    compactable,
    retry_count=prompt_too_long_retries,
)
request_messages = (*leading, *outcome.history)
```

这不会修改 canonical history、调用方传入的 tuple 或原始 `ModelRequest`。transcript 记录的是压缩前的请求快照，不是 history 对象的别名。

同一个逻辑请求只允许响应式压缩一次。第二次输入过长会抛出 `PromptTooLongRetryError`，而不是无限压缩。摘要失败时会删除刚写入的 transcript，避免工作区留下没有摘要的假 artifact。

摘要模型必须使用 raw `ModelClient`，不能套上 `RecoveryManager`。否则会形成“输入过长 -> 压缩 -> 摘要 -> 输入过长”的递归恢复链。

---

## 路径三：429、529、退避和 fallback

429 优先遵守 `Retry-After`。Python 版接受非负秒数和带时区的 RFC HTTP-date；空值、负数、`NaN`、`Infinity`、坏日期或无时区日期会抛出 `InvalidRetryAfterError`，不会 sleep，也不会重试。

没有有效 `Retry-After` 时，默认退避公式为：

```text
delay = min(base_delay_seconds * 2**attempt, max_delay_seconds)
        + random(0 .. delay * jitter_ratio)
```

默认基线是 0.5 秒，封顶 32 秒，抖动比例 25%，瞬态尝试最多 10 次。如果等待会达到当前用户回合的 deadline，直接抛出 `RecoveryDeadlineExceeded`。

529 只有连续出现才算“持续过载”。429 或成功会清零连续 529 计数；达到三次后将 `current_model` 切换为 `fallback_model`。messages 和 history 原封不动，前提是两个模型兼容同一套消息格式。

---

## 取消、deadline 与 Python 的边界

```python
token = CancellationToken()
unsubscribe = token.subscribe(lambda: print("收到取消通知"))
token.cancel()       # 幂等，只触发一次
unsubscribe()        # 退订
```

恢复层会在模型请求前后、退避等待前后、响应式压缩前后检查取消和 deadline。默认 sleeper 使用 `threading.Event.wait()`，所以正在退避时可以被取消。

这里有一个必须说清楚的 Python 差异：当前 `ModelClient.complete()` 是同步接口。取消令牌可以让下一次请求或等待及时失败，但不能像 TypeScript `AbortSignal` 那样强制中止一个已经运行中的同步 OpenAI SDK 调用。要做到真正中止，需要把模型边界改成异步客户端，或额外设计线程/进程取消协议；本章不伪装已经具备这个能力。

未知异常不会被包装成“可恢复错误”，而是原样抛出。这和 Java 服务中只捕获明确的 `RateLimitException`、`OverloadedException` 类似，避免把认证失败、DNS 错误和程序 bug 无限重试。

---

## 接入唯一 Agent Loop

`core/loop.py` 新增一个类似 Java interface 的协议：

```python
class ModelRequestExecutor(Protocol):
    def begin_turn(self) -> None: ...
    def complete(self, request: ModelRequest) -> ModelReply: ...
```

用户回合开始时，`AgentRunner` 调用一次 `begin_turn()`；每个外层模型轮调用一次 `complete()`。内部重试不会重新执行 UserPrompt Hook、history preparation、动态 Prompt、工具 schema 快照或 Stop Hook。

第 1 到第 10 章不注入 executor，仍直接调用 raw model，原有 `length` guard 继续作为明确失败。第 11 章 Bootstrap 才创建 `RecoveryManager`，并要求显式传入 `RecoveryConfig`：

```python
recovery_manager = RecoveryManager(model, compaction_manager, recovery_config)
return AgentRunner(
    model,
    tools,
    prompt,
    workspace,
    model_request_executor=recovery_manager,
)
```

恢复层只包装主 Agent Loop。记忆 selector/extractor、压缩摘要和子 Agent 仍使用 raw model，避免共享一个不属于它们的 `RecoveryState`。

---

## 和 Claude Code 的工程对照

生产级系统通常会有更多 stop reason 和状态迁移。第十一章只实现三条可验证路径：

1. 长度升级 -> 续写 -> 续写耗尽。
2. prompt-too-long -> 响应式压缩 -> 压缩耗尽。
3. 429/529 -> Retry-After 或指数退避 -> fallback -> 耗尽。

没有明确 typed signal、幂等语义和测试之前，未知错误原样失败比“什么都重试”更安全。流式恢复也没有被偷偷加入：当前模型边界是 request/response 模式，一次调用要么返回，要么抛异常。

---

## Java 开发者的阅读和调试顺序

1. 先读 `python/ch11_agent/tests/test_recovery.py`。`ActionModel` 就是一个按顺序返回结果的 Mockito Stub。
2. 进入 `agent_ch11/features/recovery.py` 的 `RecoveryManager.complete()`，观察 `request_messages`、`RecoveryState` 和 `fragments`。
3. 回到 `agent_ch11/core/loop.py`，确认正式 history 只追加最终合并后的 assistant 消息。
4. 最后阅读 `agent_ch11/adapters/openai_chat.py`，理解供应商异常为什么必须在边界处归一化。

---

## 运行与验证

本章复用 `python/.venv` 和 `python/.env`，不要每章重复创建。第十一章及以后必须配置 `OPENAI_FALLBACK_MODEL`：

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch11_agent'
& ..\.venv\Scripts\python.exe -m pip install -e ".[dev]"
& ..\.venv\Scripts\python.exe -m agent_ch11.cli --prompt "检查当前工作区并给出结论"
```

离线验证不需要 API Key，也不会访问网络：

```powershell
& ..\.venv\Scripts\python.exe -m pytest tests
& ..\.venv\Scripts\python.exe -m compileall -q agent_ch11
& ..\.venv\Scripts\python.exe -m ruff check agent_ch11 tests
& ..\.venv\Scripts\python.exe -m mypy agent_ch11 --no-incremental
```

真实 DeepSeek smoke test 只有在共享 `.env` 完整且允许联网时才运行；否则以离线测试结果为准。

---

## 小结

API 韧性不是吞掉所有异常，而是让每条恢复路径都有可验证的输入、状态和终止条件：

- 输出截断只在请求窗口内升级和续写，正式 history 只接收最终完整回复。
- 输入过长只压缩一次请求快照，当前 system prompt 和完整消息组不被破坏。
- 429/529 只由结构化信号驱动，退避、fallback、取消和总时限都可测试。
- 未知错误不重试，所有耗尽路径明确失败。

下一章会把会话级 TODO 与真正的 JSON Task DAG 分开建模，让任务可以跨进程持久化、依赖解锁和原子认领。
