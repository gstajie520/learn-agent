# 解密 Claude Code 协作机制：如何通过 Inbox 注入让 AI 队友真正实现“异步通信”？（Python版）

> 本文是第 15 章 TypeScript 原文的 Python 迁移版。代码保持原章节协议，改为适合 Java 后端开发者考古阅读的 Python 实现。

## 一、这一章解决什么问题

第 14 章已经能让 Cron 在后台产生事件，但只有一个 Agent。第 15 章把“子 Agent”升级成持久队友：Lead 创建 `alice` 后，alice 有自己的身份、历史和 Mailbox；它完成任务后把 `result` 消息发回 Lead。Lead 不需要增加 `check_inbox` 工具，而是通过公共 `EventInbox` 在空闲时启动一次事件回合。

Java 类比：`MailboxMessage` 像 `record`，`MailboxStore` 像 `MailboxRepository`，`FileMailboxStore` 是文件版 Repository，`TeammateRuntime` 是受管 WorkerService，独立 `AgentRunner` 是队友独立 Session。

## 二、Mailbox 四态状态机

每条消息包含：

```text
id / sender / recipient / kind / content / created_at_utc
```

`id` 同时是 RuntimeEvent 的 `event_id` 和工具副作用的 `idempotency_key`。正文不能为空，但保存时不会自动 `strip()`，避免改变业务文本。

```text
.agent_tutorial/
  .mailboxes.lock
  mailboxes/
    lead/ready/processing/done/quarantine/
    alice/ready/processing/done/quarantine/
```

状态迁移为 `ready -> processing -> done`、`processing -> quarantine` 和 `processing -> ready`。`claim()` 使用原子重命名取得租约；进程重启后遗留的 `processing` 会恢复为 `ready`。坏 JSON 会进入 `quarantine`，不能阻塞同一邮箱中的合法消息。

## 三、为什么每个队友必须独立 Runner

共享历史会让 alice 看到 bob 的上下文，重新创建 Runner 又会丢失历史。因此 `TeammateRuntime` 为每个队友保存 `name + role + status + runner + current_message`。第一次 `spawn` 先写 `task` 再启动 worker；任务完成后发送 `result` 给 Lead、ack 原消息并进入 `idle`。再次发消息给 idle 队友时，复用原 Runner 和原历史。

## 四、Lead 的事件回合

普通用户回合不能被 mailbox 抢占。事件先暂存，只有 `runner.run_events()` 才消费。事件回合顺序是：

1. 从 Inbox 取消息。
2. 把消息包装成普通 `user` 历史消息。
3. 使用消息 recipient 作为 `ToolContext.identity`，使用 UUID 作为幂等键。
4. 经过同一套 Hook、权限、工具注册表和 Agent Loop。
5. 模型处理成功后 ack Mailbox。

如果 ack 失败，历史和模型调用已经完成；下一次 `run_events()` 只重试 ack，不重复调用模型。这就是“至少一次投递 + 一次业务执行”的关键边界。

## 五、建议的考古顺序

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch15_agent'
& ..\.venv\Scripts\python.exe -m pytest tests/test_mailbox.py
& ..\.venv\Scripts\python.exe -m pytest tests/test_teammates.py
& ..\.venv\Scripts\python.exe -m pytest tests/test_ch15_loop.py
```

先看 Mailbox 的 `ready -> processing -> done`，再看 idle Runner 重用，最后看 ack 重试。这样阅读路径与 Java 的“领域模型 -> Repository -> Service -> 集成测试”完全对应。
