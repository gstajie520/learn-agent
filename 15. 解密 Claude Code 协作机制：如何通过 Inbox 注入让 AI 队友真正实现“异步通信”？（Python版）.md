# 解密 Claude Code 协作机制：如何通过 Inbox 注入让 AI 队友真正实现「异步通信」？（Python 版）

> 本文是第 15 章的完整 Python 迁移版，配套项目位于 `python/ch15_agent`。
> 原文的架构推导、状态机、持久化边界和对照分析完整保留。
> Java 开发者可以把 `MailboxMessage` 当成 record，把 `MailboxStore` 当成 Repository 接口，把 `TeammateRuntime` 当成受管 WorkerService，把 `EventInbox` 当成 `BlockingQueue<RuntimeEvent>`。

上下文窗口就那么大。

这句话说起来轻松，但真正踩到这个边界时，体感完全不同。一个后端重构任务同时涉及认证、数据库、API 路由和测试，单个 Agent 处理到后半程，前半程的细节已经离开上下文。它不是突然变笨，而是物理上看不见了。

第 6 章的一次性子 Agent 可以把独立任务放进干净上下文，完成后只把结果带回 Lead。但有些工作不是「一问一答」：队友需要保留自己的历史，接收后续补充，异步汇报进展，并在完成一轮工作后继续存在。

第 14 章刚刚解决了「没有人说话时 Agent 也能动」。那条链路的另一端是**时间**。本章把另一端换成**另一个 Agent**：事件不再来自闹钟，而来自一个有名字、有历史、还活着的队友。

第 15 章实现的就是这组最小闭环：**持续 Teammate、持久 Mailbox，以及把 Mailbox 消息重新送入公共 Agent Loop 的 typed event 通道**。

先把范围钉死。本章只做**文本消息的持久双向投递**：Lead 多两个工具，队友只有四个工具，消息落在磁盘上而不是内存里。没有 `check_inbox`、没有结构化 request/response、没有权限冒泡、没有 shutdown 握手。事件回到 Loop 的那条通路完全复用第 13 章的 `EventInbox`——**本章不新建第二条事件通道，也不新建第二个 Loop**。

---

## 本章导读

### 读完这一章，你能做到什么

1. 说清一次性子 Agent 和持续队友的差别不在「有没有并发」，而在**身份、历史、通信和生命周期由谁持有**。
2. 解释为什么「读文件 + 删文件」的收件箱是**至多一次**投递，以及它会在哪个具体窗口永久丢消息。
3. 说出「每消息一个文件 + 目录名就是状态」这个设计换来了什么，`ready/processing/done/quarantine` 四个目录分别代表什么事实。
4. 判断 claim 的权威 FIFO 键为什么是 `(created_at_utc, id)` 而不是文件名顺序，并举出一个按文件名排会排错的例子。
5. 解释为什么 mailbox 事件是**先进 canonical history 再 ack**，以及它是全章唯一保留「半状态」的事件类型。
6. 说清 `idle` 队友收到新消息时为什么能看到上一轮的对话，而 Alice 和 Bob 的历史为什么不会串线。
7. 说明队友的 `send_message` 里 `sender` 从哪来，为什么不能是工具输入的一个字段。
8. 判断一条队友汇报在 Lead 崩溃后会发生什么，以及为什么这是「至少一次」而不是「恰好一次」。

### 你需要先具备什么

| 前置 | 出自哪一章 | 本章怎么用它 |
| --- | --- | --- |
| Agent Loop 的一轮：模型 -> 工具 -> 结果回填 | 第 1 章 | 队友就是一个独立的 Loop，不是别的东西 |
| `ToolDefinition` / validator 精确键集合 | 第 2 章 | `spawn_teammate`、`send_message` 靠它守边界 |
| `ToolContext.identity` 是谁在调工具 | 第 3 章 | **本章重点之一**：队友的 sender 来自它，不来自输入 |
| 一次性子 Agent 的委派与返回 | 第 6 章 | 本章的对照组，读完你要能说出五个差别 |
| 文件锁 + 原子替换 + fail closed | 第 12、14 章 | Mailbox 的每一次状态迁移都沿用同一套纪律 |
| `RuntimeEvent` / `EventInbox` / `RuntimeEventPump` | 第 13 章 | **最关键的前置**，队友消息走的就是这条通路 |
| `run_events()` 与 history-before-ack | 第 14 章 | 本章在它上面只加了一处：mailbox 的半状态 |
| Profile 能力位与组合根按能力注入 | 第 5～14 章 | P15 靠 `teammate` + `mailbox` 两个能力位接上运行时 |

只来得及补一章的话，补第 13 章的「事件怎样回到同一个 Loop」。本章新增的东西集中在**事件从哪来**，而**事件怎么回去**在第 13、14 章已经定型。

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| Lead | 你直接对话的那个 Agent，名字固定是 `lead`，是保留名 |
| Teammate（队友） | 一个有名字、有独立历史、能长期存在的 Agent |
| Mailbox（收件箱） | 每个 Agent 名下的一组磁盘目录，消息就存在里面 |
| `MailboxMessage` | 一条消息的领域对象，同时也是一个 `RuntimeEvent` |
| `kind` | 消息种类，只有 `task` / `message` / `result` 三种 |
| claim（认领） | 把一条消息从 `ready` 原子搬到 `processing`，等于取得租约 |
| 租约（lease） | 「这条消息现在归我处理」的状态，未确认前不会被别人拿走 |
| ack（确认） | 「这条消息我处理完了」，从 `processing` 搬到 `done` |
| release（归还） | 放弃租约，从 `processing` 搬回 `ready`，保留重放机会 |
| quarantine（隔离） | 坏消息或处理失败的输入，搬到一边留证据，不堵住队列 |
| 至少一次投递 | 崩溃可能让同一条消息重放，但不会静默丢掉 |
| `idempotency_key` | 本章就是消息 UUID，传给这一轮所有工具用于去重 |
| event-only turn | 没有用户输入、由事件触发的一次模型回合 |
| 半状态 | 事件已进 history 但 ack 还没成功的中间态，只有 mailbox 有 |
| `wakeup` | 新消息到达时请求宿主跑一次 `run_events()` 的回调 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 问题的本质                 ← 为什么一个进程内 dict 不够
  ↓
② 只增加两个工具             ← 以及为什么没有 check_inbox
  ↓
③ MailboxMessage 建模        ← 消息和事件是同一个对象
  ↓
④ 每消息一个文件             ← 目录名就是状态，本章最重要的存储决定
  ↓
⑤ FIFO、恢复与坏消息隔离     ← 三个容易被忽略的正确性边界
  ↓
⑥ TeammateRuntime            ← 一个名字，一个持续 Runner
  ↓
⑦ 队友工具裁剪为四个         ← 少给工具不等于绕过治理
  ↓
⑧ Inbox 注入                 ← 先入历史，再确认；mailbox 的半状态
  ↓
⑨ 共享一套运行时             ← 一个 Inbox，一个 supervisor
  ↓
⑩ 失败与关闭                 ← 谁负责把在途工作放回去
  ↓
⑪ 接进组合根                 ← 能力位、18 个工具、CLI 接线
  ↓
⑫ 运行 + 实验 + 排错         ← 把上面每条结论都亲手验一遍
  ↓
⑬ 小结 + 自测                ← 记住七条 + 自测题
```

---

## 问题的本质：协作不是多开几个模型调用

一次性子 Agent 和持续队友的差别，不在于有没有并发，而在于**身份、历史、通信和生命周期由谁持有**。

| 边界 | 一次性子 Agent | 第 15 章 Teammate |
|---|---|
| 生命周期 | 完成一次委派后返回 | 完成后进入 `idle`，可接收后续消息 |
| 对话历史 | 每次委派独立 | 每个名字绑定一个独立 `AgentRunner`，后续继续复用 |
| 通信 | 返回最终结果 | 通过持久 Mailbox 双向发送文本消息 |
| 公开工具 | Lead 的 `task` | Lead 的 `spawn_teammate`、`send_message` |
| 状态 | 运行或结束 | `running`、`idle`、`failed`、`shutdown` |

### 为什么一个进程内的 dict 不够

最容易想到的实现是这样：

```python
# 看起来够用，但不够
inbox: dict[str, list[str]] = {}
inbox["lead"].append("alice 说：README 缺少恢复说明")
```

它在三个地方会出问题，而且都不是理论问题：

**第一，进程退出等于全部丢失。** 队友跑了三分钟的成果，进程一关就没了。这在「一问一答」时无所谓，因为一问一答本来就活在一次调用里；但持续队友的意义恰恰是**跨调用存在**。

**第二，读和处理之间有窗口。** 假设改成「读文件，然后删文件」：

```text
t0  读到消息内容
t1  ← 进程在这里崩溃
t2  把消息写入 history（永远没执行）
```

消息在 `t0` 已经从磁盘消失，在 `t2` 之前又没进 history。它**永久消失，而且没有任何痕迹**。这叫至多一次投递（at-most-once）。

**第三，多个 writer 追加同一个文件要额外解决三个问题。** 并发写会交错，崩在半行会留下不可解析的残行，多个读者还要各自维护游标。解决完你会发现自己写了一个小型消息队列。

### 所以本章的拆法

本章把协作拆成四个明确边界：

```text
Lead 工具：spawn_teammate / send_message
    |
    v
FileMailboxStore
  每消息一个文件，ready -> processing -> done/quarantine
    |
    v
TeammateRuntime
  持有队友 Runner、状态和 worker，并组合 Mailbox/Cron event
    |
    v
AgentRunner.run_events()
  把 typed message 注入 canonical history，再执行 event-only turn
```

每一层的职责可以一句话说清：

| 层 | 只负责一件事 | 不负责 |
| --- | --- | --- |
| 工具层 | 校验输入、注入可信 sender | 不知道消息存在哪 |
| `FileMailboxStore` | 消息的持久化和状态迁移 | 不知道队友是什么 |
| `TeammateRuntime` | 队友的生命周期和 worker 循环 | 不知道 Loop 长什么样 |
| `AgentRunner` | 事件进 history、跑一轮模型 | 不知道消息从哪来 |

第 15 章严格等于第 14 章已有能力，再增加 `teammate` 和 `mailbox`。Cron、后台任务、权限、Hook、恢复和前面章节的工具都继续存在，不另起一套 Loop。

---

## 只增加两个工具

Lead 相对第 14 章只追加两个工具（`features/teammates.py`）：

| 工具 | 必填字段 | 可观察结果 |
|---|---|---|
| `spawn_teammate` | `name`、`role`、`prompt` | 创建独立 Runner，把首个 `task` 消息持久写入队友 Mailbox，并异步启动 worker |
| `send_message` | `to`、`content` | 把 `message` 持久写入 Lead 或已存在队友的 Mailbox |

validator 是精确键集合（`set(arguments) == {"name", "role", "prompt"}`），多一个字段就整体失败，不是「忽略未知字段」。这一条和第 12、14 章的纪律一致。

### 名字必须是安全的小写 slug

Agent 名不是随便一个字符串，因为它**直接作为目录名落在磁盘上**。校验规则是一条正则加 Windows 保留名检查（`core` 里的 `canonical_agent_name`）：

```python
_AGENT_NAME = re.compile(r"^[a-z0-9]+(?:-[a-z0-9]+)*$")
_WINDOWS_RESERVED = {"con", "prn", "aux", "nul",
    *(f"com{i}" for i in range(1, 10)), *(f"lpt{i}" for i in range(1, 10))}
```

`test_safe_agent_name_and_four_state_transition` 实测的拒绝清单：

```python
for value in ("Lead", "../lead", "lead/other", "nul", "trailing.", "two_words"):
    with pytest.raises(ValueError):
        canonical_agent_name(value)
```

逐条解释为什么拒绝：

| 被拒的名字 | 如果放过会怎样 |
| --- | --- |
| `Lead` | 大小写混合在 Windows 上和 `lead` 是同一个目录，在 Linux 上不是。同一份代码在两个平台行为不同 |
| `../lead` | 目录穿越，消息会写到 workspace 外面 |
| `lead/other` | 变成两层目录，`recipient` 和路径不再一一对应 |
| `nul` | Windows 保留设备名，创建目录会失败或行为诡异 |
| `trailing.` | Windows 会静默去掉结尾的点，名字和目录名不一致 |
| `two_words` | 下划线本身无害，但允许它就要回答「大小写呢」「中文呢」，边界会一直往外滑 |

`lead` 是保留名，不能被队友占用——`spawn` 里有专门的检查（「lead 是保留身份，不能创建同名队友」），报的是具体错误，不是笼统的「名字不合法」。

**校验发生在做任何工作之前。** 名字不合法时，不会先创建目录再回滚，也不会先起一个 worker 再杀掉。

### 18 个工具，两个新的排在最后

P15 的 Lead 工具序列完整保留 P14 前缀（16 个），只在末尾追加 `spawn_teammate`、`send_message` = **18 个**。

### 这里没有 check_inbox

这是本章和多数教学实现最明显的分歧点，值得单独说。

一个直觉的做法是给 Lead 一个 `check_inbox` 工具，让模型自己决定何时查收消息。它的问题是**把一个运行时职责交给了模型的判断**：

```text
模型没想到要查    -> 消息躺在磁盘上，没人处理
模型每轮都查一次  -> 每轮多一次工具调用，绝大多数返回空
模型查的时机不对  -> 消息迟到，但没有任何机制保证它最终被查到
```

「消息什么时候到达」是运行时能确切知道的事实，不需要猜。所以本章的做法是：Mailbox 是**事件源**，消息由 `TeammateRuntime` 发布到 `EventInbox`，走第 13 章那条已经定型的通路。Lead 的工具表里根本没有这个动作。

---

## MailboxMessage：先把消息建模成事件

一条消息不是随手拼出的字典，而是严格、不可变的领域对象（`features/mailbox.py`）：

```python
@dataclass(frozen=True, slots=True)
class MailboxMessage:
    id: str                      # 全局唯一消息主键
    sender: str                  # 发送方 Agent 身份
    recipient: str               # 接收方 Agent 身份，决定邮箱目录
    kind: MailboxMessageKind     # task / message / result
    content: str                 # 原始正文；保存时不自动 strip
    created_at_utc: dt.datetime  # 带 UTC 时区，FIFO 排序用
```

**注意：消息和事件不是两个对象，是同一个对象的两个视角。** `MailboxMessage` 通过三个 `@property` 直接满足 `RuntimeEvent` 协议：

```python
@property
def event_id(self) -> str: return self.id

@property
def context_identity(self) -> str: return self.recipient

@property
def idempotency_key(self) -> str: return self.id

@property
def prompt(self) -> str: return self.content
```

`kind` 只有三种：

| kind | 谁产生 | 用途 |
|---|---|---|
| `task` | `spawn_teammate` | 队友的第一条任务，创建时就写进它的收件箱 |
| `message` | `send_message` | Lead 或队友主动发送的后续文本 |
| `result` | 队友 worker | 一轮 Runner 执行完成后的最终结果，或可观察的失败结果 |

三种就够了，因为本章只做文本投递。结构化的 request/response 属于第 16 章。

### 五条校验，一条都不放过

`__post_init__` 逐字段校验（`test_safe_agent_name_and_four_state_transition` 验证了 `content` 保留原始空格——`store.send("lead", "alice", "保留原始空格 ", "task")`）：

| 字段 | 要求 | 不满足会怎样 |
| --- | --- | --- |
| `id` | 必须是规范 UUID | 抛 `Mailbox 消息 id 必须是 canonical UUID` |
| `created_at_utc` | 必须带 UTC 时区 | 抛错 |
| `sender` / `recipient` | 走同一套 Agent 名校验 | 抛不安全名字错误 |
| `content` | 不能为空 | 抛错；且**原样保留**，不做隐式 `strip()` |
| 字段集合 | 精确等于六个键 | 磁盘读取时多余字段进 quarantine |

`content` 不做隐式 strip 这条容易被当成小事。但如果 store 悄悄改写了消息正文，那么「持久化的消息」和「发送的消息」就不是同一条，后面所有基于内容比对的逻辑（比如幂等 ack）都会失效。

### 同一个对象，两个身份

```python
event_id = message.id
idempotency_key = message.id
```

这两个 property 让 `MailboxMessage` 不需要任何转换就能进 `EventInbox`。带来三个好处：

1. **Mailbox 和 Cron 共享同一个 typed `EventInbox`**，不需要第二条事件通道。
2. **Loop 的去重逻辑不用改**：它本来就按 `event_id` 去重。
3. **消息 UUID 天然成为 idempotency key**，这一轮所有工具都能拿到同一个稳定键。

`to_payload()` 输出的是这个形状：

```json
{
  "kind": "mailbox",
  "message_id": "00000000-0000-4000-8000-000000000701",
  "sender": "lead",
  "recipient": "alice",
  "message_kind": "task",
  "content": "阅读 README 并列出缺口",
  "created_at_utc": "2026-07-30T08:00:00Z"
}
```

外层的 `kind: "mailbox"` 是事件类型，内层的 `message_kind` 才是消息种类。Loop 靠外层那个字段识别 mailbox 事件——**不是靠文本前缀猜**，也不是靠 XML 标签包裹。

`context_identity` 返回的是 **`recipient`（接收方）**——这是 Python 版的一个设计要点：队友写给 Lead 的消息，在 Lead 的事件回合里以 **lead 身份**执行（`_run` 构造 ToolContext 时用 `runtime_event.context_identity`）；而 Lead 派给队友的 task，在队友的 runner 里以队友身份执行（runner 构造时 `identity=name`）。身份跟着**收件人**走，和第 14 章 cron 的「跟创建者走」不同——因为 mailbox 的消费者天然就是收件人。

---

## 每消息一个文件，状态由目录表达

这是本章最重要的存储决定。

### 磁盘布局

Mailbox 数据位于当前 workspace：

```text
.agent_tutorial/
  .mailboxes.lock
  mailboxes/
    lead/
      ready/
      processing/
      done/
      quarantine/
    alice/
      ready/
      processing/
      done/
      quarantine/
```

`send()` 一条消息之后：

```text
mailboxes/
  alice/
    done/   (空)
    processing/   (空)
    quarantine/   (空)
    ready/
      00000000-0000-4000-8000-000000000701.json
```

那个文件的完整内容：

```json
{
  "id": "00000000-0000-4000-8000-000000000701",
  "sender": "lead",
  "recipient": "alice",
  "kind": "task",
  "content": "阅读 README 并列出缺口",
  "created_at_utc": "2026-07-30T08:00:00Z"
}
```

六个字段，一个不多一个不少。中文按 UTF-8 原样存（`json.dumps(ensure_ascii=False)`）。

### 目录名就是状态

每条消息对应一个 `{uuid}.json`。它在哪个目录里，就是什么状态：

| 状态 | 含义 | 谁负责让它离开这里 |
|---|---|---|
| `ready` | 已持久写入，尚未被消费者认领 | 消费者 claim |
| `processing` | 已被一个消费者原子 claim，尚未确认 | 消费者 ack / release / quarantine |
| `done` | 已进入消费者的完成边界 | 没有人，它是终态 |
| `quarantine` | 文件或消息无效，或队友处理该消息失败 | 没有人，留作证据 |

**这个设计的好处是：查询状态不需要读文件内容。** 想知道 Alice 有几条待处理消息，`ls` 一下 `alice/ready/` 就够了。不需要解析、不需要维护索引、不需要担心索引和实际内容不一致——**目录结构本身就是索引**。

### 四态迁移实测

一条消息走完全程（`test_safe_agent_name_and_four_state_transition` 逐条断言）：

```python
message = store.send("lead", "alice", "保留原始空格 ", "task")
assert store.claim("alice") == message       # claim 返回同一条消息
assert store.ack(message) is True           # ack -> done
assert store.ack(message) is True           # 幂等重复 ack 也成功
assert (tmp_path / ".agent_tutorial" / "mailboxes" / "alice" / "done" / f"{message.id}.json").exists()
```

### 写入纪律：临时文件 + fsync + rename

发送时不是直接写目标路径，而是三步：同目录 `tempfile.mkstemp` → 写入 + `os.fsync` → `os.replace` 提交到 `ready/{id}.json`。和第 12、13、14 章的原子替换完全同一套。

### claim 不是「读完再删」

这是和 dict 方案最本质的区别：

```text
ready/{id}.json
    -- rename -->
processing/{id}.json
```

ack 同样通过 rename 完成。消息在整个生命周期里**从来没有从磁盘上消失过**，它只是换了个目录。所以任何时刻崩溃，消息都还在，都能从目录名知道它当时处于什么状态。

对比一下两种方案在同一个崩溃点的表现：

| 崩溃时刻 | 读完就删 | 本章的 rename |
| --- | --- | --- |
| 拿到消息后、处理前 | 消息已删除，**永久丢失** | 在 `processing/`，重启恢复 |
| 处理完、确认前 | 消息已删除，无法判断是否处理过 | 在 `processing/`，重启重放一次 |
| 确认后 | 正常 | 在 `done/`，正常 |

第一行是至多一次，第二列是至少一次。**宁可重复，不可丢失**——这一条和第 14 章 cron 事件的取舍完全一致。

### 幂等 ack：一个真实的跨 runtime 时序

设想两个 store 实例先后处理同一条消息（模拟进程崩溃后另一个接手）：

```text
runtime A: claim 消息 X          -> X 在 processing
runtime A: （卡住 / 崩溃）
runtime B: 启动，recover_processing -> X 回到 ready
runtime B: claim X               -> X 在 processing
runtime B: 处理完，ack           -> X 在 done
runtime A: 恢复过来，也要 ack X   -> processing/X 已经不存在了
```

最后那一步该怎么办？Python 版 `_transition` 的处理（`adapters/mailbox_json.py`）：

```python
if not source.exists():
    # 特殊处理：ack 幂等（重复 ack 成功）
    if destination != "done":
        return False
    completed = directories["done"] / f"{message.id}.json"
    if not completed.exists():
        return False
    # 完整快照比较，防止确认不同正文的消息
    if not messages_equal(self._load(completed, message.recipient), message):
        raise MailboxStorageError(f"消息与已完成记录不一致: {message.id}")
    return True
```

**读出来比对**：内容相同就幂等成功，内容不同就**大声失败**。旧消费者不会把已完成消息重新排队，同 ID 冲突也不会被静默吞掉。

### UUID 全局唯一

`test_duplicate_id_is_rejected` 验证：同一个 UUID 在**所有 Agent、所有状态目录**中必须唯一：

```python
first.send("lead", "alice", "one", "task")
with pytest.raises(MailboxStorageError):
    FileMailboxStore(str(tmp_path), id_generator=generator).send("bob", "lead", "two", "result")
```

为什么比「每个邮箱内唯一」更严格？因为消息 id 同时是 `event_id` 和 `idempotency_key`——如果两条不同消息共用一个 id，Loop 的去重会把第二条当成重复事件直接丢掉。

### 一把工作区级锁

**Python 版与 TS 版的真实差异**：TS 版是两把锁（进程内 Promise 队列 + 跨进程 `proper-lockfile`）；Python 版是**进程内 `threading.RLock` 按 mailboxes 目录路径共享**，与其他章节（9/12/13/14）同一个模式，**没有跨进程文件锁**。跨进程正确性靠 recover_processing 恢复机制兜底（见下节），不靠互斥。`.mailboxes.lock` 是跨实现可见的锁标记文件，但不是 `proper-lockfile` 那种带 stale 心跳的文件锁。

---

## FIFO、恢复与坏消息隔离

三个容易被忽略的正确性边界，逐个说。

### FIFO 键不是文件名

文件名是 UUID，UUID 按字典序排和消息顺序没有任何关系。claim 的权威 FIFO 键是：

```python
message, source = min(candidates, key=lambda item: (item[0].created_at_utc, item[0].id))
```

先比较 UTC 创建时间；时间相同时，用规范 UUID 文本稳定打破平局。

**为什么需要 id 作为第二排序键？** 因为同一秒内可以发出多条消息。如果只按时间排，两条同时间消息的相对顺序取决于目录枚举顺序，而目录枚举顺序在不同文件系统、不同次运行之间**不保证一致**。加上 id 之后，顺序就是确定的：**重启后的顺序与目录枚举顺序无关。**

### processing 不是「已经完成」

运行时重建时必须恢复：

```text
processing -> ready -> 再次 claim
```

恢复发生在两个时机：

| 时机 | 恢复谁的邮箱 |
| --- | --- |
| `TeammateRuntime.start()` | Lead 的邮箱（`self.store.recover_processing(self.lead_name)`） |
| `spawn()` 创建队友时 | 该队友邮箱中遗留的 processing 消息（`self.store.recover_processing(name)`） |

这是至少一次投递的具体实现：崩溃可能让同一消息重放，但不能因为进程中断把它静默丢掉。

### 一条坏消息不能堵住整个收件箱

这是队列系统的经典失败模式：队头有一条无法解析的消息，后面所有正常消息全部卡住。

claim 和 recover 会逐个严格校验，坏文件**原子移入 `quarantine` 并继续寻找下一条合法消息**，不中断整次 claim。`test_bad_json_is_quarantined_without_blocking_valid_message` 实测：

```python
store.send("lead", "alice", "valid", "message")
(ready / "not-a-uuid.json").write_text("{", encoding="utf-8")   # 坏文件
assert store.claim("alice") == message                            # ← 合法消息照样取到
assert (ready.parent / "quarantine" / "not-a-uuid.json").exists() # 坏文件被隔离
```

**一次 claim 就把坏文件清出了 ready，并且拿到了那条合法消息。** 收件箱没有被堵住。

`recover_processing` 也只搬**合法**的 processing 消息回 ready，quarantine 里的不会被拉回来重试。隔离的文件保留下来供人检查。**证据不能被后续的失败冲掉。**

---

## TeammateRuntime：一个名字，一个持续 Runner

存储层讲完了，现在看谁在用它。

### Runner 工厂一次性配置

**Python 版与 TS 版的一个结构差异**：TS 版的 `TeammateRuntime` 直接在构造时持有全部依赖；Python 版把**队友 Runner 的创建**抽成了一个显式的工厂接口：

```python
runtime.configure_runner_factory(factory)   # start 之前只能配置一次
runtime.start()                            # 恢复 Lead 租约并发布 ready 消息
```

为什么？因为创建一个队友 Runner 需要模型、工具、权限、Hook、Recovery、Compaction——这些全在组合根（bootstrap）手里，`TeammateRuntime` 自己造不出来。工厂是运行时和组合根之间唯一的接缝：

```python
# bootstrap.py 里的 teammate_factory
def teammate_factory(name, role, send_definition):
    teammate_tools = create_chapter_two_tools(command, actual_file_system)
    teammate_tools.register(send_definition)
    teammate_recovery = RecoveryManager(model, teammate_compaction, recovery_config) ...
    return AgentRunner(model, teammate_tools, f"You are {name}, serving as {role}.",
                       workspace, model_request_executor=teammate_recovery,
                       identity=name, permission_policy=policy, hooks=actual_hooks, ...)
```

`configure_runner_factory` 有两道守卫：只能在 `start()` 之前配置、只能配置一次（`RuntimeError`）。测试里注入替身工厂就是靠它（`test_worker_delivers_result_and_reuses_runner` 的 `runners` 列表）。

### spawn 不等待完成

`spawn_teammate` 返回时队友还在跑。运行时为每个合法且未占用的名字创建一个内部 `_Worker` 记录：

```text
_Worker
  ├─ Teammate 快照（name / role / status，冻结对象）
  ├─ AgentRunner（这名队友独占，持有全部历史）
  ├─ 当前 worker 线程（None 表示没有在途循环）
  ├─ 当前 processing MailboxMessage（租约，close 时必须归还）
  └─ cleanup_error（close 失败记录）
```

状态快照是**整体替换**的冻结 dataclass，不是就地修改。

### 第一条任务也走 Mailbox

一个容易想到的偷懒做法是：`spawn` 时直接把 prompt 塞进队友 Runner 的 history。本章没有这么做，而是先把它作为 `task` 消息落入队友 Mailbox，再由 worker claim 出来（`spawn` 里三行：`recover_processing(name)` → `store.send(sender, name, prompt, "task")` → `_start_worker(worker)`）。

**多绕这一圈换来什么？** 三件事：

1. 第一条任务和后续消息走**完全相同**的路径，不需要两套逻辑。
2. `spawn` 之后立刻崩溃，任务还在磁盘上，重启能接着做。
3. 队友的第一轮和后续轮次拿到的 `idempotency_key` 语义一致，都是消息 UUID。

worker claim 后调用（`_run_worker`）：

```python
result = worker.runner.run(message.content, idempotency_key=message.id)
self.store.send(worker.teammate.name, self.lead_name, result.final_text, "result")
if not self.store.ack(message):
    raise MailboxStorageError(f"Mailbox ack 失败: {message.id}")
```

三行代码，顺序有讲究：**先发 result，再 ack 输入**。反过来的话，在两步之间崩溃会导致输入已确认但结果没发出——那条任务就无声无息地消失了。按现在这个顺序，最坏情况是结果发了两次，而两次的 `idempotency_key` 相同，下游可以去重。

### 状态机：完成不等于销毁

```text
spawn                -> running
一轮完成             -> idle
收到新消息           -> running
再次完成             -> idle
执行异常             -> failed
runtime close 完成   -> shutdown
```

关键在 `idle`：**Runner 没有被销毁。**

Lead 后续调用 `send_message(to="alice", ...)` 时，如果 Alice 处于 `idle`，运行时重新启动该 worker，但继续使用原来的 Runner（`send` 里：`worker.teammate.status == "idle"` 时置回 `running` 并 `_start_worker(worker)`）。

`test_worker_delivers_result_and_reuses_runner` 把这条完整钉死了：

```python
runtime.spawn("alice", "writer", "draft", sender="lead")
...
runtime.send("alice", "revise", sender="lead")   # 第二轮
...
assert len(runners) == 1                          # ← 只创建过 1 个 Runner
```

第二轮模型请求仍能看到第一轮的 user/assistant history——这就是持续队友和一次性子 Agent 最本质的差别。

### 历史不会串线

Alice 和 Bob 各自持有不同 Runner。每个队友的 system prompt 自带自己的名字和角色（`f"你是 {name}，职责是 {role}"`，`identity=name`）。Alice 看不到 Bob 在做什么，Bob 也看不到 Alice。

**这正是多队友的意义所在。** 如果两个队友共享上下文，那就没必要分成两个。分开的价值恰恰在于**各自的上下文里只有自己那份工作**。

### 三种拒绝

```python
if name == self.lead_name:
    raise ValueError("lead 是保留身份，不能创建同名队友")
if name in self._workers:
    raise ValueError(f"队友已经存在: {name}")          # 注册锁（RLock）内检查，无竞态窗口
if worker.teammate.status in {"failed", "shutdown"}:
    raise RuntimeError(f"队友当前状态不可接收消息: {worker.teammate.status}")
```

**并发同名 spawn 只有一个成功**——注册的「读-改-写」包在 `with self._lock:` 里，两个同时到达的 spawn 会被串行化。没有这道锁的话：两个调用都读到「不存在」，都创建 Worker，第二个覆盖第一个——第一个 Worker 的 Runner 变成孤儿。

`failed` 和 `shutdown` 的队友也不能继续接收消息。这条边界防止 Lead 把新任务发给一个已经坏掉的队友，让消息堆在一个永远不会被 claim 的 `ready` 目录里。

### has_pending_work 把队友算进去

**Python 版与 TS 版的一处差异**：TS 版的 `hasPendingWork` 故意**不算** idle 队友（因为 idle 是稳定态）；Python 版**算了**——`bool(self._queued) or any(worker.thread is not None and worker.thread.is_alive())`。但注意 alive 的判据是**线程在跑**（`running` 状态），不是「队友存在」（`idle` 状态线程已退出，`thread.is_alive()` 为 False）。语义上殊途同归：idle 队友不会让 Loop 永远等待。

---

## 队友工具严格裁剪为四个

Lead 继承前 14 章的完整工具面（18 个），队友没有。队友 Runner 只注册四个（`bootstrap.py` 的 `teammate_factory`）：

```python
teammate_tools = create_chapter_two_tools(command, actual_file_system)   # shell/read_file/write_file/edit_file/glob
teammate_tools.register(send_definition)                                  # send_message
```

注意：**Python 版的队友工具是第 2 章的五个 + send_message = 六个**（含 `edit_file`、`glob`），而 TS 版显式 `subset` 裁到四个（无 `edit_file`/`glob`）。`create_chapter_two_tools` 返回的是完整集，Python 版**没有再收窄**。这是两个版本工具面的真实差异，如实记录——队友的治理边界（权限/Hook/Recovery/Compaction）两者一致，只是工具广度不同。

### 没有的那些，各有原因

| 缺的工具 | 为什么不给 |
| --- | --- |
| `spawn_teammate` | 团队结构固定为一个 Lead 加多个平级队友。允许队友生成队友，拓扑会变成不可控的树 |
| `todo_write` | TODO 是 Lead 组织自己工作的手段，不是团队共享的看板 |
| `load_skill` | Skill 会带进大段上下文，与「队友上下文只装自己那份工作」冲突 |
| `task` | 一次性子 Agent 会产生第二层委派，和持续队友是两套并行的协作模型 |
| Task DAG 五个工具 | 队友从共享任务图自主找活属于第 17 章 |
| `schedule_cron` | 队友能定时唤醒自己会让生命周期复杂一个量级 |

### 工具少不等于绕过治理

这是最容易误会的一点。队友的工具**不是特殊通道**，它们和 Lead 的工具走完全相同的执行顺序：

```text
参数 schema 校验
  ↓
PreToolUse Hook
  ↓
PermissionPolicy 判决
  ↓
handler 执行
  ↓
PostToolUse Hook
```

每个队友 Runner 使用自己的名字作为 `ToolContext.identity`（`identity=name`），并复用当前章节的 Hook、`PermissionPolicy`、Recovery 和 Compaction 边界——`teammate_factory` 里这些一个不少地传进去了。队友调 `shell` 时该弹的审批一样会弹，该记的审计一样会记。

### sender 不能伪造

队友的 `send_message` 输入里**只有 `to` 和 `content`**，没有 `from`：

```python
def _send_tool(self, arguments, context):
    message = self.send(
        str(arguments["to"]), str(arguments["content"]), sender=context.identity
    )
    ...
```

`sender=context.identity` 这一行是全章最短但最重要的安全边界之一。

如果 `sender` 是工具输入的一个字段，那么 Alice 可以发一条声称来自 Bob 的消息，Lead 无法分辨。**身份必须由运行时观察，不能由被治理方申报**——这条原则和第 3 章 `ToolContext.identity` 的设计动机是同一个。

---

## Inbox 注入：先进入 canonical history，再 ack

现在把消息送回 Loop。这一节是本章和第 13、14 章接缝的地方。

### 完整顺序

队友写给 Lead 的消息会从 `ready` claim 到 `processing`（`_publish_lead`），再以 `MailboxMessage` 原对象发布到共享 `EventInbox`。Loop 空闲时执行显式 event-only turn（`core/loop.py` 的 `run_events`）：

```text
① 检查 _pending_event_acks（有欠账先补 ack，不重复调模型）
② drain 一条 typed RuntimeEvent（每次只取一个）
③ 序列化并追加到 Lead canonical history（_run 里 runtime_event_message）
④ 执行模型回合
⑤ 调用 acknowledge_events() -> Mailbox processing -> done
```

**第 ③ 和第 ⑤ 步的顺序不能倒过来。**

假设先 ack 再追加 history：

```text
t0  ack 成功，消息进 done
t1  ← 进程在这里崩溃
t2  追加到 history（永远没执行）
```

消息在 `done` 里，没人会再处理它；history 里没有它，模型永远不知道队友汇报过什么。**永久丢失，且没有任何痕迹。**

按正确顺序，同样的崩溃点：消息还在 `processing`，重启时 `recover_processing` 把它搬回 `ready`，重新投递一次。最坏结果是**重复一次**，而重复由 `idempotency_key` 兜底。

### mailbox 是唯一保留半状态的事件

这是本章在第 14 章的 Loop 上唯一的实质性改动，`run_events` 的核心分支：

```python
result = asyncio.run(self._run(...))
if self._event_pump is not None:
    try:
        self._event_pump.acknowledge_events((event,))
    except Exception:
        # history 和模型调用已经完成，保留事件身份，下一轮只补 ack。
        self._pending_event_acks[event.event_id] = (event, result)
        raise
```

对比两类事件在 ack 失败时的处理：

| 事件类型 | ack 失败后 | 为什么 |
| --- | --- | --- |
| cron 事件 | **回滚**：`release_events` 归还租约 | 事件还在 outbox 里，下次会重新产生，从头走一遍即可 |
| mailbox 事件 | **保留**：history 留着，登记进 `_pending_event_acks` | 消息重发后只需补 ack，不该重复进模型 |

`_pending_event_acks` 的下一个 `run_events()` 进来时**第一件事就是补 ack**（不 drain 新事件、不调模型，直接返回上次的结果）：

```python
if self._pending_event_acks:
    event, result = next(iter(self._pending_event_acks.values()))
    if self._event_pump is not None:
        self._event_pump.acknowledge_events((event,))
    self._pending_event_acks.pop(event.event_id, None)
    return result
```

这个中间态就是「半状态」：**已进历史，未确认**。它是 mailbox 独有的。

### 实测：ack 失败不会重复

`test_mailbox_event_is_ack_after_model_and_ack_retry_is_idempotent` 注入一个第一次必然失败的 ack（用 `store.ack = ack_once` 替换）：

```python
try:
    runner.run_events()
except MailboxStorageError:
    pass
assert runner._model.calls == 1     # ← 模型已经调过 1 次
runner.run_events()                  # 第二次：补 ack
assert runner._model.calls == 1     # ← 模型仍是 1 次，没有重复！
```

两个数字是这个实验的全部意义：

- **模型请求数：1 → 1。** ack 失败后的重试没有再调模型。
- **消息没有被重复处理。** 半状态生效。

为什么 Python 版模型调用是 1 而 TS 版实验里第一次是 0？因为两个版本的执行顺序略有差异：Python 版是**先跑模型回合、ack 失败时记半状态**（所以第一次就有 1 次模型调用）；TS 版第一次 `runEvents` 在 ack 失败时还没走到模型。两个版本都满足同一个不变量：**ack 重试不产生第二次模型调用**。这是实现细节差异，如实记录。

### 这不是「恰好一次」

必须说清楚边界：这是**至少一次接收 + Runner 内稳定 ID 去重**，不是对任意外部副作用的全局恰好一次承诺。

Mailbox message ID 会作为 idempotency key 传播下去，但真正有外部副作用的工具**仍需在自己的边界实现幂等**。运行时能保证的是「同一条消息重放时你拿到同一个键」，不能保证「你用这个键做的事只发生一次」。

---

## Mailbox、Cron 和 Background 共享一套运行时

P15 不创建第二个事件队列，也不创建第二个 task owner。

```text
EventInbox（一个）
  <- JobSupervisor 的 background event
  <- CronRuntime 的 CronEvent
  <- TeammateRuntime 的 MailboxMessage
```

`TeammateRuntime` 通过**组合**已有的 `CronRuntime` 实现这件事：

| 方法 | 怎么实现 |
| --- | --- |
| `drain_events` | 转给 `inbox.drain`，同时清掉 mailbox 的内存入队标记（`_queued`） |
| `wait_for_events` | 转给 `inbox.wait` |
| `has_pending_work` | 自己的 `_queued` + 活跃 worker 线程 |
| `acknowledge_events` | **先让 Cron ack 属于它的事件，再逐条 ack mailbox 消息** |

最后一行是关键：一个共享队列里混着两种事件，**每种事件由自己的 owner 确认**（`isinstance(event, MailboxMessage)` 分流）。互不越界。

`release_events` 对应地：mailbox 消息归还租约（`store.release`），cron 事件转给 `cron_runtime.release_events`。

### 构造时就检查共享关系

看起来相似但彼此断开的资源是一类很难排查的 bug。检查放在构造器里：

```python
if cron_runtime.supervisor is not supervisor or cron_runtime.event_inbox is not inbox:
    raise ValueError("cron_runtime 必须共享 background supervisor 和 EventInbox")
```

注意用的是 `is not`，**引用相等**，不是结构比较。组合根的 `bootstrap.py` 再加两道：

```python
if mailbox_store is None or teammate_runtime is None:
    raise ValueError("第十五章必须提供 mailbox_store 和 teammate_runtime")
if teammate_runtime.store is not mailbox_store:
    raise ValueError("teammate_runtime 必须共享 mailbox_store")
```

### wakeup 闭环

CLI 里两行绑定：

```python
teammate_runtime.bind_wakeup(request_event_turn)   # 队友汇报 -> Lead event turn
teammate_runtime.start()
```

`_publish_lead` 发布新消息后调 `self._wakeup()`。注意 wakeup 回调**失败会被吞掉**（源码注释：「唤醒回调失败不能打断 worker 线程；消息已安全落盘，可由下次 start() 恢复」）——消息已经 claim 到 processing 了，唤醒失败只是晚一点消费，不是丢失。

**如果宿主漏接 wakeup 会怎样？** 消息停在 `processing`（已 claim、未消费），下次进程的 `start()` 会 `recover_processing` 搬回 `ready` 再重新 claim。**至少一次投递把跨进程的这段缺口补上了。** 内存 dict 在这里会直接丢消息。

---

## 失败与关闭：谁负责把在途工作放回去

分布式系统里最难的部分不是成功路径，是失败路径。

### 失败也是一条消息

队友执行崩了怎么办？答案是：**崩溃也走 Mailbox。**

`_run_worker` 的 except 分支：

```python
except Exception as error:
    worker.teammate = Teammate(worker.teammate.name, worker.teammate.role, "failed")
    try:
        self.store.send(
            worker.teammate.name, self.lead_name,
            f"队友 {worker.teammate.name} 执行失败: {error}",
            "result",
        )
        self._publish_lead()
    except Exception:
        return
```

三件事按顺序发生：输入消息进 `quarantine`（在前面 except 里已经做了，带失败原因，**不回 `ready`**——避免同一条任务无限重试打崩队友）、队友状态置 `failed`、给 Lead 发一条 `result` 说明失败。

`test_failed_worker_reports_result_and_becomes_failed` 实测：

```python
event = runtime.wait_for_events(1)[0]
assert "执行失败" in str(event.to_payload()["content"])
assert runtime.state("alice").status == "failed"
```

请注意失败通知的 `message_kind` 是 `result`，**不是**某个新的 `error` 类型。这是刻意的：Lead 不需要为「失败」写一条独立的处理分支，它只是收到一条内容是失败说明的汇报。**用信息内容表达异常，而不是用协议类型表达异常。**

那个外层 `except Exception: return` 是全章少数被允许的空 catch：**失败通知本身失败时，不应该覆盖原始错误**。

### 关闭要把在途工作放回去

关闭时，正在执行的队友消息**必须回到 `ready`**，不能留在 `processing`，也不能丢。

`_run_worker` 里对应分支（判断依据和 TS 版一致）：

```python
if self._closed or (worker.stop is not None and worker.stop.is_set()):
    self.store.release(message)   # ← 关闭是我们主动发起的，不算消息的错
    worker.current = None
    return
...
self.store.quarantine(message)   # ← 执行异常才算消息的错
```

`close()` 内部逐个 worker：`stop.set()` → `thread.join(5)` → `store.release(worker.current)` → `runner.close()` → 状态置 `shutdown`。多个失败会收集起来，**抛第一个**（`raise failures[0]`）。

`release` 和 `quarantine` 的区别正好体现了两类失败的差别：

| 结束方式 | 消息去向 | 语义 |
| --- | --- | --- |
| 队友执行抛错 | `quarantine` | 这条消息本身有问题或处理不了，别再试 |
| 主动关闭 | `ready` | 这条消息没问题，只是没轮到它做完，下次接着做 |

### 关闭顺序：反向

组合根按创建顺序登记资源，`AgentRunner.close()` **反向**关闭：

```python
resources=(background_supervisor, cron_runtime, teammate_runtime)
# 关闭时反向遍历：teammate_runtime -> cron_runtime -> background_supervisor
```

依赖方向是单向的：`TeammateRuntime ──依赖──> CronRuntime ──依赖──> JobSupervisor`。**每一层关闭时，它依赖的东西还活着。**

这和 `try/finally` 的嵌套顺序、C++ 的析构顺序是同一个道理。

---

## 接进组合根

### P15 = P14 + 两个能力位

`P15 = P14 全部 + mailbox, teammate`。没有替换、没有删除、没有改语义，只是加了两位。

### 配对检查

能力位和实例必须成对出现，否则组合根**直接拒绝启动**：

| 情况 | 报错 |
| --- | --- |
| P15 但不传 `teammate_runtime` | `第十五章必须提供 mailbox_store 和 teammate_runtime` |
| `teammate_runtime.store` 不是传入的 `mailbox_store` | `teammate_runtime 必须共享 mailbox_store` |
| `cron_runtime` 不共享 supervisor/inbox | `cron_runtime 必须共享 background supervisor 和 EventInbox` |

这是**fail-closed**：宁可启动时崩掉，也不要跑起来之后发现消息进了一个没人读的队列。

### P15 的完整工具列表

P14 的 16 个前缀 + `spawn_teammate` + `send_message` = **18 个**。

**只加了两个工具。** 本章绝大部分工作量在 Mailbox 状态机、Runner 复用、半状态 ack 这些**模型看不见的部分**。

---

## 运行第 15 章

### 第 0 步：准备环境

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch15_agent'
& ..\.venv\Scripts\python.exe -m pip install -e ".[dev]"
```

### 第 1 步：先跑测试（不需要 API Key）

```powershell
& ..\.venv\Scripts\python.exe -m pytest tests
```

期望最后一行：

```text
152 passed
```

（32 个测试文件；本章新增 `test_mailbox.py` 3 个 + `test_teammates.py` 2 个 + `test_ch15_loop.py` 1 个。）

### 第 2 步：真跑

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch15.cli --prompt "创建一名 writer 队友，让她阅读 README 并汇报缺口"
```

### 你应该看到什么

Lead 大致会走这样一条路径：

```text
① 调 spawn_teammate(name="writer", role="writer", prompt="阅读 README 并汇报缺口")
   -> {"name":"writer","role":"writer","status":"running"}

② 立刻返回，Lead 继续自己的 turn
   （writer 在后台跑，Lead 不阻塞）

③ writer 跑完 -> result 写进 lead/ready -> claim -> 发布到 Inbox -> wakeup
   -> Lead 的 event turn 收到汇报
```

### 第 3 步：看磁盘上留下了什么

```powershell
Get-ChildItem -Recurse .agent_tutorial\mailboxes | Select-Object FullName
```

会看到类似结构：

```text
.agent_tutorial\mailboxes\lead\ready
.agent_tutorial\mailboxes\lead\processing
.agent_tutorial\mailboxes\lead\done
.agent_tutorial\mailboxes\lead\quarantine
.agent_tutorial\mailboxes\writer\...
```

读一条消息的内容：

```powershell
Get-Content .agent_tutorial\mailboxes\lead\done\*.json
```

六个字段一目了然：`id`、`sender`、`recipient`、`kind`、`content`、`created_at_utc`。

### 第 4 步：再跑一次，看恢复

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch15.cli --prompt "看看有没有队友的汇报"
```

这次 `start()` 会先把上一次留在 `processing` 的消息恢复出来。**这就是至少一次投递在真实进程边界上的效果。**

### 第 5 步（可选）：完整门禁

```powershell
& ..\.venv\Scripts\python.exe -m compileall -q agent_ch15
& ..\.venv\Scripts\python.exe -m ruff check agent_ch15 tests
& ..\.venv\Scripts\python.exe -m mypy agent_ch15
```

### 常见报错排查

| 现象 | 原因 | 怎么办 |
| --- | --- | --- |
| `配置错误: 缺少或填写错误的必要配置: ...` | `python/.env` 缺项 | 回第 1 章配置四个变量 |
| `Agent 名必须是安全的小写 slug` | 名字大写、含 `/`、含 `_`、Windows 保留名 | 用 `writer`、`api-writer` 这类 |
| `lead 是保留身份，不能创建同名队友` | 想创建叫 `lead` 的队友 | 换个名字 |
| `队友已经存在: xxx` | 同名队友已存在 | 直接 `send_message` 给它 |
| `找不到队友: xxx` | 给没创建过的队友发消息 | 先 `spawn_teammate` |
| `队友当前状态不可接收消息: failed` | 目标队友已失败 | 看 `xxx\quarantine` 里的失败原因 |
| `第十五章必须提供 mailbox_store 和 teammate_runtime` | 自己装配时漏了实例 | 传入共享实例 |
| 队友汇报了但 Lead 没反应 | 宿主未绑定 wakeup，或 Lead 正在执行用户 turn | 检查 `bind_wakeup`；并发时等待当前 turn 结束 |
| `lead\processing` 里一直有文件 | 上次进程没走完 | 下次 `start()` 自动恢复 |
| `TeammateRuntime 已关闭` | 关闭后还在 spawn | 检查生命周期 |
| `Mailbox 消息 id 已存在: ...` | 同一个 UUID 发了两次 | 检查自定义 `id_generator` |
| `ModuleNotFoundError: No module named 'agent_ch15'` | 没执行本章的 `pip install -e` | 回到第 0 步 |

### 动手小实验

这几个实验都不调真实模型。在 `tests/test_mailbox.py` / `test_teammates.py` 末尾追加即可，`FileMailboxStore` 的 `id_generator` / `clock` 注入是现成的。

**实验一：什么名字会被拒绝**

参考 `test_safe_agent_name_and_four_state_transition` 的拒绝循环（6 个坏名字逐个 `pytest.raises`）。学到：**名字直接当目录名，校验挡的是路径逃逸和 Windows 陷阱。**

**实验二：一条消息在磁盘上长什么样**

`store.send(...)` 后 `Get-Content` 那个 JSON 文件。**要观察的**：`id`、`event_id`、`idempotency_key` 是**同一个值**；四个状态目录一次建齐；文件里只有六个字段，**状态在目录名里，不在文件里**。

**实验三：四态迁移和幂等 ack**

`send → claim → ack → ack（重复）`，断言重复 ack 返回 `True` 且文件在 `done/`。再构造一条**同 ID 但 content 不同**的消息去 ack——预期 `MailboxStorageError`。学到：**幂等不等于宽容**。

**实验四：坏消息不堵队列**

参考 `test_bad_json_is_quarantined_without_blocking_valid_message`：ready 里塞一个坏文件，claim 仍拿到合法消息。学到：**隔离后继续，这是 quarantine 的全部意义。**

**实验五：Runner 复用（两轮协作）**

参考 `test_worker_delivers_result_and_reuses_runner`：spawn → 等结果 → `send` 第二轮 → 再等。断言 `len(runners) == 1` 和两次 `to_payload()["content"]`。学到：**idle 不是销毁，第二轮看到第一轮的历史。**

**实验六：队友崩了会怎样**

参考 `test_failed_worker_reports_result_and_becomes_failed`：工厂造一个 `complete` 就抛 `RuntimeError` 的模型。断言事件 content 含「执行失败」、状态是 `failed`。学到：**失败也是一条 result 消息，Lead 不需要单独的错误分支。**

**实验七：ack 失败不会重复调模型**

参考 `test_mailbox_event_is_ack_after_model_and_ack_retry_is_idempotent`：`store.ack` 替换成首败后成功的版本，两次 `run_events()`，断言 `runner._model.calls` 前后都是 1。学到：**重试落在 ack 上，不落在模型上。**

**实验八：同 UUID 再发被拒**

参考 `test_duplicate_id_is_rejected`：两个 store 实例用同一个 `id_generator`，第二次 `send` 抛错。学到：**UUID 是全局唯一键，冲突明确失败。**

**实验完记得把这些追加的测试删掉或改回去。**

---

## 这章刻意没有做什么

写教程最容易犯的错是把「还没做」说成「做完了」：

| 没做的事 | 为什么 | 在哪一章 |
| --- | --- | --- |
| `check_inbox` 工具 | mailbox 是运行时事件源，不该让模型猜什么时候该拉取 | 不做 |
| list / cancel 队友的工具 | 本章只验证最小闭环 | 按需 |
| 结构化 request/response 协议 | 现在只有单向 `task` / `message` / `result` | 第 16 章 |
| 计划审批冒泡 | 需要双向等待通道 | 第 16 章 |
| `shutdown_request` / `shutdown_approved` 握手 | 本章的 `close()` 是进程内资源释放 | 第 16 章 |
| 队友从共享 Task DAG 自主找活 | 需要抢占、竞争裁决和空闲自驱 | 第 17 章 |
| 广播 / 订阅 / 动态拓扑 | 拓扑固定为一个 Lead 加多个平级队友 | 不做 |
| 持久团队注册表 | 队友信息完全在 `TeammateRuntime` 进程内 | 不做 |
| 队友生成队友 | 会让拓扑变成不可控的树 | 不做 |

有一个概念特别容易误解，单独说明：

> **`idle` 只表示「Runner 还留着，正在等下一条显式消息」，不表示「它会自己去找活干」。**

空闲队友主动发现并领取工作是第 17 章的事。本章的队友是被动的：不发消息给它，它就一直待着。

---

## 与 Claude Code 的差异

参照外部仓库 `shareAI-lab/learn-claude-code` 的 `s15_agent_teams/README.md` 中对 CC 源码的分析：

| 维度 | CC 教学版 | CC 真实源码 | P15 |
| --- | --- | --- | --- |
| 存储 | 每 Agent 一个 `.jsonl` | JSON 数组 + 文件锁 | 每消息一个 `.json` + 四态目录 |
| 读取 | `check_inbox` 工具 | `useInboxPoller` 每 1s 轮询 | typed event 注入 + 显式 `run_events()` |
| 投递语义 | 至多一次（read+unlink） | 文件锁保护 | 至少一次 + 稳定幂等键 |
| 队友载体 | daemon 线程，10 轮结束 | tmux 窗格或进程内 | 独立 `AgentRunner`，不销毁 |
| 消息类型 | 2 种 | 15 种结构化类型 | 3 种（`task`/`message`/`result`） |
| 权限冒泡 | 省略 | 双向轮询 | 复用同一套治理，不跨 Agent 冒泡 |

**存储模型。** CC 教学版为每个 Agent 使用一个 `.jsonl` 文件，`read_inbox()` 执行读文件 + `unlink()` 消费式读取，存在 read/unlink 竞态。P15 使用每消息一个 `.json` 文件，目录名表达状态，原子 `rename` 迁移。

**消息读取方式。** CC 真实源码的 `useInboxPoller` 每 1 秒检查一次收件箱，自动提交为新 turn。P15 把 mailbox 事件放入共享 `EventInbox`，由宿主显式调用 `run_events()` 处理，wakeup 回调负责唤醒。

**消息类型。** CC 真实源码有 15 种结构化消息类型（`idle_notification`、`permission_request` 等），文本消息包装在 `<teammate-message>` XML 标签中。P15 只有三种，typed event 而非 XML 标签。

**投递语义。** CC 教学版的 `read_inbox` 是 at-most-once。P15 采用至少一次 + 稳定幂等键。

**权限冒泡。** CC 真实源码使用双向轮询做权限冒泡。P15 的队友和 Lead 复用同一套 Hook、`PermissionPolicy`、Recovery 和 Compaction，未单独实现跨 Agent 冒泡——这是第 16 章的内容。

如果你只想记住一件事：**CC 用轮询 + 消费式读取换取实现简单，P15 用事件注入 + 四态目录换取可恢复性。**

---

## 从 ai-agent-book 学到什么：消息信封与可观察协作

外部仓库 `bojieli/ai-agent-book` 的 `book/chapter10.md` 最贴近本章：它把不共享上下文的多 Agent 协作拆成数据平面与控制平面，并用结构化信封和 Message Bus 解决异步通信。

### P15 的对照与取舍

P15 没有照搬中心 Message Bus，而是把 ai-agent-book 的结构化消息、可观察历史和资源审计原则落到持久 Mailbox：

- **结构化信封。** ai-agent-book 用 `Envelope` 携带 sender/target/type/payload；P15 的 `MailboxMessage` 携带 `id`、`sender`、`recipient`、`kind`、`content`、`created_at_utc`，并直接实现 `RuntimeEvent`。
- **点对点 vs 发布订阅。** ai-agent-book 的中心 `MessageBus` 支持订阅和广播；P15 拓扑固定为 Lead 与多个平级队友，用持久收件箱做点对点投递。
- **可观测性。** ai-agent-book 的 `history` 让消息流可回放；P15 用磁盘四态目录和 typed history 表达同一件事，测试可以断言状态而不是只看打印文本。
- **失败隔离。** ai-agent-book 的单站点失败不阻塞 peers；P15 的队友执行失败会把输入移入 quarantine、向 Lead 发布可观察失败 `result`。
- **离线验证。** ai-agent-book 用真实浏览器测 speedup；P15 聚焦可重复离线测试：注入确定 UUID、clock 和模型替身，断言状态迁移、事件流、幂等 ack。**不声称有真实 wall-clock 加速证据。**

---

## 配套代码索引

| 文件 | 本章的角色 |
| --- | --- |
| `agent_ch15/features/mailbox.py` | `MailboxMessage` 领域模型、`canonical_agent_name`、`MailboxStore` 协议、四态状态机 |
| `agent_ch15/adapters/mailbox_json.py` | `FileMailboxStore`：四态目录、原子迁移、FIFO 排序、quarantine、幂等 ack |
| `agent_ch15/features/teammates.py` | `TeammateRuntime`：注册表、worker 生命周期、两个工具、事件转发 |
| `agent_ch15/core/events.py` | 第 13 章的 `RuntimeEvent`/`EventInbox`，本章复用不新建 |
| `agent_ch15/core/loop.py` | `run_events()`、`_pending_event_acks` 半状态、history-before-ack |
| `agent_ch15/bootstrap.py` | 组合根。配对检查、`teammate_factory`（队友 Runner 工厂） |
| `agent_ch15/cli.py` | 真实入口。共享 inbox 构造、wakeup 绑定 |
| `agent_ch15/core/profiles.py` | `P15 = P14 + "teammate" + "mailbox"` 的能力位定义 |
| `tests/test_mailbox.py` | 状态迁移、幂等 ack、quarantine、UUID 唯一性（3 个用例） |
| `tests/test_teammates.py` | Runner 复用两轮、失败汇报（2 个用例） |
| `tests/test_ch15_loop.py` | ack 失败重试不重复调模型（1 个用例） |

读代码的建议顺序：

```text
① features/mailbox.py            先搞清楚「消息是什么」
② adapters/mailbox_json.py      再看「消息怎么存」
③ tests/test_mailbox.py         用测试确认理解
④ features/teammates.py         然后是「谁来消费消息」
⑤ core/loop.py 的 run_events    最后看接缝（半状态）
⑥ bootstrap.py + cli.py         怎么装起来
```

---

## 本章小结

### 三句话版本

1. 给 Agent 加队友，难点不是多开几个模型调用，而是**队友的产出如何可靠地回到 Lead 的上下文里**。
2. 本章的答案是：**每消息一个文件，目录名就是状态，原子 `rename` 迁移，至少一次投递**。
3. 消息以 typed event 的形式**先进 canonical history，再 ack**，ack 失败保留半状态只重试确认。

### 一定要记住的七条

| # | 结论 | 一句话理由 |
| --- | --- | --- |
| 1 | Lead 只加两个工具：`spawn_teammate`、`send_message` | 没有 `check_inbox`，模型不该猜什么时候拉消息 |
| 2 | 队友有独立、可复用的 Runner 和明确状态机 | `idle` 不是销毁，第二轮消息复用既有 history |
| 3 | 一条消息一个文件，四态目录原子迁移 | 状态在目录名里，不在文件字段里 |
| 4 | processing 重启恢复，坏消息隔离，至少一次投递 | 一条坏消息不能堵住整个收件箱 |
| 5 | 先写 canonical history，再 ack | 顺序反了会造成「无痕丢消息」 |
| 6 | Teammate、Cron、Background 共用一个 Inbox 和一个 supervisor | 各自的事件由各自的 owner 确认 |
| 7 | 关闭按资源依赖逆序，全资源都尝试 | 每一层关闭时它依赖的东西还活着 |

### 一张图回顾整条链路

```text
                      ┌──────────────────────────────────────┐
                      │  Lead AgentRunner（唯一 Loop）        │
                      │  18 个工具                            │
                      └───┬──────────────────────────▲────────┘
        spawn_teammate    │                          │  ⑤ event turn
        send_message      │                          │     （run_events）
                          ▼                          │
                  ┌───────────────────┐              │
                  │ TeammateRuntime   │              │
                  │  名字 -> _Worker   │              │
                  └───┬───────────▲───┘              │
             ① 写入   │           │ ④ claim + publish │
                      ▼           │                  │
        ┌─────────────────────────┴──────────────────┴────────┐
        │  磁盘 Mailbox    .agent_tutorial/mailboxes/{name}/   │
        │                                                      │
        │   ready ──claim──> processing ──ack──> done          │
        │     ▲                  │                             │
        │     └──── release ─────┤                             │
        │                        └── quarantine（坏消息/失败）  │
        └──────────────────────────────────────────────────────┘
                      ▲           │
             ③ 写 result           │ ② claim 任务
                      │           ▼
                  ┌───┴───────────────────┐
                  │ 队友 AgentRunner       │
                  │  独立 history          │
                  └────────────────────────┘

        共享：EventInbox（一个）、JobSupervisor（一个）
```

顺着编号走一遍：① Lead 派活写进队友的 `ready`；② 队友 claim 走并执行；③ 结果写进 Lead 的 `ready`；④ 运行时 claim 并发布到共享 Inbox；⑤ Lead 在独立 event turn 里消费，先入 history 再 ack。

### 检查你是否真的读懂了

| 问题 | 回去看 |
| --- | --- |
| 为什么 `ack` 必须在写入 canonical history **之后**？ | 「Inbox 注入」的崩溃时序推演 |
| 为什么 mailbox 是唯一保留「半状态」的事件类型？ | `_pending_event_acks` 与实验七 |
| FIFO 的排序键是什么？为什么需要第二个键？ | 「FIFO 键不是文件名」 |
| `release` 和 `quarantine` 分别在什么情况下发生？ | 「失败与关闭」 |
| 为什么 `send_message` 的输入里没有 `from` 字段？ | 「sender 不能伪造」 |
| 为什么普通用户 turn 不消费 mailbox 事件？ | 「共享一套运行时」 |
| 队友汇报后 Lead 为什么可能不在本次会话响应？ | 宿主漏接 wakeup，或 Lead 正在执行用户 turn |

---

做到这些以后，队友的进展不再是一行易丢的日志，Lead 也不需要靠 `check_inbox` 猜测消息何时到达；宿主可以在明确的 event turn 边界处理它。

但现在的通道还是**单向**的：队友只能报告，不能提问。它做不到「这个操作有风险，我等你批准再动手」。下一章就在这条持久消息通道上加 request/response、计划审批和 shutdown 握手，让协作从「汇报」变成「对话」。
