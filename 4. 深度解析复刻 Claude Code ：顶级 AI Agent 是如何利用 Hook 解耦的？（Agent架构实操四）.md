上周帮一个朋友看他写的 Agent。最初的代码很简单：调用模型，拿到工具调用就执行，把结果放回 messages，再调用模型。

后来，同一个循环膨胀到了两百多行。PowerShell 日志、写文件确认、结果通知、自动备份都直接塞进了循环。每项需求单独看都合理，放在一起却让核心流程变成了谁也不敢改的 if-else 堆。

问题不在需求太多，而在扩展行为和核心编排没有分开。

![图片](https://mmbiz.qpic.cn/sz_mmbiz_png/1z26fJxNuiaEF9OfxccmqxNmg2ibzlIt2sAs4Qibxicucibibk3UsEMjz8gaaMIJVmdy17tFTJapdBPXCXuTInXxnXDodIic7Ktf89WGy0nsibjjYOU/640?from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

第 4 章要做的事情很明确：在现有 Agent Loop 中加入四个结构化 Hook 挂载点，同时不破坏第 1—3 章已经建立的消息配对、工具校验和权限边界。

Claude Code 官方目前定义了 14 类以上 Hook 事件，例如 `PermissionRequest`、`SessionStart`、`SubagentStart`、`PreCompact`、`Elicitation` 等，完整清单参见 https://code.claude.com/docs/en/hooks.md。

第 4 章按教学需要先选择覆盖一次完整工具交互的四类核心事件。

---

## 本章导读

### 读完这一章，你能做到什么

1. 说出四个 Hook 事件各自触发在流水线的哪一步，以及为什么是这四个位置。
2. 解释为什么 Hook 的返回值必须是结构化对象，而不是 `"stop"` 这类字符串。
3. 说清「Hook 说 allow」和「权限系统说 allow」的区别——前者只是建议。
4. 讲出一个 Post Hook 要求停止时，同一轮里其他工具调用会发生什么，以及为什么不能直接 `return`。
5. 解释 Stop Hook 为什么最多只能强制续跑一次。
6. 判断一个 Hook 抛异常时，会得到哪个错误码、handler 有没有执行过。

### 你需要先具备什么

| 需要 | 说明 |
| --- | --- |
| 读完第 1—3 章 | 本章不新增任何工具或权限规则，只在已有流水线上开孔 |
| 记得 `prepare → permission → invoke` | 四个挂载点全部围绕这条顺序摆放 |
| 记得 `deny > ask > allow > passthrough` | 本章的 Hook 建议要挂进这个合并逻辑 |
| 记得 `validateToolPairing()` | 本章一半的设计约束都来自「每个调用必须配对」 |

### 术语速查（本章第一次出现的词）

| 术语 | 一句话解释 |
| --- | --- |
| **Hook** | 注册在固定位置的回调。由应用注册，不是插件沙箱 |
| **挂载点** | Loop 里发布结构化上下文的确定位置。本章共四个 |
| **HookEvent** | 四个事件名之一：`UserPromptSubmit` / `PreToolUse` / `PostToolUse` / `Stop` |
| **HookContext** | 事件发布给回调的数据。**不同事件的可用字段不同** |
| **HookResult** | 回调的返回值。每个字段只有一种语义，且只在特定事件合法 |
| **HookRegistry** | 按事件保存回调列表；**注册顺序 = 执行顺序** |
| `permissionBehavior` | Pre Hook 提出的权限**建议**，不是决定 |
| `updatedInput` | Pre Hook 改写后的参数。会被重新 Zod 校验并深度冻结 |
| `blockingError` | Pre Hook 直接阻断，权限和 handler 都不跑 |
| `updatedOutput` | Post Hook 改写后的工具结果 |
| `additionalContext` | 追加的 system 消息。**只能是 system**，且延后写入 |
| `preventContinuation` | Post Hook 表示本轮不再请求模型 |
| `forceContinue` | Stop Hook 追加一条 user 消息并再问一次模型 |
| `stopHookActive` | 标记「已经强制续跑过一次」，防死循环 |
| **短路** | 出现 `blockingError` 或 `forceContinue` 时，后续同类回调不再执行 |
| **HookContractError** | 回调违反契约（返回值类型不对、用了非法字段、改了工具身份） |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先看一眼真实的执行顺序    ← 一次运行的完整日志，四个事件各打一行
  ↓
② 验收结果 + 为什么需要扩展点 ← 本章要证明什么、循环为什么不该继续长
  ↓
③ 四个事件 + HookContext    ← 位置固定、数据按事件裁剪
  ↓
④ HookResult                ← 本章设计的核心：为什么不用字符串
  ↓
⑤ HookRegistry              ← 顺序、合并规则、短路
  ↓
⑥ 四个事件逐个看            ← Submit / Pre / Post / Stop 各自能做什么
  ↓
⑦ 两个硬约束                ← Hook allow 只是建议；停止也要保持配对
  ↓
⑧ 故障映射 + P03/P04 差异    ← Hook 出错时的稳定错误码、和第 3 章逐项对比
  ↓
⑨ 组合根、CLI、边界、代码地图 ← Hook 怎么接进真实入口，刻意不做什么
  ↓
⑩ 运行、验证与小结           ← 动手 + 排错 + 四个实验 + 自测
```

---

## 先看一眼真实的执行顺序

先不看代码。P04 的真实 CLI 注册了四个只打日志的 Hook。跑一个只读任务：

```powershell
npm run ch04 -- --prompt '读取 README.md 并概括运行方式'
```

你会在 stderr 上看到这样一串（`[Permission]` 是第 3 章的审计，`[Hook]` 是本章新增的）：

```text
[Hook] UserPromptSubmit                ← ① prompt 已提交，模型还没收到请求
[Hook] PreToolUse: read_file           ← ② 工具已查到、参数已过 Zod，权限还没判
[Permission] read_file: allow (default) - No permission rule blocked the request
[Hook] PostToolUse: read_file -> ok    ← ③ handler 跑完了，结果还没写进历史
[Hook] Stop                            ← ④ 模型给出了无工具调用的回答，准备结束
```

把这四行的位置摆到第 3 章那条流水线上：

```text
用户提交 prompt
        │
        ├─── ① UserPromptSubmit ────── 能加 system 上下文，不能改/删 prompt
        ↓
   模型返回 tool_calls
        ↓
   ToolRegistry.prepare（查工具 + 解析 JSON + Zod 校验）
        │
        ├─── ② PreToolUse ──────────── 能改参数、提权限建议、直接阻断
        ↓
   PermissionPolicy / approval
        ↓
   handler（真正的副作用在这里）
        │
        ├─── ③ PostToolUse ─────────── 能改结果、能要求本轮结束
        ↓
   写入配对的 tool result
        ↓
   模型返回无工具调用的回答
        │
        └─── ④ Stop ────────────────── 能强制再问一次模型（最多一次）
        ↓
      结束
```

四个位置各自的选择理由，用一句话概括：

| 事件 | 为什么在这里 |
| --- | --- |
| `UserPromptSubmit` | 模型还没看到任何东西，是**唯一**能影响本轮输入的时机 |
| `PreToolUse` | 参数已经可信（过了 Zod），但副作用还没发生——**唯一**能无损阻断的时机 |
| `PostToolUse` | 结果已经产生，但还没进历史——**唯一**能改结果的时机 |
| `Stop` | 循环准备结束，是**唯一**能判断「任务真的做完了吗」的时机 |

注意每一行的「唯一」。**挂载点不是随便挑的位置，是那个信息刚好齐备、动作又还来得及的位置。** 早一步信息不全，晚一步无法挽回。

### 一个更有用的例子

只打日志看不出 Hook 的价值。假设你有三个真实需求：

| 需求 | 不用 Hook 会怎样 | 用 Hook 挂在哪 |
| --- | --- | --- |
| 每次运行都提醒模型核对验收标准 | 改 system prompt，但那是静态前缀，改了就破坏 KV Cache | `UserPromptSubmit` 返回 `additionalContext` |
| 禁止写某个由构建脚本生成的文件 | 在 `write_file` handler 里加 `if`，工具实现被业务规则污染 | `PreToolUse` 返回 `blockingError` |
| 工具输出太长就截断 | 在每个工具的 handler 里各写一遍截断逻辑 | `PostToolUse` 返回 `updatedOutput` |

三个需求，三个不同位置，**Agent Loop 一行不改，工具实现一行不改。** 这就是本章要换来的东西。

---

## 先定义验收结果

P 04 必须产生以下可观察行为：

| 编号 | 验收标准 | 主要事件 | 对应测试 |
| --- | --- | --- | --- |
| 1 | 只支持 `UserPromptSubmit`、`PreToolUse`、`PostToolUse` 和 `Stop` 四类事件 | 全部 | `hooks.test.ts` |
| 2 | 同一事件的同步、异步回调都严格按注册顺序运行 | 全部 | `hooks.test.ts` |
| 3 | Pre Hook 可以补上下文、改写合法输入、提出权限建议或返回结构化阻断错误 | Pre | `hooks.test.ts`、`ch04-hooks.test.ts` |
| 4 | Post Hook 可以按顺序观察并改写前一个 Hook 的输出 | Post | `hooks.test.ts` |
| 5 | 系统 `deny` 的优先级始终高于 Hook `allow` | Pre | `ch04-hooks.test.ts` |
| 6 | Pre 阻断、Hook 异常和非法 Hook 更新都要保留原 `tool_call_id` 配对 | Pre | `ch04-hooks.test.ts` |
| 7 | Post 请求停止后，同一 assistant 消息里尚未执行的调用也必须得到明确错误结果 | Post | `ch04-hooks.test.ts` |
| 8 | Stop Hook 第一次可以强制续跑，第二次仍执行回调，但不能再次续跑 | Stop | `hooks.test.ts`、`ch04-hooks.test.ts` |
| 9 | P 04 只比 P 03 增加 `hooks` capability，前 3 章行为保持不变 | 全部 | `ch04-hooks.test.ts` |

直接测试位于 `typescript/chapters/ch04/tests/hooks.test.ts` 和 `typescript/chapters/ch04/tests/ch04-hooks.test.ts`。

---

## Agent Loop 为什么需要扩展点

第 3 章的循环已经包含模型调用、工具准备、权限和执行。把细节压缩后，结构大致是：

```typescript
for (let turn = 1; turn <= maxTurns; turn += 1) {
  const reply = await model.complete(request);
  history.push(reply.message);

  if (reply.message.toolCalls.length === 0) {
    return reply.message.content;
  }

  for (const call of reply.message.toolCalls) {
    const prepared = tools.prepare(call);
    let result: ToolResult;
    if (prepared.error !== undefined) {
      result = prepared.error;
    } else {
      const decision = await permissionPolicy.decide(
        new PermissionRequest({ prepared, context }),
      );
      result = decision.isAllowed
        ? await tools.invoke(prepared, context)
        : decision.toToolResult();
    }
    history.push(toolMessage(result.content, call.id));
  }
}
```

这个循环应该继续只负责确定性编排。日志、通知、额外上下文和完成检查不是新的核心流程，它们需要的是固定挂载点。

Hook 的作用就是：在确定的位置发布结构化上下文，让已注册回调按顺序响应。

---

## 四个事件覆盖一次完整交互

`typescript/chapters/ch04/src/core/hooks.ts` 用一个冻结元组同时定义运行时事件集合和 TypeScript 联合类型：

```typescript
export const HOOK_EVENTS = Object.freeze([
  "UserPromptSubmit",
  "PreToolUse",
  "PostToolUse",
  "Stop",
] as const);

export type HookEvent = (typeof HOOK_EVENTS)[number];
```

四个事件的位置固定：

```text
用户提交 prompt
        ↓
UserPromptSubmit
        ↓
模型返回 tool_calls
        ↓
ToolRegistry.prepare
        ↓
PreToolUse
        ↓
PermissionPolicy / approval
        ↓
handler
        ↓
PostToolUse
        ↓
模型返回无工具回答
        ↓
Stop
```

它们不是四个随意命名的回调。每个事件都有自己的上下文和允许返回的字段。

---

## HookContext：不同事件只拿到自己需要的数据

`HookContext` 是冻结对象，核心字段如下：

```typescript
export interface HookContextOptions {
  readonly event: HookEvent;
  readonly message?: ChatMessage;
  readonly prepared?: PreparedToolCall;
  readonly result?: ToolResult;
  readonly history?: readonly ChatMessage[];
  readonly stopHookActive?: boolean;
}
```

字段和事件一一对应：

| 事件 | 可用字段 | 不能出现的字段 |
| --- | --- | --- |
| `UserPromptSubmit` | 一条 user `message` | prepared、result、history、stop 状态 |
| `PreToolUse` | 已校验的 `prepared` | message、result、history、stop 状态 |
| `PostToolUse` | `prepared` 和 `result` | message、history、stop 状态 |
| `Stop` | `history`、`stopHookActive` | message、prepared、result |

运行时会验证这些规则。比如给 Pre Hook 上下文塞一条 user message，会立即得到 `HookContractError`，而不是让回调猜测该使用哪个字段。

Pre 和 Post 只接收已经通过工具查找、JSON 解析和 Zod 校验的 `PreparedToolCall`。未知工具和非法参数不会进入 Hook。

---

## HookResult：不用字符串暗号表达控制流

一个 Hook 可能只观察，也可能改写或阻断。用裸字符串返回值会很快失控：`"stop"` 到底是拒绝工具、结束当前轮，还是要求模型再回答一次？

把这个问题摊开看，一个 `"stop"` 至少有四种可能的意思：

```text
"stop" =? 这次工具调用不要执行     （对应 blockingError）
      =? 执行，但本轮别再问模型了  （对应 preventContinuation）
      =? 整个 run() 立刻失败退出
      =? 别让 Stop Hook 续跑了
```

四种语义的副作用完全不同，而字符串本身没有任何地方能表达「我到底是哪一种」。更糟的是它无法校验：`"stpo"` 拼错了，运行时只会当成「未知返回值」忽略掉——**一个本该阻断危险写入的 Hook 静默失效了**，而你什么都看不到。

本章使用明确的 `HookResult`：

```typescript
export interface HookResultOptions {
  readonly permissionBehavior?: PermissionBehavior;
  readonly updatedInput?: PreparedToolCall;
  readonly updatedOutput?: ToolResult;
  readonly additionalContext?: readonly ChatMessage[];
  readonly blockingError?: ToolResult;
  readonly preventContinuation?: boolean;
  readonly forceContinue?: ChatMessage;
}
```

每个字段的语义只有一个：

| 字段                    | 含义                 | 允许事件 |
| --------------------- | ------------------ | ---- |
| `permissionBehavior`  | 向权限策略提交建议          | Pre  |
| `updatedInput`        | 替换当前调用的已验证输入       | Pre  |
| `blockingError`       | 在权限和 handler 前阻断   | Pre  |
| `updatedOutput`       | 替换工具结果             | Post |
| `preventContinuation` | 正常结束当前工具轮          | Post |
| `forceContinue`       | 追加一条 user 消息并再请求模型 | Stop |
| `additionalContext`   | 延后写入 system 上下文    | 四类事件 |

错误事件使用不属于自己的字段时，`validateFor()` 会列出非法字段并拒绝执行。

这条校验值得多说一句。假设有人在 `PostToolUse` 里返回了 `blockingError`——按字面意思是「阻断这次调用」，可 handler 早就跑完了，副作用已经发生。**这时候「阻断」是个不可能完成的请求。** 与其悄悄忽略它（Hook 作者会一直以为自己拦住了），不如立刻报错让他改到 `PreToolUse` 去。

`additionalContext` 只接受 system message。这样 Hook 无法注入孤儿 tool result，也无法伪造带 `toolCalls` 的 assistant message 来破坏 OpenAI 消息配对。

三种被禁止的注入分别会造成什么：

| 如果允许 Hook 注入 | 后果 |
| --- | --- |
| `tool` 消息 | `validateToolPairing()` 抛 `orphan tool result id: ...`，会话直接废掉 |
| 带 `toolCalls` 的 `assistant` 消息 | 模型会以为自己请求过这些工具，但永远等不到结果 |
| `user` 消息 | 模型无法区分「真实用户说的」和「Hook 塞的」，指令来源被伪造 |

**只允许 system 是这三种风险的最小交集**：system 消息不参与工具配对，也不会被误认为用户意图。

另外注意「延后写入」这个词。工具阶段产生的 `additionalContext` 不会立刻 push，而是攒到 `deferredContext` 里，等**所有** tool result 都写完才追加。原因还是配对：

```text
❌ 立刻写入
   assistant(tool_calls: call-1, call-2)
   tool(call-1, ...)
   system("Hook 加的上下文")     ← 插在两个 tool result 之间
   tool(call-2, ...)
   → 结构被破坏

✅ 延后写入
   assistant(tool_calls: call-1, call-2)
   tool(call-1, ...)
   tool(call-2, ...)
   system("Hook 加的上下文")     ← 配对完成之后才追加
```

通过校验后，`HookResult` 会把 `updatedOutput` 和 `blockingError` 通过 `copyToolResult()` 复制成新对象；`additionalContext` 用 `systemMessage()` 重建并冻结，`forceContinue` 用 `userMessage()` 重建。

Pre 的 `updatedInput` 不直接进入注册表。`normalizeUpdatedInput()` 先用原 `StoredToolDefinition.inputSchema` 重新 `safeParse`，再通过 `freezePreparedToolCall()` 用 `structuredClone` 创建参数副本并递归冻结。

因此，即使回调保留了原对象引用，并在之后修改这个引用，也不会污染注册表结果或 Agent 历史；审批与 handler 只能看到同一个不可变副本。

---

## HookRegistry：注册顺序就是执行顺序

注册表为每个事件保存独立回调列表：

```typescript
const hooks = new HookRegistry();

hooks.register("UserPromptSubmit", (context) => {
  console.error(`[Hook] ${context.event}`);
  return new HookResult();
});
```

回调可以同步返回，也可以返回 `Promise<HookResult>`。`run()` 对每个回调使用 `await`，因此两种形式都保持注册顺序：

```typescript
for (const callback of callbacks) {
  const outcome: unknown = await callback(current);
  if (!(outcome instanceof HookResult)) {
    throw new HookContractError(
      `${context.event} hook callback must return HookResult`,
    );
  }
  outcome.validateFor(context.event);
  // 合并结果，并把改写后的输入或输出传给下一个 Hook。
}
```

返回普通 object 不算合法 `HookResult`。这项运行时检查很重要，因为回调可能来自配置或插件边界，不能只依赖编译期类型。

合并时使用以下规则：

- 后一个 `updatedInput` 或 `updatedOutput` 覆盖前一个，并成为再下一个 Hook 看到的值；
- `additionalContext` 按注册顺序连接；
- `preventContinuation` 使用布尔 OR；
- 权限建议继续使用 `deny > ask > allow > passthrough`；
- Pre 遇到 `blockingError` 立即停止后续 Pre 回调；
- Stop 第一次遇到 `forceContinue` 后停止后续 Stop 回调。

六条规则背后是同一个判断：**「后来的意见」和「更保守的意见」哪个优先？** 答案按字段分：

| 字段 | 合并方式 | 为什么 |
| --- | --- | --- |
| `updatedInput` / `updatedOutput` | 后覆盖前 | 这是**流水线加工**。后一个 Hook 看到的就是前一个的产出，覆盖才符合直觉 |
| `additionalContext` | 顺序拼接 | 每条上下文都有价值，不该互相取消 |
| `preventContinuation` | 布尔 OR | 只要有一个要求停，就停。**更保守的赢** |
| `permissionBehavior` | `deny > ask > allow > passthrough` | 同上，**更保守的赢**。后一个 `allow` 覆盖不了前一个 `deny` |

用一个具体例子把「后覆盖前」看清楚。三个 Post Hook 依次注册：

```text
handler 产出            "original"
  ↓
Post Hook 1  context.result.content === "original"
             返回 updatedOutput: "rewritten once"
  ↓
Post Hook 2  context.result.content === "rewritten once"   ← 看到的是上一个的产出
             返回空 HookResult（只观察）
  ↓
Post Hook 3  context.result.content === "rewritten once"
             返回 updatedOutput: "rewritten twice"
  ↓
最终写入历史             "rewritten twice"
```

所以注册顺序不只影响日志先后，**它决定了加工链的顺序**。想做「先脱敏、再截断」还是「先截断、再脱敏」，靠的就是注册顺序。

除字段合并外，`run()` 还会在每轮回调后做三件固定的事：Pre 返回 `updatedInput` 时，`normalizeUpdatedInput()` 会重新解析并冻结新参数；`stopHookActive` 为 true 时，Stop 返回的 `forceContinue` 会在合并前被移除；`blockingError` 或 `forceContinue` 一旦产生，就短路剩余回调。

`mergeResults()` 使用 `strongerPermission()` 按 `deny > ask > allow > passthrough` 合并权限建议，因此后一个 `allow` 不会覆盖前一个 `deny`。

---

## UserPromptSubmit：补充本轮上下文

User Prompt 刚提交、模型还没收到请求时触发这个事件：

```typescript
hooks.register("UserPromptSubmit", (context) => {
  if (context.message?.role !== "user") {
    throw new Error("UserPromptSubmit context is incomplete");
  }
  console.error(`[Hook] prompt: ${context.message.content}`);
  return new HookResult({
    additionalContext: [systemMessage("本轮需要特别核对验收结果。")],
  });
});
```

`AgentRunner.run()` 先运行 Hook，再按固定顺序写入历史：

```typescript
const submitted = userMessage(prompt);
const promptHook = await hooks.runUserPrompt(submitted);
history.push(submitted, ...promptHook.additionalContext);
```

Hook 可以记录输入或增加 system context，但 P 04 不允许它替换、删除或阻断原始 user message。

---

## PreToolUse：副作用之前的扩展点

Pre Hook 运行时，工具已经存在，参数也已通过 Zod，但权限尚未判断，handler 尚未运行。

下面的 Hook 阻止写入一个受管理文件：

```typescript
hooks.register("PreToolUse", (context) => {
  const argumentsValue = context.prepared?.arguments;
  if (
    typeof argumentsValue === "object" &&
    argumentsValue !== null &&
    Reflect.get(argumentsValue, "path") === "generated.txt"
  ) {
    return new HookResult({
      blockingError: toolError(
        "hook_blocked",
        "generated.txt is managed elsewhere",
      ),
    });
  }
  return new HookResult();
});
```

命中 `blockingError` 后，后续 Pre Hook、权限、handler 和 Post Hook 都不会运行。Loop 仍会把这个错误回填给原调用：

```text
Error [hook_blocked]: generated.txt is managed elsewhere
```

Pre Hook 还可以返回 `updatedInput`。但它只能改参数，不能换工具身份。注册表会验证：

- `tool_call_id` 不变；
- 工具名不变；
- `StoredToolDefinition` 必须是同一个对象；
- 新 arguments 仍通过原 Zod schema；
- prepared call 不能带 error，也不能缺 definition 或 arguments。

锁定 definition 对象同时锁定了 effect、handler 和 schema。否则恶意或有 bug 的 Hook 可以把一个 write 工具伪装成 read，再绕过权限。

这个攻击路径值得具体走一遍：

```text
① 模型请求 write_file{"path":"a.txt","content":"..."}
② prepare 完成，definition.effect === "write"
③ 恶意 Pre Hook 返回 updatedInput，其中 definition 换成了 read_file 的定义
   （effect: "read"）
④ 如果注册表不检查 definition 身份：
      工作区硬边界看 effect !== "write" → 不表态
      confirm-file-write 规则看 name !== "write_file" → 不匹配
      → 最终 allow(default)，一次写操作零审批通过
```

所以「`StoredToolDefinition` 必须是同一个对象」这条检查用的是**引用相等**，不是名字相等——名字可以伪造，对象引用不行。

注册表还会冻结准备完成的 definition、arguments 和 prepared 外壳。Hook 不能就地修改原参数；需要改写时必须显式返回新的 `updatedInput`。注册表随后用原 Zod schema 重新解析，复制参数并深度冻结规范化副本，审批和 handler 只读取这个副本。

这一步消除了异步审批中的竞态：Hook 即使保留了原对象引用，并在用户审批期间把“值 A”改成“值 B”，权限请求和 handler 仍看到同一个已经冻结的 A，不会出现“批准 A、执行 B”。

竞态的时间线是这样的：

```text
时间 →
Hook:  返回 updatedInput{path:"safe.txt"}，同时自己留了个引用 ref
       │                                              │
Loop:  ├─ structuredClone + 深度冻结 → 副本 A          │
       ├─ 审批框显示 A：{"path":"safe.txt"} ───────────┤
       │  （等用户敲 y，可能等好几秒）                  │
       │                                    Hook 这时候偷偷改 ref.path = "C:/系统文件"
       ├─ 用户敲 y，批准的是 A                          │
       └─ handler 读取的还是副本 A ✅

如果没有 structuredClone：
       └─ handler 读取 ref → 执行的是 "C:/系统文件" ❌ 批准 A 执行 B
```

**「审批看到的」和「执行用到的」必须是同一个不可变值。** 异步审批只要存在等待窗口，这个保证就不能靠约定，只能靠拷贝加冻结。

---

## Hook allow 只是建议，不是授权

Pre Hook 可以返回：

```typescript
return new HookResult({ permissionBehavior: "allow" });
```

Loop 不会因此直接执行工具，而是把它转换为结构化建议：

```typescript
const recommendations = [
  new PermissionDecision(
    hook.permissionBehavior,
    `PreToolUse hook requested ${hook.permissionBehavior}`,
    "pre-tool-hook",
  ),
];

const decision = await permissionPolicy.decide(
  new PermissionRequest({
    prepared: effective,
    context,
    recommendations,
  }),
);
```

第 3 章的工作区硬边界、Shell 默认审批和固定规则仍一起参与合并。只要任一系统参与方给出 `deny`，Hook 的 `allow` 就不会触发审批或 handler。

这条顺序不能交换：

```text
Pre Hook → hard permission policy → approval → handler
```

Hook 是扩展机制，不是第二套权限系统。

Claude Code 官方同样强调这点（https://code.claude.com/docs/en/hooks-guide.md）：Hook 可以拒绝工具调用，但保持沉默并不会自动批准调用；没有返回决策时，调用仍走正常权限流程。

P 04 的 `passthrough` 正是这个语义：Hook 不产生任何建议，工具是否执行完全由 `PermissionPolicy` 决定。

---

## PostToolUse：串联观察和结果改写

Post Hook 在 handler 完成后、tool message 写入历史前运行：

```typescript
hooks.register("PostToolUse", (context) => {
  const result = context.result;
  if (result === undefined || result.isError || result.content.length <= 100_000) {
    return new HookResult();
  }
  return new HookResult({
    updatedOutput: toolSuccess(`${result.content.slice(0, 100_000)}\n[truncated]`),
  });
});
```

如果第一个 Post Hook 把 `original` 改成 `rewritten once`，第二个 Hook 的 `context.result` 看到的就是 `rewritten once`。注册顺序因此也是改写顺序。

handler 或权限拒绝时不会错误地运行 Post Hook：

- handler 成功或返回结构化工具错误：运行 Post；
- Pre 阻断：不运行 Post；
- permission deny：不运行 Post；
- Pre Hook 异常：不运行 Post。

---

## preventContinuation 也必须保持消息配对

Post Hook 可以用 `preventContinuation: true` 表示当前结果已经足够，本轮不再请求模型：

```typescript
return new HookResult({ preventContinuation: true });
```

困难在于，一个 assistant message 可能同时包含多个 tool call。假设第一个调用的 Post Hook 要求停止，Loop 不能直接 return，否则其余调用会成为孤儿。

「孤儿」具体长什么样：

```text
❌ 直接 return
   assistant(tool_calls: call-1, call-2, call-3)
   tool(call-1, "结果")
   ← 这里 return 了，call-2 和 call-3 没有结果
   → 这段历史如果再发给模型，OpenAI 会拒绝；
     validateToolPairing() 也会抛 missing tool results for ids: ["call-2","call-3"]
```

正确处理是：

```text
call-1 → 正常执行并触发停止
call-2 → Error [hook_stopped_continuation]
call-3 → Error [hook_stopped_continuation]
先写入全部三个 tool result
再以 call-1 的结果正常结束
```

配对错误文本固定为：

```text
Error [hook_stopped_continuation]: Skipped after PostToolUse requested a stop
```

注意 call-2 和 call-3 **不是被执行后失败**，而是**根本没被执行**。源码里那个 `stoppedResultIndex` 一旦被设置，后续调用直接跳过 `#executeTool()`：

```typescript
if (stoppedResultIndex !== undefined) {
  result = toolError("hook_stopped_continuation", "Skipped after PostToolUse requested a stop");
} else {
  const execution = await this.#executeTool(call, context, tools);
  // ...
}
```

于是有两条保证同时成立：**后续工具的副作用没有发生**，而且**消息结构仍然完整**。

停止流程不能以破坏协议为代价。

一句话记住这一节：**「提前结束」是业务需求，「消息配对」是协议约束。业务需求要在协议约束内实现，不能反过来。**

---

## Stop：最多强制续跑一次

当模型返回一条没有工具调用的 assistant message 时，Loop 准备结束，此时运行 Stop Hook：

```typescript
hooks.register("Stop", (context) => {
  const toolCount = context.history.filter(
    (message) => message.role === "tool",
  ).length;
  console.error(`[Hook] 本次会话共执行 ${toolCount} 次工具调用`);
  if (toolCount === 0) {
    return new HookResult({
      forceContinue: userMessage("请先用工具核对结果。"),
    });
  }
  return new HookResult();
});
```

第一次 `forceContinue` 会把明确的 user message 追加到历史，然后再次请求模型：

```typescript
const stopHook = await hooks.runStop(history, stopHookActive);
if (stopHook.forceContinue !== undefined) {
  history.push(...stopHook.additionalContext, stopHook.forceContinue);
  stopHookActive = true;
  continue;
}
```

第二次模型停止时，Hook 仍然执行，并看到 `stopHookActive === true`。即使回调再次返回 `forceContinue`，注册表也会移除这项请求。Loop 使用第二次回答结束。

因此一次 `run()` 最多增加一个模型 turn，不会形成 Stop Hook 死循环；生命周期日志仍能完整记录两次 Stop。

### 为什么必须限制成一次

上面那个示例 Hook 的逻辑是「工具调用数为 0 就要求模型先用工具」。如果不限次数，遇到一个固执的模型会怎样：

```text
turn 1  模型：「我认为不需要用工具，答案是 X」
        Stop Hook：toolCount === 0 → forceContinue("请先用工具核对结果。")
turn 2  模型：「我还是认为不需要用工具，答案是 X」
        Stop Hook：toolCount 仍然是 0 → forceContinue(...)
turn 3  同上
...
turn 20 AgentLimitError（撞上 maxTurns 才停）
```

这条路径没有语法错误，每一步都「合理」，结果是烧掉 20 轮模型调用换来一个 `AgentLimitError`。`maxTurns` 确实兜住了，但代价太高——**兜底机制不该被当成正常退出路径。**

`stopHookActive` 把它压到最多两轮：

```text
turn N    模型停止 → Stop Hook（stopHookActive: false）→ forceContinue → 续跑
turn N+1  模型停止 → Stop Hook（stopHookActive: true）→ forceContinue 被移除 → 结束
```

有两个细节容易搞错：

| 细节 | 实际行为 |
| --- | --- |
| 第二次还会调用 Stop 回调吗 | **会。** 只是它的 `forceContinue` 被丢弃 |
| 回调能看出自己被限制了吗 | **能。** `context.stopHookActive === true` |

第二次仍然执行回调是刻意的：日志、指标、清理这些副作用不该因为「不能续跑了」就被跳过。**限制的是控制权，不是执行权。**

---

## Hook 故障如何返回

扩展代码同样可能出错。工具阶段不能因为 Hook 抛异常而遗留未配对调用。

本章固定映射如下：

| 故障 | 工具结果 | handler 是否已执行 |
| --- | --- | --- |
| `prepare()` 自身抛异常 | `tool_preparation_error` | 否 |
| Pre 返回非法输入更新 | `hook_contract_error` | 否 |
| Pre 抛出其他异常 | `hook_execution_error` | 否 |
| 权限评估或审计异常 | `permission_evaluation_error` | 否 |
| Post 抛出异常 | `hook_execution_error` | **是** |

最后一行的「是」是这张表的重点。Post 发生在 handler 之后，所以它失败时不能声称副作用没有发生。返回稳定错误并保留配对，让模型和调用方能看到这次生命周期失败。

把两端对比一下就清楚了：

```text
Pre Hook 抛异常
  → handler 没跑，磁盘没变化，返回 hook_execution_error
  → 模型可以放心重试（重试是安全的）

Post Hook 抛异常
  → handler 跑过了，文件已经写了，返回 hook_execution_error
  → 模型不该盲目重试（可能重复写入）
```

两种情况错误码相同，但**语义上的可重试性完全不同**。P04 没有为此区分错误码——这是一处刻意的教学简化，代价是模型无法从错误码判断副作用有没有发生。真正需要这个区分的是幂等性设计，第 11 章讨论恢复策略时才会正面处理。

还有一处不对称值得注意：`hook_contract_error` 只出现在 Pre，不出现在 Post。原因在源码里——只有 Pre 的 `updatedInput` 会走 `normalizeUpdatedInput()` 那套严格校验（工具身份、Zod 重解析），Post 的 `updatedOutput` 只要是个合法 `ToolResult` 就行。**Pre 改的是「即将执行什么」，Post 改的是「已经发生的事怎么描述」，前者必须更严。**

UserPromptSubmit 和 Stop 发生在工具配对之外；它们的非法结果会让当前 `run()` 显式失败，不会静默忽略扩展错误。

这也是一处有意的差异：

| 阶段 | Hook 故障怎么处理 | 为什么 |
| --- | --- | --- |
| 工具阶段（Pre / Post） | 转成配对的工具错误，循环继续 | 不转的话消息结构会坏掉，代价太大 |
| 非工具阶段（Submit / Stop） | 直接让 `run()` 抛错 | 这里没有配对约束，失败就该显式失败 |

---

## P 03 与 P 04 的准确差异

P 04 是在 P 03 固定能力上增加 `hooks`：

```typescript
export const P04: ChapterProfile = Object.freeze({
  chapter: 4,
  capabilities: new CapabilitySet([
    "loop",
    "powershell",
    "tool_registry",
    "files",
    "policy",
    "hooks",
  ]),
});
```

组合根只允许 P 04 或后续 profile 注入 `HookRegistry`：

```typescript
if (dependencies.hooks !== undefined && !profile.capabilities.has("hooks")) {
  throw new Error("hooks require chapter 4 or later");
}
```

真实 P 04 CLI 注册四个只记录生命周期的默认 Hook：

```text
[Hook] UserPromptSubmit
[Hook] PreToolUse: read_file
[Hook] PostToolUse: read_file -> ok
[Hook] Stop
```

它们不修改参数、不覆盖输出，也不授予权限。若模型选择 `shell` 或文件写入，仍执行第 3 章的显式审批。

新手最容易问的一句是：「加了 Hook，第 3 章那套权限还算不算数？」下面这张表逐项回答。

| 维度 | P 03 | P 04 | 变了吗 |
| --- | --- | --- | --- |
| Agent Loop 的循环结构 | 请求 → 工具 → 追加 → 再请求 | 完全相同 | **没变** |
| 工具查找、Zod 校验、prepare | 一样 | 一样 | **没变** |
| 权限四态与合并优先级 | `deny > ask > allow > passthrough` | 完全相同 | **没变** |
| 工作区硬边界 | 在规则之前跑 | 位置不动 | **没变** |
| 审批与审计 | 合并后、执行前 | 位置不动 | **没变** |
| `validateToolPairing()` | 每轮都校验 | 每轮都校验 | **没变** |
| 循环里的固定挂载点 | 无 | 四个 | **新增** |
| 权限候选来源 | 规则 + 硬边界 + 审批 | 再多一路 Hook 建议 | **新增** |
| 参数改写能力 | 无 | `updatedInput`（要重校验） | **新增** |
| 结果改写能力 | 无 | `updatedOutput` | **新增** |
| 提前结束能力 | 无 | `preventContinuation` | **新增** |
| 强制续跑能力 | 无 | `forceContinue`（最多一次） | **新增** |

一句话结论：**P 04 只在循环上开了四个口，没有动循环内部的任何裁决顺序。** 这就是「解耦」的具体含义——扩展点增加了，被扩展的东西一行没改。

如果你想亲眼确认这一点，可以对比着跑两条命令：

```powershell
npm run ch03 -- --prompt '读取 README.md 并概括运行方式'
npm run ch04 -- --prompt '读取 README.md 并概括运行方式'
```

两次的 `[Permission]` 行应当一模一样，唯一差别是 P 04 多出四行 `[Hook]`。

---

## 组合根与 CLI：Hook 在真实入口中如何接入

`typescript/chapters/ch04/src/bootstrap.ts` 是组合根，负责按 `ChapterProfile` 组装模型、工具、权限策略和 Hook。P 04 使用同一个 `buildAgent`，只是多了 `hooks` capability：

```typescript
export function buildAgent(
  profile: ChapterProfile,
  dependencies: BuildDependencies,
): AgentRunner {
  // 禁止伪造同章节号但能力不同的 profile，保持教学快照固定。
  if (profileForChapter(profile.chapter) !== profile) {
    throw new Error("profile must be a fixed chapter profile");
  }
  if (dependencies.hooks !== undefined && !profile.capabilities.has("hooks")) {
    throw new Error("hooks require chapter 4 or later");
  }
  // ...
  return new AgentRunner({
    model: dependencies.model,
    tools,
    systemPrompt: SYSTEM_PROMPT,
    workspace: dependencies.workspace,
    ...(permissionPolicy === undefined ? {} : { permissionPolicy }),
    ...(dependencies.hooks === undefined ? {} : { hooks: dependencies.hooks }),
  });
}
```

这段代码把“章节能力”变成运行时约束：P 01-P 03 即使有人错误地传入 `HookRegistry`，也会在创建 Agent 前直接失败。

`AgentRunner` 构造函数里还有两行值得单独讲，它们决定了「不传 Hook」和「只传 Hook」这两种情况怎么办：

```typescript
this.#permissionPolicy =
  options.permissionPolicy === undefined && options.hooks !== undefined
    ? new PermissionPolicy()
    : options.permissionPolicy;
this.#hooks = options.hooks === undefined ? new HookRegistry() : options.hooks;
```

读懂这两行，就读懂了 Hook 为什么能「零配置接入」：

| 你传了什么 | `#hooks` | `#permissionPolicy` | 后果 |
| --- | --- | --- | --- |
| 都不传（P 01/P 02 的样子） | 空 `HookRegistry` | `undefined` | 循环里的 Hook 调用全部命中空队列，等于没有；权限阶段整段跳过 |
| 只传 `permissionPolicy`（P 03） | 空 `HookRegistry` | 你传的 | 第 3 章的行为 |
| 都传（P 04） | 你传的 | 你传的 | 本章的完整行为 |
| **只传 `hooks`，不传 policy** | 你传的 | **自动补一个空 `PermissionPolicy`** | Hook 返回的 `permissionBehavior` 仍然要经过一次真实的权限评估 |

第四行是关键。假设没有这行兜底，一个只注册了 Hook、没有配置权限策略的 Agent 会怎样？Hook 返回 `permissionBehavior: "deny"` 时，因为没有策略对象，这条建议**根本没有地方被处理**，工具就直接执行了——Hook 的拒绝会被静默丢掉。

补一个空 `PermissionPolicy()` 之后，语义变得确定：

```text
空 PermissionPolicy = 没有规则、没有审批器、没有硬边界
  ↓
默认候选是 allow（"No permission rule blocked the request"）
  ↓
Hook 的 deny 参与合并 → deny > allow → 拒绝生效
Hook 的 ask 参与合并 → 没有审批器 → fail closed → 拒绝
```

**扩展点不能因为「没配置另一个模块」而失效。** 这条原则在第 3 章表现为 fail closed，在这里表现为「宁可自动补一个空策略，也不让 Hook 的建议无处落地」。

真实 CLI 的 `liveHooks()` 位于 `typescript/chapters/ch04/src/cli.ts`，它注册四个只输出 `stderr` 的观察型回调：

```typescript
function liveHooks(): HookRegistry {
  const hooks = new HookRegistry();
  hooks.register("UserPromptSubmit", () => {
    stderr.write("[Hook] UserPromptSubmit\n");
    return new HookResult();
  });
  hooks.register("PreToolUse", (context: HookContext) => {
    stderr.write(`[Hook] PreToolUse: ${hookToolName(context)}\n`);
    return new HookResult();
  });
  hooks.register("PostToolUse", (context: HookContext) => {
    if (context.result === undefined) {
      throw new Error("PostToolUse context is incomplete");
    }
    const outcome = context.result.isError ? "error" : "ok";
    stderr.write(`[Hook] PostToolUse: ${hookToolName(context)} -> ${outcome}\n`);
    return new HookResult();
  });
  hooks.register("Stop", () => {
    stderr.write("[Hook] Stop\n");
    return new HookResult();
  });
  return hooks;
}
```

每个回调都返回空 `HookResult`，因此默认运行只增加生命周期日志，不改变参数、结果或权限建议。真实 CLI 统一用 `stderr.write` 输出 Hook 日志，`stdout` 只写最终答案；教学片段中的 `console.error` 是为了让示例更短，不代表实际入口混用输出流。

---

## 当前边界与刻意不做的事

本章只实现四个当前确实使用的事件：`UserPromptSubmit`、`PreToolUse`、`PostToolUse`、`Stop`，没有提前增加子 Agent、压缩、任务或 MCP 专属事件。

Claude Code 官方目前定义了 14 类以上 Hook 事件，例如 `PermissionRequest`、`SessionStart`、`SubagentStart`、`PreCompact`、`Elicitation` 等，完整清单参见 https://code.claude.com/docs/en/hooks.md。第 4 章的事件集合是教学子集。

Hook 回调在当前进程内按顺序执行。P 04 不提供插件隔离、独立超时或并行 Hook；并行会让改写顺序和短路语义变得含混。

与 Claude Code 官方实现的一项差异：官方将匹配的所有同类 Hook 执行完成后合并结果（合并优先级 `deny > defer > ask > allow`），而 P 04 在 `blockingError` 或 `Stop` 的 `forceContinue` 时短路，提前停止后续回调。

这是教学简化：阻断错误意味着后续回调对该工具已无意义，而且短路位置明确。并发场景下如果需要所有回调的聚合结果，可以自行扩展。

Hook 也不是沙箱。它是由应用注册的受信任扩展代码，可以抛异常或执行自己的副作用。结构化契约保护 Agent Loop 的状态和权限顺序，不提供操作系统隔离。

这句话很容易被读轻，所以说清楚它的确切含义：

```text
Hook 契约管得住的：
  ✅ 不能把 write_file 换成 read_file 执行（definition 引用相等）
  ✅ 不能让参数和被批准的内容不一致（重校验 + 深度冻结）
  ✅ 不能绕过 deny（合并优先级）
  ✅ 不能破坏 tool_call_id 配对（阻断也补齐结果）
  ✅ 不能无限续跑（stopHookActive）

Hook 契约管不住的：
  ❌ Hook 回调里自己调用 fs.writeFileSync('C:/anything', ...)
  ❌ Hook 回调里自己发 HTTP 请求
  ❌ Hook 回调里读环境变量、读密钥
```

原因很直白：Hook 是你自己写的、和 Loop 跑在同一个进程里的普通函数。**契约保护的是「Loop 的状态和裁决顺序不被扩展代码搞坏」，不是「扩展代码本身被限制能做什么」。** 前者是本章的目标，后者需要进程隔离或操作系统级沙箱，不在教学范围内。

换句话说：模型是不受信的，所以工具调用要过权限；Hook 是受信的，所以只需要过契约。搞混这两者，会得出「加了 Hook 就有沙箱了」这种危险结论。

---

## 配套代码地图

为了让读者能直接定位源码，本章把 `typescript/chapters/ch04/src/` 的全部文件列成一张覆盖表：

| 文件                                                                          | 职责                        | 讲解位置                                |
| --------------------------------------------------------------------------- | ------------------------- | ----------------------------------- |
| `core/hooks.ts`                                                             | Hook 事件、上下文、结果与注册表        | 本章核心实现                              |
| `core/loop.ts`                                                              | Agent Loop 和四个 Hook 挂载点   | 本章核心实现                              |
| `core/profiles.ts`                                                          | P 01-P 04 固定能力快照          | 上文“P 03 与 P 04 的准确差异”               |
| `bootstrap.ts`                                                              | 按 profile 组装基础设施、权限和 Hook | 上文“组合根与 CLI”                        |
| `cli.ts`                                                                    | 终端入口、人工审批和默认观察 Hook       | 上文“组合根与 CLI”                        |
| `config.ts`                                                                 | `.env` 模型配置校验             | 下文“运行第 4 章”                         |
| `chapters/ch01.ts` 至 `ch04.ts`                                            | 固定章节入口，禁止切换能力             | 上文“P 03 与 P 04 的准确差异”               |
| `core/messages.ts`                                                          | 消息模型、`tool_call_id` 配对契约  | 前文 HookResult 与 preventContinuation |
| `core/tools.ts`                                                             | 工具注册、prepare、invoke、冻结参数  | 上文“PreToolUse”                      |
| `core/permissions.ts`                                                       | 权限规则、审批、审计与合并             | 上文“Hook allow 只是建议”                 |
| `core/filesystem.ts`、`core/commands.ts`、`core/model.ts`                     | 文件、命令、模型的最小接口             | 第 2-3 章继承契约                         |
| `adapters/powershell.ts`、`adapters/openai-chat.ts`、`adapters/filesystem.ts` | PowerShell、OpenAI、文件系统实现  | 第 2-3 章继承实现                         |
| `features/builtin-tools.ts`                                                 | `shell` 与文件工具集            | 第 2 章继承实现                           |

这张图也说明了 Hook 的边界：本章没有为每个 adapter 增加回调，而是把扩展点集中在 Loop 的四个固定位置。外部实现差异仍由各自 adapter 收敛。

---

## 从 ai-agent-book 学到什么

`ai-agent-book` 第 4 章讲述的是另一条互补路线：工具分类、MCP 协议和事件驱动异步 Agent。它把“事件触发工具”定义为 Agent 注册、外部触发的一类工具，例如定时器、后台命令监控、外部消息频道；这些工具负责在用户没有主动提问时唤醒 Agent。

P 04 的 Hook 不是这套机制。Hook 是 Agent 内部生命周期的拦截点：`UserPromptSubmit`、`PreToolUse`、`PostToolUse` 和 `Stop` 都发生在同一次 `run()` 内部，用来追加日志、改写工具输入输出、建议权限或控制是否续跑。两者可以组合：

```text
外部事件（定时器、Webhook、IM）
        ↓
事件触发工具 → 生成新 prompt
        ↓
UserPromptSubmit → PreToolUse → handler → PostToolUse → Stop
```

---

## 运行第 4 章

### 第 0 步：准备环境

和前三章完全一样：`typescript/` 里 `npm ci`，`.env` 里填好三个变量。

```powershell
Set-Location 'F:\笔记\Agent实操\code'
```

### 第 1 步：先跑离线测试

```powershell
npm run test:ch04
```

预期最后两行：

```text
 Test Files  12 passed (12)
      Tests  94 passed (94)
```

从第 3 章的 61 个涨到 94 个，多出来的 33 个几乎全在 `hooks.test.ts` 和 `ch04-hooks.test.ts` 里。输出里那行 `配置错误: Missing required settings: ...` 仍然是被断言的预期输出，不是失败。

### 第 2 步：看四个 Hook 各打一行日志

```powershell
npm run ch04 -- --prompt '读取 README.md 并概括运行方式'
```

预期在 stderr 上依次看到（`[Hook]` 是本章新增的，`[Permission]` 是第 3 章的）：

```text
[Hook] UserPromptSubmit
[Hook] PreToolUse: read_file
[Permission] read_file: allow (default) - No permission rule blocked the request
[Hook] PostToolUse: read_file -> ok
[Hook] Stop
```

请特别确认 `[Hook] PreToolUse` **在** `[Permission]` **之前**。这一行顺序就是本章最核心的设计结论：Hook 先发言，权限系统后裁决。

### 第 3 步：让 Hook 和审批同时出现

```powershell
npm run ch04 -- --prompt '把一句话写入 hook-demo.txt'
```

预期顺序：

```text
[Hook] UserPromptSubmit
[Hook] PreToolUse: write_file

工具调用需要批准: write_file
原因: File writes require explicit approval from chapter 3 onward
参数: {"path":"hook-demo.txt","content":"..."}
允许本次调用? [y/N] y

[Permission] write_file: allow (terminal-approval) - User approved this tool call
[Hook] PostToolUse: write_file -> ok
[Hook] Stop
```

这一步能同时看到两件事：Pre Hook 在审批框之前跑完；Post Hook 在写入完成后才跑。**默认的四个 Hook 只打日志，不会替你批准任何东西**——审批框照样弹。

### 第 4 步：统一入口

```powershell
npm run agent-tutorial -- run --chapter 4 --prompt '读取 README.md 并概括运行方式'
```

### 第 5 步（可选）：完整离线门禁

```powershell
npm run typecheck
npm test
npm run lint
npm run format:check
npm run build
```

测试通过 `ScriptedModelClient`、结构化权限替身和内存 Hook 验证行为，不读取真实 API Key，也不访问网络。

### 常见报错排查

| 现象 | 原因 | 怎么处理 |
| --- | --- | --- |
| 看不到任何 `[Hook]` 行 | Hook 日志写 stderr，你可能把 stderr 重定向掉了 | 别加 `2>$null`；或者确认你跑的是 `ch04` 而不是 `ch03` |
| `[Hook] PostToolUse: xxx -> error` | 工具本身返回了错误结果 | 这是正常的观察日志。**Post Hook 在工具失败时也会跑** |
| 只有 `PreToolUse` 没有 `PostToolUse` | 调用被 Pre 阻断，或被权限拒绝了 | 正常行为。见「PostToolUse」末尾那四条 |
| `Error [hook_contract_error]` | 你写的 Pre Hook 返回的 `updatedInput` 不合法 | 检查：`tool_call_id` 变了？工具名变了？definition 换了对象？新参数没过 Zod？ |
| `Error [hook_execution_error]` | 你写的 Hook 抛异常了 | 看 stderr 里有没有你自己打的日志。注意 Pre 和 Post 共用这个码，得自己判断在哪一端 |
| `Error [hook_stopped_continuation]` | 同一轮里前面的调用触发了 `preventContinuation` | 正常行为。这些调用**没有被执行** |
| 启动就报 `hooks require chapter 4 or later` | 给 P01–P03 传了 `HookRegistry` | 换成 P04，或者别传 hooks |
| 自己写的 Hook 返回了普通对象，报 `must return HookResult` | 返回了 `{}` 或 `{ blockingError: ... }` 字面量 | 必须 `return new HookResult({...})`。运行时用 `instanceof` 检查 |
| Stop Hook 里 `forceContinue` 第二次没生效 | `stopHookActive === true` | 正常行为。一次 `run()` 最多续跑一次 |

---

## 验证与实验

`npm run test:ch04` 会执行 12 个测试文件、94 个测试。本章新增的两个是：

| 测试文件 | 验证内容 |
| --- | --- |
| `hooks.test.ts` | 单元层：`HookContext` 字段裁剪、`HookResult.validateFor()`、注册顺序、合并规则、`updatedInput` 重校验与冻结、短路行为 |
| `ch04-hooks.test.ts` | 端到端：Hook 与权限的优先级、阻断后 handler 未执行、`preventContinuation` 的配对、Stop 只续跑一次、P01–P03 行为未变 |

其余 10 个是前三章的累计测试，继续保证「加 Hook 没改坏旧行为」——这是验收标准第 9 条的直接体现。

值得单独看的是**验收标准 5**（系统 `deny` 优先于 Hook `allow`）的测试写法。它必须证明的不是「返回了拒绝」，而是「Hook 的 allow 被正确降级成了建议」：

```text
构造：Pre Hook 返回 permissionBehavior: "allow"
      + 一个会命中工作区硬边界的越界写入
断言：最终结果是 Error [permission_denied]: Writing outside the workspace is forbidden
      而不是 Hook 的 allow 生效
      并且文件确实没被创建
```

这条测试是本章和第 3 章的接缝。**只要它是绿的，「Hook 不是第二套权限系统」就不只是一句设计口号。**

### 四个建议动手做的小实验

**实验一：注册两个 Post Hook，观察加工链**

在 `cli.ts` 的 `liveHooks()` 里追加两个改写型 Post Hook：

```typescript
hooks.register("PostToolUse", (context) => {
  if (context.result === undefined) throw new Error("incomplete");
  stderr.write(`[Hook A] 看到: ${context.result.content.slice(0, 20)}\n`);
  return new HookResult({ updatedOutput: toolSuccess("AAA") });
});
hooks.register("PostToolUse", (context) => {
  if (context.result === undefined) throw new Error("incomplete");
  stderr.write(`[Hook B] 看到: ${context.result.content.slice(0, 20)}\n`);
  return new HookResult({ updatedOutput: toolSuccess("BBB") });
});
```

预期：`[Hook B] 看到: AAA`，最终写入历史的是 `BBB`。
学到：Post Hook 是流水线，不是并列的观察者。**实验完记得改回去。**

**实验二：让 Pre Hook 阻断一个写入**

```typescript
hooks.register("PreToolUse", (context) => {
  const args = context.prepared?.arguments;
  if (typeof args === "object" && args !== null && Reflect.get(args, "path") === "blocked.txt") {
    return new HookResult({
      blockingError: toolError("hook_blocked", "blocked.txt is managed elsewhere"),
    });
  }
  return new HookResult();
});
```

然后跑 `npm run ch04 -- --prompt '把 hello 写入 blocked.txt'`。

预期：**审批框不弹**，看不到 `[Hook] PostToolUse`，也没有 `[Permission]` 行，模型收到 `Error [hook_blocked]: blocked.txt is managed elsewhere`。
学到：`blockingError` 短路掉了权限、handler 和 Post 三个阶段。

**实验三：让 Pre Hook 的 allow 去挑战硬边界**

把上面那个 Hook 改成 `return new HookResult({ permissionBehavior: "allow" })`，然后跑越界写入：

```powershell
npm run ch04 -- --prompt '把 hello 写入 ../outside.txt'
```

预期：仍然是 `[Permission] write_file: deny (workspace-boundary) - ...`。
学到：这就是验收标准 5。Hook 的 `allow` 只是候选决定之一，`deny` 照样赢。

**实验四：在 Pre Hook 里就地修改参数**

```typescript
hooks.register("PreToolUse", (context) => {
  const args = context.prepared?.arguments;
  Reflect.set(args as object, "path", "hacked.txt");   // 就地改
  return new HookResult();                              // 不返回 updatedInput
});
```

预期：抛 `TypeError`（参数已经被深度冻结），或者在非严格模式下静默失败但**参数不变**。总之写入的还是原路径。
学到：想改参数必须显式返回 `updatedInput`，走重校验和重冻结那条路。

---

## 本章小结

### 三句话版本

1. Hook 是在四个确定位置发布结构化上下文，让扩展逻辑从核心编排里搬出去。
2. 返回值用结构化 `HookResult` 而不是字符串，因为控制流语义必须能被校验。
3. Hook 能建议、能改写、能阻断，但**不能授权**——系统 `deny` 永远优先。

### 一定要记住的七条

| # | 结论 | 出现在哪一节 |
| ---: | --- | --- |
| 1 | 挂载点选在「信息刚好齐备、动作还来得及」的位置 | 先看一眼真实的执行顺序 |
| 2 | `additionalContext` 只能是 system，且延后到配对完成后才写入 | HookResult |
| 3 | `updatedInput` 必须重过 Zod 并深度冻结，否则会「批准 A 执行 B」 | PreToolUse |
| 4 | definition 用**引用相等**锁定，防止 write 伪装成 read | PreToolUse |
| 5 | 注册顺序 = 执行顺序 = 加工顺序；保守字段例外（取更严的） | HookRegistry |
| 6 | 提前结束也要把同轮剩余调用补成配对错误 | preventContinuation |
| 7 | Stop 最多续跑一次；第二次回调仍执行，只是控制权被收回 | Stop |

### 本章代码边界（明确「还没做什么」）

| 已经有 | 还没有 | 说明 |
| --- | --- | --- |
| 四个生命周期事件 | 官方 14+ 类事件（`SessionStart`、`PreCompact`、`SubagentStart` 等） | 教学子集；按需在后续章节增加 |
| 顺序执行的进程内回调 | 插件隔离、独立超时、并行执行 | **不做**。并行会让改写顺序和短路语义含混 |
| `blockingError` / `forceContinue` 短路 | 官方那种「跑完全部再合并」 | 刻意简化；短路位置明确 |
| 结构化契约保护 Loop 状态 | 操作系统级沙箱 | **不做**。Hook 是受信任的应用代码 |
| Pre 的 `hook_contract_error` | Post 的等价严格校验 | 刻意不对称，见「Hook 故障如何返回」 |
| 统一的 `hook_execution_error` | 区分「副作用已发生 / 未发生」 | 第 11 章讨论恢复与幂等时处理 |
| `UserPromptSubmit` 追加上下文 | 替换、删除或阻断原始 prompt | **不做**。用户输入不该被扩展代码改写 |

### 检查你是否真的读懂了

1. `[Hook] PreToolUse` 和 `[Permission]` 哪个先出现？为什么不能反过来？（先看一眼真实的执行顺序）
2. 为什么 `additionalContext` 只允许 system 消息？允许 tool 消息会怎样？（HookResult）
3. 一个恶意 Pre Hook 想把 `write_file` 伪装成 `read_file`，哪一条检查拦住了它？（PreToolUse）
4. 三个 Post Hook 依次改写结果，第三个看到的是哪个版本？（HookRegistry）
5. 同一轮有三个调用，第一个触发 `preventContinuation`，另外两个的 tool result 内容是什么？它们执行了吗？（preventContinuation）
6. Stop Hook 第二次被调用时，`forceContinue` 会生效吗？回调还会执行吗？（Stop）
7. Post Hook 抛异常和 Pre Hook 抛异常，错误码相同，但语义差别在哪？（Hook 故障如何返回）

---

第 4 章最终保留的是一条可验证的固定流水线：

```text
UserPromptSubmit
        ↓
model
        ↓
prepare
        ↓
PreToolUse
        ↓
permission / approval
        ↓
handler
        ↓
PostToolUse
        ↓
paired tool results
        ↓
Stop
```

以后增加日志、通知或结果检查时，只需要注册新的回调。权限优先级、工具身份和 OpenAI 消息配对仍由同一个 Agent Loop 维护。
