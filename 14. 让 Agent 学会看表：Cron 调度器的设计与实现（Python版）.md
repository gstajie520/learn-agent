# 让 Agent 学会看表：Cron 调度器的设计与实现（Python 版）

> 本文是第 14 章的完整 Python 迁移版，配套项目位于 `python/ch14_agent`。
> 原文的 Cron 架构、状态边界、持久 outbox、leader、时区/DST、事件回合和对照分析完整保留。
> Java 开发者可以先把 `CronJob` 当成 `record`，把 `CronStore` 当成 Repository，把 `CronRuntime` 当成受管 `ScheduledExecutorService`，把 `EventInbox` 当成 `BlockingQueue<RuntimeEvent>`。

闹钟不需要你盯着它才会响。

你设好 7:00，去睡觉，去洗澡，去做饭，到点它自己响。这件事理所当然到你从来不会去想：闹钟是怎么在你不在的时候知道时间到了的。

但当你开始构建 Agent 系统的时候，这个问题就不理所当然了。

第 13 章的 Agent 已经支持后台执行：`npm install` 跑两分钟，主循环照样能去读 README。但你回头看看那条链路的**起点**——它仍然是模型的一次工具调用，而模型之所以会调工具，是因为**你先说了一句话**。

你说一句，Agent 动一下。「每天早上 9 点跑一次测试」「每 30 分钟检查一次 CI」，这类任务没有人去推，Agent 就永远不会动。

第 13 章解决的是「慢操作不要占住 Loop」，第 14 章解决的是**「没有人说话的时候，Agent 也能开始干活」**。

本章只增加一个能力，它由四部分组成：**一个新工具、一个 cron 解析边界、一份带 outbox 的原子快照、一个受管的 scheduler**。

先把范围钉死。本章只做**时间驱动**：一个 `schedule_cron` 工具，没有 list、没有 cancel、没有文件监听、没有 Webhook、没有抖动和自动过期。事件回到 Loop 的那条通路，完全复用第 13 章的 `EventInbox`——**本章不新建第二条事件通道**。

---

## 本章导读

### 读完这一章，你能做到什么

1. 说清「触发权从人手里交给调度器」之后，多出来的四个必答问题分别是什么。
2. 解释为什么 `CronJob`（时间规则）和 `CronEvent`（一次到期的工作意图）必须是两个对象，合成一个会在哪个具体崩溃点丢工作。
3. 说出五段 cron 里 DOM 和 DOW 同时被约束时是 **OR** 而不是 AND，并举出一个被它坑到的表达式。
4. 描述夏令时的 gap 和 fold 各是什么，以及「本地日历计算 + UTC instant 持久化」为什么能同时处理这两种情况。
5. 说出快照锁和 leader 锁各自解决什么问题，为什么一把锁不够。
6. 解释为什么事件是**先进 history 再 ack**，反过来会在哪个窗口丢工作。
7. 判断 Agent 停机三天后重启，一个「每分钟」的 recurring job 会补发多少个事件。
8. 说明一个定时任务在触发时还要不要过权限，以及 job 创建之后权限规则改了以哪一版为准。

### 你需要先具备什么

| 前置 | 出自哪一章 | 本章怎么用它 |
| --- | --- | --- |
| Agent Loop 的一轮：模型 -> 工具 -> 结果回填 | 第 1 章 | 定时触发不改这条链路，只换一个起点 |
| `ToolDefinition` / validator 精确键集合 | 第 2 章 | `schedule_cron` 五个必填字段全靠它守边界 |
| `PermissionPolicy` 的判决位置 | 第 3 章 | **本章重点之一**：定时任务触发时仍然过这道门 |
| Pre/PostToolUse Hook 顺序 | 第 4 章 | 事件驱动的 turn 一步都不跳 |
| 文件锁 + 原子替换 + fail closed | 第 12 章 | `state.json` 直接沿用同一套纪律 |
| `RuntimeEvent` / `EventInbox` / `RuntimeEventPump` | 第 13 章 | **最关键的前置**，cron 事件走的就是这条通路 |
| Profile 能力位与组合根按能力注入 | 第 5～13 章 | P14 靠一个 `cron` 能力位接上整套调度运行时 |

只来得及补一章的话，补第 13 章的「事件怎样回到同一个 Loop」。本章新增的东西集中在**事件从哪来**，而**事件怎么回去**完全是上一章的结论。

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| slot（槽位） | 一个具体的到期时刻，本章一律以 UTC instant 表示 |
| `CronJob` | **未来的时间规则**，可以触发无数次，是「定义」 |
| `CronEvent` | **一次已经到期的工作意图**，只对应一个 slot，是「事实」 |
| `next_run_at_utc` | 这个 job 下一次该触发的 UTC 时刻 |
| `last_slot_at_utc` | 上一次已经产生过事件的 slot，防重复触发靠它 |
| outbox | 持久事件队列。事件在被确认之前不会消失 |
| ack（确认） | 「这条事件已经进了 history」，随后从 outbox 删除 |
| DOM / DOW | day of month（几号）/ day of week（星期几） |
| DST gap | 春季跳时，某个本地时间**不存在**（如纽约 02:30） |
| DST fold | 秋季回拨，某个本地时间**出现两次**（如纽约 01:30） |
| `durable` | `True` 写进 `state.json`，跨进程重启恢复；`False` 只活在内存 |
| leader | 同一个 workspace 里唯一有权为 durable job 产生事件的那个 session |
| event-only turn | 没有用户输入、由事件触发的一次模型回合 |
| misfire（漏触发） | 停机期间错过的 slot，重启后怎么处理是个策略决定 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 三个「时间」要分清            ← slot / now / next_run，混了后面全乱
  ↓
② 问题的本质：触发权变了        ← 四个必答问题
  ↓
③ 只增加一个工具               ← 五个必填字段，以及为什么没有默认值
  ↓
④ 五段表达式：别重造五十年边界  ← DOM/DOW 的 OR 陷阱
  ↓
⑤ 时区：本地日历算，UTC 存      ← gap 和 fold 两种反常情况
  ↓
⑥ Job 和 Event 必须分开        ← 本章最重要的建模决定
  ↓
⑦ 单一原子快照 + outbox        ← 状态长什么样，怎么写进去的
  ↓
⑧ durable 和 session-only      ← 两套生命周期
  ↓
⑨ 两把锁各管什么               ← 快照锁 vs leader 锁
  ↓
⑩ 从 outbox 到 Agent           ← 先入历史，再确认
  ↓
⑪ 触发时重新授权               ← 定时任务不是权限票据
  ↓
⑫ 受管 scheduler 与关闭顺序    ← 谁负责停下来
  ↓
⑬ 接进组合根                   ← 能力位、17 个工具
  ↓
⑭ 运行 + 实验 + 排错           ← 把上面每条结论都亲手验一遍
  ↓
⑮ 小结 + 自测                  ← 记住五条 + 自测题
```

---

## 三个「时间」要分清

进入实现之前必须先分开三个时间概念。它们在日常说话时都叫「时间」，但在代码里是三个不同的量，混起来会让后面每一节都打结。

```text
now          ← 现在几点。scheduler 每次 tick 的输入
slot         ← 这次「应该在什么时候执行」。事件的属性
next_run     ← 这个 job「下一次该在什么时候执行」。job 的属性
```

用一个具体例子把三者钉死。假设一个「每分钟」的 job，创建于 `12:00:30`：

| 时刻 | now | job.next_run | 发生了什么 |
| --- | --- | --- | --- |
| 创建时 | `12:00:30` | `12:01:00` | 算出第一个未来 slot |
| 第一次 tick | `12:00:45` | `12:01:00` | `now < next_run`，什么也不做 |
| 第二次 tick | `12:01:02` | `12:01:00` | **到期了**，产生 slot = `12:01:00` 的事件 |
| 同一次迁移里 | `12:01:02` | `12:02:00` | job 的 `next_run` 被推进 |

注意第三行：事件的 `slot` 是 **`12:01:00`**，不是 `12:01:02`。**slot 是应该执行的时刻，不是实际发现它到期的时刻。** 这个区别在两个地方会救你：

- 一个「每天 9:00」的 job，因为进程忙了 8 秒才发现，事件的 slot 仍然是 `09:00:00`。模型看到的是「这是 9 点那一次」，不是「这是 9 点 08 秒那一次」。
- 判断「这个 slot 是不是已经发过了」，靠的是比对 `last_slot_at_utc`，而它记的就是 slot。

另外注意：创建时算出来的第一个 slot 是 `12:01:00`，**不是 `12:00:30` 本身**，也不是 `12:00:00`。`next_run` 永远是**严格大于当前时刻**的第一个匹配点。「创建即触发一次」不是本章的语义。

---

## 问题的本质：触发权变了

手动触发的链路是：**人 -> Agent**。人不在，链路就断了。

定时触发的链路是：**Scheduler -> Agent**。人退出了这条链路，调度器成了新的触发者。

这一个箭头的改变，会逼出四个必答问题。它们不是「可以优化的地方」，是**不回答就一定出错的地方**：

| 问题 | 不回答会怎样 | 本章在哪里回答 |
| --- | --- | --- |
| 谁解释 cron 表达式和时区？ | 「每天 9 点」到底几点没有确定答案 | 五段表达式 + 时区两节 |
| 时间到了以后，怎么持久记录「这次该执行」？ | 进程一崩，这次工作永久消失 | Job/Event 分离 + outbox |
| Agent 正忙时事件怎么办？ | 要么丢事件，要么两个 turn 同时写 history | 从 outbox 到 Agent |
| 定时任务调工具时还过权限吗？ | 一个旧 job 变成绕过当前策略的后门 | 触发时重新授权 |

第 14 章没有另起一套 Agent Loop。它严格等于第 13 章已有能力，再加一个 `cron` 能力位。最终落成四个边界：

```text
schedule_cron 工具
    |  只负责校验参数、取当前 identity、创建 job
    v
JsonCronStore
    |  保存 job、next slot 和 durable outbox；一把锁包住读改写
    v
CronRuntime
    |  受管 scheduler 定期 tick，把 pending event 发布到 EventInbox
    v
AgentRunner.run_events()
       空闲时执行 event-only turn，工具仍过 Hook 与权限
```

每一层的职责边界都值得说一句：

- **Scheduler 只判断时间并产生事件**，它不执行业务工具，也不绕过 Agent Loop。
- **持久 outbox 只负责「工作意图不能凭空消失」**，它不负责「恰好执行一次」。
- **`EventInbox` 只负责当前进程内的 typed 交接**，它不持久化任何东西。
- **`AgentRunner` 是模型、Hook、权限和工具执行的唯一入口**，本章一个字都没改这条规则。

---

## 只增加一个工具

第 14 章相对第 13 章只追加一个工具：`schedule_cron`。没有 `schedule_job` 别名，也没有 list 或 cancel。

输入有五个字段，**全部必填**：

```python
def _schedule_args(value: Mapping[str, object]) -> bool:
    return set(value) == {"cron", "prompt", "timezone", "recurring", "durable"} \
        and all(isinstance(value[name], str) and bool(str(value[name]).strip()) for name in ("cron", "prompt", "timezone")) \
        and isinstance(value["recurring"], bool) \
        and isinstance(value["durable"], bool)
```

| 字段 | 含义 | 为什么不能有默认值 |
| --- | --- | --- |
| `cron` | 严格五段 cron 表达式 | 没有「合理的默认周期」这种东西 |
| `prompt` | 到期后交给 Agent 的工作描述 | 空 prompt 的定时任务毫无意义 |
| `timezone` | IANA 时区名，如 `UTC`、`Asia/Shanghai` | **默认成宿主机时区是本章最想避免的坑** |
| `recurring` | `True` 周期，`False` one-shot | 猜错任一个方向都是严重后果 |
| `durable` | `True` 跨进程重启恢复 | 「以为存下来了其实没存」比明确报错危险得多 |

精确键集合校验的作用是**多传字段直接失败**。模型如果自作聪明加一个 `enabled: true`，不会被静默忽略，而是当场报 `invalid_arguments`。

### 三个字段不由模型填

`schedule_cron` 的输入里没有 `identity`、`id`、`next_run_at_utc`。这三样都由运行时决定：

```python
payload = dict(arguments)
payload["identity"] = context.identity     # 从 ToolContext 观察，模型无法伪造
payload["now_utc"] = self.clock.now()      # 当前时间
job = self.store.schedule_cron(payload)    # id 和 next_run 由 store 计算
```

`identity` 这一条尤其重要。如果它是输入字段，模型就能创建一个「以管理员身份运行」的定时任务——那等于把权限系统整个绕过去了。**创建者身份是观察到的事实，不是可申报的参数。**

### 四种 typed 错误

| 错误类 | `error_code` | 什么时候出现 |
| --- | --- | --- |
| `CronExpressionError` | `cron_expression_error` | 表达式不是五段、字段越界、反向范围、未知时区 |
| `CronStorageError` | `cron_storage_error` | 快照损坏、版本不符、原子写失败 |
| `CronJobNotFoundError` | `cron_job_not_found` | 引用了不存在的 job |
| `CronClosedError` | `cron_closed` | 运行时已关闭还调 schedule/tick |

注意 `CronClosedError` 的存在意义：关闭之后继续调度**必须明确失败**。

---

## 五段表达式：别重造五十年的边界

五段 cron 的顺序是固定的：

```text
分钟  小时  日  月  星期
 │     │    │   │   └── 0-7（0 和 7 都是周日）
 │     │    │   └────── 1-12
 │     │    └────────── 1-31
 │     └─────────────── 0-23
 └───────────────────── 0-59
```

**Python 版的一个真实差异**：TS 版把语法和「下一次发生时间」交给锁定版本的 `cron-parser@5.6.2`；Python 版**自己实现了完整的五段解析器**（`features/cron.py` 的 `_parse_field` + `_matches` + `next_cron_occurrence`），没有引入第三方 cron 库。支持列表、范围、步进、星号：

```python
def _parse_field(value: str, minimum: int, maximum: int) -> tuple[set[int], bool]:
    ...
    for item in value.split(","):          # 列表 "0,15,30,45"
        parts = item.split("/")            # 步进 "*/5"
        ...
        elif "-" in base:                  # 范围 "1-10"
            if start > end:
                raise CronExpressionError("Cron 范围不能倒序")
```

为什么 Python 版敢自己写？因为边界检查已经全部显式化：恰好五段、步进必须正整数、范围不能倒序、字段不能超界。**Python 版把 TS 版「交给依赖」的那些边界，全部写成了自己的拒绝分支**——`test_cron_rejects_bad_expression_timezone_and_naive_time` 逐条验证。

### 但校验要在写状态之前收口

**坏输入不会先写状态再等 scheduler 出错。** 这是一条通用纪律：校验失败必须发生在任何持久化之前，否则磁盘上会留下一个永远触发不了的僵尸 job。

### DOM 和 DOW 是 OR，不是 AND

这是五段 cron 最著名的坑，值得单独一节。

标准 cron 在**两者都被约束**时使用 **OR**。看这个表达式：

```text
0 9 1 * 1
     ↑   ↑
    1号  周一
```

直觉会读成「每月第一个周一的 9 点」。**它不是。** 它的实际含义是：

```text
每月 1 号的 09:00  ✅
        或者
每个周一的 09:00   ✅
```

`test_cron_supports_lists_ranges_steps_and_dom_dow_or` 实测验证（断言钉死）：

```python
# 0 9 1 * 1 从 2026-06-01 09:01 之后找下一次
assert next_cron_occurrence("0 9 1 * 1", "UTC", dt.datetime(2026, 6, 1, 9, 1, tzinfo=dt.UTC)) \
    == dt.datetime(2026, 6, 8, 9, tzinfo=dt.UTC)     # 6 月 8 日是周一
```

从 6 月 1 日 09:01 往后找，下一次是 **6 月 8 日**（周一），不是 7 月 1 日。如果是 AND 语义，答案会是「7 月里第一个既是 1 号又是周一的日子」——那得等很久。

Python 版实现里这段判断（`_matches`）：

```python
if dom_wild and dow_wild:
    return True          # 都是通配符：任何一天都匹配
if dom_wild:
    return dow_match     # 只看星期
if dow_wild:
    return day_match     # 只看几号
return day_match or dow_match   # 都约束了：OR
```

### 一个推论：五段 cron 表达不了「每月第一个工作日」

既然 DOM/DOW 是 OR，它只能**扩大**匹配集合，永远无法表达「同时满足两个条件」。所以「每月第一个工作日」「每月最后一个周五」五段 cron 都做不到。

正确的做法是：cron 负责「够频繁的粗筛」，业务逻辑负责「今天到底算不算」。比如用 `0 9 1-5 * *` 触发，然后在 prompt 里让 Agent 自己判断今天是不是本月第一个工作日。**把日历判断放进 prompt，而不是放进表达式。**

---

## 时区：本地日历计算，UTC 持久化

「每天 9 点」如果不说时区，就没有完整含义。所以 `timezone` 是必填的 IANA 时区名（`ZoneInfo` 校验）。实测同一个 `0 9 * * *` 在上海时区下的结果（`test_cron_supports_lists_ranges_steps_and_dom_dow_or`）：

```python
base = dt.datetime(2026, 6, 1, 0, 30, tzinfo=dt.UTC)
assert next_cron_occurrence("0 9 * * *", "Asia/Shanghai", base) == dt.datetime(2026, 6, 1, 1, tzinfo=dt.UTC)
```

上海比 UTC 早 8 小时，所以上海的 9 点是 UTC 的 1 点。

未知时区当场失败：

```python
with pytest.raises(CronExpressionError):
    validate_cron_timezone("Mars/Phobos")
```

### 两步走：本地算，UTC 存

核心策略只有一句话：**在本地日历里计算，把结果作为 UTC instant 持久化。**

Python 版 `next_cron_occurrence` 的实现是**逐分钟扫描本地日历**：

```python
local_before = after_utc.astimezone(zone).replace(second=0, microsecond=0)
...
local_current = local_before.replace(tzinfo=None) + dt.timedelta(minutes=1)
# 按业务时区的本地日历扫描。这样 DST gap/fold 不会被宿主机 UTC 扫描吞掉。
for _ in range(5 * 366 * 24 * 60):
    if _matches(local_current.replace(tzinfo=zone), normalized):
        candidates = []
        for fold in (0, 1):     # ← 检查 fold 0 和 fold 1 两个候选
            candidate = local_current.replace(tzinfo=zone, fold=fold)
            roundtrip = candidate.astimezone(dt.UTC).astimezone(zone).replace(tzinfo=None)
            if roundtrip == local_current:   # roundtrip 能对回来才有效
                candidates.append(candidate.astimezone(dt.UTC))
```

那个 `roundtrip` 检查很巧妙：**把本地时间转成 UTC 再转回本地，如果对不上原值，说明这个本地时间不存在（gap）**。Python 的 `zoneinfo` 提供了 `fold` 参数，让同一本地时刻的两个 UTC 候选都能被表达——TS 版需要库配合，Python 标准库原生支持。

所有持久化字段——`next_run_at_utc`、`last_slot_at_utc`、`slot_at_utc`——都是 UTC ISO 字符串（序列化用 `isoformat().replace("+00:00", "Z")`）。运行时拒绝 naive datetime（`test_cron_rejects_bad_expression_timezone_and_naive_time` 第三条断言），也绝不保存没有时区含义的本地分钟字符串。

### DST gap：不存在的那一小时

纽约 2026 年 3 月 8 日，时钟从 02:00 直接跳到 03:00。**02:30 这个本地时间在那一天根本不存在。** `test_dst_gap_and_fold_are_explicit` 实测：

```python
gap = next_cron_occurrence("30 2 * * *", "America/New_York", dt.datetime(2026, 3, 8, 6, tzinfo=dt.UTC))
assert gap == dt.datetime(2026, 3, 8, 7, tzinfo=dt.UTC)
```

`07:00 UTC` 换成纽约本地时间是 **03:00**。本章的策略是：**推进到第一个有效时刻**（实现里那段 `for _ in range(180)` 的 probe 循环）。

三种可选策略对比：

| 策略 | 结果 | 问题 |
| --- | --- | --- |
| 跳过这一天 | 3 月 8 日不执行 | 「每天」变成「除了那天每天」，静默漏一次 |
| 报错 | 抛异常 | 一年两次的日期让一个正常 job 崩掉 |
| **推进到有效时刻** | 03:00 执行 | 那天晚了 30 分钟——**代价最小** |

### DST fold：出现两次的那一小时

纽约 2026 年 11 月 1 日，时钟从 02:00 退回 01:00。**01:30 出现两次。** 实测：

```python
fold = next_cron_occurrence("30 1 * * *", "America/New_York", dt.datetime(2026, 11, 1, 5, 30, tzinfo=dt.UTC))
assert fold == dt.datetime(2026, 11, 1, 6, 30, tzinfo=dt.UTC)
```

第一次是 `05:30 UTC`（本地 01:30 EDT，UTC-4），第二次是 `06:30 UTC`（本地 01:30 EST，UTC-5）。**同一个本地时钟面，两个不同的 UTC instant，都被保留了。**

这正是「UTC 持久化」的价值。如果状态里存的是本地时间字符串 `"01:30"`，第二次 tick 时你会发现「`last_slot` 是 01:30，现在也是 01:30，看起来重复了，跳过吧」——于是**静默漏掉一次执行**。存 UTC instant 就不会有这个问题。

不过要说清楚：本章**没有**为 fold 做「只执行一次」的特殊处理。如果业务不能容忍，正确的做法是让 job 自己幂等（拿 `idempotency_key` 去重），而不是让调度器猜。

测试固定了这几组结果，所以**宿主机的本地时区不会影响断言**。

---

## Job 和 Event 必须分开

这是本章最重要的建模决定。

`CronJob` 是**未来的时间规则**：它可以触发无数次，它描述「什么时候该做」。
`CronEvent` 是**某一次已经到期的工作意图**：它只对应一个 slot，它是一个已经发生的事实。

### 为什么不能合成一个

假设你偷懒，只用 `CronJob` 一个对象，加个 `pending: true` 字段表示「到期了还没执行」。看看 one-shot 任务会发生什么。

**方案 A：先删定义，再执行。**

```text
时间到 → 删掉 job → 💥 进程崩溃 → 重启
                                 ↓
                        磁盘上什么都没有了
                        这次工作永久消失
```

**方案 B：先执行，再删定义。**

```text
时间到 → 执行 → 💥 崩在删除之前 → 重启
                                  ↓
                         job 还在，看起来还没执行
                         这次工作被执行两次
```

两个方案都错，而且**无论怎么排顺序都错**，因为「定义」和「这一次的工作意图」被绑在同一个记录上，你只有一次原子写的机会，却要同时表达两件事。

拆开就解决了：

```text
时间到 → 在同一个原子写里：{ 创建 event 进 outbox } + { 删除 job 定义 }
       → 崩溃 → 重启 → 定义没了（对，one-shot 不该再触发）
                      event 还在 outbox（对，这次工作还没做）
       → 继续执行 → 执行完 ack → event 才从 outbox 删除
```

`test_one_shot_removes_job_but_ack_keeps_event_until_consumed` 把这条完整钉住：

```python
store.schedule_cron(make_input(durable=True, recurring=False))
store.tick(job.next_run_at_utc)
assert store.list_jobs() == ()          # 定义已删除
assert store.ack_event(EVENT)           # event 还在 outbox，能确认
assert not store.ack_event(EVENT)       # 重复确认返回 False
assert store.pending_events() == ()     # 确认后 outbox 清空
```

### 到期时的一次原子迁移

`store.tick(now)` 在**同一个锁、同一次原子写**里完成这些动作：

```text
1. 找出所有 next_run_at_utc <= now 的 job
2. 按 next_run_at_utc 排序（相同则按 id），保证顺序确定
3. 为到期 slot 创建带稳定 UUID 的 CronEvent
4. 把 event 放进 outbox
5a. recurring → 更新 last_slot_at_utc，把 next_run_at_utc 推进到 now 之后
5b. one-shot  → 删除 job 定义（但 event 留在 outbox）
6. 只有真的产生了 durable event 才落盘
```

第 2 步的排序不是可选的（`sorted(..., key=lambda item: (item.next_run_at_utc, item.id))`）。多个 job 同时到期时，如果顺序不确定，outbox 容量满的时候「谁被挤掉」就成了随机的。

### 重复 tick 不靠字符串标记

一种常见的错误实现是用 `"%H:%M"` 之类的字符串记「今天这个点发过了」。本章不需要，因为**第一次迁移之后 job 的状态已经变了**：

- recurring 的 `next_run_at_utc` 已经推进到了 `now` 之后，再 tick 时不到期。
- one-shot 的定义已经删除，再 tick 时根本找不到它。

**防重复触发是状态迁移的自然结果，不是额外加的一层标记。**

### 停机三天：只补一个

如果 Agent 停机三天后重启，一个「每分钟」的 recurring job 会补发多少个事件？朴素实现会**一口气产生 4320 个事件**，把 outbox 和上下文全部撑爆。

本章的答案是 **1 个**。`test_recurring_tick_advances_beyond_misfire_time` 实测：

```python
store.schedule_cron(make_input(durable=True, recurring=True))
store.tick(job.next_run_at_utc + dt.timedelta(hours=3))   # 模拟停机 3 小时后
updated = store.get_job(JOB)
assert updated.last_slot_at_utc == job.next_run_at_utc     # ← 记的是最早那个 slot
assert updated.next_run_at_utc > job.next_run_at_utc + dt.timedelta(hours=3)  # ← 直接追平
```

- `last_slot_at_utc` 记的是**最早被漏掉的那个 slot**，不是「现在」。
- `next_run_at_utc` 被直接推进到 **`now` 之后**的第一个匹配点。中间的 slot 被**明确放弃**。

这是一个 misfire 策略选择，叫「只补最早一个，然后追平」。理由：一个「每分钟检查 CI」的任务，补 4320 次毫无意义——你只需要知道现在 CI 是什么状态。保留最早那一个，是为了让模型知道**漏了多久**。

---

## 单一原子快照：state.json 加 outbox

durable 状态只有一个文件：

```text
workspace/.agent_tutorial/cron/state.json
```

注意这里和第 13 章的差别：第 13 章是「每个 job 一个文件」，本章是**所有状态一个文件**。原因：**三处变化必须在一次原子替换里一起提交**：

```text
next_run_at_utc:  06-02 → 06-03    ← 推进到下一次
last_slot_at_utc: null  → 06-02    ← 记住这个 slot 发过了
outbox:           []    → [event]  ← 工作意图落盘
```

**这三处必须同时生效。** 如果 `next_run` 推进了但 event 没落盘，这次工作就丢了；如果 event 落盘了但 `next_run` 没推进，下次 tick 会再发一遍。多个文件给不了这种跨记录原子性，一个文件加 `os.replace` 给得了。

快照结构：

```json
{"version":1,"jobs":[{"id":"...","cron":"0 9 * * *","prompt":"check CI","timezone":"UTC","recurring":true,"durable":true,"identity":"user","next_run_at_utc":"2026-06-02T09:00:00Z","last_slot_at_utc":null}],"outbox":[]}
```

### durable 承诺到哪一层

`durable` **不承诺**主机断电或内核崩溃级持久性（`fsync` 了临时文件但没同步父目录）。它承诺的是：**Agent 进程正常退出、异常退出或重新启动后，可以从已经提交的快照恢复。** 这是一个明确的取舍，不是疏漏。

### 坏状态不会被静默忽略

损坏的 `state.json` 一律抛 `CronStorageError`（fail closed）。触发条件包括：

```text
未知字段        ← set(payload) != {"version", "jobs", "outbox"}
错误版本号      ← version != 1
非 durable 记录  ← durable state 里出现 session 记录
坏 JSON / 非 UTF-8
非法 job        ← next_run 不晚于 last_slot、时间不带 Z 后缀
```

`_parse_time` 要求时间字符串**必须以 `Z` 结尾**——拒绝任何没有 UTC 含义的时间格式。`version` 这一条尤其值得说：如果将来快照格式要改，读到 `version: 2` 的旧代码会**明确报错**，而不是按 v1 的规则去解释 v2 的数据。

### outbox 有容量上限

Python 版默认 **100 条**（TS 版是 50——这是一个真实差异，如实记录）。容量满时的行为是关键：

```python
if len(known) >= self.outbox_capacity:
    return ()            # ← 后续 due job 原地不动
```

**不能先推进时间再丢工作。** 被容量挡住的 job 保持原来的 `next_run_at_utc` 和 `last_slot_at_utc`，等 event 被确认、outbox 腾出位置，下一次 tick 就补上了。

如果实现成「容量满就跳过这个 job，但照样推进 next_run」，那这次工作就静默消失了——而且**看起来一切正常**。

---

## durable 和 session-only 是两套生命周期

`durable=True`：job、下一 slot 和 pending event 写入 `state.json`，新建 `JsonCronStore` 后可以恢复。

`durable=False`：job 和 outbox **只存在当前 store 实例的内存**里（`_session_jobs` / `_session_outbox` 两个 dict）。进程或 session 结束后消失，也不会混入 durable 快照。

`test_durable_job_and_outbox_recover` 实测对照（重建 store）：

```python
job = store.schedule_cron(make_input(durable=True, recurring=False))
events = store.tick(job.next_run_at_utc)
restarted = JsonCronStore(str(tmp_path))
assert restarted.list_jobs() == ()                                      # one-shot 定义已删
assert [event.event_id for event in restarted.pending_events()] == [EVENT]  # durable event 还在
```

### durable 不等于「关掉应用还在跑」

这是最容易误解的一点：

```text
❌ durable = 我关掉 Agent，到点它还会自己启动执行
✅ durable = 我下次启动第 14 章 Agent 时，规则和未确认事件还在
```

scheduler 属于 Agent 进程。**进程停止时，没有任何代码在看表。** 要做到「关机也能触发」，需要操作系统级调度（Windows 任务计划、systemd timer），本章明确不做。

### 什么时候该用哪个

| 场景 | 选哪个 | 理由 |
| --- | --- | --- |
| 「每天早上检查依赖更新」 | `durable=True` | 长期规则，重启后该继续 |
| 「20 分钟后提醒我看一下这个 PR」 | `durable=False` | 只对当前会话有意义 |
| 「每周一生成周报」 | `durable=True` | 跨会话的长期承诺 |

一个实用判据：**这个任务的意义是否依赖于当前对话上下文？**

---

## 两把锁各管什么

同一个 workspace 可能同时打开多个 Agent session。这会带来两个**完全不同**的问题，所以需要两把锁。

**Python 版的真实差异**：TS 版用 `proper-lockfile` 的跨进程目录锁加 stale/update 心跳；Python 版的快照锁是**进程内 `threading.RLock`**（按 cron 目录路径共享，与第 9/12/13 章同一个模式），**没有跨进程文件锁**。跨进程的互斥由 leader 机制承担。

| 锁 | 实现 | 负责什么 |
| --- | --- | --- |
| 快照锁 | 进程内 RLock（`_locked()`） | 保护 durable 快照的原子读改写 |
| leader 锁 | `leader.lock` marker 文件（`open("x")` 独占创建） | 限制同一 workspace 只有一个 durable scheduler |

leader 的实现非常直接——`open("x")` 是「不存在则创建、已存在直接失败」的原子操作：

```python
def try_acquire_leader(self) -> bool:
    if self._leader:
        return False
    marker = root / "leader.lock"
    try:
        with marker.open("x", encoding="ascii") as handle:
            handle.write("leader")
    except FileExistsError:
        return False
    self._leader = True
    return True
```

**为什么一把锁不够？** 快照锁防的是「同时写坏文件」（持有时间极短）；leader 防的是「同一个 slot 被投递两次」（持有时间是整个 session 生命周期）。两个 session 都在跑 scheduler，9 点到了，两个都发现 job 到期，两个都创建事件——**同一件工作被执行两次**。leader 把这件事变成显式规则：**只有 leader 为 durable job 产生事件**（`tick(now, include_durable=self._leader)`）。

leader 换人时工作不丢：A 关闭后 `release_leader()` 删掉 marker，B 再 `try_acquire_leader()` 就接管了——而 A 没确认的事件还在 outbox 里，B 拿到的是**同一个事件**。

注意 Python 版与 TS 版的一个取舍：TS 版的 leader 锁有 stale 机制（30 秒无心跳自动接管），Python 版的 marker 文件**没有 stale 超时**——持有者被 `kill -9` 后 marker 会残留，需要手工删除（`leader.lock` 是显式文件，看得见）。这是两个版本在崩溃恢复上的真实差异。

---

## 从 outbox 到 Agent：先入历史，再确认

`CronRuntime.tick()` 的职责很窄，只有四步：

```python
def tick(self) -> tuple[CronEvent, ...]:
    if self._closed:
        raise CronClosedError("CronRuntime 已关闭")
    if not self._leader:
        self._leader = self.store.try_acquire_leader()
    self.store.tick(self.clock.now(), self._leader)      # ① ②
    published: list[CronEvent] = []
    for event in self.store.pending_events(self._leader):
        if event.event_id not in self._queued:           # ③ 进程内去重
            self.inbox.publish(event)
            self._queued.add(event.event_id)
            published.append(event)
    if published and self._wakeup is not None:
        self._wakeup()                                    # ④ 唤醒 Agent
    return tuple(published)
```

第 ③ 步 `_queued` 是一个 `set`，解决「事件已经在 inbox 里排队，但还没被 ack，下一次 tick 又看到它还在 outbox」的情况。

### 事件进入 Loop 的顺序

`AgentRunner.run_events()`（`core/loop.py`）的实现：

```python
def run_events(self) -> RunResult | None:
    event = self._deferred_runtime_events.pop(0) if self._deferred_runtime_events else None
    if event is None and self._event_pump is not None:
        drained = self._event_pump.drain_events(1)     # ← 每次只取一条！
        event = drained[0] if drained else None
    if event is None:
        return None
    ...
    result = asyncio.run(self._run(getattr(event, "prompt", "处理运行时事件"), event))
    if self._event_pump is not None:
        self._event_pump.acknowledge_events((event,))   # ← 执行成功才 ack
    return result
    # 异常时：release_events —— 释放内存去重标记，事件留在 outbox 重投
```

三个关键点：

**每次只取一个事件**（`drain_events(1)`）。这和第 13 章（一次 drain 整批）是个**反向变化**。为什么要退掉批量？因为**不同事件可能有不同的 identity**：一个 model turn 只有一个 `ToolContext`，装不下两个 identity。批量注入要么用错身份（安全问题），要么只用最后一条的。**逐条处理是身份隔离的必然结果。**

**`release_events` 的设计**。执行异常时不 ack、从 `_queued` 里摘掉标记——**下一次 tick 会重新发布同一个 durable outbox 事件**。这是「宁可重复，不可丢失」的实现。

**失败重投的语义边界**：`acknowledge_events` 失败抛 `CronStorageError`（「Cron event 不再处于 pending」），异常向上传播让调用方明确知道确认出了问题。

### 先入历史，再确认

回看 `run_events` 的顺序：`_run(event)` 把事件**追加进 history 并跑完整个模型回合**，然后才 `acknowledge_events`。反过来会怎样：

```text
先 ack（从 outbox 删除）
  ↓
💥 进程在这里崩溃
  ↓
重启：outbox 是空的，history 里也没有这条消息
      这次工作永久消失，而且没有任何痕迹
```

按正确顺序，同样的崩溃点：outbox 里事件还在 → 下次 tick 重新发布 → 重新执行。代价是**可能重复执行一次**。**宁可重复，不可丢失。** 而重复的那部分风险由 `idempotency_key` 兜底。

### Agent 正忙的时候

更微妙的情况是：**用户 turn 进行中，恰好轮询到了 cron 事件。** 这时候不能直接注入——那会把一条定时任务的消息插进用户对话中间，而且身份是错的。实现的做法是暂存在 `_deferred_runtime_events`：

```python
def _inject_runtime_events(self, wait_for_pending: bool, *, allow_context_events: bool) -> None:
    events = self._event_pump.drain_events()
    ...
    for event in events:
        if event.context_identity is not None and not allow_context_events:
            self._deferred_runtime_events.append(event)   # ← 暂存
        else:
            injectable.append(event)
```

`context_identity is not None` 就是「这条事件带着自己的身份」的信号（cron 事件有，第 13 章的后台完成事件没有）。用户 turn 里（`allow_context_events=False`）带身份的事件**不追加 history、不 ack**，等用户回合结束后由 `run_events()` 按保存的事件上下文执行。

### 事件上下文怎么构造

确认成功后，事件携带的身份和幂等键进入工具上下文（`_run` 里）：

```python
context = ToolContext(
    self._workspace,
    self._identity if runtime_event is None or runtime_event.context_identity is None
        else runtime_event.context_identity,    # ← cron 事件用创建者身份
    None if runtime_event is None else runtime_event.idempotency_key,   # ← eventId 当幂等键
)
```

`createCronEvent` 做的映射是：

```text
job.identity  → event.context_identity   ← 创建者身份
event.eventId → event.idempotency_key    ← 这次触发的唯一 id
```

用 `eventId` 当幂等键是自然的选择：同一个事件重投时 id 不变，不同 slot 的事件 id 不同。**「同一次工作意图」和「同一个幂等键」天然对应。**

---

## 触发时重新授权

创建一个 cron job，意味着「允许保存一条未来的工作意图」。它**不意味着**未来所有操作都被永久授权。

这条边界如果搞错，定时任务会变成一个绕过权限系统的后门。

### 完整顺序一步不跳

event-only turn 执行时，Runner 用事件的 identity 构造**新的** `ToolContext`。模型随后提出的每个工具调用仍按公共顺序处理（`_execute_tool` 一个字没改）：

```text
工具参数校验（validator）
  ↓
PreToolUse Hook
  ↓
当前 PermissionPolicy 判决
  ↓
handler 执行
  ↓
PostToolUse Hook
  ↓
对应 tool result 回填
```

**没有任何一步因为「这是定时任务」而被跳过。** 如果当前策略拒绝，handler 调用次数就是零。

### 以哪一版权限为准

假设你周一创建了一个 job：「每天 9 点用 `shell` 跑一次部署脚本」。周三你收紧了权限策略。周四 9 点这个 job 触发了，会发生什么？

```text
❌ 按创建时的规则 → 部署照样执行，你周三的收紧毫无作用
✅ 按触发时的规则 → 被拒绝，handler 一次都没调用
```

本章选后者。理由：**权限策略表达的是「现在允许什么」，不是「当初允许过什么」。** 如果定时任务能凭旧规则执行，攻击者只要提前埋一个 job 就能绕过任何后续收紧。

```text
cron prompt 是「未来要重新交给 Agent 的输入」
cron prompt 不是「权限票据」
```

### 那为什么还要保存创建者 identity

因为**审计和权限判决都需要知道「这是谁的任务」**。多用户场景下，alice 创建的「清理她的临时目录」如果用当前交互者 bob 的身份执行，审计日志就记错了人，按用户划分的权限也会操作错目录。**身份来自过去，权限来自现在。**

---

## 受管 scheduler 与关闭顺序

**Python 版的真实差异**：TS 版的 scheduler 通过 `JobSupervisor.startManaged()` 注册，和后台 job 共享唯一 task owner；Python 版的 `CronRuntime` **自己起一个 `threading.Thread(daemon=True)`**，不经过 JobSupervisor。源码很直接：

```python
def start(self) -> None:
    if self._closed:
        raise CronClosedError("CronRuntime 已关闭")
    if self._thread is not None:
        return
    self._stop.clear()
    self._thread = threading.Thread(target=self._run, name="cron-runtime", daemon=True)
    self._thread.start()

def _run(self) -> None:
    while not self._stop.is_set() and not self._closed:
        try:
            self.tick()
        except Exception:
            time.sleep(self.poll_seconds)
        self._stop.wait(self.poll_seconds)     # ← 可中断的 sleep
```

scheduler 循环：醒来 → tick → 睡 `poll_seconds`（默认 1 秒）→ 再醒。`_stop.wait()` 让关闭时能立即中断，不用等满一秒。

### 关闭顺序：反过来

组合根把资源按这个顺序交给 Runner：

```python
resources=tuple(item for item in (background_supervisor, cron_runtime) if item is not None)
```

`AgentRunner.close()` **逆序**关闭，所以先执行 `cron_runtime.close()`，再关 `background_supervisor`。

`close()` 内部做四件事，顺序同样重要：

```python
def close(self) -> None:
    if self._closed and self._thread is None:
        return
    self._closed = True          # ① 新的 tick 立即失败
    self._stop.set()             # ② 停掉循环
    if self._thread is not None:
        self._thread.join(max(1.0, self.poll_seconds * 2))   # ③ 等线程收束
        self._thread = None
    if self._leader:
        self.store.release_leader()    # ④ 释放 leader
        self._leader = False
```

这是一条通用规律：**依赖别人的先关，被依赖的后关。** 和构造顺序正好相反。

### 可注入的时间边界

测试注入 `CronClock`（`clock` 参数）和固定 ID 生成器，不依赖真实墙钟：

| 注入点 | 作用 |
| --- | --- |
| `clock` | 控制「现在几点」，可以瞬间跳到三天后 |
| `id_generator` / `event_id_generator` | 产生可预测 id，输出能和文档对照 |
| `outbox_capacity` | 调小，容量满的行为一步就能测到 |

`test_cron_store.py` 的 `make_input` 直接传 `now_utc` 固定基准时间，整个测试套件**不需要 API Key，不访问网络，不睡真实时间**。

---

## 接进组合根

### 一个能力位

`P14 = P13 + "cron"`。`test_ch14_integration.py` 断言 `profile_for_chapter(14) is P14`。

### 三道配对检查

`bootstrap.py` 的检查（`test_ch14_integration.py` 实测构建必须成功）：

```python
if profile is P14:
    if background_supervisor is None or cron_runtime is None:
        raise ValueError("第十四章必须提供共享 background_supervisor 和 cron_runtime")
    if cron_runtime.supervisor is not background_supervisor or cron_runtime.event_inbox is not background_supervisor.inbox:
        raise ValueError("cron_runtime 必须共享 background_supervisor 和 EventInbox")
    tools.register(cron_runtime.tool_definition)
```

第三道最值得说。想象这个 bug：

```text
CLI 造了 supervisor A 和 inbox A，交给 Runner
CronRuntime 内部自己造了 supervisor B 和 inbox B
```

代码能跑，什么都不报错。但 cron 事件全部发进 inbox B，而 Runner 在监听 inbox A——**定时任务永远不会触发，而且没有任何错误信息**。这类 bug 排查起来极其痛苦，所以组合根直接把它变成启动时的显式失败。对象身份检查（`is not`）和第 12/13 章的配对纪律一脉相承。

### P14 的完整工具列表

P13 的 15 个前缀 + `schedule_cron` = **16 个**。测试断言的是 `"schedule_cron" in runner._tools.names`。

**只加了一个工具。** 本章绝大部分工作量在解析器、outbox、leader、事件延后注入这些**模型看不见的部分**。

### CLI 怎么接线

```python
inbox = EventInbox()
supervisor = JobSupervisor(JsonBackgroundJobStore(workspace), inbox)
runtime = CronRuntime(JsonCronStore(workspace), inbox, supervisor=supervisor)
```

注意 Python 版的 event_pump 选择：**P14 的 event_pump 是 `cron_runtime` 而不是 supervisor**（`event_pump=cron_runtime if cron_runtime is not None else background_supervisor`）。因为 cron 事件的 ack 语义和后台事件不同——`CronRuntime.acknowledge_events` 会真正从 durable outbox 删除事件，而 Supervisor 的 ack 只是内存去重。

---

## 运行第 14 章

### 第 0 步：准备环境

```powershell
Set-Location 'E:\cj\study\learn-agent\python\ch14_agent'
& ..\.venv\Scripts\python.exe -m pip install -e ".[dev]"
```

（Windows 的 `zoneinfo` 需要 `tzdata`，已在 `pyproject.toml` 声明。）

### 第 1 步：先跑测试（不需要 API Key）

```powershell
& ..\.venv\Scripts\python.exe -m pytest tests
```

期望最后一行：

```text
146 passed
```

（29 个测试文件；本章新增 `test_cron.py` 4 个 + `test_cron_store.py` 3 个 + `test_ch14_integration.py` 1 个。）

### 第 2 步：真跑

```powershell
& ..\.venv\Scripts\python.exe -m agent_ch14.cli --prompt "每天上海时间 9 点检查 CI，周期执行并持久化"
```

模型调用 `schedule_cron` 后，durable 状态会写到当前 workspace 的：

```powershell
Get-Content .agent_tutorial\cron\state.json
```

可以看到 `jobs` 数组里的 job 定义和 `next_run_at_utc`。等下一次触发时间到达（或调小表达式如 `* * * * *` 每分钟），tick 会把它迁移到 outbox 并注入事件。

### 第 3 步（可选）：完整门禁

```powershell
& ..\.venv\Scripts\python.exe -m compileall -q agent_ch14
& ..\.venv\Scripts\python.exe -m ruff check agent_ch14 tests
& ..\.venv\Scripts\python.exe -m mypy agent_ch14
```

### 常见报错排查

| 现象 | 原因 | 怎么办 |
| --- | --- | --- |
| `配置错误: 缺少或填写错误的必要配置: ...` | `python/.env` 缺项 | 回第 1 章配置四个变量 |
| `cron_expression_error: Cron 表达式必须正好包含五段` | 传了六段（带秒） | 去掉秒字段 |
| `cron_expression_error: Cron 范围不能倒序` | `8-2` 这种反向范围 | 改成 `2-8` |
| `cron_expression_error: 未知 Cron 时区: ...` | 时区名拼错 | 用 IANA 名，如 `Asia/Shanghai` |
| `cron_storage_error: Cron state 无效` | `state.json` 被手工改坏 | 对照格式修，或删掉整个 `cron/` 目录重来 |
| `cron_storage_error: Cron event 不再处于 pending` | 重复确认同一个事件 | 正常保护行为 |
| `ValueError: 第十四章必须提供共享 background_supervisor 和 cron_runtime` | 绕过 CLI 自己组装缺了依赖 | 补齐共享实例 |
| `ValueError: cron_runtime 必须共享 background_supervisor 和 EventInbox` | 造了两套 runtime | 让 CLI 组装，或传同一个 supervisor/inbox |
| 触发的事件迟迟不出现 | 调度是每秒 tick，但事件要等 Runner 空闲 | 正常行为；`run_events()` 在空闲时消费 |
| job 创建后立即没触发 | `next_run` 是严格大于当前时刻的第一个匹配点 | 正常行为；「创建即触发」不是本章语义 |

### 动手小实验

这几个实验都不调真实模型。在 `tests/test_cron.py` / `test_cron_store.py` 末尾追加即可。

**实验一：DOM/DOW 的 OR 语义**

```python
# 0 9 1 * 1 从 2026-06-01 09:01 之后找下一次
print(next_cron_occurrence("0 9 1 * 1", "UTC", dt.datetime(2026, 6, 1, 9, 1, tzinfo=dt.UTC))
# → 2026-06-08 09:00（周一），不是 7 月 1 日
```

学到：`0 9 1 * 1` 是「1 号 **或** 周一」，不是「第一个周一」。

**实验二：DST gap 推进到有效时刻**

参考 `test_dst_gap_and_fold_are_explicit`：纽约 2026-03-08 的 02:30 不存在，`30 2 * * *` 推进到 03:00（`07:00 UTC`）。改时区为 `Australia/Sydney`（南半球 DST 日期不同）再试，观察不同结果。

**实验三：停机后只补一个事件**

参考 `test_recurring_tick_advances_beyond_misfire_time`：recurring job 在 `next_run + 3 小时` 才被 tick。断言只产生了 1 个事件（slot 是最早那个）、`next_run` 被追平。**改大偏移量到 3 天**，事件数仍然是 1。

**实验四：one-shot 删定义、ack 前事件保留**

参考 `test_one_shot_removes_job_but_ack_keeps_event_until_consumed`：tick 后 `list_jobs()` 为空但 `pending_events()` 还有事件。学到：**定义和工作意图是两个生命周期。**

**实验五：durable 重建恢复**

参考 `test_durable_job_and_outbox_recover`：新建 store 后 durable 事件还在。对照一个 `durable=False` 的 job——重建后**干净消失**。

**实验六：leader 竞争**

```python
store_a = JsonCronStore(str(tmp_path), ...)
store_b = JsonCronStore(str(tmp_path), ...)
assert store_a.try_acquire_leader() is True
assert store_b.try_acquire_leader() is False    # marker 文件已存在
store_a.release_leader()
assert store_b.try_acquire_leader() is True     # 释放后 B 接管
```

学到：`open("x")` 的原子性就是互斥锁。

**实验完记得把这些追加的测试删掉或改回去。**

---

## 与 Claude Code 的差异

参照 `learn-claude-code/s14_cron_scheduler/README.md` 中对 CC 源码的分析：

**工具数量。** CC 暴露三个 cron 工具：`CronCreate`、`CronDelete`、`CronList`；P14 只实现一个 `schedule_cron`。

**特性开关。** CC 的 cron 工具受编译时 `feature('AGENT_TRIGGERS')`、运行时 GrowthBook 标志和本地环境变量三重控制；P14 无条件注册 `schedule_cron`，只由 Profile 决定。

**存储与锁。** CC 将 durable 任务写入 `.claude/scheduled_tasks.json`，并用 `.scheduled_tasks.lock` 防止同项目多个 session 重复触发；P14 使用 `.agent_tutorial/cron/state.json` 保存 job 与 outbox 的单一原子快照，用进程内快照锁 + `leader.lock` marker 选举唯一 durable scheduler。**P14 的 outbox 是持久事件队列，CC 的 JSON 文件不保存已触发但未消费的事件**——这是两者最本质的差异。

**时区语义。** CC 的 cron 表达式按本地时区解释，模型不能显式传 IANA 时区；P14 要求调用方传 `timezone`，全部持久化为 UTC instant。P14 对 DST gap/fold 有显式测试。

**防惊群与过期。** CC 为重复任务加入确定性抖动（延迟最多为周期的 10%，上限 15 分钟），重复任务 7 天自动过期。P14 没有抖动、自动过期或 QoS。

**任务上限。** CC 设置 `MAX_JOBS = 50`；P14 没有 job 数量上限，只有 outbox 容量上限（默认 100）。

**交付方式。** CC 触发后以 `priority: 'later'` 入队命令队列，API 容量紧张时按更低 QoS 服务；P14 把触发事件放入共享 `EventInbox`，由 `run_events()` 在空闲时逐条执行，不区分优先级。

---

## 从 ai-agent-book 学到什么：事件流与安全点

`ai-agent-book/chapter4/agent-with-event-trigger/README.md` 对应实验 4-5，`async-agent/README.md`（Flux）对应实验 4-6。它们没有引入新的模型推理原语，而是把「什么时候让事件进入 Agent」变成框架层可控的工程边界。

### P14 的对照与取舍

P14 没有照搬 Flux 的紧急度和批量机制，但把同一套事件边界原则落地到 Cron：

- **结构化事件。** P14 的 `CronEvent` 带 `event_id`、`job_id`、`identity`、`prompt`、`slot_at_utc`，`RuntimeEvent` 还带 `idempotency_key`。触发 turn 不需要靠裸字符串猜上下文。
- **安全点消费。** ai-agent-book 强调事件只在循环边界消费；P14 的 `run_events()` 与用户 turn 共用同一套 history 写入路径，Agent 忙时**带身份的事件**被暂存在 `_deferred_runtime_events`，空闲后才进入 event-only turn。
- **逐条处理。** Flux 为效率批量追加非紧急事件；P14 每轮最多取一个 event。不同 identity、时区和幂等键不会混进同一个 model turn。
- **持久 outbox。** Flux 用状态检查点保存 trajectory 与 task progress；P14 的 durable 检查点保存 job 与 pending outbox，事件确认前不删除工作意图。范围更窄，但正好覆盖「进程重启后 Cron 工作不丢」的核心。
- **重新授权。** P14 保存创建者 identity，触发时重建 `ToolContext`，工具仍经过 Hook 和 `PermissionPolicy`。

**P14 明确不做的事**：没有 `INTERRUPT`/`IMMEDIATE`/`DEFERRED` 紧急度分类，Cron event 统一排队到空闲；没有事件批量；没有文件监听、Webhook；也没有完整 trajectory 检查点。

---

## 配套代码索引

| 文件 | 行数 | 本章的角色 |
| --- | --- | --- |
| `agent_ch14/features/cron.py` | 474 | 全章核心。表达式解析器（`_parse_field`/`_matches`）、DST 处理、`CronJob`/`CronEvent`、四种 typed 错误、`CronRuntime`、`next_cron_occurrence` |
| `agent_ch14/adapters/cron_json.py` | 267 | `JsonCronStore`：原子快照 + outbox、条件迁移、durable/session 双生命周期、leader marker |
| `agent_ch14/core/events.py` | 83 | 第 13 章的 `RuntimeEvent`/`EventInbox`，本章复用不新建 |
| `agent_ch14/core/loop.py` | 477 | `run_events()` 事件回合、`_deferred_runtime_events` 延后注入、事件身份进 ToolContext |
| `agent_ch14/bootstrap.py` | — | 组合根。P14 三道配对检查（共享 supervisor/inbox 的对象身份校验） |
| `agent_ch14/cli.py` | — | 真实入口。共享 EventInbox 构造两个 runtime |
| `agent_ch14/core/profiles.py` | — | `P14 = P13 + "cron"` 的能力位定义 |
| `tests/test_cron.py` | — | 表达式/时区/DST/DOM-DOW OR（4 个用例） |
| `tests/test_cron_store.py` | — | durable 恢复、one-shot ack 语义、misfire 追平（3 个用例） |
| `tests/test_ch14_integration.py` | — | P14 依赖配对、schedule_cron 注册（1 个用例） |

读代码的建议顺序：

```text
① features/cron.py 的 validate_cron_expression + _parse_field
   → 五段解析和边界拒绝

② features/cron.py 的 _matches
   → DOM/DOW 的 OR 判定（21 行）

③ features/cron.py 的 next_cron_occurrence
   → 本地日历扫描 + fold 候选 + gap probe（本章最精妙的一段）

④ features/cron.py 的 CronRuntime.tick
   → leader + store.tick + 去重发布 + wakeup

⑤ adapters/cron_json.py 的 tick
   → 原子迁移：创建 event + 推进/删除 job

⑥ core/loop.py 的 run_events + _inject_runtime_events
   → 事件怎样回到 history、延后注入怎么做

⑦ tests/test_cron*.py
   → 用测试反查你有没有漏掉的分支
```

---

## 本章小结

### 三句话版本

给 Agent 加 Cron，不是写一个无限循环每秒看表。真正的工程边界是：解析器和显式时区得到正确的下一 UTC slot；job 迁移和 pending event 放进同一个锁定原子快照；事件重新进入唯一 Agent Loop，触发时重新经过权限。所有「看起来更方便」的做法——先删定义再执行、先 ack 再入历史、批量注入不同身份的事件——都会在某个具体崩溃点把工作弄丢。

### 一定要记住的五条

1. **Job 和 Event 分开。** 「定义」和「这一次的工作意图」生命周期不同。合成一个对象，one-shot 的删除时序怎么排都会丢工作或重复工作。
2. **本地日历计算，UTC instant 持久化。** fold 的两个 01:30 是两个不同的 UTC 值，都能保留；gap 的 02:30 不存在，推进到有效时刻。
3. **先入 history，再 ack。** 反过来，崩溃窗口里工作意图既不在 outbox 也不在 history——永久消失。宁可重复（幂等键兜底），不可丢失。
4. **逐条处理事件。** 一个 model turn 只有一个 ToolContext，装不下两个 identity。带身份的事件在用户回合中被暂存，空闲时才注入。
5. **触发时重新授权。** 身份来自过去（创建者），权限来自现在（当前策略）。cron prompt 是输入，不是权限票据。

### 检查你是否真的读懂了

不看前文回答这八个问题：

1. `0 9 1 * 1` 表示什么？为什么不是「每月第一个周一」？
2. slot 和 now 有什么区别？事件的 `slot_at_utc` 记的是哪一个？
3. one-shot job 到期后，磁盘上的 `jobs` 和 `outbox` 各剩什么？
4. 停机三天的「每分钟」job 重启后补发几个事件？为什么？
5. DST fold 的两个 01:30 为什么不会被判成重复触发？
6. durable=false 的 job 重启后去哪了？
7. 用户回合进行中 cron 事件到了，会注入当前 history 吗？identity 会污染吗？
8. job 创建后权限规则收紧了，触发时按哪一版执行？

答不上来的，回去看对应小节。

---

下一篇讲 Agent Teams：当任务大到一个 Agent 装不下，需要组队的时候，持久队友和异步收件箱怎么做。
